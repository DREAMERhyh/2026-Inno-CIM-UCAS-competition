# 存算一体芯片中非线性误差对推理精度的影响研究

## 项目设计文档

---

## 一、赛题原始题目

### 题目
**存算一体芯片中非线性误差对推理精度的影响研究**

### 赛题信息
随着神经网络模型规模持续扩大，传统冯诺依曼架构下面临日益严峻的"存储墙"瓶颈，即数据在存储单元与计算单元之间频繁搬运导致的能效与带宽限制。存算一体（或称存内计算）技术通过将计算操作直接嵌入存储器件内部，有效规避了数据搬运开销，显著提升了能效比。然而，基于模拟器件实现的存算架构易受输入信号幅度、温度漂移及器件工艺偏差等环境因素干扰，导致输入-权重乘积累加操作呈现非理想的非线性特性，进而影响计算精度与系统稳定性。因此，如何通过算法增强神经网络在实际存算芯片部署中对非线性误差的容忍能力与鲁棒性，已成为推动存算一体技术进一步发展的关键研究方向之一。

### 非线性误差数学模型
将归一化后的理想输入与电路实际响应之间的映射关系建模为三次多项式函数：

$$y = \alpha \cdot x^3 + (1-\alpha) \cdot x$$

其中：
- $x$ 表示经归一化处理后的理想输入值
- $y$ 表示经非线性失真后的实际输入信号
- 参数 $\alpha$ 用于衡量非线性程度。$\alpha=0$ 时退化为理想线性关系；$|\alpha|$ 越大，非线性效应越显著
- $\alpha$ 的符号决定失真方向：$\alpha>0$ 时正输入区域被放大、负输入被压缩；$\alpha<0$ 时则相反

### 赛题任务
1. **任务1：非线性误差的敏感性分析**——系统评估不同非线性强度（$\alpha$ 取不同值）对网络推理精度的影响，包括整网精度衰减趋势、单层输出分布偏移、以及误差在网络中的逐层累积行为
2. **任务2：非线性感知训练（Nonlinearity-Aware Training）**——在训练阶段加入非线性映射模型，探究其对网络收敛性和泛化性的影响；对比微调（fine-tuning）与从头训练（from scratch）策略对非线性鲁棒性的影响
3. **任务3：鲁棒性增强方法设计**——针对非线性误差，提出特定的优化方法，提升模型对抗非线性误差的能力

### 拓展研究方向
- 拓展1：网络结构与参数量对非线性误差的影响
- 拓展2：对比分析随机高斯噪声注入与非线性失真对模型鲁棒性的不同影响机制
- 拓展3：结合量化误差与非线性误差对模型推理精度的影响进行分析

---

## 二、赛题核心解读

### 2.1 赛题的本质问题

赛题的核心问题是：**在存算一体芯片中，模拟域计算单元的非理想特性会在神经网络的每一层引入输入相关的非线性失真。这种失真不同于传统深度学习中的随机噪声（如高斯噪声、Dropout），而是一种确定性的、信号幅度相关的系统误差。我们需要回答：这种误差对神经网络有多大的破坏力？如何通过算法手段让模型学会在这种误差下正常工作？**

### 2.2 赛题的三个层次

赛题设计具有清晰的递进逻辑，形成"发现问题→基础应对→创新优化"的完整研究链路：

| 层次                 | 对应任务 | 核心问题                                           | 方法论                               |
| -------------------- | :------: | -------------------------------------------------- | ------------------------------------ |
| **第一层：认知问题** |  任务1   | 非线性失真对模型有多大的破坏力？误差如何传播？     | 敏感性分析、层分布偏移、误差累积曲线 |
| **第二层：基础应对** |  任务2   | 如果训练时让模型"见过"失真，能否提高鲁棒性？       | 非线性感知训练、微调 vs 从头训练对比 |
| **第三层：创新优化** |  任务3   | 在感知训练基础上，还有什么办法能进一步提升鲁棒性？ | 架构增强、分层加权训练、消融实验验证 |

拓展研究则从三个维度扩展研究的深度：结构因素的影响（拓展1）、与其他扰动类型的对比（拓展2）、多误差源联合分析（拓展3）。

### 2.3 研究路线的逻辑自洽性

```
任务1：发现"敌人长什么样"
  ├→ 正向失真破坏力是负向的3.4倍
  ├→ 误差从浅层向深层累积，fc层是瓶颈
  └→ 这为任务2和任务3提供了明确的优化方向

任务2：验证"告诉模型有敌人"是否有效
  ├→ NAT-Scratch将α=+0.3精度从55%提升至64%
  ├→ Fine-tuning效果有限（预训练权重的"干净偏见"难以消除）
  └→ 这为任务3提供了基线——还能在此基础上进一步提升吗？

任务3：超越感知训练，架构层面的创新
  ├→ 基于任务1的发现（fc层最脆弱），设计残差校准模块
  ├→ 基于任务1的发现（深层更敏感），设计分层加权训练
  ├→ 消融实验量化每种方法的独立贡献
  └→ 最优方案将α=+0.3精度进一步提升至69%

拓展研究：从"单模型"扩展到"跨架构+跨扰动类型+多误差源"
  ├→ 拓展1：不同架构（浅层/深层纯前馈/深层残差）的鲁棒性差异
  ├→ 拓展2：非线性失真 vs 高斯噪声——机制完全不同
  └→ 拓展3：非线性+量化联合——发现超加性效应和Dithering现象
```

---

## 三、各任务设计思路

### 3.1 任务1：非线性误差的敏感性分析

**核心思路**：在不改变模型训练方式的前提下，系统量化非线性失真对已训练模型推理精度的影响，并深入分析误差在神经网络各层的传播和累积特性。

**为什么这样做？**
- 只有先搞清楚"非线性失真到底有多大的破坏力"，才能针对性地设计防护策略
- 整网精度扫描（不同 $\alpha$ 下的 accuracy）提供了宏观视角
- 层输出分布偏移分析（RME + 余弦相似度）提供了微观视角——误差从哪一层开始失控？
- 跨层误差累积曲线揭示了误差传播的动力学特征

**实验设计**：
- 在已训练的干净模型（SimpleCNN, 84.90%）上，仅修改推理阶段的输入信号
- 对所有 Conv2d 和 Linear 算子的输入激活值注入非线性映射
- 扫描 $\alpha \in [-0.3, 0.3]$（步长 0.1），覆盖从"轻微失真"到"严重失真"的全范围
- 固定 500 张测试样本，捕获 5 个关键层的输出，计算与干净输出的 RME 和余弦相似度

**关键发现**：
- 正向失真（$\alpha>0$）破坏力是负向（$\alpha<0$）的 3.4 倍
- 误差从浅层向深层累积，fc 层是最脆弱的瓶颈（余弦相似度仅 0.895）
- 浅层（conv1/conv2）几乎不受影响（余弦 >0.99），深层（conv4/fc）严重失真

---

### 3.2 任务2：非线性感知训练（NAT）

**核心思路**：在模型训练阶段就引入非线性失真，让模型"在噪声中学习"，从而在推理时遇到失真也能保持精度。对比两种策略：在预训练干净权重上微调（Fine-tuning）vs 从头随机初始化训练（From Scratch）。

**为什么这样做？**
- 任务1 已证明非线性失真对模型推理精度有显著破坏力——直接部署干净模型是不可行的
- 感知训练是深度学习中最直接的领域适应方法：训练分布 = 测试分布
- Fine-tuning 和 Scratch 的对比可以回答："模型已有的知识是帮助还是阻碍适应失真？"

**实验设计**：
- 训练时每个 batch 随机从 $\alpha \in [-0.3, 0.3]$ 均匀采样，注入到所有 Conv/Linear 输入端
- 验证/测试阶段保持干净（$\alpha=0$），确保评估的是模型的"泛化鲁棒性"
- Fine-tuning：加载干净权重（84.90%），用更低学习率（1e-3）训练
- Scratch：随机初始化，用标准学习率（0.01）训练，相同 epoch 数下的对比基线

**关键发现**：

**SimpleCNN（浅层纯前馈）**：
- NAT-Scratch（120 epoch）将 $\alpha=+0.3$ 精度从 55.08% 提升至 64.23%（**+9.15%**）
- Fine-tuning 效果有限，且增加轮数反而不利于极限鲁棒性（80 epoch 精度 56.08%，低于 40 epoch 的 57.98%）
- 从头训练优于微调——预训练权重的"干净特征偏见"难以通过微调消除
- NAT 同时起到正则化作用：Scratch 干净精度 86.49%，比原始模型高出 1.59%

**ResNet-18（深层残差）**：
- NAT-Scratch 干净精度 92.27%，与基线（92.09%）几乎持平——残差网络表达能力在干净数据上已近饱和
- **NAT 对中等失真（|α|=0.2）有显著改善**：α=-0.2 时从 32.98% 提升至 51.82%（**+18.84%**，跨所有实验最大单项提升）
- **NAT 对极限失真（|α|=0.3）几乎无效**：α=+0.3 时 15.64%，反而比基线（17.08%）退化 1.44%
- **反常现象**：Fine-tuning 在 α=+0.3 时（19.89%）反而优于 Scratch（15.64%）——预训练权重在极限失真下保留了更多信息
- **结构性限制**：ResNet-18 的 18 个注入点 + 残差连接使失真信号在 16 层中反复叠加，NAT 无法阻止这一结构性放大效应

**VGG-11（深层纯前馈）**：
- NAT-Scratch 干净精度 90.18%，比基线（89.80%）提升 0.38%
- **Scratch 在所有失真强度下全面优于 Fine-tuning**，恢复了 SimpleCNN 的规律（与 ResNet-18 的反常现象不同）
- **极限失真下仍有稳健提升**：α=+0.3 时从 36.70% → 44.74%（**+8.04%**），α=-0.3 时从 51.83% → 64.72%（**+12.89%**，三模型中负向失真最大单项提升）
- **NAT 效果最均衡**：在所有 α 区间提供 2-13% 的稳健增益，无退化现象
- 过拟合程度为三模型中最重（训练-测试精度差约 10%），但不影响鲁棒性提升

**三模型 NAT 规律总结**：

| 指标                 |    SimpleCNN     |      VGG-11      |           ResNet-18           |
| -------------------- | :--------------: | :--------------: | :---------------------------: |
| Scratch α=+0.3 提升  |      +9.15%      |      +8.04%      |            -1.44%             |
| Scratch α=-0.3 提升  |      +5.48%      |   **+12.89%**    |            +6.27%             |
| 最佳提升区间         |      α=+0.3      |      α=-0.3      |            α=-0.2             |
| Scratch vs Fine-tune | Scratch 全面占优 | Scratch 全面占优 | 极限正向下 Fine-tune 反常占优 |
| 极限失真可救性       |  可救（55→64%）  |  可救（37→45%）  |       不可救（17→16%）        |
| NAT 正则化增益       |   大（+1.59%）   |   中（+0.38%）   |         微（+0.18%）          |
| 过拟合程度           |    低（~3%）     |    高（~10%）    |           中（~8%）           |
| **综合评价**         |   极限正向最优   |  **最均衡稳健**  | 中等失真极有效，极限失真无效  |

**跨架构 NAT 效果规律**：
- **中等失真（|α|≤0.2）**：NAT 对三种架构都有效，残差网络在特定区间受益最显著（α=-0.2 时 +18.84%），但纯前馈网络增益更均衡
- **极限失真（|α|=0.3）**：浅层网络和深层纯前馈的 NAT 仍有效（+8-9%），残差网络的 NAT 退化或无效（-1.44%）
- **NAT 效果与注入点数量负相关**：5 注入点（SimpleCNN）→ 9 注入点（VGG-11）→ 18 注入点（ResNet-18），极限失真下的 NAT 提升依次递减（+9.15% → +8.04% → -1.44%）
- **Scratch vs Fine-tuning**：纯前馈网络 Scratch 全面占优；残差网络在极限正向下出现 Fine-tuning 反常反超
- **VGG-11 是最均衡的 NAT 受益者**：无 SimpleCNN 的低精度天花板限制，无 ResNet-18 的结构性缺陷，在所有失真强度下提供稳健增益

---

### 3.3 任务3：鲁棒性增强方法设计

**核心思路**：在 NAT-Scratch 的基础上，叠加三种互补的鲁棒性增强方法，通过消融实验量化每种方法的独立贡献。

**为什么这样做？**
- 任务2 的 NAT-Scratch 虽然有效，但 $\alpha=+0.3$ 时精度仍有 22.26% 的跌幅——有进一步优化空间
- 任务1 已明确指出 fc 层是最脆弱的瓶颈，这为方案A（残差校准模块）提供了直接的动机
- 任务1 也发现深层比浅层更敏感，这为方案B（分层加权训练）提供了依据
- 三种方案从不同角度（架构、训练策略、采样策略）切入，具有互补性

**三种方法的设计逻辑**：

| 方案  | 类型     | 核心逻辑                                                                                                                                        |
| :---: | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| **A** | 架构增强 | 在 GAP 之后、FC 之前插入残差校准 MLP，学习"失真特征→干净特征"的补偿映射。残差连接确保最坏情况下退化为恒等映射，不损害干净精度                   |
| **B** | 训练策略 | 浅层（conv1/2）用轻失真 $\alpha \in [-0.15,0.15]$，深层（fc）用强失真固定 $\pm 0.3$。精准打击最脆弱的瓶颈层，同时保护浅层特征不受过度噪声破坏   |
| **C** | 采样策略 | 70% 概率从 $[0,0.3]$ 采样正向 $\alpha$，30% 从 $[-0.3,0)$ 采样负向。基于任务1 的发现——正向失真破坏力是负向的 3.4 倍，让训练"更多看到"强破坏场景 |

**消融实验设计**：
- Exp1（仅方案A）：验证校准模块的独立效果
- Exp2（方案A+B）：验证分层加权的叠加效果
- Exp3（方案A+B+C）：验证非对称采样的进一步贡献

**关键发现**：
- **分层加权（方案B）是贡献最大的单项**：Exp1→Exp2，$\alpha=+0.3$ 精度从 63.63% 跃升至 68.76%（+5.13%）
- 残差校准模块（方案A）有正则化效果（干净精度 +0.70%），但单独不足以应对强失真
- 非对称采样（方案C）边际贡献极小（+0.38%），可舍弃
- **最优方案 = Exp2**：干净精度 87.37%，$\alpha=+0.3$ 精度 68.76%，累计提升 13.68 个百分点

---

## 四、拓展研究设计思路

### 4.1 拓展1：网络结构与参数量对非线性误差的影响

**核心问题**：SimpleCNN 的结论是否具有普适性？不同架构（浅层 vs 深层 vs 残差）对非线性失真的敏感性是否有本质差异？

**为什么选这三个模型？**

| 模型      | 参数量 | 结构类型                           | 在对比中的角色                         |
| --------- | :----: | ---------------------------------- | -------------------------------------- |
| SimpleCNN | 0.24M  | 浅层纯前馈（4 Conv）               | **基线**                               |
| VGG-11    |  9.2M  | 深层纯前馈（8 Conv + MaxPool）     | **深度变量对照**：深度增加但不引入残差 |
| ResNet-18 | 11.17M | 深层残差（18层 + Skip Connection） | **结构类型对照**：深度和残差同时引入   |

通过这三个模型的对比，可以独立回答两个问题：
1. 深度增加是否必然导致非线性脆弱？（SimpleCNN vs VGG-11）
2. 残差连接是缓解还是加剧了误差累积？（VGG-11 vs ResNet-18）

**关键发现**：
- ResNet-18 是最脆弱的：$\alpha=+0.3$ 时仅 17.08%（vs SimpleCNN 55.08%，VGG-11 36.70%）
- VGG-11 介于两者之间（36.70%），深层纯前馈比浅层脆弱但远优于残差网络
- **残差连接在非线性失真下成为误差放大器**：$output = F(x) + x$ 的加法路径使失真信号交叉污染
- **NAT 效果是架构依赖的**：
  - SimpleCNN：NAT-Scratch 全面提升，α=+0.3 时 +9.15%
  - VGG-11：NAT-Scratch 均衡稳健，α=+0.3 时 +8.04%，α=-0.3 时 +12.89%（三模型最大单项提升）
  - ResNet-18：NAT 仅在中等失真有效（α=-0.2 时 +18.84%），极限失真无效甚至退化
  - **注入点数量决定 NAT 上限**：5 点（SimpleCNN）→ 9 点（VGG-11）→ 18 点（ResNet-18），极限失真下 NAT 提升依次递减
- **Fine-tuning vs Scratch 的优劣因架构而异**：SimpleCNN 上 Scratch 全面占优；ResNet-18 在极限正向失真下 Fine-tuning 反而更优（19.89% vs 15.64%），打破了"Scratch 总是更好"的规律

---

### 4.2 拓展2：高斯噪声与非线性失真对比

**核心问题**：深度学习中常见的随机噪声（高斯噪声）和存算一体芯片特有的非线性失真，对模型的影响机制是否相同？是否可以通用防护？

**实验设计逻辑**：
- 非线性失真 = 输入相关、确定性映射。同一输入始终产生相同失真
- 高斯噪声 = 输入无关、随机加性扰动。同一输入每次产生不同噪声
- 如果两者的破坏模式相同，通用防护策略即可；如果不同，需要针对性设计

**关键发现**：
- **SimpleCNN**：高斯噪声比非线性失真更致命（$\sigma=0.3$ 时 37.02% vs $\alpha=+0.3$ 时 55.08%）
- **ResNet-18**：完全反转——非线性是灾难（17.08%），高斯几乎免疫（57.15%）
- **VGG-11**：两种扰动下表现最均衡（高斯 66.39%，非线性 36.70%）
- **残差连接是双刃剑**：对零均值随机噪声起缓冲作用（平均化效应），但对信号相关失真起放大作用（信号叠加效应）

---

### 4.3 拓展3：量化误差与非线性误差联合影响

**核心问题**：真实存算一体芯片中，MAC输出后需经ADC模数转换，同时引入非线性失真（模拟域）和量化误差（数字域）。两种误差同时存在时，是否会相互放大？

**实验设计逻辑**：
- 单独量化扫描（bit ∈ [8,6,4,3,2]）：确定各模型的量化悬崖阈值
- 联合扫描（$\alpha \times$ bit 网格，35种组合）：检测是否存在超加性叠加效应
- 注入顺序严格遵循物理路径：先非线性（模拟MAC），再量化（ADC）

**关键发现**：
- **SimpleCNN**：两种误差超加性恶化（联合跌幅 > 单独之和），需要同时防御
- **ResNet-18**：量化鲁棒但非线性致命（地板效应抵消），优化重点是MAC非线性校准
- **VGG-11**：发现独特的**Dithering效应**——适量量化噪声（4bit）反而提升了非线性失真下的精度（+6.53%），量化噪声打破了非线性失真的确定性破坏模式

---

## 五、项目技术路线总结

```
实验基线建立
├── SimpleCNN 干净训练 (84.90%)
├── 模型管理框架（支持多架构切换）
└── 非线性注入 + 量化注入 + 高斯噪声注入模块

任务1：发现问题
├── α ∈ [-0.3, 0.3] 整网精度扫描
├── 5层输出 RME + 余弦相似度分析
└── 发现：正向破坏>负向，fc层最脆弱

任务2：基础应对（跨架构验证）
├── SimpleCNN
│   ├── NAT Fine-tuning (40ep / 80ep)
│   ├── NAT Scratch (120ep)
│   └── 结论：Scratch > Fine-tune，α=+0.3 提升 +9.15%
├── ResNet-18
│   ├── NAT Fine-tuning (80ep)
│   ├── NAT Scratch (120ep)
│   └── 结论：中等失真大幅改善（α=-0.2 时 +18.84%），极限失真无效（α=+0.3 时 -1.44%）；Fine-tuning 在极限正向下反常占优
└── VGG-11
    ├── NAT Fine-tuning (80ep)
    ├── NAT Scratch (120ep)
    └── 结论：Scratch 全面占优，α=+0.3 提升 +8.04%，α=-0.3 提升 +12.89%（三模型最大）；最均衡的 NAT 受益者

任务3：创新优化
├── 方案A：残差校准模块 (FeatureCalibration)
├── 方案B：分层加权 α 采样
├── 方案C：非对称采样 (70%/30%)
├── 消融实验：Exp1 → Exp2 → Exp3
└── 最优方案：A+B，α=+0.3 提升至 68.76% (+13.68% 累计)

拓展研究：跨架构 + 跨扰动 + 多误差源
├── 拓展1：SimpleCNN vs VGG-11 vs ResNet-18
│   └── 残差连接放大非线性失真，VGG-11 最均衡
├── 拓展2：非线性 vs 高斯噪声
│   └── 机制完全不同，鲁棒性无法跨域迁移
└── 拓展3：非线性 + 量化联合
    ├── SimpleCNN：超加性恶化
    ├── ResNet-18：地板效应抵消
    └── VGG-11：Dithering效应（量化噪声改善精度）
```