# 存算一体芯片逐层非线性失真下神经网络精度退化的成因与读出层修复

> 本文件由仓库内的实验记录整理而成。所有数字都能在仓库文件里找到出处，出处写在数字旁边。
> 写作口径见 §1.2。文中标注"单 seed"的结果只跑了一个随机种子。
> 全文共六个研究阶段，产物目录为 `outputs/`（CIFAR-10）与 `outputs_cifar100/`（CIFAR-100）。

---

## 0. 摘要

存算一体芯片的模拟域乘累加有输入相关的非线性失真。我们用三次多项式
$y=\alpha\hat x^3+(1-\alpha)\hat x$（$\hat x$ 为逐样本 $\infty$-范数归一化）建模这种失真，
在 CIFAR-10 与 CIFAR-100 上跑了六个阶段的实验。

主要结果有六条。

第一，失真没有毁掉特征里的标签信息。$\alpha=+0.3$ 时，在冻结主干的倒数第一层输入上重训一个线性分类器，
能拿到 47.89%；而模型自带的分类头只有 27.76%。两者差 20.12 个百分点
（`outputs_cifar100/v3_probe_info_death/probe_results.csv`）。

第二，由此得到一个便宜的办法：把主干整个冻结，只重训最后的线性层（我们叫 readout-NAT）。
CIFAR-100 上 $+0.3$ 的尾部从 28.09 提到 43.31（24ep、20k、3 seed 均值），花 12 分钟；
全网络重训要 1~6 小时。**代价是干净精度掉 6.45 ± 0.15 个点**
（`results_master.csv` 第 121、127、129 行）。这个代价可以用双头门控收回来，见第三条办法。
CIFAR-10 上同样成立且更强（单 seed）：VGG-11 的尾部从 36.71 提到 **79.64**，干净代价只有 0.58 个点。

第三，失真强度 $\alpha$ 可以在线估出来。特征过一个岭回归即可，$R^2$ 0.827~0.935
（`outputs_cifar100/v5_full_recipe/recipe_simple_cnn.csv`）。部署时不用预先知道失真多大。

第四，把主干里的 MaxPool 从 2 个加到 5 个，不动训练脚本，$+0.3$ 从 28.09 提到 42.47（+14.38，
CIFAR-100、clean 训练、单 seed），训练成本为零
（`outputs_cifar100/v3_methods/simple_cnn_mp_clean_uniform/alpha_sensitivity.csv`）。

第五，训练端的四条路全部失败：改采样预算、对抗训练、课程式、扩大训练范围，
没有一条能救 $+0.3$ 的尾部。这四条是独立做的，数字见 §5.1。

第六，正面失真的破坏有一个具体形态。它把深层特征的幅度压小（`outputs_cifar100/v4_activation_stats/act_stats.csv`），
特征的有效秩在 $+0.3$ 掉 20.6%、在 $+0.5$ 掉 50.9%；负侧只掉 2.0% 和 14.2%
（`outputs_cifar100/v5_rank_collapse/rank_stats.csv`）。丢的是维度，不是类间距离。

另有一条方法学上的教训（L9）：尾部指标的种子方差普遍大于 clean ——
同协议实测范围 ±0.39~3.40（clean 是 ±0.07~1.85），深层模型的 clean 也能到 ±1.85。
单 seed 看到的 4.53 个百分点的效应，补到 3 个种子后只剩 1.17。
所以小于 3 个点的尾部差值我们不当结论。
另外还踩过一个坑：**三个种子如果来自不同脚本，方差会被估高约 6 倍**（0.39 → 2.56）。
追查做了三轮（种子噪声 → 采样粒度 → RNG 消耗位置），**三次归因全部失败**：
评估路径已交叉验证无辜、训练轨迹逐 epoch 贴合，但 `task2_nat.py` 的权重仍稳定低 5.55 pp。
⇒ **精度数字不可跨训练实现移植**（§14.5、§14.6，按 known-unknown 记录）。

---

## 1. 研究问题与数据口径

### 1.1 失真模型

芯片的模拟域 MAC 有器件失配和增益非线性。我们把输入侧的失真写成三次多项式：

$$y = \alpha \cdot \hat x^3 + (1-\alpha)\cdot \hat x,\qquad \hat x = \frac{x}{\|x\|_\infty}$$

$\hat x$ 按 batch 维逐样本归一化，避免样本之间互相影响（`utils/nonlinearity.py`）。
失真是确定性的、和输入幅度相关，不是随机噪声。

失真通过 `register_forward_pre_hook` 挂在每个 `nn.Conv2d` 和 `nn.Linear` 的输入侧。
逐层都注入，每层可以有自己的 $\alpha$。除特别说明外，本文的扫描用同一个 $\alpha$ 施加到所有层。

$\alpha$ 的符号决定方向。$\alpha>0$ 时中段被压低，$\alpha<0$ 时中段被抬高（§2 第 3 条）。

### 1.2 度量口径

全阶段统一用四个数，定义写在 `experiments_ledger.md` §0：

| 指标 | 定义 |
|---|---|
| `clean` | $\alpha=0$ 的精度 |
| `drop@0.3` | clean 减去 $\alpha=+0.3$ 的精度 |
| `mid_band` | $\alpha\in\{-0.2,-0.1,+0.1,+0.2\}$ 四点的平均 |
| `mean7` | $\{-0.3,-0.2,-0.1,0,+0.1,+0.2,+0.3\}$ 七点的平均 |

评估协议固定为：全量测试集（10,000 张）、best 权重、纯推理。
每个 run 的 `alpha_sensitivity.csv` 里 $\alpha=0$ 那一行必须等于同目录 `metrics.json` 的 `best_test_acc`，
误差容限 0.01。这个自检结果记在 `results_master.csv` 的 `pairing_check` 列里，
**152 行（归档时点，以重扫为准，2026-09-20）中 0 FAIL** ——
其中 **58 行 PASS**（有 `metrics.json` 可配对），**94 行标 n/a**（复用扫描 20 行、读出适配 74 行，本就没有可配对的 metrics）。

训练用的 $\alpha$ 采样区间，除注明外都是 $\alpha\sim U[-0.3,+0.3]$。

### 1.3 数据集与架构

两个数据集。CIFAR-10（10 类，干净精度 84.90~92.09）与 CIFAR-100（100 类，干净精度 59.99~72.09）。
基线数字见 `results_master.csv` 第 2–4 行与第 26–28 行。

四个主干（参数量由 `models/` 下的模型定义直接数出来，随类别数变化）：

| 主干 | 参数量（CIFAR-10 / CIFAR-100） | 结构 | 定义文件 |
|---|---|---|---|
| SimpleCNN | 242,474 / **254,084** | 4 个卷积 + **2** 个 MaxPool | `models/simple_cnn.py` |
| SimpleCNN+3×MaxPool | 242,474 / **254,084** | 同上，MaxPool 加到 **5** 个。参数量不变 | `v3_methods_simplecnn.py` 的 `SimpleCNNMaxPool` |
| VGG-11 | 9,231,114 / **9,277,284** | 5 个 MaxPool | `models/vgg11.py` |
| ResNet-18 | 11,173,962 / **11,220,132** | — | `models/resnet.py` |

（`results_master.csv` 的 `params` 列用的是 **CIFAR-100** 那一组数，对 CIFAR-10 的行也照填，
所以 CIFAR-10 行上的 254084 / 9277284 / 11220132 偏大约 1.2 万~3 万，见勘误 8。）

另外用到几个训练过的鲁棒主干：NAT-scratch / NAT-finetune（全网络在失真下重训）、
Exp1/Exp2/Exp3（校准模块 + 分层采样 + 非对称采样，见 `models/robust_cnn.py`）。

### 1.4 六个阶段做了什么

| 阶段 | 内容 | 报告文件 |
|---|---|---|
| 一 | 敏感性扫描、NAT 训练、架构增强消融 | `README.md` §7–§15 |
| 二 | 外推边界、Dithering 成因、深层迁移 | `二阶段优化+实施记录+结果分析.md` |
| 三 | 台账统一 + 四条新结论（含 readout-NAT） | `第三阶段报告.md`、台账 §2–§9 |
| 四 | 读出端 vs 训练端的分解、在线路由、架构手段 | `第四阶段报告.md`、台账 §12 |
| 五 | 完整配方实测、池化成因、CIFAR-10 复制 | `第五阶段报告.md`、台账 §13 |
| 六 | 定律化与落地：粒度/RNG 归因（均否证）、秩-恢复曲线、深层门控、硬件语义对齐 | 台账 §14–§15、`deploy_notes.md`、`docs/PROJECT_RETROSPECTIVE.md` §6 |

---

## 2. 我们发现了什么：L1–L9

下面九条是六个阶段积累下来的结论。前八条关于模型，第九条关于怎么测量。每条给出代表数字和出处文件。

### L1 反相关律：干净精度越高，掉得越狠

在同一数据集内，模型越强，$+0.3$ 处掉得越多。CIFAR-100 上：

| 模型 | clean | drop@+0.3 |
|---|---|---|
| SimpleCNN | 59.99 | 31.90 |
| VGG-11 | 67.09 | 50.17 |
| ResNet-18 | 72.09 | 69.39 |

出处：`results_master.csv` 第 26–28 行。

这条律在**同一数据集内**成立。跨数据集不能直接比 drop，因为类别数不同：
CIFAR-10 的 SimpleCNN clean 84.90、drop 29.82，看起来比 CIFAR-100 的 31.90 小，
但两者难度差很远（`results_master.csv` 第 2 行 vs 第 26 行）。

**正负不对称**：一阶段在 CIFAR-10 的 SimpleCNN 上测到，$\pm 0.3$ 处正向掉的幅度是负向的 3.4 倍
（29.82 / 8.84 = 3.37）。这个比值是**在 $|\alpha|=0.3$ 这一个点上**算的。
换成 CIFAR-100 的同一模型，比值只有 1.87（31.90 / 17.08）。
在 $|\alpha|=0.2$ 处，CIFAR-10 的比值降到 2.32。所以"3.4 倍"不是常数，
它是特定数据集、特定模型、特定 $\alpha$ 下的读数。

### L2 容量决定 NAT 的形态

NAT 指训练时就把失真注入进去。它在不同容量的模型上表现不一样。

小模型（SimpleCNN）是"再分配"：正侧赚、负侧赔。看第三阶段的单点读出适配实验，
把读出专门适配到 $\alpha=+0.3$，该点从 28.09 涨到 52.79（+24.70），
但 clean 掉 20.36、$\alpha=-0.3$ 掉 25.71，七点均值 38.06 反而低于基线 49.29
（`outputs_cifar100/v3_readout_repair/readout_repair_alpha+0.30.csv` 的 c 行 vs a 行）。

深层模型是"全轴塌陷"：ResNet-18 在 $\pm0.3$ 处只剩 2.70 / 3.25。

**新增限定**：NAT 的收益绑在它自己的分类头上。把 NAT 主干当特征提取器再配一个适配的读出，
在**同一读出训练预算**下与直接用干净主干**基本打平**：8 epoch 时差 5.26 个百分点（35.90 vs 41.16），
把预算提到 24 epoch 后差距缩到 **−1.78**，落在读出侧的种子噪声里（±1.31）。
**所以没有证据支持"NAT 主干更好"，也没有证据支持它更差 —— 等预算下两者相当。**
（8 epoch 那个 5.26 的差距，三分之二是欠训练造成的，见 §5.3 第 3 条。）

出处：台账 §12.1、§12.4。


**加注（第六阶段 P0/P4a）—— 粒度不是杠杆，未解的是脚本间差异。**

§13.12 观察到的 5.5 pp 差，**不是** α 采样粒度造成的。同脚本消融（`v3_methods` 内切换粒度）：

| 对比 | 差 | $z$ |
|---|---|---|
| **同脚本、换粒度**（逐层独立 26.57 vs 每批共享 26.55） | **+0.02** | **0.08** |
| **同粒度、跨脚本**（shared：task2 21.03 vs v3 26.55） | **+5.52** | **9.4** |

**效应跟着脚本走，不跟着粒度走。** 两个脚本在数据加载、超参、优化器、调度器、钩子机制、
采样位置、评估口径上已逐项核对一致，同 seed 的逐 epoch 训练 loss 也几乎重合（差 ≤0.006），
**但尾部稳定地差 5.5 pp，原因未查清**。已按"超过 5 pp 的无法解释效应"红线停下报告。

**所以：不推荐"必须逐层独立采样"**（两种粒度实测无差别）；但两个脚本的数字仍**不可混用**。

**口径注（P4f 定稿）**：凡是用 `task2_nat.py` 训出的权重（NAT-scratch / NAT-finetune，
`results_master.csv` 的 `nat_*` 行、§12.1 的 2×2、§12.7 的迁移表、§5.3 的训练精度表）
都属于**该实现的特定读数**，与 `v3_methods` 系的同类数字**不可直接比较**
（差 5.55 pp，原因未查清）。**数值不修正**，只在引用处标口径。

**根因（P4e 交叉评估查明，P4f 干预否证）**：交叉评估证明**评估路径无辜**、差异在权重侧；
但提议的"RNG 消耗位置"解释**被干预实验否证**（同脚本把消耗数 5→1，尾部只动 +0.11）。
**5.55 pp 的组间差至今未归因**，按 known-unknown 记录（台账 §14.5、§14.6）。原文如下：
`v3_methods` 的 shared 仍消耗 5 个随机数（先抽 5 个再取第 1 个广播，为的是让默认路径 RNG 流不变），
`task2_nat` 只消耗 1 个 —— 于是从**第 2 个 batch 起两条 α 序列完全错开**（第 1 个相同：都是 −0.277）。
交叉评估另证：**评估路径完全无辜**（8 个权重 × 2 条路径，逐位相同）。
出处：台账 §14.5 结果。
### L3 两条通路：迁移几乎正交，但训练时互相干扰

这里"两条通路"指非线性失真与加性高斯噪声。

**迁移上几乎不共享**。做了 NAT 训练之后，高斯鲁棒性几乎没变：
$\sigma=0.3$ 只从 3.98 到 4.40（+0.42）。反过来，只做高斯训练会让 $\alpha=+0.3$
从 28.09 掉到 22.01（−6.08）；两个都做（joint）掉到 15.27，相对只做 $\alpha$ 训练的
26.03 又掉 10.76（`results_master.csv` 第 56、58、59 行）。

**迁移的幅度同时依赖架构和配方**。同一数据集内，最大跨扰动迁移值从 0.98 到 5.26，跨 5 倍：

| 配方对 | $\max|\Delta_\sigma|$ |
|---|---|
| SimpleCNN：exp2（校准）vs plain | 1.16 |
| SimpleCNN：NAT-scratch vs clean | 2.43 |

> 口径注：NAT-scratch 一行的 checkpoint 来自 `task2_nat.py`（granularity=shared）。
> 「迁移幅度依赖配方」这条结论反而与该口径一致 —— 采样粒度本身就是一种配方差异。
| SimpleCNN+3×MaxPool：NAT vs clean | 0.98 |
| ResNet-18：exp2 vs clean | 1.41 |
| VGG-11：exp2 vs clean | **5.26** |

出处：台账 §7、§12.7，`outputs_cifar100/v3_probe_gaussian_transfer/transfer_vgg11.csv`、`transfer_resnet18.csv`。

**趋势上**：高斯鲁棒性必须显式训练，做非线性训练换不来它。反过来做高斯训练会伤非线性。
如果两种失真同时存在，只做高斯训练就够，再加非线性注入是负担（§4.4）。

### L4 正面尾部是训练端打不动的地方

四条独立的负结果，都冲着 $\alpha=+0.3$ 去，都失败。详细数字见 §5.1。

最强的证据是第 4 条：把训练范围扩到 $\pm0.5$，测试 $\alpha=+0.5$ 仍然只有 2.71%、
$\alpha=+0.6$ 只有 1.30%；同期 $\alpha=-0.5$ 有 31.19%。差 28.5 个百分点。
所以这不是"训练时没见过"的问题（`results_master.csv` 第 62 行）。

### L5 Dithering：低位量化反而救精度

VGG-11 在 $\alpha=+0.3$ 下，4bit 量化后的精度 43.27%，8bit 只有 36.74%，**低位反而高 6.53 个点**
（CIFAR-10，`outputs/extension5_dithering_vgg11/group_a_bits_scan.csv`）。

三个候选解释逐一检验（`outputs_cifar100/extension5_dithering_vgg11/`）：

- **随机去相关（H1）：否决**。等剂量的纯噪声是有害的，不是有益。
  样本级翻转统计：4bit 量化净救回 284 个样本（saved 655 / killed 371），
  等剂量纯噪声净增 −20 和 −9（`flip_statistics.csv`）。
- **分布重塑（H2）：方向确认**。分类器输入分布的峰度从 0.8473 回到 0.9802，与 +2.84 个点同向。
- **MaxPool 交互（H3）：定性复现**。三架构排序是 VGG-11(5 个)正 / ResNet-18(0 个)≈0 / SimpleCNN(2 个)强负。

**标度律**：增益随任务难度缩水。CIFAR-10 是 +6.53，CIFAR-100 只剩 +2.84（保留 43%）；
同期基线精度从 36.74 掉到 16.91（`outputs_cifar100/extension5_dithering_vgg11/group_a_bits_scan.csv`）。

### L6 读出这一环：信息没死，可恢复

这是整个项目里最有用的发现。

在冻结的干净主干上，拿出倒数第一层的输入特征（GAP 之后的 128 维），重训一个线性分类器：

| 条件 | 重训线性探针 | 模型自带的头 | 差值 |
|---|---|---|---|
| clean | 52.91 | 59.60 | −6.69 |
| $\alpha=+0.2$ | 51.40 | 47.05 | +4.35 |
| **$\alpha=+0.3$** | **47.89** | **27.76** | **+20.12** |
| $\alpha=+0.4$ | 42.65 | 12.24 | +30.41 |
| $\alpha=+0.5$ | 36.38 | 6.00 | +30.38 |
| $\sigma=0.3$ | 10.89 | 3.89 | +7.00 |

出处：`outputs_cifar100/v3_probe_info_death/probe_results.csv`。

随机猜是 1%。所以在 $\alpha=+0.3$ 时，特征里还有 47.89% 的标签信息能被线性解码出来。
坏掉的是那个冻结的分类头。即使在训练分布之外的 $\alpha=+0.5$，探针还有 36.38%。

需要说明两点。第一，探针精度低于模型自己的干净精度（52.91 < 59.60），
所以这里的差值是一个下界，真实可恢复量更大。第二，这个"可恢复"只说明线性可达，
不说明失真没伤到特征 —— 特征的方向结构确实变了，证据是在干净特征上拟合的探针
迁移过去只剩 17.97%（$\alpha=+0.3$），而同条件重训的探针有 47.89%。

**跨层损伤是叠加放大**。单层注入 $\alpha=+0.3$ 时 conv1/conv4/fc 几乎无害，
conv2 掉 12.43、conv3 掉 7.56。但五层同时注入掉 31.90，而按可加预期只该掉 23.33，
超出 8.57 个百分点（`outputs_cifar100/v3_pairwise/pairwise.csv`）。

这里要区分两件事。注入位置上，最敏感的是 conv2/conv3（浅-中层）；
观测位置上，全层失真时 fc 的输出偏移最大。一阶段说的"fc 最脆弱"是**观测位置**的结论，
两者不冲突，但混着说会误导（台账 §2.2 的修正建议）。

**加注（第六阶段 P1）**：L4 的秩塌陷与 L6 的读出可恢复量，在三个模型上接上了 ——
横轴取有效秩损失、纵轴取恢复率（readout-NAT@α ÷ $\alpha=0$ 的 clean），
$\alpha=+0.3$ 处三点是：

| 模型 | 特征维度 | 秩损失 | 恢复率 |
|---|---|---|---|
| SimpleCNN（clean 主干） | 128 | 20.6% | 72.2% |
| VGG-11（Exp2 主干） | 512 | 33.3% | 70.2% |
| ResNet-18（Exp3 主干） | 512 | 58.7% | 36.9% |

三点**单调递减**（$r = -0.960$），但**三点不足以定曲线形状**。
形状提示：不是直线，更像**先平后塌** —— 前两点只差 2.0，第三点比第二点低 33.3。
逐 α 曲线里，ResNet-18 在负侧就已开始塌（$\alpha=-0.3$ 时秩损失 67%、恢复率 23%），
SimpleCNN / VGG-11 在负侧几乎不塌。
**"塌陷 ≠ 不可恢复"**：ResNet-18 掉了 58.7% 的秩，读出仍拿回 36.9%，而原头只有 8.6%。
出处：台账 §14.1 结果；`outputs_cifar100/v5_rank_collapse/rank_stats{,_exp2vgg11,_exp3resnet18}.csv`。


### L7 失真强度可以在线估出来

在特征上拟合一个岭回归来估 $\alpha$。结果：

| 数据集 / 主干 | $R^2$ | MAE | 出处 |
|---|---|---|---|
| CIFAR-100 / SimpleCNN | 0.935 | 0.0361 | `outputs_cifar100/v5_full_recipe/recipe_simple_cnn.csv` |
| CIFAR-100 / SimpleCNN+5×MaxPool | 0.8323 | 0.0595 | `outputs_cifar100/v5_full_recipe/recipe_simple_cnn_mp.csv` |
| CIFAR-10 / SimpleCNN | 0.9327 | 0.0373 | `outputs/v5_full_recipe/recipe_simple_cnn.csv` |
| CIFAR-10 / SimpleCNN+5×MaxPool | 0.8270 | 0.0607 | `outputs/v5_full_recipe/recipe_simple_cnn_mp.csv` |

在 CIFAR-100 的 7 个测试点上，估计值分别是 −0.287 / −0.189 / −0.093 / **+0.003** / +0.098 / +0.189 / +0.269。
基本没有系统性偏差。部署时不需要预先知道失真多大。

四个组合的 $R^2$ 落在 **0.827~0.935**。注意**加了 MaxPool 的主干上 $R^2$ 会掉到 0.83**
（同样是因为池化把一部分信息压掉了），所以引用区间时下界用 0.827。

**门控什么时候值得装，要同时满足两个前提**（第五阶段提出、第六阶段 P2 修订）：
① 鲁棒头赔掉的干净精度要够大（有东西可回收）；
② **两个头在整个 α 轴上都不能崩掉** —— ResNet-18 的 Exp3 自带头在负侧崩（α=−0.2 只有 10.27%），
导致软门控把坏头的那点灾难插值进来，红利变成 **−1.14**（比单用鲁棒头还差）。
原来的"红利 = 代价回收"不成立（七点 $r=-0.533$，去掉 CIFAR-100 后符号反转）。详细见 §4.2。

### L8 池化的保护，主要来自降维

把主干里的 MaxPool 从 2 个加到 5 个，其余全不动：

| 训练配方 | 池化 | clean | $\alpha=+0.3$ | mean7 |
|---|---|---|---|---|
| clean 训练 | 2×MaxPool | 59.99 | 28.09 | 49.29 |
| clean 训练 | **5×MaxPool** | 60.99 | **42.47** | 53.75 |
| NAT 训练 | 2×MaxPool | 60.85 | 26.03 | 49.24 |
| NAT 训练 | **5×MaxPool** | 61.18 | **41.55** | 54.09 |

出处：`outputs_cifar100/v3_methods/` 下的 `simple_cnn_mp_clean_uniform/`、`plain_uniform/`、
`simple_cnn_mp_plain_uniform/` 三份 `alpha_sensitivity.csv`；台账 §12.9。
> 四行**全部**是 `v3_methods_simplecnn.py` 的产物（逐层独立采样）——表内不混协议。
> 早先「NAT 训练」那一行填过 `task2_nat.py` 的 60.70 / 21.41（那是**每批共享 α** 的口径），
> 与同表的 41.55 不可比。见 §8 勘误 10。

改动只是网络定义，训练成本是零。$\alpha=+0.3$ 上 **+14.38**（clean 训练）或 **+15.52**（NAT 训练，同协议同 seed 配对）；
（本文早期版本这里写的是 **+20.14**，那是拿 `task2_nat.py` 的 21.41 当基线算的 ——
**其中约 5.5 pp 是 α 采样粒度造成的，不是架构造成的**，见 §8 勘误 10 与台账 §13.12 结果。）
干净精度不降反升。

**三个候选原因被排除**：

1. **不是池化把失真"吸收"掉了**。归一化三次映射在非负激活上与 MaxPool 精确可交换，
   误差是 0.000e+00（机器精度），6 档 $\alpha$ × 全部池化层
   （`outputs_cifar100/v5_maxpool_mechanism/commutativity.csv`）。既然可交换，次序就不重要，
   池化不"吸收"任何东西。破缺只出现在含负值的张量上，那是 abs-max 归一化和 signed-max 池化
   尺度不一致造成的实现细节，不是数学性质。
2. **不是激活集中到了不变区**。5-pool 模型的易感度 $S$ 反而更高（conv4：0.1527 vs 0.1134，高 35%），
   $P(|\hat x|\ge0.9)$ 几乎不变（1e-4~1e-3 量级）。唯一明确不同的地方是空间元素数 16 vs 256
   （`outputs_cifar100/v5_maxpool_mechanism/invariant_frac.csv`）。
3. **不是 max 的选择性**。把 max 换成"窗内 top-2 均值"——它与失真不可交换——
   保护没有消失，$\alpha=+0.3$ 是 42.98，比 max 的 42.47 还高 0.51
   （`outputs_cifar100/v3_methods/simple_cnn_mp_top2_clean_uniform/alpha_sensitivity.csv`）。
   **可交换性不是这条保护的原因**，它只是一条恰好同时成立的数学性质。

**定量分解维持"初步"**。用 3 个种子的 drop@+0.3（剔除各模型 clean 不同带来的干扰）：

| 池化 | seeds | clean | $\alpha=+0.3$ | **drop@+0.3** |
|---|---|---|---|---|
| 2×max（第一阶段 train.py，100ep，异协议，仅作锚点） | 1 | 59.99 | 28.09 | 31.90 |
| **5×avg** | **3** | 61.38 ± 0.07 | 39.61 ± 0.95 | **21.78 ± 0.91** |
| 5×top-2 | 1 | 61.94 | 42.98 | 18.96 |
| **5×max** | **3** | 60.90 ± 0.12 | 40.29 ± 1.58 | **20.60 ± 1.54** |

逐 seed 的 drop：5×max 是 (18.52, 22.20, 21.09)，5×avg 是 (23.05, 20.97, 21.31)。
两者差 **+1.17 个百分点**。

**这个差值能不能被 3 个种子分辨？不能。** 判据要看差值的标准误，不是直接和两组的标准差比。
两组各 3 个种子，差值的标准误
$SE = \sqrt{1.54^2 + 0.91^2}/\sqrt{3} \approx 1.03$，于是 $z \approx 1.17/1.03 \approx 1.1$。
$z \approx 1.1$ 意味着这个差值完全可能来自抽样波动。按跑前写死的判据，
落在 1~2 的点区间内，我们维持"初步"标注，不再补实验
（`experiments_ledger.md` §13.8；判据写在同节前半部分，跑之前就定好了）。

（**更正**：本文初稿在这里写成"该差值小于两组的种子标准差（±1.54 / ±0.91）"。
字面上不成立 —— 1.17 大于 0.91。正确的比较对象是差值的标准误约 1.03，见上。
台账 §13.8 里也是这么写的，已一并改掉。）

用异协议的 2×max 当锚点算占比，降维是 10.12 个点（90%）、非均值聚合是 1.17 个点（10%）。
这个 90/10 和早期的 66/34 之差，全部来自种子抖动和锚点协议不同，不是新发现。

### L9（方法学）指标方差的实测值

这条不是关于模型的，是关于怎么测的。

方差要按**测什么**分开看，因为不同的东西方差差很多。

**(甲) 重训主干的方差**（同一脚本、同一条采样代码路径、换种子重训整个网络；ddof=0）：

| 指标 | 模型 | 3-seed 标准差 | 出处 |
|---|---|---|---|
| clean | CIFAR-100 / SimpleCNN+5×MaxPool / 120ep | **±0.118** | `results_master.csv` 第 66–68 行 |
| clean | CIFAR-100 / SimpleCNN+5×avg / 120ep | **±0.068** | 第 63–65 行 |
| clean | CIFAR-100 / SimpleCNN，plain（逐层独立 α） | **±0.419** | `plain_uniform*`，台账 §13.10 |
| $\alpha=+0.3$ | CIFAR-100 / SimpleCNN，plain（逐层独立 α） | **±0.392** | 同上 |

> **脚注（混协议读数，已降级）**：台账 §12.11 报过一组 "clean ±0.51 / $\alpha=+0.3$ ±3.15"。
> 那 3 个种子里 seed 42 来自 `task2_nat.py`（每 batch 共享一个 α），
> 另两个来自 v3_methods（逐层独立）。**同一协议下尾部方差只有 ±0.392，混协议后被抬到 ±2.561（ddof=0）/
> ±3.15（ddof=1）**，约 6.5 倍。历史值保留在台账 §13.10 的脚注里，作为"协议不纯会估高方差"的实例。

**(乙) 固定主干、只重训读出的方差**（主干不变，换种子训读出）：

| 指标 | 主干 | 3-seed 标准差 | 出处 |
|---|---|---|---|
| clean | SimpleCNN（plain 主干） | ±0.64 | 第 74–75、114–115、116–117 行 |
| clean | SimpleCNN（5×MaxPool 主干） | ±0.19 | 第 104–105、106–107、108–109 行 |
| clean | VGG-11（Exp2 主干） | ±0.26 | 第 80–81、84–85、88–89 行 |
| **clean** | **ResNet-18（Exp3 主干）** | **±1.85** | 第 92–93、96–97、100–101 行 |
| $\alpha=+0.3$ | SimpleCNN（plain 主干） | ±1.31 | 第 121、127、129 行 |
| $\alpha=+0.3$ | VGG-11（Exp2 主干） | ±3.18 | 第 80–81、84–85、88–89 行 |
| $\alpha=+0.3$ | ResNet-18（Exp3 主干） | ±3.40 | 第 92–93、96–97、100–101 行 |

两条规律：

1. **尾部比 clean 抖得多。** 同协议、同一组 run 里，clean 的方差是 ±0.07~1.85，
   尾部是 ±0.39~3.40。**实测范围看，尾部普遍大于同组的 clean。** 由此定下规则：
   **尾部指标（$\pm0.3$）的比较必须至少 3 个种子。单 seed 下小于 3 个点的差值不当结论。**
   补充一条（P1-6 查出）：**算种子方差之前，先确认这几个 run 出自同一脚本、同一采样代码路径。**
   协议不纯会把方差估高约 6.5 倍（实测 0.39 → 2.56）。
2. **深层模型的 clean 也不稳。** 在 ResNet-18 上，同一个固定主干、只换读出种子，
   clean 的三个读数是 63.54 / 67.61 / 63.84 —— 第 3 个比另两个高 2.6~4.1 个点。
   2 个种子时报出来的是 ±0.15，3 个种子下是 ±1.85。所以规则要扩一条：
   **深层模型上，所有指标（包括 clean）都要至少 3 个种子。**
   "干净精度稳定、只有尾部噪声大"这条经验不能外推到深层。

**这里要说明一个限制。** 上表乙组的方差是**读出侧**的（主干固定），
甲组的 ±0.07~0.42 才是全网络重训的方差。两者不能直接比。
**深层主干本身的重训方差从没测过** —— VGG-11 和 ResNet-18 各只有一个训练产物。
所以不能说"深层主干方差更大"，只能说"深层上已知的方差已经到 1.85，主干那一份还是未知数"。
报告里"浅模型 ±0.1~0.6 / 深层 ±1.9、跨 26 倍"这句总结把两类方差混在了一起，
严格说只在量级上有意义。

**关于"加池化会不会引入额外方差"，对照已经闭合（P1-6）。**

同一脚本、同一协议下（`v3_methods_simplecnn.py --mode clean`，CIFAR-100，120ep，三个种子各重训一次）：

| 主干 | 3 个种子的 clean | **3-seed 标准差（ddof=0）** | 3 个种子的 $+0.3$ | $+0.3$ 标准差 |
|---|---|---|---|---|
| plain（2×max） | 60.85 / 61.20 / 60.19 | **±0.419** | 26.03 / 26.73 / 26.95 | ±0.392 |
| 5×max | 60.99 / 60.97 / 60.73 | **±0.118** | 42.47 / 38.77 / 39.64 | ±1.580 |
| 5×avg | 61.34 / 61.48 / 61.33 | **±0.068** | 38.29 / 40.51 / 40.02 | ±0.952 |

出处：`outputs_cifar100/v3_methods/{plain_uniform*,simple_cnn_mp_clean_uniform*,simple_cnn_mp_avg_clean_uniform*}/alpha_sensitivity.csv`
与台账 §13.10。

**结论（按跑前写死的判据 ② 落点）**：$s_{plain} = 0.419 \ge 0.40$，所以：

> **plain 的 clean 方差明显大于两个池化变体（0.419 vs 0.118 / 0.068，都是 n=3）。
> 这提示降维可能同时降低了 clean 的种子方差 —— 标"提示性"，确认需要更多种子。**

方向要说清楚：**不是"加池化增大了方差"，而是加了池化之后方差反而更小。**
一个说得通的猜测是池化把深层空间自由度从 256 降到 16，输出对初始化与数据顺序的敏感度下降。
但**这只是提示**：0.419 vs 0.118 的点估计差异在 n=3 下本身就站不稳，
判据里也明令禁止把它写成定论。

**这一跑还顺带暴露了一个更重要的问题。** 预备时以为"同脚本下 plain 只跑过 2 个种子"，
实际是错的：`--mode budget --profile uniform`（`BUDGET_PROFILES["uniform"] = [0.30]*5`）
与 `--mode plain` 的采样代码**逐字相同**，所以 s42 那一格一直存在，只是记在 `v3_budget_uniform` 名下。
新跑的 `plain_uniform` 与它读数**逐位一致**（best 60.85 @ ep112、7 点扫描全同）
—— 这次训练实际是一次**可复现性验证**。细节见台账 §13.10。

**这条错误的连带后果见下一节（L9 的尾部方差）。**

**这条规则真的改变了结论**。$\pm0.3$ 上一个 4.53 个点的效应，补到 3 个种子后只剩 1.17（§5.2）。
另一个例子：ResNet-18 的 Exp2 与 Exp3 两种主干在尾部差 2.11 个点，落在噪声带里，
只能写成"两者相当"，不能写"Exp2 更优"（台账 §12.11 第 4 条）。

---

## 3. 我们提出了什么办法

四个办法，按重要性排。每个都写清楚：怎么做、涨多少、花多少。

### 3.1 readout-NAT：冻结主干，只重训最后一层

**怎么做**。芯片上的模拟权重一个都不动。把最后一层线性分类器换成新的，
在 $\alpha\sim U[\pm0.3]$ 的失真特征上训练它。训练样本 20k，Adam，24 个 epoch（新协议；
早期是 8 个 epoch）。整个主干冻结，只优化 128×100（或对应类别数）的矩阵。

**效果**。CIFAR-100，全部是 3 个种子的均值：

| 主干 | 该主干自己的头 @+0.3 | readout-NAT @+0.3 | 增量 | 出处（`results_master.csv` 行号） |
|---|---|---|---|---|
| SimpleCNN（clean 训练） | 28.09 | **43.31** | **+15.22** | 74–75、114–115、116–117 |
| SimpleCNN + 5×MaxPool | 42.47 | **49.18** | **+6.71** | 104–105、106–107、108–109 |
| VGG-11（Exp2 主干） | 15.98 | **47.79** | **+31.81** | 80–81、84–85、88–89 |
| ResNet-18（Exp3 主干） | 6.22 | **26.82** | **+20.60** | 92–93、96–97、100–101 |

每一行里"自己的头"那一列是单个主干读出的结果（主干固定，"自己的头"不随种子变）。

CIFAR-10（8 epoch，20k 样本，对干净模型自己的头）：

**CIFAR-10（24 epoch、20k 样本、单 seed）**：

| 架构 | 该主干原头 @+0.3 | readout-NAT(24ep) | 增量 | clean 代价 | mean7 | 对照：8ep 旧读数 |
|---|---|---|---|---|---|---|
| SimpleCNN | 55.08 | **72.51** | +17.43 | −0.84 | 80.80 | 70.44（+2.07） |
| SimpleCNN + 5×MaxPool | 70.59 | **81.60** | +11.01 | −0.63 | 84.87 | 未做 |
| VGG-11 | 36.71 | **79.64** | **+42.93** | −0.58 | 85.61 | 77.59（+2.05） |
| ResNet-18 | 17.09 | **44.37** | **+27.28** | −0.71 | 74.68 | 31.63（**+12.74**） |

出处：`outputs/v3_readout_repair/readout_repair_c10_*_ep24_alpha+0.30_n20000.csv`（新跑）
与 `readout_repair_c10_*_alpha+0.30_n20000.csv`（8ep 旧跑）。

**24ep 在这三个架构上都比 8ep 高**（+2.07 / +2.05 / +12.74），与预登记的方向一致
（台账 §13.9）。**ResNet-18 提升最多**，说明它在 8ep 下欠训练最严重。
另外 clean 代价也变小了（−0.58~−0.84，8ep 时是 −0.66~−3.51）。

**口径说明**：这一节的 CIFAR-10 数字是**单 seed**（seed 42），与第三阶段的 CIFAR-10
读数同口径；CIFAR-100 的那些是 3 seed 均值。两批数不要混着做差。

**跨种子的稳定性**（CIFAR-100 / SimpleCNN / 20k 口径，3 个种子）：
$+0.3$ 的增益是 +13.07 / +10.91 / +13.36，均值 **+12.45 ± 1.31**；
clean 代价是 −6.34 / −5.46 / −5.32，均值 **−6.45 ± 0.15**
（`results_master.csv` 第 121、127、129 行）。增益远大于方差。
**CIFAR-10 侧没有做多种子**，这是它比 CIFAR-100 弱的一处。

**成本**。CIFAR-100 / SimpleCNN 的读出训练 8 epoch 约 4 分钟，24 epoch 约 12 分钟；
VGG-11 约 25 分钟、ResNet-18 约 30 分钟（`第五阶段报告.md` §3）。
对照：全网络 NAT-scratch 120ep 约 60 分钟，200ep 的 Exp3 约 6.5 小时。

**为什么这么做有道理**。依据是 L6：$\alpha=+0.3$ 时特征里还有 47.89% 的线性可达信息，
而冻结头只拿到 27.76%。既然缺的是解码器，那就换一个解码器。
$\alpha=+0.3$ 处 readout-NAT 拿到 44.09%（50k 样本口径），探针的上界是 47.89%，
差约 4 个点。所以这个方法基本吃满了可恢复的那一部分。

**一个反例要写清楚**。把读出只适配到 $\alpha=+0.3$ 这一个点，是错的。
该点确实涨到 52.79（+24.70），但 clean 掉 20.36、$\alpha=-0.3$ 掉 25.71，
七点均值 38.06 反而低于基线 49.29（`results_master.csv` 第 72–73 行与第 120–121 行）。
要在一个区间上训练，不能只对一个点。

**clean 代价能不能消掉**。试过在训练读出时混入干净样本。
$\text{clean\_ratio}=0.3$ 时 clean 回收 0.86 个点但尾部增益掉 2.95；
$=0.5$ 时回收 1.28 但尾部掉 1.47，且两档之间非单调。
判断是权衡中性，默认纯失真分布更好（台账 §9 第 9 条，`results_master.csv` 第 122–125 行）。
后来发现这个代价可以用门控消掉，见 §3.3。

### 3.2 在线估计失真强度

**怎么做**。在特征上拟合岭回归 `Ridge(alpha=1.0)`：
把 13 个 $\alpha$ 点（0, ±0.05, …, ±0.3）上的训练特征拼起来，回归到 $\alpha$ 值本身。
13 × 20k = 26 万个样本，一次闭式解，毫秒级。

**效果**。$R^2$ 0.827~0.935，MAE 0.036~0.061，四个组合全部成立（§2 的 L7 表）。

**成本**。几乎为零。一个 128 维的点积加一个偏置。

**为什么有用**。部署现场不知道失真有多大。有了这个估计器，
就能按估计值在两个头之间做选择，见下一条。

### 3.3 双头 + 门控

**怎么做**。主干上挂两个线性头：

- **干净头**：原来那个，$\alpha=0$ 时最好。
- **鲁棒头**：在 $\alpha$ 混合特征上重训的那个（就是 §3.1 的读出）。

估计出 $\hat\alpha$ 之后按它决定用哪个头。两种做法：

- **硬路由**：$|\hat\alpha|>\tau$ 就用鲁棒头，否则用干净头。$\tau$ 在验证集上选。
- **软门控**：不设阈值，直接在两个头的 logits 上按 $w=\text{clip}(|\hat\alpha|/0.3,0,1)$ 线性插值。

**效果（CIFAR-100 / SimpleCNN+5×MaxPool / 3 seeds）**：

| 配置 | clean | $\alpha=+0.3$ | mean7 |
|---|---|---|---|
| 只用干净头 | **60.99** | 43.21 | 54.12 |
| 只用鲁棒头 | 55.10 | **48.58** | 52.19 |
| 硬路由（预登记配置） | 60.92 | 47.90 | 54.49 |
| **软门控** | 60.70 | **49.10** | **55.08** |
| oracle（每个 $\alpha$ 点取优） | 60.99 | 48.58 | 54.96 |

出处：`outputs_cifar100/v5_full_recipe/recipe_simple_cnn_mp.csv`。

**预登记的那一版没有达标**。跑之前写死的判据是三项同时成立：
mean7 > 54.0、clean > 60.5、$\alpha=+0.3$ > 48.5。硬路由拿到 54.49 ✓ / 60.92 ✓ / **47.90 ✗**，
尾部差 0.60 个点。**这个失败判定维持不变**，判据跑完不改。

原因诊断得很清楚：鲁棒头单独在 $+0.3$ 是 48.58，刚刚过线；路由把它削到 47.90。
$\hat\alpha$ 的 MAE 是 0.0595，所以少数真实 $|\alpha|=0.3$ 的样本被估成 $|\hat\alpha|<0.175$，
被送去干净头，而干净头在 $+0.3$ 只有 43.21。

**软门控是事后变体**。它在看到预登记失败之后才引入，所以它的"三判据全达标"
**不构成独立证据**。这一点在仓库里一直标着。

**软门控相对硬路由还有第二条独立理由：不挑分布。**

这条在第五阶段测过（`outputs_cifar100/v5_alpha_drift/drift_stress.csv`，零 GPU 成本）。
做法是：训练时 $\alpha\sim U[\pm0.3]$、阈值也在同样分布的验证集上选；部署时**不重选超参**，
只把 $\alpha$ 换成新分布，按新分布在 7 个点上给已有结果重新加权。六种分布：
训练分布、只正向、只负向、双峰、尾部集中、单峰。

结果（SimpleCNN+5×MaxPool，3 seeds 均值）：

| 测试分布 | 干净头 | 鲁棒头 | 硬路由 | **软门控** | oracle |
|---|---|---|---|---|---|
| 训练分布 $U[\pm0.3]$ | 54.12 | 52.19 | 54.49 | **55.08** | 54.96 |
| 只正向 | 54.53 | 52.85 | 55.49 | **56.07** | 55.87 |
| 只负向 | 55.43 | 52.25 | 55.10 | **55.50** | 55.56 |
| 双峰 | 53.71 | 52.03 | 53.25 | **54.40** | 54.02 |
| 尾部集中 | 45.63 | **48.57** | 48.10 | **49.11** | 48.57 |
| 单峰 $+0.3$ | 44.51 | 49.08 | 48.57 | **49.83** | 49.24 |

软门控在 **6/6 种分布上都最好**，其中 4 种还超过"每个点取优"的 oracle。
**硬路由在分布漂移下会退化**：尾部集中时它只有 48.10，**低于鲁棒头单独的 48.57**——
阈值是在训练分布上选的，漂移之后 $|\hat\alpha|$ 的边缘分布变了，误路由率上升；
软门控没有硬边界，退化平缓。

**这条理由有边界条件，不能单独引用。** 在 plain 主干上，尾部集中和单峰这两种分布下，
软门控和鲁棒头基本打平（−0.01 / −0.16）。原因是 plain 的干净头在尾部太差（36.14 / 30.57），
没有东西可"集成"。**所以"不挑分布"这个好处也依赖主干自身的尾部可用性**（§4.2）。

另外要说清这条的适用范围：它支持的是**软门控优于硬路由**，
**不**支持"门控优于单头"——后者要看 §4.2 那条干净代价的账。
这条本文初稿只在 §7 提了一句，属于漏写，现补在此处。

**成本**。一个额外的 128×100 线性头（训练 24 epoch，约 12 分钟）加一个岭回归（毫秒）。

### 3.4 MaxPool 密集主干

**怎么做**。改网络定义。在 SimpleCNN 的 conv1/conv3 后和末尾各加一个 MaxPool，
2 个变 5 个，和 VGG-11 对齐。参数量不变（254,084），训练脚本不变。

**效果**。$\alpha=+0.3$ 从 28.09 到 42.47（clean 训练），从 26.03 到 41.55（NAT 训练；两侧同为 `v3_methods` 逐层独立采样）。
mean7 +4.46 / +4.85。干净精度不降反升（+1.00 / +0.48）。
出处：§2 的 L8 表。

**这两个 +14.38 / +20.14 都是单 seed**（台账 §12.9）。
NAT 训练那一行的基线若取 `task2_nat.py` 的 21.41，就是**混协议**（那个脚本每批共享一个 α，见 §8 勘误 3）。
本文改用**同协议**的 3 seed 均值 **26.57**（26.03 / 26.73 / 26.95，逐层独立采样）后，
架构带来的增益是 **约 +15.0**（41.55 − 26.57），不是 +20.14。
若按同 seed 配对（mp 的 seed 42 对 plain 的 seed 42）则是 **+15.5**（41.55 vs 26.03）。
mp-NAT 那一侧只有单 seed，尾部噪声 ±1.58，所以记成 **约 +15 ± 1.6**。

**成本**。零。不动训练，不动数据，不花 GPU 时间。

**和读出手段同时用，比任何一个单独用都高 —— 但不是严格相加。**

这一条要先把口径说清楚，因为涉及两个不同的读出实现和两条不同的评估路径。

| 配置 | $+0.3$ | 口径 |
|---|---|---|
| ① 基线：plain 主干 + 原头 | 28.09 | v3 路径，连续均匀 α 头 |
| ② 架构单独：5×max 主干 + 原头 | 42.47 | v3 路径 |
| ③ 读出单独：plain 主干 + 连续 α 头，24ep | 43.38 | v3 路径 |
| ④ **架构 + 读出：5×max 主干 + 连续 α 头，24ep** | **49.12** | v3 路径，单 seed |

③ 和 ④ 的读数来自 `outputs_cifar100/v3_readout_repair/readout_repair_c100_clean_ep24_alpha+0.30_n20000.csv`
与 `readout_repair_c100_mp_clean_alpha+0.30_n20000.csv` 的 `e_readout_NAT` 行。
**49.12 是"连续均匀 α、每 batch 重采样、24 epoch、20k 样本、单 seed"这个口径的数**，
评估走 `v3_readout_repair` 的整模型前向路径。

对照：§3.3 里的 **49.10** 是**另一个口径** —— 13 点离散 α 池、3 seed 均值、
评估走 `v5_full_recipe` 的手工线性头路径，而且是软门控（双头插值）而不是单头。
两个数差 0.02，但它们不是同一个东西（§6 第 7 条已记录两条路径最多差 1.2 个点）。

**"同时用"的结论在两个口径下都成立，但两个口径给出的都是"次可加"，不是相加：**

| 口径 | 基线 | 架构单独 | 读出单独 | 两者 | 单独之和的预期 | 实际 |
|---|---|---|---|---|---|---|
| v3（连续 α 头，单 seed） | 28.09 | 42.47 (+14.38) | 43.38 (+15.29) | **49.12 (+21.03)** | 57.76 | **少 8.64** |
| v5（13 点离散头，3 seed） | 28.19 | 43.21 (+15.02) | 46.50 (+18.31) | **48.58 (+20.39)** | 61.52 | **少 12.94** |

v5 口径的四个数全部取自 `outputs_cifar100/v5_full_recipe/`（`acc_clean_head` 与 `acc_robust_head`
在 $\alpha=+0.3$ 上的 3 seed 均值）。

所以准确的说法是：**两个手段在尾部可以同时受益，但它们不独立相加**——
两个手段各自的单点增益之和为 **+29.67**（v3 口径：+14.38 +15.29）/ **+33.33**（v5 口径：+15.02 +18.31）个点，
合起来拿到其中的**六到七成**（**+21.03 / +20.39**），**损失三到四成**。

拆开看合起来比单独用高多少（v3 口径 / v5 口径）：

| 比什么 | v3（连续 α 头，单 seed） | v5（13 点离散头，3 seed） |
|---|---|---|
| 比**两者中较好的**单独用法 | +5.74（49.12 vs 43.38） | +2.08（48.58 vs 46.50） |
| 比**两者中较差的**单独用法 | +6.65（49.12 vs 42.47） | +5.37（48.58 vs 43.21） |
| 比**基线** | **+21.03**（49.12 vs 28.09） | **+20.39**（48.58 vs 28.19） |

台账 §12.10 里"叠加"这个词指的是"能同时受益"，不是数学意义上的相加。

**方向更正**：本文此前写成"合起来只拿到单独之和的三到四成"，**把分数说反了**。
按上表，拿到的是**六到七成**（21.03/29.67 = 70.9%，20.39/33.33 = 61.2%），
损失的是**三到四成**。

均值上没有同时受益（④ 的 53.41 < ② 的 53.75），因为读出的干净代价吃掉了收益。
这个代价可以用门控拿回来。

---

## 4. 这些办法在哪些条件下成立

### 4.1 readout-NAT 的适用范围

**这一节的每个数字都标了口径（数据集、epoch、seed 数），因为不同批次的协议不一样。**

成立的地方：

- **三个主干家族 × 两个数据集全部成立。** CIFAR-100（24ep、20k、3 seed，对各自原头）：
  SimpleCNN +15.22、5×MaxPool +6.71、VGG-11(Exp2 主干) +31.81、ResNet-18(Exp3 主干) +20.60。
  CIFAR-10（24ep、20k、**单 seed**，对各自原头）：SimpleCNN +17.43、5×MaxPool +11.01、
  VGG-11 +42.93、ResNet-18 +27.28。
- **8 epoch 口径下的跨架构增益**（第三阶段，20k，单 seed）：CIFAR-100 是 +13.07 / +23.22 / +13.57，
  CIFAR-10 是 +15.36 / +40.88 / +14.54。**这是旧协议的读数，只能当同协议内的相对比较用**
  （§12.4 已证 8ep 是欠训练的下界）。
- **跨种子稳健**（CIFAR-100 / SimpleCNN / 20k，3 seed）：+12.45 ± 1.31。
  CIFAR-10 侧没做多种子。

边界要说清楚：

- **增益取决于原头在尾部丢了多少，不是一个固定数。**
  实测范围是 **+6.71 到 +42.93**。同一个 VGG-11，在 CIFAR-10 上是 +42.93（原头只有 36.71），
  在 CIFAR-100 的 Exp2 主干上只有 +31.81；而 5×MaxPool 主干只有 +6.71，
  因为它的原头本来就有 42.47。所以"提升 13~26 个点"这类说法不成立。
- **恢复率随任务变易而上升**（这一条只在 **8ep 同协议**下测过，因为要两端可比）：
  readout-NAT 精度除以 clean 基线，CIFAR-10 是 83.0% / 86.4% / 34.3%，
  CIFAR-100 是 68.6% / 59.8% / 22.6%。任务越容易，恢复得越彻底。
  24ep 下两端都更高（CIFAR-10 是 85.4% / 88.7% / 48.2%），但 CIFAR-100 侧没有
  与 CIFAR-10 完全对应的主干配对，所以标度律的定量比较仍以 8ep 那组为准。
- **clean 有代价**：CIFAR-100 是 −3.19 ~ −6.67 个点，CIFAR-10 24ep 是 −0.58 ~ −0.84。
  混入干净样本的缓解办法无效（§3.1）。能消掉它的只有门控。
- **依赖读出层可训练**。如果芯片把读出也固化在阵列里，这个方法不能用。
- **CIFAR-10 的读出只跑了单 seed**，不像 CIFAR-100 有 3 seed。
  想做两端的等强度比较，得把 CIFAR-10 也补到 3 seed。

### 4.2 门控的适用范围：同一个办法在两个数据集上一个有效一个无效

这是第五阶段最值得写清楚的一件事。

**CIFAR-100 上有效**：软门控 mean7 55.08，比只用一个头都好，也超过"每个点取优"的 oracle 54.96。

**CIFAR-10 上无效**。同一套超参、同一套流程，只换数据集：

| 配置 | clean | $\alpha=+0.3$ | mean7 |
|---|---|---|---|
| mp·只用干净头 | **87.48** | 70.34 | 82.68 |
| mp·只用鲁棒头 | 86.61 | **82.91** | 85.02 |
| mp·硬路由 | 87.15 | **82.91** | 85.12 |
| mp·软门控 | 87.42 | 82.56 | **85.19** |
| oracle | 87.48 | 82.91 | 85.26 |

出处：`outputs/v5_full_recipe/recipe_simple_cnn_mp.csv`。

软门控和单鲁棒头几乎分不出来：mean7 差 0.17，$+0.3$ 上鲁棒头反而高 0.35。
软门控也不再超过 oracle（−0.07），而在 CIFAR-100 上它是超过的（+0.12）。
在 plain 主干上更差：软门控 mean7 82.20，低于鲁棒头 82.48、低于硬路由 82.52。

**为什么会这样**。门控做的事只有一件：**把干净头的能力收回到鲁棒头上**。
它能收回来的上限，就是鲁棒头自己赔掉的干净精度。

| 主干 / 数据集 | 鲁棒头赔的 clean | 门控的净收益（mean7） |
|---|---|---|
| CIFAR-100 · mp | **−5.89** | **+2.90** |
| CIFAR-10 · mp | **−0.87** | **+0.17** |
| CIFAR-10 · plain | **−0.54** | **−0.28** |

CIFAR-100 类别多、尾部难，鲁棒头必须赔掉 5.89 个点的干净精度，所以门控有东西可收。
CIFAR-10 上鲁棒头的干净代价只有 0.87，几乎没东西可收，于是门控退化成噪声源。

**这条修正了原来的说法**。第三阶段曾经说"clean 代价是方法的固有属性"。
第四阶段发现门控能消掉它。第五阶段把范围收窄：能消掉，但只在代价大的时候值得消。

**CIFAR-10 的判据情况**。第五阶段在 CIFAR-10 上做了一次真正的事前预登记：
判据 C1–C5 在跑之前写死，超参全部锁定（`experiments_ledger.md` §13.7 前半部分）。
结果 C1 ✓（85.19 > 85.02）、C2 ✓、C3 ✓、**C4 ✗（7 个 $\alpha$ 点里只有 1 个超过两边较优者，
CIFAR-100 上是 3 个）**、C5 ✓。按跑前规则，C1∧C2∧C3 成立就算复现成立。

**但当时只跑了 1 个主干 seed，所以这个"通过"要复查（P2-7）。**
C1 的裕度只有 +0.17 个点，而 SimpleCNN 家族的主干级 mean7 噪声在 ±0.18~0.58（§1.6 的部署表）。
于是补训了 mp 主干的 seed 43/44，每个主干各跑一次完整配方，
按跑前写死的判据看 $m_i = \text{mean7}(\text{软门控}) - \max(\text{mean7 干净头}, \text{mean7 鲁棒头})$：

| 主干 seed | head seeds | mean7 干净头 | mean7 鲁棒头 | mean7 软门控 | **$m_i$** |
|---|---|---|---|---|---|
| 42 | 3 | 82.6843 | 85.0186 | 85.1871 | **+0.1686** |
| **43** | 1 | 82.1914 | 84.9043 | 84.9014 | **−0.0029** |
| 44 | 1 | 82.6100 | 85.0029 | 85.1743 | **+0.1714** |

出处：`outputs/v5_full_recipe/recipe_simple_cnn_mp{,_bs43,_bs44}.csv`；台账 §13.11。

**判据 ② 命中**（$m_{43} \le 0$）：**C1 从"弱通过"改判为"不可分辨"。**

三个数读起来是：**两个 +0.17、一个 −0.003**，差值在 0 ~ +0.17 之间飘，三个主干种子里有一个正好落在 0 上。
平均 +0.112，全部远小于主干级的 mean7 噪声。
所以 CIFAR-10 上准确的定性是 **"软门控与单头不可区分"**，
不是"软门控略好"，也不是"软门控更差"。**部署推荐不变**（CIFAR-10 一直推荐单鲁棒头），
改的只是措辞 —— 这正是预登记时写好的两条分支。

**门控什么时候值得装：七个点，两个前提。**

口径：代价 = 鲁棒头在 $\alpha=0$ 的 clean − 干净头在 $\alpha=0$ 的 clean（负 = 鲁棒头更差）；
红利 = mean7(软门控) − mean7(鲁棒头)。

| 点 | 鲁棒头的 clean 代价 | 门控红利 | 出处 |
|---|---|---|---|
| CIFAR-100 / mp | **−5.89** | **+2.90** | `outputs_cifar100/v5_full_recipe/recipe_simple_cnn_mp.csv` |
| **VGG-11 / Exp2** | **−3.45** | **+0.68** | `outputs_cifar100/v5_full_recipe/recipe_robust_vgg11_exp2vgg11.csv` |
| **ResNet-18 / Exp3** | **−4.36** | **−1.14** | `...recipe_robust_resnet18_exp3resnet18.csv` |
| CIFAR-10 / mp，主干 42 | −0.87 | +0.17 | `outputs/v5_full_recipe/recipe_simple_cnn_mp.csv` |
| CIFAR-10 / mp，主干 43 | −1.04 | −0.00 | `..._bs43.csv` |
| CIFAR-10 / mp，主干 44 | −0.84 | +0.17 | `..._bs44.csv` |
| CIFAR-10 / plain | −0.54 | −0.28 | `outputs/v5_full_recipe/recipe_simple_cnn.csv` |

（后四行与第一行是第五阶段的结果；**中间两行是第六阶段 P2 新加的**，
它们正好落在原来空着的 2~5 pp 那一段 —— 之前最想知道的就是这一段。）

**七点的 Pearson $r = -0.533$；把 CIFAR-100 那个远点去掉，n=6 时 $r$ 变成 $+0.351$ —— 符号反转。**
也就是说，在 CIFAR-10 与两个深层主干这六个点上，**代价越大，红利反而略微越小**。
原来看到的 $r=-0.994$（第五阶段，n=5）**完全靠 CIFAR-100 一个点撑着**。

**所以"红利 = 代价回收"这条关系不成立。** 正确的表述是两个前提**同时**要满足：

1. **鲁棒头的 clean 代价要够大** —— 代价太小（≲1 pp）就没有东西可回收，
   门控退化成噪声源：CIFAR-10/mp 的 −0.87 → +0.17，−0.84 → +0.17，plain 的 −0.54 → −0.28。
2. **两个头在整个 α 轴上都不能"崩掉"** —— 这一条是第六阶段新查出来的，
   CIFAR-100/mp 与 CIFAR-10 都满足，所以之前没暴露。

**ResNet-18 违反第二条，红利是负的（−1.14）。** 逐 α 看原因很清楚：

| α | 干净头 | 鲁棒头 | 软门控 | 软门控 − max(两头) |
|---|---|---|---|---|
| **−0.2** | **10.27** | **45.64** | **39.21** | **−6.43** |
| −0.1 | 46.61 | 60.96 | 57.01 | −3.95 |
| −0.3 | 2.77 | 28.81 | 27.62 | −1.19 |
| +0.2 | 25.63 | 56.86 | 54.16 | −2.69 |
| 0.0 / +0.1 | 72.69 / 65.94 | 68.33 / 67.31 | 72.78 / 69.20 | +0.09 / +1.89 |

ResNet-18 的**自带头在负侧是崩的**（−0.2 只有 10.27、−0.3 只有 2.77）——
这是 Exp3 非对称采样故意把资源投给正侧的结果。
软门控按 $w=|\hat\alpha|/0.3$ 插值，在真实 $\alpha=-0.2$ 处 $w\approx0.67$，
于是 logits = 0.33 × 坏头 + 0.67 × 好头，**三分之一权重的坏头就把 45.64 拖到 39.21**。
**插值没有任何机制知道"其中一个头在这个 $\alpha$ 上是坏的"。**

VGG-11 两头都可用（负侧 −0.2 = 51.54、−0.3 = 34.79），所以插值拿到 +0.68，
在 4/7 个点上超过两头较优者 —— 小，但是正的。

**结论**：软门控维持 **"事后变体 + CIFAR-100 特化"** 的定性，不升格。
CIFAR-100 式的细粒度任务上它有效（+2.90）；CIFAR-10 上**与单头不可区分**；
**深层主干上不能默认它会帮忙，必须先查两个头的逐 α 曲线**（ResNet-18 上是 −1.14）。

> **审计标注**：这一节触及 L7 表述的改写，是按 P2 预登记的第二支执行的
> （该支在跑前就写明"代价变大但红利没变大 → 加强'不是线性'"）。台账 §14.2 结果。


### 4.3 架构手段的适用范围

5×MaxPool 在 CIFAR-100 上 +14.38（clean 训练）/ +15.52（NAT 训练，同协议同 seed 配对），在 CIFAR-10 上
干净头同协议对比是 $+0.3$ 从 57.19 到 70.34（+13.15）、mean7 从 76.98 到 82.68（+5.70）。
两个数据集都成立，量级相当。

代价是零。这条是四个办法里唯一不花训练成本的。

### 4.4 两条通路同时存在时怎么做

| 训练条件 | clean | $\alpha=+0.3$ | $\sigma=0.3$ | 双扰动最坏点均值 |
|---|---|---|---|---|
| 不训练（clean 模型） | 59.99 | 28.09 | 3.98 | 16.04 |
| 只做 $\alpha$ 训练 | **60.85** | 26.03 | **4.40** | 15.22 |
| **只做 $\sigma$ 训练** | 55.79 | **22.01** | 27.19 | **24.60** |
| 两个都做（joint） | 55.20 | 15.27 | **31.31** | 23.29 |

出处：`results_master.csv` 第 56、58、59 行；台账 §9.8。

双扰动并存时最优的是 **只做 $\sigma$ 训练**，不是两个都做。
joint 里的 $\alpha$ 注入换来的 $\sigma$ 侧收益只有 +4.12，却付出 $\alpha$ 侧 −6.74 的代价。
所以工程建议是：要同时防两种非理想，只做高斯增强训练，不要叠加非线性注入。

---

## 5. 我们排除了什么

负结果和正结果一样重要。下面写清楚每一条为什么走不通，用的是哪些数字。

### 5.1 训练端四条路，全部失败

同一个 harness（SimpleCNN / CIFAR-100 / 120ep / seed 42），只改采样或调度。
基准是均匀采样（`budget_uniform`）。

| 编号 | 做法 | clean | $\alpha=+0.3$ | 相对基准 | 成本 | 出处 |
|---|---|---|---|---|---|---|
| 基准 | 逐层独立 $\alpha\sim U[\pm0.3]$ | 60.85 | 26.03 | — | 60 min | `results_master.csv` 第 56 行 |
| ① | 预算分配：浅层多 | 60.72 | **19.56** | **−6.47** | 52 min | 第 53 行 |
| ① | 预算分配：深层多 | 60.90 | 25.46 | −0.57 | 51 min | 第 54 行 |
| ① | 预算分配：按梯度敏感性 | 60.07 | 26.13 | +0.10 | 51 min | 第 55 行 |
| ② | $\alpha$ 对抗训练（PGD×2） | 60.27 | 24.32 | −1.71 | **136 min** | 第 52 行 |
| ③ | 课程式 $\alpha$（$\pm0.05\to\pm0.3$） | 60.25 | **21.78** | **−4.25** | 64 min | 第 57 行 |
| ④ | 训练范围扩到 $\pm0.5$ | 61.03 | **20.48** | **−5.55** | 41 min | 第 62 行 |

四条路各自的原因：

- **预算分配不是有效手段**。四个轮廓（均匀 / 浅层多 / 深层多 / 按敏感性）没有一个达到
  预登记的 2 个点的判据。最好的"深层多"是 mean7 +0.75，仍在阈值内。
  方向性的收获是：预算往深层压优于往浅层压。另外，按梯度敏感性分配（HAWQ 式的思路）
  在这个设定下无效，因为敏感性估计集中在 conv2/3/4，而单点损伤集中在 conv2/3，两者排序不一致。
- **对抗训练没必要**。$\alpha$ 只有 5 维，是"资源型"扰动，不是对抗样本那种精细方向。
  最坏情形训练只是把模型推向更保守的解，不产生净收益，而且贵 2.3 倍。**省掉 PGD 内循环就好。**
- **课程式反而有害**。$\alpha$ 范围扩到 $\pm0.3$ 时，CosineAnnealing 的学习率已经接近 0，
  模型来不及适应。从宽范围直接起步才是对的。
- **扩大训练范围救不了正侧**。这是四条里最强的一条。即使模型在 $\pm0.5$ 上训练过，
  测试 $\alpha=+0.5$ 仍只有 2.71%、$\alpha=+0.6$ 只有 1.30%；同期 $\alpha=-0.5$ 有 31.19%。
  所以正侧尾部**不是"训练时没见过"的问题**。

**为什么正侧这么难**。有两个层面的证据。

幅度层面：把每层输入的 L2 范数量出来（`outputs_cifar100/v4_activation_stats/act_stats.csv`）：

| 层输入 L2 | clean | $\alpha=+0.3$ | $\alpha=-0.3$ |
|---|---|---|---|
| conv1 | 53.376 | 53.376 | 53.376 |
| conv2 | 93.012 | 84.089 | 103.396 |
| conv4 | 31.579 | **19.681** | 40.020 |
| fc | 24.261 | **12.532** | 38.007 |

归一化三次映射在端点 $|\hat x|=1$ 处恒等，所以每个样本的最大值不变。
它做的事是把中段的质量重新分配：**$+\alpha$ 压低中段（压缩），$-\alpha$ 抬高中段（放大）**，
而且随深度累积（fc/conv1 的比值：clean 0.45 → $+0.3$ 时 0.23 → $-0.3$ 时 0.71）。

维度层面：把 fc 输入特征的有效秩算出来（`outputs_cifar100/v5_rank_collapse/rank_stats.csv`）：

| $\alpha$ | 有效秩 | 相对 $\alpha=0$ 的损失 | 条件数 κ(99%) | Fisher |
|---|---|---|---|---|
| 0.0 | 38.78 | — | 18.40 | 0.49096 |
| **+0.3** | **30.78** | **−20.6%** | 20.48 | 0.45669 |
| **+0.5** | **19.04** | **−50.9%** | 25.93 | 0.36248 |
| −0.3 | 38.00 | −2.0% | 19.36 | 0.39061 |
| −0.5 | 33.27 | −14.2% | 21.25 | 0.32953 |

正侧的秩塌陷比负侧严重得多。在 $\pm0.3$ 这一对上差 **10.3 倍**（20.6% vs 2.0%），
在 $\pm0.5$ 这一对上差 **3.6 倍**（50.9% vs 14.2%）——
两对的倍数不一样，所以标准说法是"同一 $\alpha$ 幅度下正侧明显更严重"，
不要说"一律 10 倍以上"。而且 **Fisher 判据在 $+0.3$（0.45669）反而高于 $-0.3$（0.39061）**，
可精度是 28.09 对 42.91。所以坏的形态是**维度丢了**，不是**类间距离变小**。

这和幅度压缩是同一件事的两面：中段被压低 → 激活质量挤到少数方向上 → 有效秩下降。

**读出的可恢复性和这个不冲突**。秩掉到 30.78 之后，在剩下的低秩子空间里信息仍然线性可达，
所以重训读出还能拿回 +20.60 个点。真正不可训练的是**主干参数空间**，不是特征。

### 5.2 池化保护的三个候选原因，逐一排除

见 L8。三条排除线的依据分别是：

1. 可交换性：机器精度 0.000e+00（`commutativity.csv`）。既然可交换，池化不可能"吸收"失真。
2. 不变区集中：$S$ 更高、不变区占比不变（`invariant_frac.csv`）。
3. max 的选择性：top-2 与失真不可交换，保护却与 max 等同（42.98 vs 42.47）。

**这里要说清楚一条**。前两条建立在 6 档 $\alpha$ × 8 层的完整测量上，数据量足够。
第三条只用了 **单 seed**。top-2 比 max 高 0.51 个点，落在噪声带内。
所以准确的表述是：**没有证据支持"max 有独占的额外贡献"**，
而"top-2 和 max 一样好"这个正面结论只有 1 个种子。

**早期的一个说法被推翻了**。第五阶段最早写出的是"保护 = 降维 71% + max 算子特性 29%"。
top-2 的结果出来之后，这个 71/29 的命名不成立 —— top-2 与 max 的降维程度完全相同，
max 的那 29% 找不到依据。改成 drop 口径后，用异协议的 2×max 当锚点，单 seed 算出来是 66/34、3 seed 算出来是 90/10，
但 3 seed 分辨不出来（两组之差 +1.17，差值的标准误约 1.03、z≈1.1），所以最终维持"初步"。

顺带发现早期的 4.53 个点是采样碰巧。3 个 seed 里，seed 42 的 5×max drop 是 18.52，
恰好是三个里面最低的那个（另两个是 22.20 和 21.09）。这就是为什么单 seed 的效应不能信。

### 5.3 其他被推翻的说法

按发现顺序。每条写清楚当初为什么信、后来怎么发现不对。

**① "校准模块导致欠拟合"**

*当初为什么信*：SimpleCNN + 校准的训练精度卡在 75.6%，看起来像模块压制了拟合能力。

*怎么发现不对*：把各 run 的训练精度列出来对比（台账 §8）：

| run | 失真输入下的训练精度 |
|---|---|
| NAT-scratch（**无校准**） | 72.41 |
| NAT-finetune（**无校准**） | 74.22 |
| Exp1 / Exp2 / Exp3（**都有校准**） | 75.60 / 76.04 / 75.93 |
| VGG-11 NAT-scratch（**无校准**，9.9M 参数） | **99.97** |

> 口径注：这三行 NAT 训练精度来自 `task2_nat.py`（**granularity=shared**，每批共享一个 α）。
> 结论靠的是 0.25M vs 9.9M 的容量对比，不依赖粒度；但绝对数值带这个口径。

无校准的反而更低。真正的限制是**容量**：0.25M 的 SimpleCNN 在"100 类 + 逐层失真"下
拟合不了训练集，9.9M 的 VGG-11 能到 99.97%。

**② "$+\alpha$ 是放大器"**

*当初为什么信*：三次项 $x^3$ 在正侧单调增，直觉上会放大信号。

*怎么发现不对*：把每层输入的幅度量出来（§5.1 的表）。$+\alpha$ 把中段的 L2 压低一半
（fc 输入 24.261 → 12.532，p99 从 4.78 到 2.68），$-\alpha$ 才是抬高的一方（→ 38.007）。
方向完全相反。原因是逐样本 max 归一化把端点钉住了，$x^3$ 项在中段起的是压低作用。

这个纠正带来了正确的解释：$+\alpha$ 系统性压小深层特征的幅度和对比度，
类间可分性被压平，所以正侧更致命。

**③ "移动目标假说"（NAT 主干难适配）**

> 口径注：这里的 NAT-scratch **主干**是 `task2_nat.py` 训出的固定产物（granularity=shared）。
> 「读出预算」这条结论不依赖粒度，但该主干的所有数字带这个口径。

*当初为什么信*：2×2 分解里，（NAT 主干 + readout-NAT）在 $+0.3$ 是 35.90，
（clean 主干 + readout-NAT）是 41.16，差 5.26。看起来像 NAT 训练把特征变成了移动目标。

*怎么发现不对*：把读出训练从 8 epoch 提到 24 epoch，两个主干各跑一遍。
NAT 主干追回 3.48 个点（35.90 → 41.60），clean 主干追回 2.22（41.16 → 43.38），
差距缩到 **−1.78**，落在种子噪声里（读出侧 ±1.31）。
所以**三分之二的差距只是读出训练轮数不够**（台账 §12.4）。
这个发现顺带说明：第四阶段之前所有 readout-NAT 的结果都是欠训练的读数，应当作下界看。

**④ "joint 训练在双扰动场景下最优"**

*当初为什么信*：joint 在 $\sigma=0.3$ 上拿到 31.31，比 clean 模型的 3.98 高太多。
拿 clean 模型的 $\sigma$ 值当作"只做 $\alpha$ 训练"的对照，算出双扰动最坏点均值 23.29 最优。

*怎么发现不对*：补上缺的两格 —— 真的 $\alpha$-only 训练后 $\sigma=0.3$ 是 4.40（不是 3.98），
以及 $\sigma$-only 这一格。补完之后双扰动最坏点均值最优的是 **$\sigma$-only 的 24.60**，
joint 的 23.29 反而低。结论反转为：joint 里的 $\alpha$ 注入是净负担。

**⑤ "fc 层是最脆弱的层"**

一阶段说 fc 最脆弱。第三阶段把注入位置和观测位置分开测之后发现：
从**注入位置**看，最敏感的是 conv2（−12.43）和 conv3（−7.56），fc 单点注入只掉 0.68；
从**观测位置**看，全层失真时 fc 的输出偏移最大。
两句话都对，但必须说清楚是在哪一头说的（台账 §2.2）。

**⑥ "插值即集成"**

*当初为什么信*：CIFAR-100 上软门控在 $-0.3$ / $+0.2$ / $+0.3$ 三个点上，
同时超过干净头和鲁棒头两者的较优者（49.12 > 48.56、55.16 > 54.09、49.10 > 48.58），
mean7 55.08 也超过"每个点取优"的 oracle 54.96。看起来两个头的错误模式不相关，
线性组合因此比硬性选择更好。

*怎么发现不对*：在 CIFAR-10 上按事前预登记重跑同一套办法（超参全部锁定）。
超过两边较优者的点数从 3/7 掉到 **1/7**；
软门控相对 oracle 从 $+0.12$ 变成 **−0.07**；
门控的净收益从 $+2.90$ 掉到 $+0.17$；在 plain 主干上甚至是负的（−0.28）。

所以"插值即集成"没有复现。复现的只是**弱支配**（软门控不比任何单头差），
而这一条在裕度只有 +0.17 个点、只有一个主干种子的情况下也说不太响。
替换成的说法是 §4.2 那条：门控的净收益等于鲁棒头赔掉的干净精度。

**⑦ "71/29 分解"**

见 §5.2。

---

## 6. 局限与没解决的问题

按重要性列。每条尽量写清楚差多少、为什么没做。

**1. L8 的定量分解没有定下来。** 降维和非均值聚合这两个成分的比例，
3 个种子分辨不出来（差值 +1.17，差值的标准误约 1.03、z≈1.1；两组种子标准差为 ±1.54 / ±0.91）。
要把它定下来，按 $\sigma\approx1.2$、想分辨 1.2 个点估算，需要每组 8 个种子以上。
这条路的根本问题是 avg 和 max 的差距本身就太小。
**能定下来的只有方向：保护主要来自降维。**

**2. 软门控是事后变体，而且是 CIFAR-100 特化的。** 它是在看到硬路由失败之后才引入的。
CIFAR-10 上补到 3 个主干种子后（P2-7），主判据 C1 **改判"不可分辨"**
（$m$ = +0.169 / −0.003 / +0.171）。所以它在 CIFAR-10 上的定性是
**"与单头不可区分"** —— 既不能说好，也不能说差。
**"事后变体 + CIFAR-100 特化"这个措辞维持不变。**
剩下没测的是 VGG-11 / ResNet-18 这类深层主干上的门控行为（那里鲁棒头的 clean 代价未知，
可能又是另一个量级）。

**3. 深层主干只有单 seed。** VGG-11 和 ResNet-18 的主干是固定的单次训练产物。
读出侧的 3 seed 只反映读出训练的数据顺序和 $\alpha$ 采样随机性，**不包含主干训练方差**。
而深层上已知的方差已经不小：ResNet-18 固定主干上换读出种子，clean 就能从 63.54 摆到 67.61
（±1.85），尾部摆到 ±3.40。**主干那一份还没测过。**
所以深层的结果里，凡是小于 3 个点的差值都不该当结论。
要把这一块补上，得对每个深层主干重训 2 次（VGG-11 200ep 约 4 小时 ×2，ResNet-18 同）。

**4. 秩塌陷只在浅层、单模型、单数据集上测过。** 有效秩的测量只用了 CIFAR-100 的干净
SimpleCNN（128 维 fc 输入）。VGG-11 / ResNet-18 的特征维度不同，没有测。
所以"正侧塌陷的是维度"这条只在 128 维、4 层卷积的模型上验过。

**5. CIFAR-10 的配方：mp 侧已有 3 个主干种子，plain 侧还是单 seed。**
P2-7 把 mp 主干补到了 3 个（§13.11），所以 §4.2 的门控结论建立在 3 个主干种子上。
但 **plain 主干的那套配方（§4.2 表里的 ⑤–⑧ 行）仍然是单主干 seed**，
CIFAR-10 的读出适配（§3.1 表）也全部是单 seed。这两块没补。

**6. top-2 池化只有单 seed。** 它在四种池化里 mean7 最高（54.70），但没复核。
"top-2 与 max 等价"这个判断建立在 0.51 个点、单种子的差值上。

**7. 两条读出评估路径有系统偏差。** `v3_readout_repair`（整模型前向）和
`v5_full_recipe`（手工构造线性头）在非零 $\alpha$ 处差最多 1.2 个点，$\alpha=0$ 处严格一致。
跨这两条路径比较数字时要小心。本文里的消融阶梯（①–⑧）都取自同一条路径，不受影响。

**8. CIFAR-10 的读出适配此前只有 8 epoch 的口径（已补）。** 这条本文初稿写成"CIFAR-10 上没有
5×MaxPool 主干的 readout-NAT，这块是空的"，**两处都不准确**，现更正：

- **"空的"说得太满。** `outputs/v5_full_recipe/recipe_simple_cnn_mp.csv` 的 `acc_robust_head` 列
  就是在 mp 主干上训的读出（$+0.3$ = 82.91，3 seed 均值）。它和 `v3_readout_repair` 口径的差别有**两点**，
  不只是采样方式：
  1. **α 的取法不同** —— `v5_full_recipe` 用 13 点离散池（每点 2 万样本拼成 26 万固定特征集），
     `v3_readout_repair` 是**每个 batch 重采样的连续均匀** $\alpha\sim U[\pm0.3]$；
  2. **头看到的数据量不同** —— 前者 24 epoch × 26 万样本，后者 24 epoch × 2 万样本，差 13 倍。
  所以正确的说法是：**"缺的是连续 α 采样口径的版本"**，而不是"没有"。
- **CIFAR-10 的 8 epoch 版本也是欠训练的**，不能与 CIFAR-100 的 24ep 并列引用。
  补齐后 24ep 的读数确实更高（见 §3.1 的表）。

补齐的四个 run 登记在台账 §13.9，协议与 CIFAR-100 的 24ep 行逐字一致，主干不重训。

**9. 池化保护的 CIFAR-10 侧只有架构读数。** 2×max / 5×avg / 5×top2 四行对照
只有 CIFAR-100 的，CIFAR-10 上只有 mp 与 plain 两行。

**10. Dithering 的标度律只有两个数据点。** "增益随可用头部空间缩放"这条，
CIFAR-10 是 +6.53、CIFAR-100 是 +2.84，两点连线。
没有第三个难度档来验证这个关系的形状。

**11. 训练的种子方差没有系统性标定。** 做了 3 seed 的只有几处：CIFAR-100 的
plain / 5×max / 5×avg 主干（clean ±0.42 / ±0.12 / ±0.07，尾部 ±0.39 / ±1.58 / ±0.95）、
读出适配（clean ±0.19~1.85，尾部 ±1.31~3.40）。其余大量结论建立在单 seed 上，
包括**全部 CIFAR-10 的训练结论**和**VGG-11 / ResNet-18 的主干本身**。
（台账 §12.11 那组 ±3.15 是混协议读数，已降级，见 §8 勘误 3。）

**12. 实验过程中的三次事故，已披露。**

- 并发 4 个带 DataLoader worker 的训练任务导致 Windows 提交内存耗尽（error 1455），
  中断了 Exp3 训练与两个探针。之后改为严格串行
  （`第三阶段报告.md` §7）。
- 读出脚本早期版本误用 `model.fc(f)`，会二次触发 fc 上的失真钩子（双重失真），
  使 $\alpha=-0.3$ 处偏低 2.34 个点。修正为 `F.linear(f, w, b)` 并重跑后，
  数据与台账逐位一致（台账 §9 第 10 条）。
- 第五阶段出现过一次 CUDA 显存不足，原因是 resnet18 与 top2 同时训练，已单独补跑。

**13. 一阶段"3.4 倍不对称"的表述需要收紧。** 它是 CIFAR-10 / SimpleCNN / $|\alpha|=0.3$
这一个点的比值。换成 CIFAR-100 同模型是 1.87。跨阶段引用时应写明条件。

---

## 7. 下一步

按价值排序。每条写清楚要花多少。

**1. ~~把 CIFAR-10 的主干补到 3 个种子~~ 已完成（P2-7，台账 §13.11）。**
结论：C1 改判"不可分辨"（$m$ = +0.169 / −0.003 / +0.171）。软门控在 CIFAR-10 上定性为
"与单头不可区分"。**下一步的同类问题是深层主干**：VGG-11 / ResNet-18 上的门控行为完全没测过。

**2. ~~在第三个量级上检验门控红利那条关系~~ 已完成（P2，§14.2 结果）。**
结果是**这条关系不成立**：新加的两个深层点代价落在预期带内（−3.45 / −4.36）
但红利没有随之变大（+0.68 / **−1.14**），七点 $r=-0.533$、去掉 CIFAR-100 后符号反转。
已改写为两个前提（代价够大 + 两头都不崩），见 §4.2。
现在有五个点，但它们只落在两个量级上：一个 −5.89（红利 +2.90），
四个挤在 −0.54 ~ −1.04 的窄带里（红利 −0.28 ~ +0.17）。去掉那个远点后，四点里看不出单调性。
**缺口是 2 ~ 5 个点这一段**。可以换一个中等难度的配置（更少类别、更低分辨率、或换主干），
把鲁棒头的干净代价调到 2~4 个点之间，看红利落在哪。这才是真正能判"是不是线性"的一跑。

**3. ~~验证「α 采样粒度」这条候选假说~~ 已否证；5.5 pp 的组间差按 known-unknown 收官。**
三次归因尝试全部失败：种子噪声（§13.10）→ 采样粒度（§14.4a）→ RNG 消耗位置（§14.6）。
已排除：评估路径（交叉评估 8/8 逐位相同）、训练动力学（逐 epoch loss 差 ≤0.016）、
粒度（同脚本差 +0.02）、消耗数（同脚本差 +0.11）。
**剩下的图景**：`task2_nat.py` 的 NAT 权重稳定落在 21.0±1.0，`v3_methods` 的六个 run 全在 26.58±0.29。
⇒ **任何精度数字不可跨训练实现移植；部署校准必须与部署训练同实现、同路径。**
机制问题列入第七阶段。
**4. 把 L8 的两成分分解做实（每组 8 个种子，约 10 次新训练、9~10 小时）。**
或者换一个效应量更大的对照。avg 与 max 的差距本身太小，是这条路的根本限制。
可以考虑换成"池化窗口大小"（2×2 vs 4×4）或者"池化层的位置分布"这类效应更大的对照。

**5. ~~把有效秩的测量推广到深层~~ 已完成（P1，§14.1 结果）。**
三点（SimpleCNN 128 维 / VGG-11 512 维 / ResNet-18 512 维）在 $\alpha=+0.3$ 上单调：
秩损失 20.6% / 33.3% / 58.7% 对应恢复率 72.2% / 70.2% / 36.9%（$r = -0.960$）。
**趋势成立，三点不足以定曲线形状**；形状提示是「先平后塌」，已进停车场。
测 VGG-11 / ResNet-18，看塌陷率和可恢复性之间是不是有定量关系。
如果能把"秩掉多少 → 读出最多能恢复多少"写成一条曲线，L4 和 L6 就能串起来。

**6. 自适应阈值。** 现在的 $\tau$ 是固定值，在验证集上选一次。真实芯片的失真分布会漂移。
第五阶段测过 6 种漂移分布，软门控在全部 6 种上都最好，硬路由在尾部集中时甚至低于鲁棒头。
但阈值本身没有跟着漂移调整。可以按 $\hat\alpha$ 的置信度自校准。

**7. 多头扩展。** 现在是两头（干净 + 非线性鲁棒）。可以加第三个头做高斯鲁棒，
用 $\hat\alpha$ 和 $\hat\sigma$ 两个估计值来路由。数据集已经有了（joint / σ-only 两组权重）。

**8. 硬件语义对齐。** 把 readout-NAT 的"读出层"映射到真实芯片的数字后端
（ADC 之后的部分），评估面积、延迟、功耗。这是这个方法能不能落地的关键。
现在只说了"主干权重不动"，没有算过实际的硬件代价。

**9. 把 CIFAR-10 的读出补到 3 个种子。** 现在 CIFAR-10 侧全是单 seed，CIFAR-100 侧是 3 seed，
两端的比较强度不对等。补两次训练（每次 5~30 分钟）就能对齐。
（CIFAR-10 的 5×MaxPool readout-NAT 已在 §13.9 补齐，见 §3.1 表。）

---

## 8. 勘误

写论文时逐项核对仓库文件，发现下面几处对不上。都列在这里，本文一律用核对后的数字。

**勘误 1：台账 §9.5 表里 `budget_rear` 的七点均值。**

台账写的是 **54.03**。按 `outputs_cifar100/v3_methods/budget_rear/alpha_sensitivity.csv`
的原始读数（−0.3 = 47.42、mid_band = 54.0325、clean = 60.90、+0.3 = 25.46）重算，
七点均值 = (47.42 + 54.0325×4 + 60.90 + 25.46) / 7 = **49.99**。
54.03 是 mid_band 的值，被误填进了七点均值那一格。

**勘误 1 自身的更正**：本文初稿在这一条里把 α=−0.3 写成了 46.29。那是错的。
`budget_rear` 的 α=−0.3 真值是 **47.42**（`alpha_sensitivity.csv` 第一行，
也是 `results_master.csv` 第 54 行的 `neg_0.3` 字段）。
**46.29 属于另一行** —— 是 `range_0.50`（第 62 行）的 α=−0.3，两者被看串了。
公式和 49.99 这个结果不受影响（公式里用的就是 47.42），错的是引用的数值清单。

`第三阶段报告.md` §2.2b 里的 49.99 是对的。判决文字里"mean7 +0.75"也是按 49.99 算的，自洽。

**勘误 2：台账 §9.5 表的 `sens` 行串列。**

该行有 7 个单元格，表头只有 6 列。多出来的是 mid_band（52.80），
被误当成独立一列插进去了。`budget_sens` 的七点均值按第 55 行重算是 **48.24**，
与 `第三阶段报告.md` 一致，台账同一行的 48.24 也对。

**勘误 3：台账 §12.11 表的七点均值列，三行都不对。**

| seed | 台账写的 mean7 | 按同行的 7 点数值重算 |
|---|---|---|
| 42 | 49.24 | **48.47** |
| 43 | 49.19 | **49.77** |
| 44 | 47.72 | **48.27** |

重算方式：(neg0.3 + mid_band×4 + clean + pos0.3) / 7，数据取自 `results_master.csv`
第 29、60、61 行。三行合起来是 48.84 ± 0.67，台账写的是 48.72 ± 0.86。
**clean 列和 ±0.3 列本身没有算错**，但是这两列上的**协议不纯**后来被查出是个大问题
（见本节末段：尾部方差被估高约 6 倍）。七点均值那一列则确实要改。
`第五阶段报告.md` 引用了 clean 与 +0.3 两列，所以也继承了这个协议问题。

另外记一笔，**这一条后来变成了大问题（P1-6 查出）**。这一节的 3 个种子混用了两个脚本 ——
seed 42 来自 `task2_nat.py`，seed 43/44 来自 `v3_methods_simplecnn.py --mode plain`。
两者都是"注入失真、不加校准"，但**采样代码不同**：前者每 batch 采**一个** α 共享给所有层，
后者**逐层独立**采样。同一个 seed 42，前者 $\alpha=+0.3$ 是 **21.41**，后者是 **26.03**，差 4.62。

**这个混用把尾部方差抬高了约 6 倍。** 补跑同脚本的 plain s42 之后：

| 口径 | clean 标准差 | $\alpha=+0.3$ 标准差 |
|---|---|---|
| 纯协议（三个都来自 v3_methods `--mode plain`） | ±0.419 | **±0.392** |
| 混合协议（§12.11 原样） | ±0.412 | **±2.561**（ddof=0；台账写的 ±3.15 是 ddof=1） |

所以 §12.11 那句"尾部噪声 ±3.1 pp"是**混协议读数**，不是单一协议下的真实方差。
它原来被当作全项目最重要的方法学警示，现在要降级和补充：

1. **具体数值降级**：±3.15 改为"混协议读数"，历史值在台账 §13.10 里降为脚注保留。
2. **规则本身保留**：尾部确实比 clean 抖，但证据换成**同协议**的几组
   （浅层 NAT ±0.39、架构变体 ±0.95~±1.58、深层读出 ±3.18~±3.40、同组 clean ±0.07~±1.85）。
   **"尾部指标必须 ≥3 seeds"这条不变。**
3. **新增一条**：算种子方差之前，先确认这几个 run 出自**同一脚本、同一采样代码路径**。
   这次实测的代价是方差被估高 6.5 倍（0.39 → 2.56）。

（这一条同时修掉了本文初稿里"结论是看离散程度，不受影响"那句 —— 说得不对，**受影响，而且不小**。）
**勘误 4：5×max 的 3-seed 七点均值。**

台账 §13.8 和 `第五阶段报告.md` §1.2 都写 **52.88**。
按三个种子的 `alpha_sensitivity.csv` 重算：53.749 / 52.819 / 53.056，均值 **53.21**。
（写 52.88 时把三个数的和算成了 158.63，实际是 159.62。）
这一格是补充信息，判据用的是 drop，**判决不变，L8 的结论不变**。
但它影响一句话：修正后，四种池化里七点均值最高的是 top-2 的 54.70（单 seed），
5×max 是 53.21、5×avg 是 53.55，三者仍在同一个量级。

**勘误 5：台账头部和 §1 的行数。**

台账第 3 行原写 `results_master.csv（48 行）`，§0 末写 `48/48 行 PASS`，§1 写 `完整 48 行`。
这是第三阶段刚建台账时的数字，之后没有跟着更新。
**已改**：三处均更新为 147 行（2026-09-18，含 P1-6 与 P2-7 新增的 3 行）。
**归档时点重扫（2026-09-20）为 152 行数据，配对自检 0 FAIL。**
归档轮把 5 行 1-epoch 冒烟跑排除出口径（口径变更记录见 `experiments_ledger.md` 头部
与 `docs/PROJECT_RETROSPECTIVE.md` §8 裁决记录）。

**同一处还有第二个问题（2026-09-18 修）**：`build_ledger.py` 的 `v3_readout_repair` 收割
与 `v3_methods` 收割一样，原本只扫 `outputs_cifar100/`。所以 **CIFAR-10 的读出适配行一直没进台账**
（既包括第三阶段的 3 个 8ep 文件，也包括本次新补的 4 个 24ep 文件）。
已把这两块收割都改成双数据集，本次新增 14 行（4×2 新 + 3×2 旧）。

**勘误 6（已在原文内标注，此处汇总）：ResNet-18 的 clean 方差。**

第四阶段按 2 个种子报的是 ±0.15。第五阶段补上第 3 个种子（clean = 67.61）后变成 ±1.85。
原文已修正，本文用 ±1.85。

**勘误 7：avg 池化的七点均值（已在台账内勘误）。**

早期记作 53.29，按 `outputs_cifar100/v3_methods/simple_cnn_mp_avg_clean_uniform/alpha_sensitivity.csv`
重算是 **53.43**。台账 §13.4 已有勘误注记，本文用 53.43；3 个种子的均值是 53.55。

**勘误 8：`results_master.csv` 的 `params` 列对 CIFAR-10 的行填了 CIFAR-100 的数。**

`build_ledger.py` 里 `PARAMS` 是个硬编码字典（simple_cnn 254084、vgg11 9277284、resnet18 11220132），
两个数据集共用。但这三个模型的最后一层是 `nn.Linear(..., num_classes)`，参数量随类别数变。
按 `models/` 下的定义直接数：

| 模型 | CIFAR-10（10 类） | CIFAR-100（100 类） | 差值 |
|---|---|---|---|
| SimpleCNN | 242,474 | 254,084 | 11,610 |
| VGG-11 | 9,231,114 | 9,277,284 | 46,170 |
| ResNet-18 | 11,173,962 | 11,220,132 | 46,170 |

所以 CIFAR-10 那些行上的 `params` 偏大。**不影响任何精度结论**，
但如果有人拿这一列做容量对比，要按类别数取对应的一组。

**勘误 9：CIFAR-10 两个架构的"原头 @+0.3"读数在本文两版之间静默变了 0.01。**

复核时发现，同一个模型自己的分类头在 $lpha=+0.3$ 上，两条评估路径给出不同的值：

| 模型 | `outputs/task1_*/alpha_sensitivity.csv`（第一阶段 `train.py` + task1 扫描） | `outputs/v3_readout_repair/readout_repair_c10_*_alpha+0.30_n20000.csv` 的 `a_original_fc` 行 |
|---|---|---|
| VGG-11 | **36.70** | **36.71** |
| ResNet-18 | **17.08** | **17.09** |

两个都是真实读数，差 0.01，来源就是 §6 第 7 条记录的那条"两条读出路径有系统偏差"
（在那里最大差 1.2 个点，这里只有 0.01）。

**本文的处理**：§3.1 与附录 A 的 CIFAR-10 表里，"原头 @+0.3"一列用 **36.71 / 17.09**，
也就是 `a_original_fc` 那一行的值。理由是这个表算的是**同一文件内**"原头 → readout-NAT"的增量，
两个数必须来自同一条路径，否则增量会被跨路径偏差污染。
`results_master.csv` 第 3、4 行保留 **36.70 / 17.08**（task1 路径），不动它 —— 那是台账里"该模型官方基线"的口径。

**本文初稿在第一版里用的是 36.70 / 17.08，改写表格时换成了 36.71 / 17.09 但没有说明。
按本项目"静默改数不允许"的规矩，记这一条勘误。** 数值上不影响任何结论
（增量分别是 +42.93 vs +42.92、+27.28 vs +27.29）。

**勘误 10：L8 表的「NAT 训练」行在同一行里混了两个脚本（同类问题第二次出现）。**

| 列 | 原填的值 | 来自 | 同行的其他列来自 |
|---|---|---|---|
| clean | **60.70** | `task2_nat.py`（granularity=**shared**） | — |
| $\alpha=+0.3$ | **21.41** | 同上 | — |
| mean7 | **49.24** | `v3_methods --mode plain`（granularity=**per-layer**） | — |

同一张表里，上行是 v3_methods 的 41.55，本行却是 task2 的 21.41 —— **两行的训练协议不同，
差值（+20.14）因此不可比**。已全部改为 v3_methods 的 60.85 / 26.03 / 49.24，
架构增益相应改为 **+15.52**（同协议同 seed 配对）。

**这是同一类错误（跨脚本混用）在勘误 3 之后的第二次出现**，而且这次混在**一行之内**，
更隐蔽。处置见台账 §13.13 的新制度。

**勘误 11：探针表的 headline 数字 27.76 是 8000 样本子集口径，不是全量 10 000。**

`outputs_cifar100/v3_probe_info_death/probe_results.csv` 是本文 §2 L6 与 §3.3 引用的探针产物。
它的 clean 行 `head_acc` = **59.6**，而同一模型同一配置的规范口径是 **59.99**
（`outputs_cifar100/simple_cnn/metrics.json` 的 `best_test_acc`，与
`task1_simplecnn/alpha_sensitivity.csv` 的 α=0 行逐位一致）。差 0.39 个百分点，
是配对自检容差（0.01）的 **39 倍**。

只读复算证实：同一权重、同一归一化下，`--n_test 8000` 给出 59.6000 / 27.7625，
`--n_test 10000` 给出 59.9900 / 28.0900 —— **CSV 里那两个数字精确对上 8k 口径**。

**这个口径在台账 §6 有登记**（原文"8k 训练特征 → 8k 测试特征"），且台账自己已注明
"故上表 headroom 是**下界**"。**问题在于它没有传播进本文。**

**影响评估**：

- headroom **+20.12 = 47.89 − 27.76 内部自洽**（两项同为 8k 子集），**结论方向未被推翻**；
- 但本文把 **27.76** 当作"模型自带分类头"的精度引用，而项目规范口径在 α=+0.3 时是
  **28.09**（`outputs_cifar100/task1_simplecnn/alpha_sensitivity.csv`）。两者相差 0.33。

**本文的处理**：涉及该探针的两个数字（27.76 / 59.6）在 §2 L6、§3.3 与摘要处**保留原值**，
但**加注口径**；全量 10k 的重跑列为第七阶段 M0c（在 CPU 上跑，因 GPU 被 M4 占用）。
（本条由第七阶段的多会话互校查出，登记于 2026-09-24。）


**核对过但没有问题的部分。** 用脚本把 `results_master.csv` 全部 152 行的七点均值重算了一遍，
又逐一抽了 20 组数字与台账正文、报告正文对照，以下都对得上：
CIFAR-100 三个干净基线（59.99/28.09/42.91、67.09/16.92/24.48、72.09/2.70/3.25）、
NAT-scratch 与 NAT-finetune 四行、task3 三个 Exp、ext6 的 Exp2/Exp3 四个深层配方、
四条负结果的 clean 与 ±0.3、信息探针全表、成对超加性全表、有效秩全表、
Dithering 的 bits 扫描与翻转统计、漂移压力测试 12 行、CIFAR-10 配方四组头、
readout-NAT 的 24ep 四组（VGG-11 的 58.22±0.67、ResNet-18 的 44.74±1.70 等）。

---

## 9. 复现材料

### 9.1 主要脚本

| 脚本 | 用途 |
|---|---|
| `utils/nonlinearity.py` | 失真注入（hook 接口） |
| `train.py` | 干净模型训练 |
| `task1_sensitivity_analysis.py` | $\alpha$ 敏感性扫描 |
| `task2_nat.py` | NAT 训练 |
| `task3_robust.py` | 校准 + 分层采样 + 非对称采样的消融 |
| `v3_probe_info_death.py` | 信息探针 |
| `v3_probe_pairwise.py` | 单层 / 成对 / 全层注入 |
| `v3_probe_gaussian_transfer.py` | 跨扰动迁移 |
| `v3_readout_repair.py` | readout-NAT |
| `v3_methods_simplecnn.py` | 采样与调度方法对照；含 `simple_cnn_mp` / `_avg` / `_top2` 变体 |
| `v4_activation_stats.py` | 逐层幅度统计 |
| `v4_probe_alpha_routing.py` | 在线 $\alpha$ 估计与路由 |
| `v5_probe_maxpool_mechanism.py` | 交换律与不变区统计 |
| `v5_full_recipe.py` | 完整配方（双头 + 门控） |
| `v5_probe_alpha_drift.py` | 分布漂移压力测试 |
| `v5_probe_rank_collapse.py` | 有效秩 / 条件数 / Fisher |
| `v5_p0c_criteria.py` | CIFAR-10 预登记判据 C1–C5 的评估 |
| `build_ledger.py` | 扫盘重建 `results_master.csv` |
| `tools/recalc_mean7.py` | 由台账的四个字段重算每行 mean7（本文核对数字时用的就是这个脚本） |
| `deploy_notes.md` | 第六阶段 P3：把结论映射到 CIM 数字后端的推演（非代码） |

### 9.2 关键产物与本文数字的对应

| 本文的位置 | 数据文件 |
|---|---|
| §1.2 协议自检 | `results_master.csv` 的 `pairing_check` 列 |
| §2 L6 探针表 | `outputs_cifar100/v3_probe_info_death/probe_results.csv` |
| §2 L6 层组合表 | `outputs_cifar100/v3_pairwise/pairwise.csv` |
| §2 L5 Dithering | `outputs_cifar100/extension5_dithering_vgg11/{group_a_bits_scan,flip_statistics,activation_stats}.csv` |
| §2 L7 估计器 | `outputs_cifar100/v5_full_recipe/*.csv`、`outputs/v5_full_recipe/*.csv` |
| §2 L8 池化 | `outputs_cifar100/v3_methods/simple_cnn_mp*/alpha_sensitivity.csv` |
| §2 L8 交换律 | `outputs_cifar100/v5_maxpool_mechanism/commutativity.csv` |
| §2 L8 不变区 | `outputs_cifar100/v5_maxpool_mechanism/invariant_frac.csv` |
| §2 L9 方差 | `results_master.csv` 第 74–117 行 |
| §3.1 readout-NAT | `outputs_cifar100/v3_readout_repair/*.csv`、`outputs/v3_readout_repair/*.csv` |
| §3.3 完整配方 | `outputs_cifar100/v5_full_recipe/recipe_simple_cnn*.csv` |
| §4.2 门控边界 | `outputs/v5_full_recipe/recipe_simple_cnn*.csv` |
| §5.1 幅度 | `outputs_cifar100/v4_activation_stats/act_stats.csv` |
| §5.1 有效秩 | `outputs_cifar100/v5_rank_collapse/rank_stats.csv` |
| §4.4 漂移 | `outputs_cifar100/v5_alpha_drift/drift_stress.csv` |
| §2 L2 加注（采样粒度） | `outputs_cifar100/task2_simplecnn/scratch{,_s43,_s44}/alpha_sensitivity.csv`
  与 `outputs_cifar100/v3_methods/plain_uniform{,_s43,_s44}/alpha_sensitivity.csv`（台账 §13.12 结果） |
| §2 L6 加注（秩-恢复曲线） | `outputs_cifar100/v5_rank_collapse/rank_stats{,_exp2vgg11,_exp3resnet18}.csv`
  + `outputs_cifar100/v3_readout_repair/readout_repair_c100_*_ep24_*.csv`（台账 §14.1 结果） |
| §4.2 门控七点 | `outputs_cifar100/v5_full_recipe/recipe_robust_{vgg11,resnet18}_*.csv`（台账 §14.2 结果） |
| §7 硬件推演 | `deploy_notes.md` |

### 9.3 复现注意

- 运行脚本要用 conda 环境 `pytorch_env`。直接敲 `python` 在本机解析到别的解释器。
- 输出重定向到文件时设 `PYTHONIOENCODING=utf-8`，否则会因字符编码报错中断。
- 显存 8GB。训练任务必须串行。项目里出过两次内存事故（§6 第 12 条）。
- `checkpoints/` 与 `*.log` 在 `.gitignore` 里，不进版本库。
- 两个数据集的产物目录分开：`outputs/`（CIFAR-10）、`outputs_cifar100/`（CIFAR-100）。

---

## 附录 A：对外部审计方初步结论的意见

审计方给了 8 条初步结论。核对数据之后，其中 6 条我同意，2 条需要修正或收紧。

**同意但需要精确化的：**

**结论 1（信息没死，坏的是读出）** —— 同意，方向正确。要补一句：
"最后一层读出"说的是**修复位置**，不是**失真位置**。失真注在每一层，
而且注入位置上最敏感的是 conv2/conv3（单点分别掉 12.43 和 7.56），fc 单点注入只掉 0.68。
坏的只是"分类头解不出特征里还剩的信息"这件事。
另外探针精度低于模型自己的干净精度（52.91 < 59.60），所以 +20.12 这个数是**下界**。

**结论 2（readout-NAT，提升 13~26 个点）** —— 同意方法，但**这个区间不准**。
24 epoch 协议下实测的增益是 **+6.71 到 +31.81**：

**CIFAR-100（24ep / 20k / 3 seed）**：

| 主干 | 自己的头 @+0.3 | readout-NAT | 增量 |
|---|---|---|---|
| SimpleCNN + 5×MaxPool | 42.47 | 49.18 | **+6.71** |
| SimpleCNN（clean 训练） | 28.09 | 43.31 | **+15.22** |
| ResNet-18（Exp3 主干） | 6.22 | 26.82 | **+20.60** |
| VGG-11（Exp2 主干） | 15.98 | 47.79 | **+31.81** |

**CIFAR-10（24ep / 20k / 单 seed）**：

| 主干 | 自己的头 @+0.3 | readout-NAT | 增量 |
|---|---|---|---|
| SimpleCNN + 5×MaxPool | 70.59 | 81.60 | **+11.01** |
| SimpleCNN（clean 训练） | 55.08 | 72.51 | **+17.43** |
| ResNet-18 | 17.09 | 44.37 | **+27.28** |
| VGG-11 | 36.71 | 79.64 | **+42.93** |

"13~26"大致对应的是 8 epoch 时代的跨架构读数（+13.07 / +23.22 / +13.57）。
准确的表述是：**增益取决于原头在尾部丢了多少，实测从 +6.71 到 +42.93。**
另外"四架构、两个数据集都成立"要收窄：mp 是 SimpleCNN 的池化变体、参数量完全相同，
不是独立的架构家族；CIFAR-10 侧全是单 seed，CIFAR-100 侧才是 3 seed。

**结论 5（MaxPool 的三个候选解释被排除）** —— 同意，但要说清楚证据强度不一样。
前两条排除（吸收失真、不变区集中）有完整数据支撑；
第三条（max 的选择性）只有**单 seed** 的一场对照。
所以能下的判断是"没有证据支持 max 有独占贡献"，
而"top-2 和 max 一样好"这个正面说法还没复核。
另外 2/3 : 1/3 的定量分解，审计方说"三个种子分不出来，维持初步"——这一条完全同意，
补充一个数字：3 seed 下的点估计是 **1.17 个点**（占 10%），不是 4.53。

**结论 8（方法学教训）** —— 同意，但要把两类方差分开说。
"±1.85"是**固定深层主干、只换读出种子**时 clean 的方差；"±0.07~0.12"是**重训浅层主干**的方差。
两者不是一回事，不能直接比。深层主干本身的重训方差没有测过。
能下的判断只有：**深层上已知的方差已经到 1.85（clean）和 3.40（尾部），
所以深层上所有指标都要 3 个种子，不只是尾部。**

**需要收紧的：**

**结论 3（$\alpha$ 可以在线估计）** —— 基本同意，数字要改。
审计方写 $R^2$ 0.83~**0.94**。四个组合的实测是 0.8270（CIFAR-10/mp）、0.8323（CIFAR-100/mp）、
0.9327（CIFAR-10/plain）、0.935（CIFAR-100/plain）。所以是 **0.83~0.94**，
统一记作 **0.827~0.935**（不要写成 0.83~0.94，也别在同一段里既写 0.94 又写 0.935），
且注意**加了 MaxPool 的主干上 $R^2$ 掉到 0.83**
（同样是因为 MaxPool 把 $\alpha$ 的信息压缩了一部分）。MAE 是 0.036~0.061。

**结论 4（门控的边界）** —— 结论对，但**证据强度要说清楚**。
CIFAR-100 那一侧是三判据全达标（55.08 / 60.70 / 49.10），且软门控超过 oracle（+0.12），
证据是硬的。CIFAR-10 那一侧跑完 3 个主干种子后是**"不可分辨"**（原来只有 1 个主干种子时写的是"弱通过"）：
补到 3 个主干种子后，$m$ 的三个值是 **+0.169 / −0.003 / +0.171** ——
一个正好落在 0 上，平均 +0.112，全部小于主干级 mean7 噪声（±0.18~0.58）。
所以"代价小时不值得加门控"这句话，在 CIFAR-10 上的准确形态是
"**加不加分不出来**"，而不是"加了没用"。
另外审计方没有提到一条：在 CIFAR-10 的 plain 主干上，软门控是**负的**（mean7 −0.28）。

**结论 6（训练端四条路全部失败）** —— 同意，四条都成立，数字见 §5.1。
补一句：其中"预算 rear 的 −0.57"和"对抗的 −1.71"都在种子噪声带内，
所以严格说不是"更差"，是"没有提升"。真正有说服力的是第 4 条（范围外推），
它的证据不在 20.48 这个相对值上，而在"在 $\pm0.5$ 上训过、测 $+0.5$ 仍只有 2.71%"这个绝对值上。

**结论 7（有效秩塌陷）** —— 同意，数字无误。
但要说清楚这个测量只在 **CIFAR-100 的干净 SimpleCNN（128 维）** 上做过，
没有跨架构、没有跨数据集。所以"丢的是维度"这条的适用范围是有限的。

---

## 附录 B：一页纸的结论表

| 编号 | 一句话结论 | 关键数字 | 出处 |
|---|---|---|---|
| L1 | 模型越强，失真掉得越狠 | C100：59.99/31.90 → 72.09/69.39 | `results_master.csv` 26–28 |
| L2 | NAT 在小模型是再分配、在深层是全轴塌陷 | ResNet-18 @±0.3：2.70 / 3.25 | 同上 28 |
| L3 | 两条通路迁移几乎正交，训练时互相干扰 | 只做 $\sigma$ 训练使 $+0.3$ 掉 6.08；joint 相对只做 $\alpha$ 训练又掉 10.76 | 同上 58、59 |
| L4 | 正侧尾部训练端打不动（四条独立负结果） | 训过 $\pm0.5$，测 $+0.5$ 仍只有 2.71% | 同上 62 |
| L5 | 低位量化反而救精度，增益随难度缩水 | C10 +6.53 → C100 +2.84 | `extension5_dithering_vgg11/group_a_bits_scan.csv` |
| L6 | 信息没死，缺的是解码器 | 探针 47.89 vs 冻结头 27.76（+20.12） | `v3_probe_info_death/probe_results.csv` |
| L7 | 失真强度可以在线估出来 | $R^2$ 0.827~0.935，MAE 0.036~0.061 | `v5_full_recipe/*.csv` |
| L7b（加注） | 门控值得装要两个前提：代价够大 + 两头都不崩 | 七点 $r=-0.533$；ResNet-18 红利 **−1.14** | 台账 §14.2 |
| L8 | 池化的保护主要来自降维 | $+0.3$：28.09 → 42.47（零成本） | `v3_methods/simple_cnn_mp_clean_uniform/` |
| L9 | 尾部方差 ±0.39~3.40 普遍大于 clean（±0.07~1.85） | 单 seed 的 4.53 效应 → 3 seed 后 1.17；混协议会把方差估高 6 倍 | 台账 §13.8、§13.10 |

| 办法 | 涨多少 | 花多少 | 边界 |
|---|---|---|---|
| readout-NAT | $+0.3$ 上 +6.71 ~ +42.93（C100 为 3 seed，C10 单 seed） | 4~30 分钟，主干不动 | 读出层必须可训练 |
| 在线 $\alpha$ 估计 | $R^2$ 0.827~0.935 | 毫秒 | — |
| 双头 + 门控 | mean7 最高 +2.90（CIFAR-100）；CIFAR-10 上 +0.17/−0.00/+0.17 → 不可分辨 | 12 分钟 | 只在鲁棒头干净代价大时值得 |
| 5×MaxPool 主干 | $+0.3$ 上 +14~20 | **零** | 改网络定义 |
| σ 增强训练（双通路） | $\sigma=0.3$ 上 +23.21 | 45 分钟 | $\alpha$ 侧要付 −6.08 |
