# 部署推演：把研究结论映射到 CIM 数字后端

> 第六阶段 P3 交付物。**零 GPU、零实验，全部是推演**。
> 每个数字要么来自仓库里的实测（标出处），要么是本文件里现算的（标算法）。
> 判定分三档：**可行** / **需改动** / **不可行**。

---

## 0. 一句话结论

| 组件 | 判定 | 一句话依据 |
|---|---|---|
| **readout-NAT**（冻结主干、只重训最后一层） | **需改动** | 要求读出层在**数字域**，或阵列里那一小块**可重写**。若读出也被固化在阵列里，该方法不成立 |
| **在线 α 估计**（岭回归） | **可行** | 128/512 维点积，算力占主干前向的约 4e-4 |
| **软门控**（双头 logits 插值） | **可行**（前提同第一条） | 纯数字域逐元素运算，每次推理约 400 次操作 |
| **冻结主干** | **可行** | 就是"不训练主干"，天然符合权重固化 |
| **MaxPool 密集主干** | **设计阶段零成本 / 部署后不可行** | 它是网络**定义**的改动，只能在投片前决定 |
| **全网络重训** | **不可行** | 阵列里的权重是烧录/固化的，训练端四条路本来也救不了尾部 |

---

## 1. 场景设定（本推演的前提）

- 模拟阵列负责卷积/全连接的 MAC，权重**固化在阵列里**（这是 CIM 省功耗的前提）。
- 阵列输出经 **ADC** 回到数字域。
- 失真发生在**模拟域的输入侧**：$y=\alpha\hat x^3+(1-\alpha)\hat x$，逐层注入
  （模型见 `utils/nonlinearity.py`，全文一致）。
- 数字后端（ADC 之后）可以做任意运算，代价是面积/延迟/功耗。

**本推演不涉及**：ADC 的位宽、阵列的规模、模拟域的校准电路。那些在本文的数据里没有对应测量。

---

## 2. 权重存放位置：readout-NAT 到底改了什么

**改的是什么**：只改最后那个 `nn.Linear`（读出层）。主干一个参数都不动
（`v3_readout_repair.py` 里对主干 `requires_grad_(False)` 是逐字落实的）。

**读出层有多大**：

| 主干 | 读出层形状 | 参数量 | 占全模型参数 | 出处 |
|---|---|---|---|---|
| SimpleCNN（CIFAR-100） | 128 → 100 | 12,800 | **5.04%** | `models/simple_cnn.py` |
| VGG-11（CIFAR-100） | 512 → 100 | 51,200 | **0.55%** | `models/robust_vgg11.py` |

（参数量按 `in×out + out` 现算；全模型参数 254,084 / 9,277,284 取自 `results_master.csv` 的 `params` 列。）

**两种放法**：

- **(A) 读出层也在模拟阵列里** → **不可行**。CIM 阵列的权重通常一次写入（烧录/固化/或代价很高的重写），
  而 readout-NAT 要求**在部署现场重写它**。这等于要求阵列支持"现场重写一小块"，
  在大多数 CIM 设计里不成立。
- **(B) 读出层在数字域** → **可行**。数字域的 12.8k / 51.2k 个权重随便改，
  训练成本就是本研究的实测值（12~30 分钟，`第五阶段报告.md` 部署表）。

> **判定：需改动。** 这一条是全部结论里唯一需要硬件配合的地方，
> 但它要求的改动方向与 CIM 的常见架构一致 —— ADC 之后本来就是数字电路，
> 把最后那一层放在数字域，很多设计本来就这么做。**落地的关键问题是
> "你的 CIM 把哪一层当读出"，而不是"能不能改"。**

---

## 3. 在线 α 估计：算力账

**做什么**：把读出层的输入特征 $f$ 过一个岭回归，得到 $\hat\alpha = w^\top z + b$
（$z$ 是标准化后的特征）。标准化用的 $\mu,\sigma$ 和 $w,b$ 都是**部署前离线算好**的常数。

**每次推理的代价**：一个 $d$ 维点积 + 一次加法。

| 主干 | 特征维度 $d$ | 每次推理 MAC | 主干前向 MAC | **占比** |
|---|---|---|---|---|
| SimpleCNN | 128 | 128 | ≈ 33.9 M | **3.8e-4 %** |
| VGG-11 | 512 | 512 | ≈ 209.4 M | **2.4e-4 %** |

（主干 MAC 按各卷积层 $c_{in}c_{out}k^2h^2$ 现算；只算卷积，全连接与池化忽略不计。）

**判定：可行。** 算力上完全可以忽略（万分之四）。真正的代价在**面积**：
多一份 $d$ 维权重的存储（128 或 512 个数），以及一个乘加阵列的一小段。

**精度实测**：$R^2$ 0.827~0.935、MAE 0.036~0.061（4 个组合，`论文v3.md` §2 L7 表）。
$R^2$ 在加 MaxPool 的主干上会掉到 0.83 —— 因为池化把一部分 α 信息压掉了。

---

## 4. 门控插入位置

**做什么**：两个头各出一组 logits，按 $w=\mathrm{clip}(|\hat\alpha|/0.3,\,0,\,1)$ 插值：

```
logits = (1 - w) * logits_clean + w * logits_robust
```

**插在哪**：两个线性头**之后**、`argmax` **之前**。全是数字域的逐元素运算。

**每次推理的代价**：$2\times C$ 次乘（$C$=类别数）、$C$ 次加、$C$ 次 clip 加一次除法。
CIFAR-100 下 $C=100$ → **约 400 次操作**。相对主干的 3.4e7~2.1e8 次 MAC，可以忽略。

**判定的前提**：必须能**同时拿到两个头的 logits**。这意味着
① 两个头都在数字域（同 §2 的判定）；
② 要有一份"干净头"的权重常驻 —— 它的参数量与读出层相同（12.8k / 51.2k），
可以固化在数字域的 ROM 里，**不需要重写**。

**判定：可行**（前提同 §2）。这里有个额外的好处：干净头是**冻结不动**的那个，
正好可以烧进 ROM；只有鲁棒头需要可写。

> **但第六条要说清楚：门控不是无条件的。** 第六阶段 P2 实测，
> 深层主干上门控可能**有害**（ResNet-18/Exp3 红利 **−1.14**，
> 因为它的自带头在负侧崩，插值把灾难带了进来）。**装之前先查两个头的逐 α 曲线**
> （`论文v3.md` §4.2、台账 §14.2 结果）。

---

## 5. 三档判定的完整依据

### 可行

- **冻结主干**：这就是本方法的定义（不改阵列权重）。实证：readout-NAT 在 4 个主干 × 2 个数据集上成立。
- **在线 α 估计**：算力 3.8e-4 %，精度 $R^2$ 0.83~0.94。
- **软门控**：数字域约 400 次操作/推理，可 ROM 固化干净头。

### 需改动

- **读出层必须落在数字域（或可重写）**：这是唯一的结构性要求。
  改动量：读出层只占全模型参数的 **5.04%**（SimpleCNN）/ **0.55%**（VGG-11），
  把它挪出阵列对阵列面积的影响很小。

### 不可行

- **部署后改网络结构**（MaxPool 密集主干）：架构改动只能在投片前决定。
  好消息是它是**零训练成本**的（`论文v3.md` §3.4），**设计阶段加上即可**。
- **部署后重训主干**：阵列权重固化。而且训练端四条路本来也救不了正侧尾部
  （`论文v3.md` §5.1，四条独立负结果）。
- **把 α 估计器塞进模拟域**：没有必要 —— 它本来就是数字域的后处理，
  而且模拟域再做一次非线性会让失真模型自身失效。

---

## 6. 从"测试集读数"到"现场收益"之间还隔着什么

这一段是**风险清单**，不是结论。

1. **失真形态的假设**。本研究全部收益都是在"逐层、逐样本 max 归一化、三次多项式"这个模型下测的。
   真实芯片的失真未必是这个参数化形式。参考：本研究测过**加性高斯噪声**这条对照通路，
   它的可恢复性差得多（探针 headroom 只有 +7.00，非线性是 +20.12）。
2. **ADC 量化与非线性失真的联合**。ext3 测过：SimpleCNN 上 4bit QDQ 是**超加性恶化**（Δ = −4.75），
   VGG-11 上是**次加性**（Dithering，Δ = +4.66）。也就是说"数字域后处理能救多少"要**分层看**。
3. **跨脚本的 α 采样序列差必须处理**（第六阶段 P0 发现、P4e 归因）：
   `task2_nat.py` 与 `v3_methods_simplecnn.py` 训出的 NAT 权重，尾部稳定差 **5.5 pp**。
   P4e 交叉评估查明**评估路径无辜**（8 个权重 × 2 条路径逐位相同），差异在权重侧，
   根因是两组用了**随机流的不同位置**（§14.5）。**落地时两套数字不可混用。**
   **α 采样粒度本身不是杠杆**（同脚本切换只差 +0.02，$z=0.08$）。

---

## 7. 本文件没做的事

- 没有硬件实测，也没有 RTL/功耗仿真。
- 没有把"读出层挪到数字域"的**具体接口**写出来（时序、位宽、与 ADC 的对接）。
- 没有评估**在线重训读出**在现场是否可行（若芯片允许，那是另一条路；
  本研究只假设了"离线训好再烧录"）。
- 没有考虑**多任务/多模型共享阵列**的情形。

---

## 附：本文件用到的实测数字与出处

| 数字 | 值 | 出处 |
|---|---|---|
| readout-NAT 增益 | $+0.3$ 上 +6.71 ~ +42.93 | `论文v3.md` §3.1 表 |
| readout-NAT 成本 | 12~30 分钟（读出训练） | `第五阶段报告.md` §3 |
| 读出层参数占比 | 5.04% / 0.55% | 本文件 §2 现算 |
| α 估计器 $R^2$ / MAE | 0.827~0.935 / 0.036~0.061 | `论文v3.md` §2 L7 表 |
| 探针 headroom（非线性 vs 高斯） | +20.12 vs +7.00 | `outputs_cifar100/v3_probe_info_death/probe_results.csv` |
| 4bit QDQ 联合误差 | SimpleCNN Δ=−4.75（超加性）/ VGG-11 Δ=+4.66（Dithering） | 台账 §3 |
| 跨脚本尾部差（归因见 §14.5） | **5.52**（$z=9.4$），评估侧无辜、差异在权重 | 台账 §14.5 结果 |
| 门控红利（深层） | VGG-11 +0.68 / ResNet-18 −1.14 | 台账 §14.2 结果 |
