# 第五阶段报告

> 日期：2026-09-18　｜　状态：**定稿**
> 进度：P0-A（resnet18-Exp3 s43）、P0-B（top-2 池化）、P0-C（CIFAR-10 跨数据集复现）、
> **P0-4（L8 精简版 3-seed 复核）**、P0-D（本报告）**全部完成并入账**
> 配套：`experiments_ledger.md`（§13 为第五阶段）、`results_master.csv`（126 行，配对自检 0 FAIL）

---

## 0. 摘要

第五阶段把第四阶段的两条主结论（**读出瓶颈**、**MaxPool 架构杠杆**）推进到"机制 + 端到端配方"：

1. **P0-3(a) 交换律**：在失真实际作用的域（**非负激活**）上，归一化三次映射与 MaxPool **精确可交换**
   （max|Δ| = **0.000e+00**，全部层 × |α|≤0.3）；破缺只出现在**含负值**的张量上，原因是
   `abs-max 归一化 vs signed-max 池化` 的实现不一致（**工程注记**）。
2. **P0-3(b) 不变区假说否证**：5-pool 模型的易感度 S **更高**（conv4：0.153 vs 0.113）、
   不变区占比几乎不变 → 保护**不来自**"质量向恒等区集中"；改提**自由度缩减假说**（深层元素数 16~64 vs 256）。
3. **P0-3(c) 算子替换 + top-2 补报 + L8 最终 3-seed 复核（§13.8）**：avg-pool 下保护**未消失、只缩水**；
   top-2 补报否证了"71/29"的命名（top-2 与 max 的降维程度相同，却在 +0.3 上高 0.51 pp、mean7 高 0.95 pp）
   ⇒ **"max 的选择性/可交换性"被排除为成因**。
   **精简版 3-seed 复核（5×max / 5×avg 各 3 seeds，同协议）**：
   drop@+0.3 = **20.60 ± 1.54**（5×max）vs **21.78 ± 0.91**（5×avg），
   **avg − max = +1.17 pp** —— 落在预登记的 **1~2 pp 区间（判据 ③）**
   ⇒ **"降维 66% / 非均值聚合 34%"的 2 : 1 分解降级为"初步"**，
   且 **1.17 pp 小于两组的 seed 标准差，3 seeds 无法分辨**（升为定论需 ≥8 seeds/组）。
   **L8 终版 = 保护主要来自降维**（锚点口径 90 / 10，同样标"初步"）；
   单 seed 时的 4.53 pp 已确认为**采样产物**（seed 42 的 5×max drop 恰是其 3 seed 中最低的一个）。
4. **P0-2 完整配方端到端实测**：
   - **预登记配置（硬 τ 路由）未达标**（mean7 54.49 ✓ / clean 60.92 ✓ / **+0.3 47.90 ✗**，差 0.60 pp）；
   - **软门控变体（logit 插值）三项全达标**：**mean7 55.08 / clean 60.70 / +0.3 49.10**，
     且**超过硬选择的 oracle（54.96）** —— 插值即**集成**（**该"集成"主张跨数据集复现失败，见第 7 条**）；
   - 消融阶梯量化了四组件贡献（见 §1.3）。
5. **P1-5 α 分布漂移**：软门控在 6 种漂移分布下**全部最优**（4/6 超 oracle）；
   **硬路由在漂移下退化**（尾部集中时竟低于 robust 头）。
6. **P2-6(a) L4 机制**：正侧失真使**有效秩塌陷**（+0.3 −20.6%、+0.5 −50.9%；负侧仅 −2.0%/−14.2%），
   而 Fisher 判据反而更高 → **损伤形态是"维度丢失"而非"类间距缩小"**。
7. **P0-C CIFAR-10 跨数据集复现（真正的事前预登记：判据 C1–C5 于跑前写死）**：
   **形式裁决成立**（C1∧C2∧C3），**但"CIFAR-100 的插值即集成"未复现** ——
   C4 由 3/7 降到 **1/7**、软门控由"赢 oracle +0.12"变为**"输 oracle −0.07"**。
   机制被钉死为：**门控红利 = 鲁棒头 clean 代价的回收量**
   （CIFAR-100 −5.89 → +2.90；CIFAR-10/mp −0.87 → +0.17；CIFAR-10/plain −0.54 → **−0.28**）。
   → **软门控仍带"事后变体"色彩的强主张被撤回，替换为有机制的定量律。**

**"≈54.5" 推算的裁决**：**作废**。实测 mean7 = 54.49（巧合命中）但 **+0.3 = 47.90**
（推算引用的 49.12 来自不同实现的头）。用户的事前判断正确。

---

## 1. 逐项结果

### 1.1 P0-3(a) 交换律：在非负域上**精确成立**

| 位置 | α | max\|Δ\| |
|---|---|---|
| pool1~pool5 的**输入**（post-ReLU，非负） | ±0.1/±0.2/±0.3 | **0.000e+00** |
| conv1~conv4 的**原始输出**（含负值） | +0.3 | 4.4e-01 ~ 3.4e+00 |

**机制澄清**：`nonlinearity` 用 **abs-max** 归一化，`nn.MaxPool2d` 用 **signed max**；
非负时二者一致 → 恒等式精确；含负值（如 BN 之前的卷积输出）时尺度不匹配 → 破缺。
**推论**：MaxPool 的鲁棒性提升**不能**归因于"池化吸收失真"。

### 1.2 P0-3(b)(c) 保护来源：**主要来自降维**（3-seed 复核后，"66/34"降级为"初步"）

(b) 不变区统计（conv 输出，归一化域）：

| arch | layer | E\|x̂\| | S | P(\|x̂\|≥0.9) | H×W |
|---|---|---|---|---|---|
| 2-pool | conv4 | 0.1215 | 0.1134 | 0.0001 | 256 |
| 5-pool | conv4 | **0.1743** | **0.1527** | 0.0014 | **16** |

→ 假说**否证**：5-pool 的 S 更高、不变区占比不变；唯一显著差异是**空间元素数 16 vs 256**。

(c) 算子替换。2×max 基线**取自第一阶段 `train.py` 的 100ep 产物，协议不同，仅作锚点、不参与同协议对比**；
5×avg 与 5×max 为**同协议（v3_methods 120ep / clean / uniform）3 seeds**：

| 池化 | seeds | clean | α=+0.3 | **drop@+0.3** | mean7 |
|---|---|---|---|---|---|
| 2×max（基线 · **异协议锚点**） | 1 | 59.99 | 28.09 | **31.90** | 49.29 |
| **5×avg**（交换律破缺） | **3** | **61.38 ± 0.07** | 39.61 ± 0.95 | **21.78 ± 0.91** | 53.55 |
| 5×top-2（预登记插值检验点，不承重） | 1 | 61.94 | 42.98 | 18.96 | 54.70 |
| **5×max**（交换律成立） | **3** | **60.90 ± 0.12** | 40.29 ± 1.58 | **20.60 ± 1.54** | 52.88 |

逐 seed drop —— 5×max：**(18.52, 22.20, 21.09)**；5×avg：**(23.05, 20.97, 21.31)**。

**判据裁决**：`avg − max = +1.17 pp`（3 seeds 均值）→ 落在**预登记的 1~2 pp 区间（判据 ③）**
⇒ **既不能确认也不能删除"非均值聚合"成分 → 维持"初步"，不再追加实验**。
关键限定：**1.17 pp 小于两组的 seed 标准差**（±1.54 / ±0.91），**3 seeds 无法分辨该成分**
（按 σ≈1.2、欲分辨 1.2 pp 估，升为定论需 **≥8 seeds/组**）。
**单 seed 时得到的 4.53 pp 已确认为采样产物**——seed 42 的 5×max drop（18.52）恰是其自身 3 seed 中**最低**的一个。

**三条排除线（这三条是稳健的，与上面"初步"的成分不同）**：
① 保护**不**来自"吸收失真"——f_α 与 max 在非负域**精确可交换**（机器精度）却非成因；
② **不**来自"不变区集中"（5-pool 的易感度 S 反而更高、不变区占比不变）；
③ **不**来自"max 的选择性/可交换性"——top-2 与 f_α **不可交换**，保护却与 max 等同（42.98 vs 42.47，单 seed）。

**锚点口径的占比（仅展示，不参与同协议对比）**：以异协议的 2×max（drop 31.90）为锚 →
降维 **10.12 pp（90%）**、非均值聚合 **1.17 pp（10%）**。
⚠️ 该 90/10 与旧稿 66/34 之差**全部来自 seed 抖动 + 锚点协议差异**，**不是新机制**。

**顺带（方法论旗标 ⑥ 的补充证据）**：mp / avg 两个架构变体的 **clean 方差仅 ±0.12 / ±0.07**，
与 ResNet-18 的 **±1.85** 相差 15~26 倍 ⇒ **clean 的 seed 方差按架构变化，浅模型经验不可外推到深层**；
且**纯架构变体（加池化）不引入额外 clean 方差**。

### 1.3 P0-2 完整配方与消融阶梯（**本阶段核心**）

**配方**：冻结 `simple_cnn_mp` + 双线性头（原始 + 24ep/13-α 池鲁棒头）+ 岭回归 α 估计器 + 门控。
**α 估计器**（mp 特征）：R² = 0.8323，MAE = 0.0595。

**消融阶梯（3 seeds 均值）**：

| 配置 | clean | α=+0.3 | α=−0.3 | α=+0.2 | **mean7** |
|---|---|---|---|---|---|
| ① mp·仅 clean 头 | **60.99** | 43.21 | 48.05 | 54.09 | 54.12 |
| ② mp·仅 robust 头 | 55.10 | 48.58 | 48.56 | 52.82 | 52.19 |
| ③ mp·硬路由（**预登记**） | 60.92 | 47.90 | 48.29 | 53.53 | 54.49 |
| **④ mp·软门控 ★** | 60.70 | **49.10** | **49.12** | **55.16** | **55.08** |
| ⑤ plain·仅 clean 头 | 59.99 | 28.19 | 44.09 | 48.10 | 49.65 |
| ⑥ plain·仅 robust 头 | 55.85 | 46.50 | 48.91 | 53.06 | 52.39 |
| ⑦ plain·硬路由 | 59.99 | 46.48 | 48.71 | 52.27 | 53.57 |
| ⑧ plain·软门控（撤 mp） | 59.96 | 46.21 | 49.18 | 53.84 | 54.17 |

**组件贡献（逐个量化）**：

| 组件 | 贡献 | 证据 |
|---|---|---|
| 鲁棒头 | **+5.37 pp @+0.3**（43.21→48.58），代价 clean −5.89 | ① vs ② |
| 门控（软门控 vs 仅鲁棒头） | **+2.90 pp mean7** | ② vs ④ |
| 门控实现（软 vs 硬） | **+0.59 pp mean7**（尾部 +1.20） | ③ vs ④ |
| **MaxPool 架构**（mp vs plain） | **+0.92 mean7、+2.89 @+0.3** | ④ vs ⑧ |

**判据裁决（严格分层）**：
- **预登记配置③ → 未达标**（+0.3 = 47.90 < 48.5）：**维持原判，不改写**。
- **事后变体④（软门控）→ 三项全达标**（55.08 / 60.70 / 49.10）；**当时的"新发现：插值即集成"**
  （在 −0.3/+0.2/+0.3 三处超过两端较优者，故能超越硬选的 oracle）
  —— **该主张已在 CIFAR-10 上未复现，见 §1.7 与下文审计标注**。
- **审计标注**：④ 是**看到失败后引入的事后变体**，正式宣称前需在未见过结果的任务上重新预登记。
  → **该预登记已执行（P0-C，§1.7）**：形式判据成立，但**"插值即集成"这一强主张未复现**，
  已按跑前规则降级（详见 §1.7 与 §4①）。

### 1.4 P1-5 α 分布漂移压力测试

| 测试分布（mp 主干） | clean 头 | robust 头 | 硬路由 | **软门控** | oracle |
|---|---|---|---|---|---|
| ① U[±0.3]（训练分布） | 54.12 | 52.19 | 54.49 | **55.08** | 54.96 |
| ② 仅正向 | 54.53 | 52.85 | 55.49 | **56.07** | 55.87 |
| ③ 仅负向 | 55.43 | 52.25 | 55.10 | **55.50** | 55.56 |
| ④ 双峰 | 53.71 | 52.03 | 53.25 | **54.40** | 54.02 |
| ⑤ 尾部集中 | 45.63 | **48.57** | 48.10 | **49.11** | 48.57 |
| ⑥ 单峰 +0.3 | 44.51 | 49.08 | 48.57 | **49.83** | 49.24 |

→ **软门控 6/6 最优**；**硬路由在漂移下退化**（⑤ 竟低于 robust 头）；plain 主干上软门控红利消失
（尾部集中时 −0.01）= **依赖主干自身的尾部可用性**。

### 1.5 P2-6(a) 正侧不可训练区的机制：**有效秩塌陷**

| α | **有效秩** | κ(99%) | Fisher |
|---|---|---|---|
| 0.0 | **38.78** | 18.40 | 0.49096 |
| **+0.3** | **30.78（−20.6%）** | 20.48 | 0.45669 |
| **+0.5** | **19.04（−50.9%）** | 25.93 | 0.36248 |
| −0.3 | 38.00（−2.0%） | 19.36 | 0.39061 |
| −0.5 | 33.27（−14.2%） | 21.25 | 0.32953 |

→ 正侧**塌陷率是负侧的 10 倍以上**；且 **Fisher 在 +0.3 反而高于 −0.3** →
损伤是**维度丢失**，不是类间距缩小。与 P1-4 的"中段压缩"互为因果。
**与 L6 的衔接**：塌陷后的低秩子空间里信息**仍线性可达**（读出层可恢复 +21 pp），
"不可训练"仅成立于**主干参数空间**。

### 1.6 P0-1 部署表 v2（24ep 新协议，readout-NAT）

| 配置 | seeds | clean | **α=+0.3** | 中带 | mean7 |
|---|---|---|---|---|---|
| SimpleCNN · clean 主干 | 3 | 55.81±0.64 | 43.31±**0.16** | 53.41 | 51.56±0.58 |
| SimpleCNN · NAT 主干 | 1 | 55.94 | 41.60 | 53.48 | 51.56 |
| **SimpleCNN · 5×MaxPool 主干** | 3 | 56.38±0.19 | **49.18±0.32** | 54.54 | **53.17±0.18** |
| **VGG-11 · Exp2 主干** | 3 | **64.97±0.26** | **47.79±3.18** | 60.96 | **58.22±0.67** |
| ResNet-18 · Exp3 主干 | **3** | **65.00±1.85** | **26.82±3.40** | **51.19** | **44.74±1.70** |
| （对照）ResNet-18 · 原始头 | 1 | 72.69 | 6.22 | 37.04 | 32.83 |

**要点**：
- **24ep 协议确实优于 8ep**（VGG-11：+0.3 45.24→47.79；ResNet-18：**18.80→26.82**，+8.02）✓ 满足 P0-1 判据（无回退）。
- **尾部 seed 噪声在深层被放大**：VGG-11 ±3.18、ResNet-18 ±3.40（clean 仅 ±0.26 / ±1.85）。
- **⚠️ 修正（P0-1 第 3 seed 完成后的新发现）**：ResNet-18 的第 3 seed clean = **67.61**，
  比另两 seed（63.54 / 63.84）高 2.6~4.1 pp → **2-seed 时报的 "clean ±0.15" 是采样假象**，
  3-seed 下 clean 方差升至 **±1.85**。**"clean 稳定、只有尾部噪声大"这条经验不可外推到深层**
  （SimpleCNN ±0.64 → VGG-11 ±0.26 → ResNet-18 ±1.85）。深层的**一切**指标都需 ≥3 seeds。
- **结论不受影响**：ResNet-18 仍是全部深层配方中正侧尾部最差者（+0.3 = 26.82 vs VGG-11 47.79，
  差 **20.97 pp ≫ 噪声**），且 readout-NAT 相对其自身原始头（6.22）仍有 **+20.60 pp**。

### 1.7 P0-C CIFAR-10 复制（mp + 软门控）：**弱支配复现，集成增益未复现**

**目的**：软门控是"看到 CIFAR-100 硬路由失败后"才引入的**事后变体**。本实验在**尚无任何配方结果的
CIFAR-10 上**、**锁定全部超参**、跑前写死判据 C1–C5（台账 §13.7）→ 一次真正的跨数据集预登记验证。

**主干**：CIFAR-10 `simple_cnn_mp` clean 训练 120ep、seed 42、34.6 min；
best clean **87.48**（= metrics.best_test_acc ✓）、+0.3 **70.59**、mean7 **82.51**。

**消融阶梯（3 seeds 均值）**：

| 配置 | clean | α=+0.3 | α=+0.2 | α=−0.3 | **mean7** |
|---|---|---|---|---|---|
| ① mp·仅 clean 头 | **87.48** | 70.34 | 82.55 | 81.09 | 82.68 |
| ② mp·仅 robust 头 | 86.61 | **82.91** | 85.66 | **82.82** | 85.02 |
| ③ mp·硬路由 | 87.15 | **82.91** | 85.57 | 82.72 | 85.12 |
| **④ mp·软门控** | 87.42 | 82.56 | 85.62 | 82.57 | **85.19** |
| ⑤ plain·仅 clean 头 | 84.90 | 57.19 | 74.26 | 76.31 | 76.98 |
| ⑥ plain·仅 robust 头 | 84.36 | 79.07 | 82.61 | 80.89 | 82.48 |
| ⑦ plain·硬路由 | 84.90 | 79.07 | 82.57 | 80.91 | 82.52 |
| ⑧ plain·软门控 | 84.92 | 78.30 | 81.59 | 80.62 | 82.20 |
| — oracle | 87.48 | 82.91 | 85.66 | 82.82 | 85.26 |

**判据裁决（按跑前规则，一字不改）**：C1 ✓（85.19 > 85.02，裕度仅 **+0.17**）、C2 ✓、C3 ✓、**C4 ❌（1/7，CIFAR-100 为 3/7）**、C5 ✓（85.19 ≥ 78.49）
→ 形式裁决 **"跨数据集复现成立"**。

> **C1 为"弱通过"（必须与 ✓ 同时标注）**：+0.17 pp 的裕度**落在 mean7 的种子噪声带内**——
> 参照 §1.6 的**主干级**噪声（SimpleCNN 家族：mp 主干 mean7 ±0.18、plain ±0.58），
> 而本配方只更换了**头与估计器的 seed**（同一主干），故其自身的 seed std 仅 ±0.03（虚小，**不可**用作误差尺度）。
> 结论：C1 **形式通过、实质与"两头不可区分"相容**。要把它变成强结论，需在 CIFAR-10 上补 **2 个主干 seed**（已列 §6）。

**但实质结论必须修正**：复现的是**弱支配（软门控 ≥ 任一头）**，
**不是** CIFAR-100 主张的**集成增益（超越 oracle）**。四条独立证据：

1. C4 由 3/7 降到 **1/7**；
2. **软门控 − oracle：+0.12 → −0.07**（CIFAR-100 赢 oracle，CIFAR-10 输）；
3. 门控红利（soft − robust 头）mean7：**+2.90 → +0.17**；
4. **plain 主干上软门控为负**（mean7 82.20 < robust 头 82.48、< 硬路由 82.52）
   → §1.4 的边界条件"软门控红利依赖主干自身的尾部可用性"**再次成立**。

**机制解释 —— 门控红利 = 鲁棒头 clean 代价的回收量**：

| 主干 / 数据集 | 鲁棒头的 clean 代价 | 门控红利（soft − robust，mean7） |
|---|---|---|
| CIFAR-100 · mp | **−5.89** | **+2.90** |
| CIFAR-10 · mp | **−0.87** | **+0.17** |
| CIFAR-10 · plain | **−0.54** | **−0.28** |

→ 门控**不是普适红利**：它的全部作用是"把 clean 头的能力收回到鲁棒头"，
**红利上限即鲁棒头付出的 clean 代价**。代价大（CIFAR-100 细粒度）时有物可收；
代价小（CIFAR-10）时无物可收，门控退化为噪声源。这同时解释了 C1 裕度与 C4 的缩水。

**工程结论（与 CIFAR-100 不同）**：CIFAR-10 上 **② 单鲁棒头与 ④ 双头+软门控几乎不可区分**
（mean7 85.02 vs 85.19；+0.3 上鲁棒头反而高 0.35）→ **当鲁棒头的 clean 代价 ≲1 pp 时，直接部署单鲁棒头**。

**架构杠杆在 CIFAR-10 上同样成立**：同协议对比 **clean 头**，mp 相对 plain 在 +0.3 上
**57.19 → 70.34（+13.2）**、mean7 **76.98 → 82.68（+5.7）**
（CIFAR-100 对应 **+15.0 / +4.5**）✓ 支持 L8 的跨数据集稳健性。
**α 估计器跨数据集同样精确**：mp 特征 R² 0.827 / MAE 0.061；plain R² 0.933 / MAE 0.037 ✓ 支持 L7 前半。

---

## 2. L1–L8 终稿

| 定律 | 终版表述 |
|---|---|
| **L1 反相关律** | clean↑ → drop@0.3↑，同数据集内跨容量单调。跨数据集不可直接比较 drop 绝对值。 |
| **L2 容量决定 NAT 形态** | ① 中带可救、正侧尾在训练端不可救；② SimpleCNN 呈"再分配"、深层呈"全轴塌陷"；③ NAT 收益绑定其自身的头，作为特征提取器**无优势**（差距仅 1.78 pp，噪声内）。 |
| **L3 两通路机制** | ① 迁移近正交但有单向代价（α 训练对 σ 零收益；σ 训练使 α 掉 6~13 pp）；② 迁移幅度**同时依赖架构与配方**（0.98~5.26，跨 5 倍）；③ **幅度机制**：+α 压缩、−α 放大（端点不变）；④ MaxPool 不改变幅度斜率。 |
| **L4 正侧不可训练区** | 训练端四条路径（预算/对抗/课程/范围）全部无效；**机制（第五阶段）= 有效秩塌陷**（+0.3 −20.6%、+0.5 −50.9%），非类间距缩小。 |
| **L5 Dithering** | 4bit 增益 = 分布重塑 × MaxPool 密度；与随机性无关；增益随"可用头部空间"缩放（CIFAR-10 +6.53 → CIFAR-100 +2.84）。 |
| **L6 读出瓶颈律** | α 失真的信息损伤主要发生在**读出**而非表征（headroom +20.12 pp @+0.3）；该律在四架构 × 双数据集成立；**第五阶段补充**：塌陷后的低秩子空间里信息仍线性可达。 |
| **L7 可路由律** | ① **失真可在线估计**：岭回归 R² **0.83~0.94**、MAE **0.036~0.061**（跨双数据集 × 双主干共 4 组，全部成立）。② **门控的净收益 = 鲁棒头 clean 代价的回收量**（P0-C 修正）：CIFAR-100/mp 代价 −5.89 → 红利 **+2.90 mean7**；CIFAR-10/mp 代价 −0.87 → 红利 **+0.17**；CIFAR-10/plain 代价 −0.54 → 红利 **−0.28**。→ **当鲁棒头 clean 代价 ≲1 pp 时，应直接用单鲁棒头，门控不值得**。③ 分布漂移下门控比硬路由稳健（6/6 最优），但该优势同样**依赖主干自身的尾部可用性**。 |
| **L8 池化保护律（新）** | 池化对 α 失真的保护**主要来自降维**：同协议 3 seeds 下，5×avg 相对 2×max 锚点降低 drop 约 **10 pp**；而 5×avg 与 5×max 的 drop 差仅 **+1.17 ± 1.5 pp**（3 seeds），**在 seed 噪声内不可分辨** ⇒ "**降维 66% / 非均值聚合 34%**"的 2 : 1 分解**已被 3-seed 复核降级为"初步"、不予裁决**（判据 ③）。**三条排除线（稳健）**：① 保护**不**来自"吸收失真"——f_α 与 max 在非负域**精确可交换**（机器精度）却非成因；② **不**来自"不变区集中"（5-pool 的易感度 S 反而更高、不变区占比不变）；③ **不**来自"max 的选择性/可交换性"——**top-2 与 f_α 不可交换，保护却与 max 等同**（42.98 vs 42.47）。**架构杠杆本身跨数据集成立**：+0.3 上 CIFAR-100 **+15.0**、CIFAR-10 **+13.2**（clean 头同协议）。 |

---

## 3. 部署配方表 v2（全实测、3 seeds）

**① CIFAR-100 / SimpleCNN 系列** —— 软门控在此**有效**（鲁棒头 clean 代价 −5.89 pp）

| 场景 | 配方 | clean | α=+0.3 | 中带 | mean7 | 成本 |
|---|---|---|---|---|---|---|
| **综合最优** | mp 主干 + 双头 + **软门控** | 60.70 | **49.10** | **55.16** | **55.08** | ≈12 min |
| 单头简化 | mp 主干 + 仅 robust 头 | 55.10 | 48.58 | 52.82 | 52.19 | ≈12 min |
| 保留 clean 优先 | mp 主干 + 仅 clean 头 | **60.99** | 43.21 | 54.09 | 54.12 | 0 |

**② CIFAR-10 / SimpleCNN 系列** —— 软门控在此**无效**（鲁棒头 clean 代价仅 −0.87 pp）

| 场景 | 配方 | clean | α=+0.3 | 中带 | mean7 | 成本 |
|---|---|---|---|---|---|---|
| **综合最优（推荐：单头，免门控）** | mp 主干 + **仅 robust 头** | 86.61 | **82.91** | 85.70 | 85.02 | ≈12 min |
| 备选 | mp 主干 + 双头 + 软门控 | 87.42 | 82.56 | **85.94** | **85.19** | ≈12 min |
| 保留 clean 优先 | mp 主干 + 仅 clean 头 | **87.48** | 70.34 | 84.97 | 82.68 | 0 |
| 原始基线 | plain 主干 + 仅 clean 头 | 84.90 | 57.19 | 80.11 | 76.98 | 0 |

> **读表要点**：② 的"综合最优"给**单鲁棒头**而非软门控——两者 mean7 差 0.17、+0.3 上鲁棒头反而高 0.35。
> 这正是 P0-C 的机制结论（§1.7）：**门控的净收益 = 鲁棒头 clean 代价的回收量**，代价 ≲1 pp 时门控不值得。

**③ 深层与其他**

| 场景 | 配方 | clean | α=+0.3 | 中带 | mean7 | 成本 |
|---|---|---|---|---|---|---|
| **深层 VGG-11** | 冻结 Exp2 主干 + readout-NAT(24ep) | 64.97 | **47.79±3.18** | 60.96 | **58.22±0.67** | ≈25 min |
| 深层 ResNet-18 | 冻结 Exp3 主干 + readout-NAT(24ep) | **65.00±1.85** | **26.82±3.40** | **51.19** | **44.74±1.70** | ≈30 min |
| 双通路并存（α+σ） | σ-only 训练 | — | — | — | 双扰动最坏点 **24.60** | ≈45 min |
| **不推荐** | 全网络 NAT/Exp3（尾部目标） | — | ≤ 24.69 | — | ≤ 49.24 | 1~6.5 h |

---

## 4. 方法与可复现性

- 新增脚本：`v5_probe_maxpool_mechanism.py`、`v5_full_recipe.py`、`v5_probe_alpha_drift.py`、
  `v5_probe_rank_collapse.py`、`v5_p0c_criteria.py`（P0-C 判据评估）；
  `v3_methods_simplecnn.py` 增 `simple_cnn_mp_avg/_top2` 变体。
- **`v3_methods_simplecnn.py::build_loaders` 补 CIFAR-10 分支**（收尾期修补）：
  该脚本的 `--dataset` 此前只影响路径与类别数、**数据恒为 CIFAR-100**，
  在 CIFAR-10 上会因标签 0–99 越界而在第 1 个 batch 崩溃。
  修补后按 `DATASET_STATS` 分派，**对 CIFAR-100 路径逐位等价**（已用同一 loader 冒烟验证）。
- 产物：`outputs_cifar100/v5_*`、`outputs*/v3_readout_repair`、`outputs*/v3_methods/*`（原产物零改写）。
- `results_master.csv` 由 `build_ledger.py` 只读扫描重建（**126 行，配对自检 0 FAIL**）；
  `build_ledger.py` 的 `v3_methods` 收割已从"仅 CIFAR-100"扩为**双数据集**（否则 CIFAR-10 新跑不入账）。
- **串行/显存纪律**：本阶段因并发出现过 1 次 **CUDA OOM**（resnet18 s43 与 top2 同时训练），
  已单独补跑；此前第三阶段的 error 1455（系统提交内存）未再发生。
- **已知局限**：
  ① **软门控维持"事后变体 + CIFAR-100 特化"的定性，不升格**：P0-C 的事前预登记中 C1∧C2∧C3 **形式成立**，
     但 **C1 属"弱通过"**（裕度 +0.17 pp 落在 mean7 的**主干 seed 噪声带 ±0.18~0.58** 内，
     且本实验只用了 **1 个主干 seed**——3 seeds 变的是头与估计器，不是主干）、
     **C4 仅 1/7**、软门控**不再超过 oracle**（+0.12 → **−0.07**）→
     **"插值即集成"未复现**，降级为"弱支配"；其可用范围由新机制律限定：
     **门控红利 = 鲁棒头 clean 代价的回收量**（CIFAR-100 −5.89 → +2.90；CIFAR-10 −0.87 → +0.17；plain −0.54 → −0.28）
     ⇒ **只在鲁棒头 clean 代价大的任务（CIFAR-100 式细粒度）上有效，是 CIFAR-100 特化的方法**。
     要断言"软门控严格优于单鲁棒头"，需在 CIFAR-10 上补 **2 个主干 seed** 凑成 3；
  ② 自适应 τ 未做（τ 固定自验证集）；多头扩展（α̂ + σ̂ 三头）未做；
  ③ **L8 的 3-seed 复核已完成（§13.8），结论仍为"初步"**：5×avg 与 5×max 同为 3 seeds（同协议），
     二者 drop 差 **+1.17 pp**，落在预登记的 **1~2 pp 区间（判据 ③）** → **2 : 1 分解不予裁决**；
     且该差值**小于两组的 seed std（±0.91 / ±1.54）**，3 seeds 无法分辨，升为定论需 **≥8 seeds/组**。
     稳健的是方向性结论：**保护主要来自降维**，以及三条排除线（非"吸收失真"、非"不变区集中"、非"max 选择性"）；
  ④ **深层指标（含 clean）的 seed 方差被 2-seed 低估**：ResNet-18 的 clean 在 3 seeds 下为 ±1.85（此前按 2 seeds 报 ±0.15）；
     "clean 稳、只有尾部噪声大"**不可外推到深层**（SimpleCNN ±0.64 → VGG-11 ±0.26 → ResNet-18 ±1.85）；
  ⑤ 两条读出评估路径（`v3_readout_repair` 的整模型前向 vs `v5_full_recipe` 的手工线性头）
     在非零 α 处存在 **≤1.2 pp** 的系统性差异（α=0 处严格一致）→ **跨路径比较须注意**；
     本报告的消融阶梯（①–⑧）全部取自同一条路径，不受影响。

---

## 5. 研究故事线（终版）

```
① 发现敌人（L1/L3/L4）
   正向失真破坏力 3.4× 于负向（跨三阶段四证据线）；
   其机制 = 中段压缩（P1-4）→ 有效秩塌陷（第五阶段）→ 正侧尾部结构性不可训练（L4）

② 训练端失效的穷尽（L4 的四条独立负结果）
   预算分配 / α-对抗 / 课程式 / 训练范围外推 全部无效 → 尾部不是"采样假设"问题

③ 真正的杠杆在读出（L6/L7）
   信息未死（headroom +20.12 pp）→ 冻结主干只训读出（readout-NAT）即获 +13~23 pp、成本 1/15~1/30
   → 进一步：在线 α 估计（R² 0.83~0.94）+ 门控 = 恢复 clean 且尾部更优（L7）

④ 架构端亦有独立杠杆（L8）
   池化密度的保护**主要来自降维**（3 seeds：5×avg 相对 2×max 锚点降 drop ~10 pp；
   5×avg 与 5×max 之差仅 **1.17 ± 1.5 pp**，噪声内不可分辨 →"非均值聚合"成分维持"初步"）
   三条排他线（稳健）：不来自"吸收失真"（可交换却非成因）、不来自"不变区集中"、
   不来自"max 的选择性/可交换性"（top-2 不可交换却与 max 同等）
   → 与读出杠杆**在尾部可叠加**

⑤ 完整配方（第五阶段实测 + 跨数据集事前预登记）
   CIFAR-100：mp 主干 + 双头 + 软门控 = mean7 55.08 / clean 60.70 / +0.3 49.10
              （预登记硬路由版未达标 47.90）
   CIFAR-10 ：同一配方只在形式上复现（85.19 / 87.42 / 82.56）——
              真正复现的是"弱支配"而非"集成"（C4 1/7、不再超过 oracle），
              且那里单鲁棒头就已够用（85.02）
   ⇒ 配方定稿的准确表述：
     门控红利 = 鲁棒头 clean 代价的回收量；代价 ≲1 pp 时用单鲁棒头

⑥ 工程结论
   对 CIM 部署：**冻结模拟权重 + 数字域读出适配**是最优路径，三步：
   - 架构端先上 **MaxPool 密集主干**（零训练成本；+0.3 上 CIFAR-100 +14.4、CIFAR-10 +15.5，
     为本研究单项最大的架构杠杆）
   - 读出端训 **α-混合鲁棒头**（24ep，≈12 min；尾部 +5.4 pp / +12.6 pp）
   - **门控仅在鲁棒头 clean 代价大时添加**（CIFAR-100 式细粒度任务）；
     clean 代价 ≲1 pp 的任务（CIFAR-10）直接用单鲁棒头即可
   若需同时抵御 α 与 σ，应做 **σ-only 增强训练**（joint 的 α 成分是净负担）。
```

---

## 6. 第六阶段候选

1. **门控红利的定量律验证**（接续 P0-C，最高优先）：本阶段已把红利钉成
   "**鲁棒头 clean 代价的回收量**"，需在**第三个数据集/主干**上检验该关系的线性度；
   并测**自适应 τ**（按 α̂ 的置信度自校准）与 **α̂+σ̂ 三头**扩展。
2. ~~CIFAR-10 配方复现~~ **已完成（P0-C，§1.7）**，遗留一项：CIFAR-10 上**补 2 个主干 seed**，
   判断 C1 的 **+0.17 pp** 裕度是否稳健（现有 3 seeds 变的是头与估计器，不是主干）。
3. ~~L8 的 3-seed 复核~~ **已完成（§13.8）：判据 ③ 命中（+1.17 pp），"2 : 1 分解"维持"初步"、不予裁决**。
   若要点估计定论，需 **≥8 seeds/组**（现 σ≈1.2、待分辨 1.2 pp）——或改测一个**效应量更大**的算子对照
   （avg 与 max 的差距本身太小，是这条路的根本瓶颈）。
   另需检验 top-2 的 mean7 优势（**54.70**，单 seed 四者最高）是否为真实效应（同样受 seed 噪声支配）。
4. **有效秩与可恢复性的定量关系**：把 L4（秩塌陷）与 L6（读出可恢复）用同一指标串起来，
   给出"给定秩损失下的可恢复上界"。
5. **硬件语义对齐**：把 readout-NAT 的"读出层"映射到真实 CIM 的数字后端（ADC 后处理），
   评估其可行性（面积/延迟/功耗）。
