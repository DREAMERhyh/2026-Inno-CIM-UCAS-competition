# 技术报告版 / 竞赛版：A 组同步 + 通读复核

> **执行者**：claude-0b 派出的独立会话（本工作区新的技术报告/竞赛版独占写者）。
> **任务**：① 核对 `781d8c2` 的 A1/A2 修复是否到位；② 把 journal 这一轮已认定要改的同类问题
> （A3/A4/A7/A9/A10）在 `paper/technical-report/` 与 `paper/competition/` 里逐处找一遍；
> ③ 通读两份稿，找复核者没扫到的同类问题。
> **纪律**：**不采信复核报告写的数字**——每条都打开源文件自己算一遍，分 CONFIRMED（改）/
> REFUTED（不改，写算法）/ UNCLEAR（不改，写找了哪些文件）。
> **时间**：2026-09-24。**未 commit**，等工作区验收。
>
> **一句话结论**：A 组在技术报告版**命中 8 条**（其中 A5 是复核者未列但成立的同类）；
> 竞赛版命中 **3 条**（全是「口径迁移只搬数字、没搬定语」）；另有 **2 条新发现**在技术报告版
> （F01 图注与"方向稳定成立"被 ResNet-18 否证、干净代价用错协议）。
> 两条 REFUTED（复核报告的数字我复算不出来或前提不同）写在 §三，不改。

---

## 〇、改动总览

| # | 位置（改后行号） | 判据 | 性质 |
|---|---|---|---|
| T1 | `technical-report:493` | CONFIRMED（A1） | 图注被自己的数据否证；`781d8c2` 只改了正文、**漏了图注** |
| T2 | `technical-report:438` | CONFIRMED（A3） | 表三行不同主干；第三行是第二行的训练基底 |
| T3 | `technical-report:569`、`1147-1148` | CONFIRMED（A4） | "近似正交可叠加"应为"次可加" |
| T4 | `technical-report:616` | CONFIRMED（A6） | H1/H2/H3 机理数字全是 CIFAR-10 的，挂 CIFAR-100 图注下 |
| T5 | `technical-report:404` | CONFIRMED（A7） | "headroom 单调上升"被括号里的 30.82 否证 |
| T6 | `technical-report:778` | CONFIRMED（A8） | "差距远超门槛"有一行只差 2.85 |
| T7 | `technical-report:65`、`467`、`482`、`1133`、`1144` | CONFIRMED（A10） | "四条全部失效"应降级 |
| T8 | `technical-report:63`、`458`、`1143` | CONFIRMED（A13） | 成本 1/15~1/30 与自己的成本表算不出 |
| T9 | `technical-report:458-459` | CONFIRMED（新） | 干净代价 3.19~6.67 是 8ep 口径，本表是 24ep |
| T10 | `technical-report:61`、`404-412`、`1141` | CONFIRMED（A5） | 探针是 oracle-α，全文未标注 |
| T11 | `technical-report:362`、`373` | CONFIRMED（新） | "正侧跌落显著大于负侧 / 方向稳定成立"被 ResNet-18 否证 |
| C1 | `competition:49-51` | CONFIRMED（新） | "跌幅 75 个百分点…重于 VGG-11（36.70%）"——跌幅与精度混用 |
| C2 | `competition:150`、`156` | CONFIRMED（新） | 同上（正文 + 图注的普适不对称断言） |
| C3 | `competition:566` | CONFIRMED（新） | 括号里的 10.87/19.75 是 **CIFAR-100** 的数（本文全篇 CIFAR-10） |
| C4 | `competition:460-461` | CONFIRMED（新） | A7 同形：用 +0.5 的 +1.08 推及整个"深度外推区"，+0.6 处是 −1.58 |

**验收（A1/A2 是否到位）**：
- **A2** ✅ **到位**。`technical-report:1143` 已是 `+15.2\sim+31.8`（781d8c2 改的），grep 全文无 `13~23` 残留；
  技术报告摘要本来就不含区间数字；竞赛版不含读出适配内容，无对应处。
- **A1** ⚠ **未完全到位**。`781d8c2` 只改了技术报告**正文**（471–476，"负侧几乎不动只在浅层的 SimpleCNN 上成立"），
  **图 5 图注第一句仍是旧的**「正侧失真使末端特征的有效秩塌陷，负侧几乎不动」——
  于是图注与紧随的正文**直接互相矛盾**。已补改（T1）。
- 另外，claude-0b 简报里说 A2 改的是"technical-report:471/1090"，实际改的是结论节（现 1143 行）与……
  摘要处**本来就没有** 13~23，无需改。**竞赛版没有 A2 的对应处**（它不覆盖第三阶段以后的内容）。

---

## 一、A 组逐条（含源文件与算式）

### A1 @ `technical-report:493`（图 5 图注）：CONFIRMED

- **源文件**：`outputs_cifar100/v5_rank_collapse/rank_stats.csv`（SimpleCNN）、
  `rank_stats_exp2vgg11.csv`（VGG-11）、`rank_stats_exp3resnet18.csv`（ResNet-18）
- **算式**：$\alpha=0$ 的 `rank_eff` 为基准，算 $(\text{rank}(\alpha)-\text{rank}(0))/\text{rank}(0)$
- **我算出**（逐位取自三个 CSV 的 `rank_eff` 列）：

  | 主干 | rank(0) | rank(+0.3) | rank(−0.3) | 正侧 | **负侧** |
  |---|---|---|---|---|---|
  | SimpleCNN | 38.782 | 30.782 | 37.999 | −20.63% | **−2.02%** |
  | VGG-11 | 141.959 | 94.629 | 116.527 | −33.34% | **−17.91%** |
  | ResNet-18 | 83.143 | 34.337 | 27.162 | −58.70% | **−67.33%** |

  ⇒ **ResNet-18 的负侧（−67.3%）比正侧（−58.7%）掉得更多**，"负侧几乎不动"只在 SimpleCNN 上成立。
- **改法**（图注第一句改为分主干陈述）：
  > 正侧失真使末端特征的有效秩塌陷；负侧的形态随主干而异
  > （SimpleCNN 几乎不动，VGG-11 与 ResNet-18 两侧都塌，ResNet-18 的负侧更甚）。

### A2 @ `technical-report:1143`：已到位（不重做）

- **源文件**：`outputs_cifar100/v3_readout_repair/` 的六个 `*ep24*` 文件（三主干 × 三 seed）
- **算式**：`e_readout_NAT` 的 `0.3` 列三 seed 均值 − `a_original_fc` 的 `0.3` 列
- **我算出**：43.31−28.09=**+15.22**；47.79−15.98=**+31.81**；26.82−6.22=**+20.60** ⇒ 区间 **+15.2~+31.8** ✓
  与 `technical-report:1143` 现文一致；全文 grep 无 `13~23` 残留 ✓

### A3 @ `technical-report:438`（表 `tab:nat`）：CONFIRMED

- **源文件**：`outputs_cifar100/v3_readout_repair/` 的
  `readout_repair_c100_plain_s4{2,3,4}_ep24_*.csv`（SimpleCNN）、
  `readout_repair_c100_exp2_vgg11_s4{2,3,4}_ep24_*.csv`（VGG-11）、
  `readout_repair_c100_exp3_resnet18_s4{2,3,4}_ep24_*.csv`（ResNet-18）；
  以及台账 `results_master.csv` 的 `cifar100|{vgg11,resnet18}|clean|7pt` 行
- **算式**：取每个文件 `a_original_fc` 行的 `0.3` 列（即各读出实验所依附主干的原始头）
- **我算出**：`a_original_fc @0.3` = **28.09 / 15.98 / 6.22**（三主干，三 seed 同值）；
  而表里"干净模型（不处理）"行写的是 **28.09 / 16.92 / 2.70**，
  其中 16.92 与 2.70 出自 `results_master.csv` 的 `task1_vgg11` / `task1_resnet18` 行——**另一批主干**。
- **另证**：表第三行 ResNet-18 的 **6.22 与该列读出适配所依附主干的原始头是同一个值**
  （台账 §13.9 亦记 `Exp3(200ep) 24.69 | 6.22`）⇒ **第三行是第二行的训练基底**。
- **改法**：第一行改为「原始分类头（同主干）」28.09 / 15.98 / 6.22，并加表注：
  > 表注（口径）：第一行为各读出实验所依附主干的原始分类头（`v3_readout_repair` 的
  > `a_original_fc` 行在 α=+0.3 的读数；SimpleCNN 的该主干即 `task1` 权重，故与台账逐位一致）。
  > 第三行为各架构上最好的全网络鲁棒训练配方（SimpleCNN 取 v3 路径逐层独立 NAT 的 3 seed 均值 26.57，
  > VGG-11 与 ResNet-18 取 200 ep 的 Exp3，24.69 / 6.22）。注意 ResNet-18 的第三行与该列第一行是
  > **同一块主干**——全网络配方并没有改善它的原始头。（`task1` 主干的 16.92 / 2.70 属另一批权重，
  > 不参与本表逐列比较。）

  **结论方向不变**：43.31 > 26.57、47.79 > 24.69、26.82 > 6.22 ✓

### A4 @ `technical-report:569`、`1147-1148`：CONFIRMED

- **源文件**：`outputs_cifar100/v3_readout_repair/readout_repair_c100_mp_clean_alpha+0.30_n20000.csv`
  （架构单独 42.47、架构+读出 49.12）、`readout_repair_c100_clean_ep24_alpha+0.30_n20000.csv`（读出单独 43.38）；
  正确表述抄自 `论文v3.md:646-660`
- **算式**：49.12−28.09=+21.03（实际）；(42.47−28.09)+(43.38−28.09)=14.38+15.29=**+29.67**（单独之和）；
  少 29.67−21.03=**8.64**；回收比例 21.03/29.67=**70.9%**
- **我算出**：与主稿"次可加、合起来拿到六到七成、损失三到四成"一致；
  且 49.12 所在文件名为 `_clean_`（**单 seed**，无 seed 后缀）。
- **改法**：
  > 架构杠杆与读出杠杆在尾部**可以同时受益，但显著次可加、不是相加**：5×MaxPool 主干 + readout-NAT
  > 的 α=+0.3 达 **49.12**（相对基线 28.09 即 +21.03），而两个手段单独的增益之和是 +29.67
  > （架构 +14.38、读出 +15.29）——**合起来只拿到六到七成，损失三到四成**。
  > 该组为**单 seed** 口径（连续均匀 α、24 ep、20k 样本，同 v3 路径）；13 点离散 α、3 seed 的
  > v5 口径给出同样的次可加结论。

  摘要与结论里的"与之正交的/可叠加"同步改为"另一条零成本的独立杠杆"/"显著次可加、不独立相加"。

### A5 @ `technical-report:61`、`404-412`、`1141`：CONFIRMED（复核者未列，我判为同类）

- **源文件**：`outputs_cifar100/v7_probe_audit/ra_onprotocol_main.csv`（两列探针：
  `probe_same_cond` 在**测试点的失真特征**上训练；`probe_clean_train` 只在干净特征上训练）
- **算式**：`headroom_same` = `probe_same_cond` − `head_acc`
- **我算出**：α=+0.3 时 48.35−28.09=**20.26**（用的是 same_cond 列）；
  同行的 `probe_clean_train` 只有 **18.1**——**比模型自带头（28.09）还低**
  ⇒ headroom 是**逐 α 训练的 oracle 量**，技术报告全文没有任何地方说明这一点。
- **改法**：正文加「口径标注（必读）」一段（探针逐 α 条件训练、headroom 是 oracle-α 诊断量、
  不是可部署读数；不知道 α 的实测读数是 43.31；干净特征探针只有 18.1），摘要与结论的
  headroom 处加 `oracle-$\alpha$` 限定词。

### A6 @ `technical-report:616`：CONFIRMED

- **源文件**：CIFAR-10 侧 `outputs/extension5_dithering_vgg11/{activation_stats,arch_dithering_summary,flip_statistics}.csv`；
  CIFAR-100 侧 `outputs_cifar100/extension5_dithering_vgg11/` 同名文件
- **我算出**（逐位对照两目录）：

  | 量 | 技术报告写的 | CIFAR-10 侧实际 | **CIFAR-100 侧实际** |
  |---|---|---|---|
  | 分类器输入峰度 clean→8bit | 1.486 → 0.110 | **1.486 → 0.1096** ✓ | 2.5406 → 0.8473 |
  | 4bit 峰度 | 0.214 | **0.2144** ✓ | 0.9802 |
  | 翻转 4bit 救回:杀死 | 4.15:1 | **860:207 = 4.154** ✓ | 655:371 = 1.77 |
  | ResNet-18 4bit−8bit | +0.39 | **+0.39** ✓ | **−0.21** |
  | SimpleCNN 4bit−8bit | −13.68 | **−13.68** ✓ | **−14.71** |

  ⇒ 技术报告这一节的**每一个机理数字都是 CIFAR-10 的**，而它的图注写 CIFAR-100。
- **改法**：段首加「以下机理数字全部取自 CIFAR-10 侧」并给出目录；段末补一段
  CIFAR-100 侧的平行读数（峰度 2.541→0.847→0.980、翻转 655:371、−0.21、−14.71），
  并指出三条判决在两个数据集上同向——**这反而是一条跨数据集稳健性证据**。

### A7 @ `technical-report:404`：CONFIRMED

- **源文件**：`outputs_cifar100/v7_probe_audit/ra_onprotocol_main.csv` 的 `headroom_same` 列
- **我算出**：`all|alpha=+0.4` = **30.9**、`all|alpha=+0.5` = **30.82** ⇒ 是**下降**，不是上升。
- **改法**：「headroom 随失真增强先放大、后趋于平坦（+0.3→+0.4 增幅最大，+0.4→+0.5 微降 0.08——不是单调上升）」

### A8 @ `technical-report:778`：CONFIRMED

- **源文件**：`outputs_cifar100/v7_readout_capacity/capacity_ladder.csv`（`alpha=0.3` 行）
- **我算出**：线性头（torch）51.4733；k-NN 38.12 → **−13.35**；瓶颈 MLP 43.38 → **−8.09**；
  两层 MLP 48.62 → **−2.85**。**−2.85 < Δ_proc = 3.45** ⇒ 按本文自订门槛不可裁决。
- **改法**：写明三个差值，并注明两层 MLP 落在门槛内、按本文规则不可裁决（方向仍劣于线性头）。

### A9 @ 两份稿：**不适用**（REFUTED-of-applicability）

- 复核者的 A9（"4/6 应为 5/6"）针对 `journal:824`；`grep "4/6"` 在
  `paper/technical-report/main.tex` 与 `paper/competition/main.tex` **均无命中**
  （这两份稿都没有漂移压力测试那一节）。**无需改动**。

### A10 @ `technical-report:65`、`467`、`482`、`1133`、`1144`：CONFIRMED

- **源文件**：`outputs_cifar100/v3_methods/{budget_front,budget_rear,budget_sens,adv_uniform,curriculum_uniform,range_0.50,plain_uniform}/alpha_sensitivity.csv`
- **我算出**（各文件 `0.3` 行）：

  | 路径 | α=+0.3 | 相对基准 26.03 |
  |---|---|---|
  | 预算分配（三种轮廓） | 19.56 / 25.46 / **26.13** | **+0.10**（最好） |
  | α 空间对抗训练 | 24.32 | **−1.71**（落在 3 pp 门槛内） |
  | 课程式采样 | 21.78 | −4.25 |
  | 范围外推 ±0.5 | 20.48 | −5.55 |

  ⇒ 只有后两条可称"显著劣于基准"。
- **改法**：摘要/小节标题/结论的"全部失效"改为分级表述
  （两条显著劣于基准、一条落在单种子门槛内记为不可分辨、一条与基准同级；**四条都没有改善尾部**）。

### A13 @ `technical-report:63`、`458`、`1143`：CONFIRMED

- **源文件**：`论文v3.md:518-520`（唯一出处，仓库里没有成本产物文件——复核者也标注了这点）
  + 技术报告表 `tab:nat` 自己的成本列
- **算式**：12/60 = **1/5**；30/390 = **1/13**；12/390 = **1/32**（390 min = 6.5 h）
- **我算出**：「1/15~1/30」既不等于整体区间（1/5~1/32），也不等于任何一对逐主干配对
  （SimpleCNN 1/5、VGG-11 12/25=…按 25 min 对 6.5 h 是 **1/15.6**、ResNet-18 **1/13**）。
  「训练时间低 1~2 个数量级」也过强：1/5~1/32 只有 **0.7~1.5 个数量级**。
- **改法**：统一为「约 1/5~1/30 的主干重训成本（12~30 min 对 1~6.5 h）」与「低约一个数量级」。
  （端点取整：最短/最长配比的精确值是 12/390 = 1/32.5，写成"约 1/30"；括号里给了两个区间让读者自查。）

---

## 二、通读新发现（复核者未扫到，同类病因）

### T9 @ `technical-report:458-459`：CONFIRMED —— 干净代价用了 8ep 口径

- **源文件（8ep 口径）**：台账 `experiments_ledger.md:838-848`（并表"20k/8ep"）：
  CIFAR-100 的 clean 代价为 SimpleCNN −6.34、VGG-11 −4.52、ResNet-18 −3.19 ⇒ 区间 **3.19 ~ 6.67**；
  同一句亦见 `论文v3.md:719`。
- **源文件（24ep 口径，即本表）**：`readout_repair_c100_{plain,exp2_vgg11,exp3_resnet18}_*ep24*` 的
  `e_readout_NAT` 与 `a_original_fc` 的 `0.0` 列
- **我算出**（三 seed 均值）：55.81 / 64.97 / 65.00 对 59.99 / 68.05 / 72.69
  ⇒ 代价 **4.18 / 3.08 / 7.69**，即区间 **3.08 ~ 7.69**。
- **判据**：技术报告这句紧跟在 **24 ep/3 seed 表**之后，却用了 8 ep 的数 ⇒ 正是"口径迁移"。
- **改法**：改为「3.08~7.69 个百分点」并给出 α=0 处的六个数（可追溯）。

### T11 @ `technical-report:362`、`373`：CONFIRMED —— 方向性断言被 ResNet-18 否证

- **源文件**：`results_master.csv` 的 `cifar100|{simple_cnn,vgg11,resnet18}|clean|7pt` 行
  （`clean` / `drop_at_0.3` / `neg_0.3`）、`outputs/task1_*/alpha_sensitivity.csv`（CIFAR-10）
- **算式**：不对称比 = 正侧跌幅 ÷ 负侧跌幅
- **我算出**：

  | 主干 | C100 clean | 正侧跌 | 负侧跌 | C100 比 | C10 clean | 正侧跌 | 负侧跌 | **C10 比** |
  |---|---|---|---|---|---|---|---|---|
  | SimpleCNN | 59.99 | 31.90 | 17.08 | 1.87 | 84.90 | 29.82 | 8.84 | 3.37 |
  | VGG-11 | 67.09 | 50.17 | 42.61 | 1.18 | 89.80 | 53.10 | 37.97 | 1.40 |
  | ResNet-18 | 72.09 | 69.39 | 68.84 | **1.01** | 92.09 | 75.01 | 79.71 | **0.94** |

  ⇒ ① F01 图注「正侧跌落**显著**大于负侧」在 ResNet-18 上不成立（69.39 对 68.84）；
  ② 正文「跨条件稳定成立的只有**方向**」也被 C10/ResNet-18 的 **0.94**（反号）否证。
- **改法**：图注补「该方向在 SimpleCNN 与 VGG-11 上明显，在 ResNet-18 上几乎消失：两侧跌幅 69.39 对 68.84」；
  正文补全六个比值并改为「方向随深度衰减，并在最深的主干上（C10/ResNet-18）反号」。

### C1/C2 @ `competition:49-51`、`150`、`156`：CONFIRMED —— 跌幅与精度混用 + 普适断言

- **源文件**：`outputs/task1_{simplecnn,vgg11,resnet18}/alpha_sensitivity.csv`
- **我算出**：CIFAR-10 上 α=+0.3 跌 29.82 / 53.10 / 75.01；α=−0.3 跌 **8.84 / 37.97 / 79.71**。
  - 摘要原文「跌幅 75 个百分点，显著重于 VGG-11（36.70%）与 SimpleCNN（55.08%）」——
    括号里的两个数是**剩余精度**不是**跌幅**（跌幅是 53.10 与 29.82），同一句混了两个量。
  - 正文「而 α=−0.3 时的跌幅小得多」与图注「正向失真（α>0）的破坏力显著大于负向」
    在 ResNet-18 上**不成立**（负侧 79.71 > 正侧 75.01）。
- **改法**：摘要括号改「跌至 36.70% / 跌至 55.08%」并给 3.4 倍加限定（SimpleCNN）；
  正文/图注补 ResNet-18 的两侧对称事实。
  **不动** `competition:643` 的「破坏力不对称（任务1 的 3.4 倍）」——它已标注出处，读者可自查。

### C4 @ `competition:460-461`：CONFIRMED —— A7 的同形（单点推及整个区间）

- **源文件**：`outputs/extension4_alpha_wide/alpha_wide_scan_summary.csv`（`simple_cnn` 的
  `nat_finetune` − `clean`）
- **我算出**：+0.35 **+1.29**、+0.4 **+1.63**、+0.5 **+1.08**、+0.6 **−1.58**
  （clean 21.07、finetune 19.49）；同期 Scratch 为 +5.89 / +0.77 / −10.77 / **−10.89**
- **判据**：原文「是三种权重中唯一在**深度外推区**不恶化的」用 +0.5 的单点读数推及整个外推区，
  被最外端的 −1.58 否证——与 A7（"单调放大"用 +0.4 推及 +0.5）是**同一个形状**：
  把一个点的性质写成整个区间的性质。
- **改法**：改为「唯一在 α=+0.4 与 +0.5 处仍不恶化（+1.63 / +1.08）；到最外端 α=+0.6 才转为 −1.58，
  但仍远优于 Scratch 的 −10.89」。

### C3 @ `competition:566`：CONFIRMED —— CIFAR-100 的数混进 CIFAR-10 稿

- **源文件**：`outputs_cifar100/extension5_dithering_vgg11/group_a_bits_scan.csv`：
  **3 bit = 10.87、4 bit = 19.75**（CIFAR-100）；
  `outputs/extension5_dithering_vgg11/group_a_bits_scan.csv`：**3 bit = 42.97、4 bit = 43.27**（CIFAR-10）
- **判据**：竞赛版全篇是 CIFAR-10 口径（该节图注亦写 CIFAR-10）⇒ 括号里应是 42.97 / 43.27。
- **改法**：括号改为「（+21.90 对 +7.82），但绝对精度已经腰斩（42.97% 对 43.27%，约为干净模型 89.80% 的一半）」。

---

## 三、REFUTED / UNCLEAR（**不改**，写清算法或找不到什么）

### R1 `competition:466`「VGG-11 在负侧全区间保持 +5~14 个百分点」：**不改**

- **源文件**：`outputs/extension4_alpha_wide/alpha_wide_scan_summary.csv`（`vgg11`，`nat_scratch` − `clean`）
- **我算出**：−0.1 **+2.51**、−0.2 **+6.72**、−0.3 **+12.89**、−0.35 +14.17、−0.4 +14.27、−0.5 +10.49、−0.6 **+5.31**
- **判定**：若"负侧"按上文语境（与 SimpleCNN 的 +5.48 同为 −0.3 起）读，区间恰为 **+5.31~+14.27** ✓ 成立；
  若按字面含 −0.1/−0.2，则最低 +2.51 ✗。**口径歧义，未找到原始定义** ⇒ 不改，留给作者定夺。

### R2 `technical-report:601`、`679`、`729`「门控红利 −5.89/+2.90/+0.68/−1.14」：**不改（我复算全部一致）**

- **源文件**：`outputs_cifar100/v5_full_recipe/recipe_simple_cnn_mp.csv`、
  `recipe_robust_vgg11_exp2vgg11.csv`、`recipe_robust_resnet18_exp3resnet18.csv`
- **算式**（按台账 §13.7 口径：代价取 α=0 的三 seed 均值差，红利取 **mean7** 的 `acc_softgate − acc_robust_head`）
- **我算出**：C100/mp 代价 60.99−55.10=**5.89** ✓、红利 55.0833−52.1871=**+2.90** ✓；
  VGG-11 代价 68.05−64.5967=**3.45** ✓、红利 59.7681−59.0886=**+0.68** ✓；
  ResNet-18 代价 72.69−68.3267=**4.36** ✓、红利 51.0071−52.1471=**−1.14** ✓。
  **全部逐位复现**，不改。

### R3 `technical-report:679-681`「21.03±1.00 / 26.58±0.29 / 相差 5.55」：**不改（一致）**

- **源文件**：`outputs_cifar100/v6_p4e_crosspath/crosspath.csv` + `outputs_cifar100/v3_methods/plain_uniform_c1_s42/alpha_sensitivity.csv`
- **我算出**：{21.41, 22.02, 19.67} 均值 **21.03**、总体 sd **0.996** ≈ ±1.00 ✓；
  六个 run {26.03, 26.73, 26.95, 26.71, 26.39, **26.66**} 均值 **26.578**、sd **0.295** ≈ **26.58 ± 0.29** ✓
  （第六个是「消耗数 5→1」干预臂 26.66，技术报告行 645 有记）；26.58−21.03 = **5.55** ✓。

### R4 `technical-report:604`、`676`「方差被估高约 6 倍」：**不改（术语问题，项目一贯表述）**

- **我算出**：混协议组 {21.41, 26.73, 26.95} 的**标准差** 2.561（总体），同协议组 **0.392**——
  比值 **6.53**；若按**方差**比则是 42.6 倍。
- **判定**：严格说"方差"应为"标准差"，但台账 1808 与 `论文v3.md` 多处（45/419/1243/1560）都是同一措辞，
  属项目一贯口径，**改它会与主稿分叉** ⇒ 不改，仅登记。

### R5 `technical-report:535`「不变区占比不变」：**UNCLEAR**

- **源文件**：`outputs_cifar100/v5_maxpool_mechanism/invariant_frac.csv`
- **我算出**：`frac_le_0.3`（conv4）为 5-pool **0.8304** 对 2-pool **0.9292**；
  `frac_ge_0.9` 为 **0.00136** 对 **0.00012**——两个现成列都**变了**（且方向相反）。
- **判定**：找不到"不变区"在原始分析里的定义（台账与产物里都没有该字段），
  我复不出"不变"这个结论 ⇒ **不改**，标注为待作者澄清。

### R6 `technical-report:549`「差 +1.17（标准误 1.03，z≈1.1）」：**不改（我复算正确）**

- **我算出**：5×max 的 drop@0.3 = 60.99−42.47=18.52 / 60.97−38.77=22.20 / 60.73−39.64=21.09 → 均值 **20.6033**、sd **1.5413**；
  5×avg = 61.34−38.29=23.05 / 61.48−40.51=20.97 / 61.33−40.02=21.31 → 均值 **21.7767**、sd **0.9110**；
  差 **1.1733**、SE = √(1.5413²/3+0.9110²/3) = **1.0337**、z = **1.135** ⇒ 与原文 20.60 / 21.78 / +1.17 / 1.03 / ≈1.1 **全部一致**。
  （复核报告 E10 说"21.78−20.60=1.18"，那是**舍入后相减**；+1.17 才是未舍入值——**原文是对的**。）

### R7 `technical-report` 其它抽查（全部逐位一致，未改）

- 表 `tab:phase12` / `tab:ablation`：64.23/44.74/15.64/56.08/39.96/19.89（`outputs/task2_*/{scratch,finetune}`）、
  63.63/68.76/69.14 与 87.19/87.37/87.59（`outputs/task3_simplecnn/Exp*/`，且 α=0 行 = `metrics.json` 的
  `best_test_acc` ✓）——全部一致。
- 注入位置（conv2 −12.43 / conv3 −7.56 / fc −0.68 / 全层 31.90 / 线性累加 23.33 / 超加性 −8.57）：
  台账 §"注入位置" 428-448 行逐位一致 ✓
- `fc/conv1 的 L2 范数比 0.45 → 0.23`：台账 740 行 ✓
- headroom 抬高表（53.56/59.00/59.99、48.35/55.72/28.09、+20.26/+27.63）：
  `v7_readout_samplesize/{samplesize_curve,samplesize_ext}.csv` ✓
  （小注：同表 53.56 出自 `samplesize_curve.csv` 的 n=8000 行，而 48.35 出自
  `ra_onprotocol_main.csv` 的 same_cond 列，`samplesize_curve` 同配置是 48.32——两列口径差 0.03，
  项目规范以 `ra_onprotocol_main` 为准，**不改**，登记备查。）
- 自检声明「152 行 / 0 FAIL / 58 PASS / 94 n/a」：我按 `results_master.csv` 重数
  **152 行、58 PASS、74+20 = 94 n/a、0 FAIL** ✓
- 门控「15.73 / 69.23 / −0.003」：`outputs/v5_full_recipe/recipe_simple_cnn_mp_bs43.csv`
  的 α=+0.3 行 `acc_clean_head` = **69.23** ✓（台账 2422/2435 亦有）
- R²/MAE「0.827~0.935 / 0.036~0.061」：四个组合 C10-plain 0.9327/0.0373、C10-mp 0.827/0.0607、
  C100-plain 0.935/0.0361、C100-mp 0.8323/0.0595 ✓
- 竞赛版 tab:nat / tab:ablation / tab:gauss / tab:dose / tab:transfer、Δ = −7.23 / +7.82 / +21.90 /
  −0.10 / 0.00、+0.77 / −10.77 / +1.08 / +5.48 / +8.22 / 12.58 / 15 点全胜与平均 +10.22、
  容差窗口、85~92% 恢复率——**逐项复算全部一致**（算式见下）
  - Δ 公式：`a_joint − a_nl×a_quant/a_clean`；VGG-11 3bit：36.74×51.46/89.73 = 21.07，42.97−21.07 = **+21.90** ✓
  - VGG-11 Exp2 相对 clean 的 15 点平均：(24.02−13.53)+(36.11−18.30)+…+(16.00−10.01) = 153.25，/15 = **+10.22** ✓

---

## 四、发现但**未处理**的项（超出本次授权范围，请 claude-0b 决定）

1. **A14（fragility 无引用）在技术报告版同样存在**：`technical-report:827` 的「不做 RMS 归一化 →
   负侧结论反号」整段抄自该文献的核心告警（arXiv:2606.11375 的 v2 更正），而
   `technical-report:819` 还写着「都是本轮内实测触发的，**不是转引文献**」；
   技术报告参考文献 8 条里没有该文献。**属归属类**（需要新增 `\bibitem` 与著录信息），
   建议与 journal 的 A14 一并处理——我未擅自加参考文献条目。
2. **B 组在技术报告版的对应**（措辞强于证据）：如 `technical-report:74-75` 的
   「跨实现比较精度数字**会得到不可移植的结果**」（B2）、`1067-1069` 的入场券要求。
   技术报告是归档稿，保留了"硬结论"的写法 ⇒ 未改，留给作者定夺。
3. `technical-report:1034`「已知下界来自高斯对照（20.26 → 7.50）」措辞含混
   （两个数不是同一口径的上下界）⇒ 未改（低优先）。

---

## 五、四件套

```
里程碑：A 组同步 + 两份稿通读
关键数字 + 出处文件：
  A1  秩 @±0.3 归一损失 20.63/33.34/58.70 对 2.02/17.91/67.33
      出处 outputs_cifar100/v5_rank_collapse/rank_stats{,_exp2vgg11,_exp3resnet18}.csv
  A3  原始头 @+0.3 = 28.09/15.98/6.22  出处 outputs_cifar100/v3_readout_repair/*_ep24_*.csv 的 a_original_fc
      （表里的 16.92/2.70 出自 results_master.csv 的 cifar100|{vgg11,resnet18}|clean|7pt）
  A4  49.12 / 42.47 / 43.38 / 28.09 → +21.03 对 +29.67（少 8.64）
      出处 readout_repair_c100_mp_clean_alpha+0.30_n20000.csv、readout_repair_c100_clean_ep24_*.csv
  A5  probe_same_cond 48.35 对 probe_clean_train 18.1，head_acc 28.09
      出处 outputs_cifar100/v7_probe_audit/ra_onprotocol_main.csv
  A6  C10 侧 1.486/0.110/0.214、4.15:1、+0.39、−13.68；C100 侧 2.541/0.847/0.980、1.77:1、−0.21、−14.71
      出处 outputs{,_cifar100}/extension5_dithering_vgg11/{activation_stats,arch_dithering_summary,flip_statistics}.csv
  A7  headroom @+0.4/+0.5 = 30.9 / 30.82  出处 同上 ra_onprotocol_main.csv
  A8  51.4733 / 38.12 / 43.38 / 48.62 → −13.35 / −8.09 / −2.85
      出处 outputs_cifar100/v7_readout_capacity/capacity_ladder.csv
  A10 24.32 / 21.78 / 20.48 / 26.13 对基准 26.03 → −1.71 / −4.25 / −5.55 / +0.10
      出处 outputs_cifar100/v3_methods/*/alpha_sensitivity.csv
  A13 12~30 min 对 1~6.5 h → 1/5、1/13、1/32；出处 论文v3.md:518-520
  T9  clean 代价 8ep 3.19~6.67（台账 838-848）对 24ep 3.08~7.69（同 A3 的源文件 α=0 列）
  T11 不对称比 C100 1.87/1.18/1.01、C10 3.37/1.40/0.94
      出处 results_master.csv + outputs/task1_*/alpha_sensitivity.csv
  C3  C100 3/4bit = 10.87/19.75；C10 3/4bit = 42.97/43.27
      出处 outputs{,_cifar100}/extension5_dithering_vgg11/group_a_bits_scan.csv
判据命中哪个分支：本任务无预登记判据（属复核+同步）。自订的判定标准是"每条自己算一遍"：
  A 组 8 条 CONFIRMED（A1/A3/A4/A5/A6/A7/A8/A10/A13，A2 验收为"已到位"）、
  A9 在两稿中不存在（不适用）、2 条新发现（T9/T11）+4 条竞赛版新发现（C1/C2/C3/C4）全部 CONFIRMED；
  4 条 REFUTED/口径歧义（R1–R4 不改）、1 条 UNCLEAR（R5 不改）。
  改动共 25 个位置（技术报告 20、竞赛版 5，全部用编辑工具逐处人工改），编译后页数不变（22 / 13）。
  实际 diff（`git diff --numstat`）：`paper/technical-report/main.tex` **+71/−24**、
  `paper/competition/main.tex` **+12/−9**；两个 `.pdf` 是编译产物。
  **未碰** `paper/journal/main.tex`（它工作区里的改动来自另一个会话）、`paper/figures/gen_figures.py`。
有没有触发红线：无。
  · 未碰 paper/journal/main.tex（只读；check_papers.py 会连带编译它，写的是 pdf/aux/log 等编译产物）
  · 未跑 GPU、未启动任何训练（M4 训练链不受影响）
  · 未用字符串替换脚本改 .tex（全部逐处人工编辑；未用 heredoc 批量替换）
  · 未把 markdown 写进 .tex（检查器：两份稿 0 处残留）
  · 表格单元格内未用 \cref（新增的表注在 minipage 里，也不含 \cref）
```

---

## 六、`tools/check_papers.py` 完整输出

> 以下是**全部改动落定之后**的那一次运行（第二次；第一次在改完技术报告与竞赛版前 4 处后跑的，
> 结果相同）。退出码 **0**。

```
$ PYTHONIOENCODING=utf-8 /d/anaconda3/envs/pytorch_env/python tools/check_papers.py
================================================================
论文提交前检查
================================================================

[journal]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：25 页 / 0 错误 / 0 未定义引用

[technical-report]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：22 页 / 0 错误 / 0 未定义引用

[competition]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：13 页 / 0 错误 / 0 未定义引用

================================================================
结果：全部通过 ✓
EXIT=0
```

---

# 第二轮：B 组（措辞降级）+ E5 术语方案

> **来源**：claude-0b 派活（2026-09-24）。范围 = `docs/INDEPENDENT_REVIEW.md` §乙 B1--B8 + §戊 E5。
> **纪律同前**：每条自己打开源文件算；分 CONFIRMED（改）/ 不适用（写清核查动作）/ 已到位（不重做）。
> **本轮改动 7 处**（技术报告 5、竞赛版 2），**E5 只写方案、不动 `.tex`**（等 claude-0b 对两边）。

## 一、B 组逐条

### B1 @ `technical-report:859-866`：CONFIRMED（降级 + 补适用范围）

- **源文件**：`outputs_cifar100/v7_etf_readout/proc_sensitivity.csv`
- **我算出**（六臂逐值）：

  | α | sk_lbfgs | sk_saga | adam_50 | adam_150 | adam_400 | sgd_mom_150 | 全距 | argmax | argmin |
  |---|---|---|---|---|---|---|---|---|---|
  | 0.0 | 53.22 | 54.91 | **56.31** | 54.89 | **52.85** | 53.27 | 3.46 | adam_50 | adam_400 |
  | +0.3 | 48.25 | 50.75 | **51.57** | 50.37 | 48.55 | **48.12** | 3.45 | adam_50 | **sgd_mom_150** |

  - α=0：极值**都**来自 Adam 的不同轮数，"训多久"跨度 3.46 = 全距 ✓（原文这句是对的）
  - α=+0.3：极值**跨了两个因子**；Adam 内轮数跨度 = 51.57−48.55 = **3.02**，
    同轮数的优化器跨度 = 50.37(adam_150) − 48.12(sgd_mom_150) = **2.25**
- **与复核者的一处不同**：B1 说"轮数造成的跨度只有 3.02，**小于**换优化器造成的跨度"——
  我按"同轮数比优化器"算是 **3.02 > 2.25**（3.45 那个差是把 argmax→argmin 整体归给优化器的算法）。
  ⇒ 他的"方向不干净"**偏重**，但"该主张在 α=+0.3 上要打折扣"**成立**。
- **改法**：保留 α=0 的干净观测，补 α=+0.3 的两个跨度，并把结论降级为
  「**这条在 α=0 上最干净，且只在本文测过的这一个特征集与头规模上成立，属提示性结论**」。

### B2 @ `technical-report:68-71`、`724-728`、`1162-1164`：CONFIRMED（三处全部降级）

- **源**：无新增数字（5.55 的出处见第一轮 R3）；问题在**措辞强度**。
- **改法**：三处统一从"**任何**精度数字**不可**跨实现移植 / **必须**同实现"改为
  「**本文观测到**一个同超参、同种子、跨实现稳定 5.55 个百分点的差异，**经三次干预实验仍未归因**；
  在归因之前，**我们的做法是**不跨实现引用精度数字」，并显式写明
  **不排除"某一脚本里还有一个尚未找到的具体差别"**（即：不把一个未定位的差异升格为普遍律）。

### B3 @ 两份稿：**不适用**

- **核查动作**：`grep "四路"`／`grep "对照一\|对照二\|对照三\|对照四"` 在
  `paper/technical-report/main.tex` 与 `paper/competition/main.tex` **均 0 命中**；
  技术报告的探针节只有单条"对照通路的判别力"（高斯对照 σ=0.3 的 7.50），没有"四路加固"的叙述。
- ⇒ 无需改动（这条属 journal 专有）。

### B4 @ 两份稿：**不适用**

- **核查动作**：`grep "fragility"`／`grep "0.4188"`／`grep "临界崩塌"` 在两份稿 **均 0 命中**；
  技术报告没有 fragility 那一路对照（σ* 系列数字只在 journal 里）。
- ⇒ 无需改动。

### B5 @ `competition:139-143`：CONFIRMED（技术报告版**已到位**，不重做）

- **源文件**：`results_master.csv`
- **我算出**（重数）：**152 行**、**58 PASS**、**0 FAIL**、**74** `n/a(读出适配)`、**20** `n/a(扫描复用权重)`；
  按数据集拆：cifar10 = 17 PASS / 24 n/a，cifar100 = 41 PASS / 70 n/a。
- **技术报告 `:155-158`** 已经写成"152 行、0 FAIL（58 PASS / 94 n/a）"——三类数都报了 ✓ **不动**。
- **竞赛版原文**「这个自检覆盖了本文引用的**每一行数据**」**不成立**，两条证据：
  1. 竞赛版引用的**优化一**（`outputs/extension4_alpha_wide/`）在台账里是 `n/a(扫描复用权重)`——
     它复用已训权重做 15 点扫描，没有可配对的 `metrics.json`；
  2. 竞赛版引用的**拓展 2 / 拓展 3 / 优化二**在台账里**根本没有行**（收割器未覆盖），
     且它们的目录里也没有 `metrics.json`（`find outputs/extension{2,3,5}_* -name metrics.json` = 0 个）；
     优化二的 `group_a_bits_scan.csv` 更是**位宽扫描**、没有 α=0 行。
- **改法**：改为「**适用与例外**：由新训练模型产出的行（任务 1--3、优化三）全部通过该自检，
  在全局台账中记为 PASS；拓展 2、拓展 3、优化一、优化二**不适用**……其读数直接取自产物表本身」。

### B6 @ 两份稿：**不适用**

- **核查动作**：`grep "唯一"` 在技术报告只有 4 处——池化机制"唯一显著差异"（合理）、
  历史叙事"唯一可辨识的实现差异"（引述被否证的旧判断）、`963-965` "全部结论里唯一需要硬件配合的地方"；
  竞赛版 0 处。**两稿都没有**"读出适配是唯一可行的一类修复"这种贡献列表式表述，
  也就不存在"唯一 vs 第二个杠杆"的自相矛盾。
- （顺带：技术报告 `:965` 的"唯一需要硬件配合"与表 `tab:deploy` 一致，**不矛盾**，不改。）

### B7 @ `competition:165-168`：CONFIRMED（技术报告版**上一轮已做**）

- **源文件**：`results_master.csv`（CIFAR-100 三行）+ `outputs/task1_*/alpha_sensitivity.csv`（CIFAR-10）
- **我算出**（不对称比 = 正侧跌幅 ÷ 负侧跌幅，|α|=0.3）：

  | 主干 | C100 比 | C10 比 |
  |---|---|---|
  | SimpleCNN | 1.87 | 3.37 |
  | VGG-11 | 1.18 | 1.40 |
  | ResNet-18 | **1.01** | **0.94** |

- **技术报告**：第一轮已把 C100 的三个比写进 `:373`（"方向随深度衰减"），本轮**不动** ✓
- **竞赛版**：本轮补上 C10 的三个比（`165-168`），并写明"方向性随深度衰减，在最深的主干上完全消失"。
- 这条**对论文有利**：多一个"方向性随深度衰减"的结论，同时挡住"你只挑了一个主干"的质疑。

### B8 @ `technical-report:806-808`：CONFIRMED（补算式）

- **源文件**：台账 §16.0a 的三臂对照表（`experiments_ledger.md:3115` 附近）+
  `outputs_cifar100/v7_probe_audit/c4_private_split.csv`
- **算式**：$\Delta_{\text{不相交}}(\alpha{=}+0.3)$ = 分布内 5000 − 不相交 5000 = **46.15 − 43.96 = 2.19**；
  再 $27.63-2.19=$ **25.44 ≈ +25.4**（台账 §16.5a 亦记此式）
- **我算出**：复核者说"−2.2 的算式没给"**成立**；其值 **2.19** 确为"不相交划分比分布内低"的量，
  **不是**"按样本量外推"（那是另一列 $\Delta_{\text{样本量}}=1.98$）。
- **改法**：写出 $=27.63-2.19$，并注明 2.19 的出处（§16.0a 三臂对照）
  与移用方式（在 $n$=8000/5000 口径测得、按同口径移用到 $n$=50000 档）。

---

## 二、E5 术语统一方案（**待 claude-0b 对齐；本轮未改 `.tex`**）

**现状统计**（我逐词数的）：

| 术语组 | `technical-report` | `competition` | `journal`（只读，供对齐） |
|---|---|---|---|
| (a) 模型自带的分类头 | 自带头 3 / 模型自带分类头 2 / 原始头 3 / 原始分类头 2 / 干净头 1 | 0 | 自带头 6 / 模型自带分类头 3 / 自带分类头 4 / 原始头 4 / 原始分类头 1 / 干净头 5 |
| (b) 池化主干 | `SimpleCNN+3$\times$MP` 1 / `5$\times$MaxPool 主干` 1 / `mp 主干` 1 / `CIFAR-100/mp` 1 / `CIFAR-10/mp 主干` 1 | 0 | `mp 主干` 2 / `MaxPool 主干` 2 |
| (c) 重训的线性头 | 读出适配 13 / readout-NAT 3 / 鲁棒头 3 | 0 | 读出适配 33 / readout-NAT 3 / 鲁棒头 18 |

> ⚠ **竞赛版三组全是 0**——它是第一阶段稿，不含读出适配/池化杠杆/门控。**E5 只涉及技术报告**。

**我的建议（请 claude-0b 定，我按你的裁定改）**：

1. **(a) 统一为「原始头」**（判定依据：这四种叫法在技术报告里**指的是同一个对象**——
   `a_original_fc` 的值；门控表里的 `acc_clean_head` 也是它，行 596 的"自带头 10.27"就是
   `acc_clean_head@-0.2`）。
   - 首次出现（图 `fig:probe` 图注）写「**原始头**（模型自带的分类头，`a_original_fc`）」
   - 其余：`模型自带分类头`（2）/`自带头`（3）/`原始分类头`（2）→ **原始头**
   - 门控节（`fig:gating` 起）：`干净头` → **原始头**，该节首次写「原始头（门控表的 `acc_clean_head`）」
2. **(b) 统一为「mp 主干」**，首次给全称：**「SimpleCNN+3×MaxPool（简称 mp 主干，= 总数 5 个 MaxPool）」**
   - 表 `tab:arch` 的 `SimpleCNN+3$\times$MP` → `SimpleCNN+3$\times$MaxPool`
   - `5$\times$MaxPool 主干`（570）、`CIFAR-100/mp`（591）、`CIFAR-10/mp 主干`（602）→ **mp 主干**
   - 理由：`mp` 已是产物路径名（`v3_methods/simple_cnn_mp_*`、`outputs/v5_full_recipe/recipe_simple_cnn_mp.csv`），
     全称在正文里太长；**首次给全称 + 简称**即可两全。
3. **(c) 方法名统一为「读出适配」；头的名字用「读出适配头」**；门控节的「鲁棒头」**保留**
   - 首次出现（图 `fig:readoutnat` 图注）写「**读出适配**（readout-NAT）」——保留原记号作同义对照，
     因为台账与产物文件名都是 `readout-NAT`/`readout_repair`
   - 其余 `readout-NAT`（570、941）→ **读出适配**
   - 门控节：**「鲁棒头」保留**（它是 `recipe` 表的列名 `acc_robust_head`，且"原始头/鲁棒头插值"这一叙事
     需要两个短名），但该节首次出现时注明「鲁棒头（即读出适配头，此处沿用产物列名 `acc_robust_head`）」
   - **理由**："鲁棒头"与"读出适配头"概念相同但**不是同一批产物**（v5 `recipe` vs v3 `readout_repair`），
     合并成一个词反而会掩盖口径差异——建议用同义对照而不是硬合并。

**如果要两边完全一致，`journal` 侧需要同步的替换量**：模型自带分类头 3 + 自带分类头 4 + 原始头 4
+ 原始分类头 1 → 原始头（12 处）；干净头 5 → 原始头；mp 主干/MaxPool 主干 → 首次给全称；
readout-NAT 3 → 读出适配。**具体以你的裁定为准。**

---

## 三、四件套（第二轮）

```
里程碑：B 组（措辞降级）在两份稿上
关键数字 + 出处文件：
  B1  α=0 全距 3.46（56.31/52.85 均 Adam）vs α=+0.3 轮数跨度 3.02、同轮数优化器跨度 2.25
      出处 outputs_cifar100/v7_etf_readout/proc_sensitivity.csv
  B5  results_master.csv 152 行 / 58 PASS / 0 FAIL / 74 n/a(读出适配) / 20 n/a(扫描复用权重)；
      C10 侧 17 PASS / 24 n/a；ext2/ext3/ext5 不在台账且目录内 0 个 metrics.json
  B7  C100 不对称比 1.87/1.18/1.01；C10 3.37/1.40/0.94
      出处 results_master.csv + outputs/task1_*/alpha_sensitivity.csv
  B8  Δ不相交 = 46.15 − 43.96 = 2.19；25.44 = 27.63 − 2.19
      出处 experiments_ledger.md §16.0a 三臂表 + §16.5a
判据命中哪个分支：B1--B8 逐条给出 CONFIRMED / 不适用（B3/B4/B6，附 grep 核查动作）/ 已到位（B5 技术报告版）。
  B 组命中 5 条需改（B1/B2/B5(竞赛版)/B7(竞赛版)/B8），3 条不适用。
  改动 7 个位置（技术报告 5、竞赛版 2）。E5 只出方案、未改代码。
有没有触发红线：无。
  · 未碰 paper/journal/main.tex（只读；本轮它还从 25 页长到 26 页，是另一个会话改的）
  · 禁字符串替换脚本 —— 7 处全部用编辑工具逐处改
  · 无 markdown 残留（检查器两份稿均 0）
  · 未跑 GPU；未 commit
```

## 四、`tools/check_papers.py` 输出（第二轮，B 组改动落定后）

```
$ PYTHONIOENCODING=utf-8 /d/anaconda3/envs/pytorch_env/python tools/check_papers.py
================================================================
论文提交前检查
================================================================

[journal]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：26 页 / 0 错误 / 0 未定义引用

[technical-report]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：22 页 / 0 错误 / 0 未定义引用

[competition]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：13 页 / 0 错误 / 0 未定义引用

================================================================
结果：全部通过 ✓
EXIT=0
```

---

# 第三轮：`100 epoch` 遗留 + E5 术语统一（技术报告侧执行）

> **来源**：claude-0b 转来修图会话的遗留一处 + 批准 E5 方案（三条全批准）。
> ⚠ **本轮改动未经编译**——claude-0b 指示“M4 训练 16:37 重启、提交内存只剩 0.64 GB，
> 先别跑 `tools/check_papers.py`（它要跑六次 xelatex）”。因此本轮**只做了静态结构自检**（见 §三）。

## 一、`technical-report:535`（原 `:533`）：CONFIRMED —— "100 epoch" 是把另一批的定语搬过来了

- **我自己复核了三处证据**（未采信转述）：

  | 证据 | 我读到 | 命令/文件 |
  |---|---|---|
  | 虚线那端的主干 | `total_epochs: **120**`、`best_epoch: 114` | `outputs_cifar100/simple_cnn/metrics.json` |
  | 实线那端的主干 | `total_epochs: **120**` | `outputs_cifar100/v3_methods/simple_cnn_mp_clean_uniform/metrics.json` |
  | 台账 `epochs` 列 | `cifar100\|simple_cnn\|clean\|7pt` = **120**；`cifar10\|simple_cnn\|clean\|7pt` = **100** | `results_master.csv` |

  ⇒ F07 画的是 **CIFAR-100** 的池化杠杆（28.09 → 42.47），两端都是 **120 ep**；
  "100 epoch" 是 **CIFAR-10 那批 `outputs/task1_*`** 的定语被搬了过来——
  与 A3/A6 同一个病因（只搬数字、没搬定语），这次搬错的是 **epoch**。
- **改法**（照 journal）：不写 epoch 数，只写"第一阶段脚本"，保留"异协议/仅作锚点"的意思，
  并把"异协议"的理由落到**不同训练脚本**（有本项目 §14.5/§14.6 的实测支撑）：
  > 虚线为**第一阶段脚本**的产物：它与实线出自**不同训练脚本**（本项目实测跨脚本的精度数字不可直接比较），
  > 故属异协议、仅作锚点，不与实线并列比较。
- 另：claude-0b 提到的"无增强配方"错述**不在技术报告里**（`grep "无增强"` 技术报告 0 命中；
  竞赛版的两处"无增强"指"未加 Exp1/2/3 增强模块"，用法正确，**不改**）。

## 二、E5 术语统一（技术报告侧**已执行**，15 个位置）

**改后术语统计**：`自带头` 0 / `原始分类头` 0 / `干净头` 0 / `模型自带` 1（仅首次对照）
/ `原始头` 11 / `mp 主干` 4 / `读出适配` 16 / `readout-NAT` 1（仅首次对照）/ `鲁棒头` 3（保留）。

| # | 行 | old → new |
|---|---|---|
| 1 | `176` | `SimpleCNN+3$\times$MP` → `SimpleCNN+3$\times$MaxPool`（表 `tab:arch`） |
| 2 | `392-393` | `模型自带分类头` → **`原始头`（首次对照：即模型自带的分类头，产物里的 `a_original_fc`）** |
| 3 | `400` | `自带头 28.09%` → `原始头 28.09%` |
| 4 | `415` | `低于模型自带分类头（28.09）` → `低于原始头（28.09）` |
| 5 | `426-427` | `readout-NAT` → **`读出适配（readout-NAT，…）`（首次对照）**；`各模型自带头` → `各模型的原始头` |
| 6 | `441` | `原始分类头（同主干）` → `原始头（同主干）` |
| 7 | `450` | 表注 `所依附主干的原始分类头` → `原始头` |
| 8 | `541` | **新增简称引入**：`5 个 MaxPool 的那一支以下简称 mp 主干（表 \ref{tab:arch} 的 SimpleCNN+3×MaxPool）` |
| 9 | `576` | `5×MaxPool 主干 + readout-NAT` → `mp 主干 + 读出适配` |
| 10 | `597-598` | `CIFAR-100/mp 上鲁棒头` → `CIFAR-100 的 mp 主干上，`**`鲁棒头`（即读出适配头，此处沿用产物列名 `acc_robust_head`）** |
| 11 | `602` | `ResNet-18 的自带头` → `原始头` |
| 12 | `609` | `CIFAR-10/mp 主干 43` → `CIFAR-10 的 mp 主干（主干种子 43）` |
| 13 | `958` | 部署表 `读出适配（readout-NAT）` → `读出适配`（非首次，按方案去掉对照） |
| 14 | `968` | 部署表 `干净头是冻结不动的那一个` → `原始头是…` |

**与 journal 对齐用的术语表**：`原始头`（首次对照：模型自带的分类头，`a_original_fc`；
门控节注明 = `acc_clean_head`） / `mp 主干`（首次全称：SimpleCNN+3×MaxPool，= 总数 5 个 MaxPool）
/ `读出适配`（首次对照：readout-NAT；头的名字用"读出适配头"） / `鲁棒头`（保留，首次注明
= 读出适配头、列名 `acc_robust_head`）。

## 三、自检状态（⚠ **本轮未编译**）

按 claude-0b 的红线（提交内存 0.64 GB、M4 训练在跑），**没有跑 `tools/check_papers.py`**。
替代做法是一次**纯静态结构自检**（不调用 xelatex）：

```
[technical-report] env mismatch: none | odd-$ lines: none
[competition]      env mismatch: none | odd-$ lines: none
（另：markdown 残留 = 0 / 0；花括号平衡差 = 0）
```

检查内容：13 类环境的 `\begin`/`\end` 配对、逐行 `$` 奇偶（跳过注释行）、全文花括号平衡、
markdown 残留（`**`/反引号/行首 `>`）。
**⇒ 待办：错峰跑 `tools/check_papers.py`，退出码 0 后才算数**（我会在第零时间补跑）。

## 四、四件套（第三轮）

```
里程碑：100ep 遗留修复 + E5 术语统一（技术报告侧）
关键数字 + 出处文件：
  epoch 事实：C100 两端主干均 120 ep（outputs_cifar100/simple_cnn/metrics.json 的 total_epochs=120、
    best_epoch=114；outputs_cifar100/v3_methods/simple_cnn_mp_clean_uniform/metrics.json 的 120）；
    results_master.csv 的 epochs 列 c100=120 对 c10=100 ⇒ "100 epoch" 是 C10 的定语
  术语统计（改后）：原始头 11 / mp 主干 4 / 读出适配 16 / 鲁棒头 3；自带头·原始分类头·干净头 = 0；
    readout-NAT 与"模型自带"各剩 1，均为方案要求的首次同义对照
判据命中哪个分支：遗留 1 条 CONFIRMED 并已改；E5 三条方案全部落到技术报告（15 个位置），
  竞赛版无对应处（三组术语在竞赛版均为 0）；"无增强"错述不在技术报告、竞赛版两处用法正确故不改
有没有触发红线：无（未碰 journal；全用编辑工具；无 markdown 残留；未跑 GPU；未 commit）
  ⚠ 但本轮**未编译**（遵循 claude-0b 的内存红线）——检查器结果待补
```

---

# 第四轮：C 组（缺失的局限）+ D 组（定位与相关工作）—— **只做 technical-report**

> **来源**：claude-0b 派活，材料 `docs/verify/C_group_limits.md` 与 `docs/verify/related_work_draft.md`。
> **竞赛版不做**（已结束，保持现状）。**仍按红线未跑 `tools/check_papers.py`**（M4 崩过两次、
> 物理可用 0.59 GB）——只做不调 xelatex 的静态自检。
> **落 C 组的门槛**（复核者定）：不是"我们没做什么"，而是"**因为没做 X，由本文发现引出的问题 Y 仍无法回答**"。

## 一、C 组：5 条落进技术报告的局限节，1 条判"不适用"

技术报告的局限节（§"局限、停车场与下一步"）原有 (1)--(6)，**新增 (7)--(11)**：

| C 编号 | 落点（改后行号） | 判定 | 与 journal 的差异 |
|---|---|---|---|
| C1 读出杠杆未在 $|\alpha|>0.3$ 评估 | `:1108` **(7)** | 适用，已写 | 同 |
| C2 架构杠杆只在有池化主干上验证 | `:1115` **(8)** | 适用，已写 | 同（技术报告表 `tab:arch` 的 MaxPool 列本就是佐证） |
| C3 CIFAR-10 读出单 seed / 跨数据集标度律 | — | **不适用**（见下） | **技术报告没有这条论断** |
| C4 证据只来自 CIFAR 小 CNN | `:1122` **(9)** | 适用，已写 | 弱化：技术报告没有"可迁移测量顺序"一节，改挂 §6 工程推演**未限定架构**这一点 |
| C5 探针是 oracle-α，缺可部署臂 | `:1127` **(10)** | 适用，**只写"正在测量中"占位、不判结果** | 同 |
| C6 量化 × 读出适配从未组合 | `:1133` **(11)** | 适用，已写 | 同 |
| C7 §16.7a 的内容 | — | **不做**（材料说明已进论文） | — |

**C3 判"不适用"的理由（附核查动作）**：
- `grep "标度律"` 在 `paper/technical-report/main.tex` **0 命中**；`grep "恢复率"` **1 命中**，
  但那处是**秩-恢复**的定义（`fig:rankrec` 图注："恢复率 = 读出适配 @+0.3 ÷ 原始头 @0"），
  与"跨数据集恢复率标度律"（C10 83.0/86.4/34.3% vs C100 68.6/59.8/22.6%）**不是同一个东西**。
- 更根本的是：**C10 侧的读出适配产物（`outputs/v3_readout_repair/`）根本没有进技术报告的论述**——
  技术报告 `:212-219` 的"口径注"明写：第一、二阶段数值来自另一条实现线、与后文只作定性对照，
  而第三阶段以后的数值来自 `v3_readout_repair.py` 等。跨数据集标度律是 journal 的论断。
- ⇒ **硬塞一条会变成"我们的借口"**，故不写。

## 二、D 组

### （甲）相关工作段 @ `technical-report:118-127`（放进 §1"定位说明"块内）

技术报告没有"相关工作"节，而 §1 的**定位说明块**本来就在讲"这不是新方法、是 linear probing"——
按 claude-0b 的指示"按它自己的结构放"，相关工作放在这里最自然。新增内容：
- **Tent**（冻结全部主干、只更新 BN 仿射参数）~\cite{tent}
- **SHOT**（冻结特征提取器、只学分类头、不需源数据）~\cite{shot}
- **Kumar 等**（预训练特征好 + 偏移大时线性探针可优于全量微调，机制是微调会扭曲底层特征）~\cite{lpft}
- **三条差异**：① 偏移的性质（确定性、逐层累积、输入相关，换测试数据不会消失）；
  ② 可动参数的范围与性质（TTA 是设计选择，CIM 里是硬件约束）；
  ③ 给出的量（准确率 vs 可恢复量上界 + 几何形态 + 代价权衡）。
- **只引 3 条**（宁少勿软）✓

### （乙）「不只是探针」补句 @ `technical-report:483-488`（§3.4 末尾）

> 这条结论不只有探针读数背书：冻结主干、只重训一个线性头，在 α=+0.3 上**实际达到 43.31**
> （SimpleCNN，3 seed，24 ep 协议；同期原始头 28.09）——那是一个**真实可部署的分类器**。
> 同口径的 **oracle-α** 探针读数为 48.35，即部署物取到了上界的 **89.6%**（43.31/48.35）。
> **注意分母是 oracle-α 口径**（假定失真强度已知），因此这个比例**不是**"部署时可以达到 89.6%"的意思。

**数字我自己复算**：43.3067/48.35 = **0.8957** ≈ 89.6% ✓（43.31 的三 seed 出处见第一轮 A2 的算式；
48.35 出自 `ra_onprotocol_main.csv`）。按材料的写者注意事项，**协议（24ep/3seed）与 oracle-α 都写进了句子** ✓

### （丙）用词禁令：**核查动作**（技术报告无需执行）

- `grep "last-layer"` = **0**、`grep "recalibration"` = **0**、`grep "last layer"` = **0**
- ⇒ 技术报告里**没有**这个另造的用法，禁令无需执行；归档版统一用「冻结主干、只重训最后的线性层」
  /「冻结主干、只更新分类头」这类描述性说法（原文如此）。

### 附带：**补两条原有论断的引用**（claude-0b 点名的遗留）

技术报告在本轮之前**就在做 LP-FT 与探针饱和的论断却无引用**（journal 同一处引了 `lpft` 与 `gjepa`）：

| 位置 | 原句 | 补入 |
|---|---|---|
| `:108` | "…是一条有理论与**十数据集实证**支撑的已知结论" | `~\cite{lpft}` |
| `:115` | "…正被文献警示可能**虚假饱和**" | `~\cite{gjepa}` |

**参考文献新增 4 条**（`:1233-1236`）：`lpft`（Kumar，arXiv:2202.10054）、`tent`（Wang，arXiv:2006.10726）、
`shot`（Liang，arXiv:2002.08546）、`gjepa`（Rabby，arXiv:2608.20516）。
著录格式照技术报告既有风格（`[C]//ICLR. 2022.` / `[R]. 2026. arXiv:…`）；
`lpft` 与 `gjepa` 的著录**逐字抄自 `paper/journal/main.tex`**（只读），未自行编造发表信息。

## 三、自检状态（⚠ **本轮同样未编译**）

按红线（物理可用 0.59 GB、M4 刚崩过两次）**没有跑 `tools/check_papers.py`**。替代的静态自检：

```
[technical-report] env:ok | odd-$:ok | markdown:ok | brace-diff:0
[competition]      env:ok | odd-$:ok | markdown:ok | brace-diff:0
```

另加一项**引用完整性**检查（新增 4 条 bibitem 后的必要动作）：

```
cite keys  : cifar, gjepa, lpft, shot, tent
bibitem keys: cifar, cim_survey, dither, gjepa, lpft, nat, puma, quant, resnet, shot, tent, vgg
缺失的 bibitem（cite 了但没有条目）: （空 = 全部有定义）
```

**⇒ 待办：错峰跑 `tools/check_papers.py`**（本轮与第三轮的改动**都还没编译验证过**）。

**再加一项：`\label` / `\ref` 完整性**（这正是检查器"数未定义引用"那一项所查的）：

```
labels  : 30 个（含 tab:arch / sec:deploy / sec:findings …）
被引用但没有 label 的: sec:xxx   ← 它只出现在文件头部第 32 行的注释里
                       （"% 章节引用一律写成「第 \ref{sec:xxx} 节」。"），LaTeX 会忽略整行
⇒ 实际未定义引用数 = 0
```

⇒ 检查器四项里，**markdown 残留**与**未定义引用**两项我已静态验证通过；
剩下两项（能否编译、编译错误数）只能等 xelatex。

## 四、四件套（第四轮）

```
里程碑：C 组（缺失的局限）+ D 组（定位与相关工作），只做 technical-report
关键数字 + 出处文件：
  C1  读出产物表头只有 -0.3…0.3 七个点（outputs*_cifar100/v3_readout_repair/*.csv）；
      α=+0.4/+0.5 处原始头 12.32/6.09、headroom +30.90/+30.82（v7_probe_audit/ra_onprotocol_main.csv）
  C2  maxpool_count = simple_cnn 2 / vgg11 5 / resnet18 0
      （extension5_dithering_vgg11/arch_dithering_summary.csv）
  C4  参数量跨度 0.25M~11.2M、全部为 ReLU+卷积/池化堆叠（材料给 build_ledger.py:28-34，与技术报告表 tab:arch 一致）
  C5  探针逐 α 训练（ra_onprotocol_main.csv 的 probe_same_cond 列）—— 只写"正在测量中"占位
  C6  dithering 的 alpha 列取值 = {0.0, 0.3}（extension5_dithering_vgg11/activation_stats.csv）
  D乙 43.31/48.35 = 0.8957 ≈ 89.6%（我自己复算；43.31 的三份出处见第一轮 A2）
  D补 引用键完整性：cite 5 个（含新增 4 个）全部有 bibitem，无未定义引用风险
判据命中哪个分支：C 组 5 条适用（全部指到具体产物）、1 条判"不适用"并给出 grep 核查动作（标度律 0 命中）；
  D 组三条全部落地；用词禁令为"无需执行"（0 命中）。**未把任何一条写成"我们的借口"**。
有没有触发红线：无。只碰 paper/technical-report/main.tex；全用编辑工具逐处改；无 markdown 残留；
  未跑 GPU；未 commit；**未跑 check_papers.py**（遵红线）——第三、四两轮共 2 次改动待编译验证
```

---

# 第五轮：跨文档一致性审计的三条修复

> **来源**：claude-0b 的跨文档一致性审计（journal 25 页版对照）。
> **三条都落在技术报告侧**：① 摘要池化增益用了旧口径；② 一句被自己下一句否证的断言；③ 成本比口径不同。

## 一、三条修复（+1 处我额外发现）

| # | 位置（改后行号） | 原 | 改后 |
|---|---|---|---|
| 1 | `:66`（**摘要**）+ `:1216`（**结论**） | 池化杠杆 `（$+14.38$ 个百分点）` | `（同协议同 seed 配对 $+15.52$ 个百分点）` |
| 2 | `:503` | `基准（均匀采样）为 26.03。`**`\textbf{没有一条超过基准}`**`；按本文自订的单种子门槛…` | 删掉那半句（后半句已说准：两条显著劣于、一条不可分辨、一条同级） |
| 3 | `:63`（摘要）、`:474`（§3.4 正文）、`:1211`（结论） | `1/5$\sim$1/30`（区间端点配对） | `1/5$\sim$1/16` + **逐主干配对** |

**第 1 条的额外发现**：审计点名的是**摘要**，但我 grep `14.38` 在技术报告里有 **2 处**——
另一处在**结论**（`:1216`），是同一个对象、同一个旧口径；而 `journal:1503` 的**结论**已经用了
"同协议同 seed 配对 +15.52"。⇒ 一并改。
（剩下 `:599` 的 `架构 $+14.38$` **保留**：那是"两个杠杆次可加"算式里的**架构单独增益**，
基数 28.09、与主稿 `论文v3.md:646-660` 的 v3 口径表一致，不是同一个量。）

**第 3 条的配对（journal:584-585 同款）**：读出 12 / 25 / 30 min 对全网络 1 / 6.5 / 6.5 h
⇒ **1/5、1/16、1/13**。原写法用区间端点（12 min 对 6.5 h = 1/32.5；30 min 对 1 h = 1/2）
跨度到没有信息量。

## 二、`tools/check_papers.py` 第一次运行（本批修复**之前**的状态）

> claude-0b 放行后跑的（物理 0.77 GB / 提交 1.15 GB 窗口）。**连跑两次，结果相同**。

```
[journal]
  ✓ 源码静态检查：无 markdown 残留
  ✗ 编译：28 页 / 1 错误 / 0 未定义引用

[technical-report]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：23 页 / 0 错误 / 0 未定义引用      ← 第三、四轮的改动全部编译通过

[competition]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：13 页 / 0 错误 / 0 未定义引用

结果：1 份有问题 —— **不要提交**   EXIT=1
```

**technical-report 与 competition 都是 0 错误** ⇒ **C 组 5 条、D 组三条、4 条新参考文献、
术语统一、跨文档修复之前的全部改动都通过了编译**（tech 从 22 页涨到 23 页，是新增内容的正常增长）。

### ⚠ journal 的那 1 个错误**不是瞬时态**（我只读定位，未改）

```
paper/journal/main.log:1013: ! Missing $ inserted.
paper/journal/main.tex:1447 末尾：
  $\Delta_{\text{proc}}=3.45$ pp 的\textbf{三到五倍}，所以\textbf{主效应远大于"换个方式拟合读出"}$；
                                                                              ↑ 此处多一个 $
```
- 位置正是**另一个会话正在写的 C 组第 7 条**（CIFAR-10 单 seed / 恢复率标度律，见 `:1442-1449`）。
- **连跑两次结果完全相同** ⇒ 不是"读到半成品"的瞬时态，是**真实的未闭合 `$`**。
- **不是我的领地，我没有改**。⇒ 请转给 journal 的写者（`latexmk` 若通过则说明它已自行修好）。

## 三、我发现但**未改**的一处新差异（请 claude-0b 裁定）

**「读出单独增益」的取值不一致**（两个杠杆次可加那一句）：

| 文档 | 写法 | 隐含算式 | 口径 |
|---|---|---|---|
| 技术报告 `:599` | 两个手段单独的增益之和是 $+29.67$（架构 $+14.38$、**读出 $+15.29$**） | 43.38 − 28.09 | **单 seed**（`readout_repair_c100_clean_ep24` 的 43.38） |
| `journal:854` | 架构单独增益 $+14.38$、**读出单独增益 $+15.22$** | 43.31 − 28.09 | **3 seed**（43.31） |

- **两个数都算得出来、各自自洽**；但技术报告那一句里 **42.47（架构单独）与 43.38（读出单独）
  出自同一批单 seed 产物**，而我第一轮的 A4 修复是**照抄主稿 `论文v3.md:646-660` 的 v3 口径表**。
- journal 的那句是 **+14.38（单 seed 的 42.47）与 +15.22（3 seed 的 43.31）混用**。
- **观察**：若目标是"跨文档一致"，两边要统一；若目标是"句内同口径"，**技术报告这一处更自洽**。
  **我没有自己拍板改动**（审计没列这一条），交你裁。

### ✅ claude-0b 的裁定（2026-09-24）：**两处都保留，各写各的口径**

> 判据：**这两句的结构不同，所以要求不同的口径**——技术报告那句在讲"两个杠杆各自的增益"，
> 同批单 seed 最自然；journal 那句在讲"次可加"，**必须让"架构单独"与"叠加"同实例相减** ⇒ 只能用 42.47。
> **强行统一反而会错**（把技术报告改成 3 seed，句内就不自洽；把 journal 改成 43.38，违反同实例相减）。
> 边界重申：**"术语/口径统一的目标是消歧，不是减少差异"**。

**对我的唯一要求**：确保技术报告那句**注明了"单 seed 口径"**。原文虽已有"该组为单 seed 口径"
（`:601` 附近），但落在**增益之和的下一句**、且没有点明 `+14.38`/`+15.29` 也在其中。**已强化**：

> （架构 $+14.38$ 与读出 $+15.29$ 分别取自同批**单 seed** 产物的 42.47 与 43.38）
> ——合起来只拿到六到七成，损失三到四成。
> **本段四个数（28.09 / 42.47 / 43.38 / 49.12）全部为单 seed 口径**（连续均匀 α、24 ep、20k 样本，同 v3 路径）；
> 13 点离散 α、3 seed 的 v5 口径给出同样的次可加结论。

⇒ **本批待编译的改动由 6 处变为 7 处**（`technical-report:599-603`）。

## 四、自检状态（本批 6 处改动**未编译**）

按你的指示"改完等我说了再跑检查器"——本批只做静态自检：

```
[technical-report] env:ok | odd-$:ok | markdown:ok | brace-diff:0
（残留检查：`1/30` 0 命中、`没有一条超过基准` 0 命中、`全部不优于` 0 命中）
```

**⇒ 待办：`check_papers.py` 第三次运行**（等窗口通知）。

## 五、四件套（第五轮）

```
里程碑：跨文档一致性审计三条修复（+1 处同源发现）
关键数字 + 出处文件：
  池化增益 +15.52 = 41.55 − 26.03（同协议同 seed 配对，v3_methods/*_uniform/）；
    对照 +14.38 = 42.47 − 28.09（干净训练臂，不构成同协议对照）—— journal:793/803/1503 同款
  成本比 1/5、1/16、1/13 = 12/60、25/390、30/390（论文v3.md:518-520 的分母）
  「没有一条超过基准」被 +0.10 否证 —— 与 journal:684 的写法（删该半句）对齐
判据命中哪个分支：三条不一致全部按 journal 的口径修（第 1 条另发现结论处同源、一并修）；
  journal 的 1 个编译错误定位到 :1447 的未闭合 $（非本会话领地，未改、连跑两次确认非瞬时态）；
  另发现 1 处「读出单独增益 +15.29 vs +15.22」未改，交 claude-0b 裁定
有没有触发红线：无。只碰 paper/technical-report/main.tex；编辑工具逐处改；无 markdown 残留；
  未跑 GPU；未 commit；journal 只读
```

