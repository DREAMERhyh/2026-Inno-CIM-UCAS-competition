# 台账错误形状扫描（ledger_sweep）

> 出处：claude-0b 派活（2026-09-24）。**拿 `docs/INDEPENDENT_REVIEW.md` 查出的 A 级错误的"形状"，
> 回头扫 `experiments_ledger.md` §0–§16。**
> 报告人：claude-65（持台账写锁）。
> **纪律**：每条都自己到源文件算，**不从论文或复核报告转引**；台账里的数也回源核。
> **红线 4**：只报告、不改既有判据；**事实性错误（错字/计数）直接改**；
> 边界 = **改"写错了"，不改"当时是那样想的"**。

---

## 分母（先报）

| 形状 | 台账里 grep 到的处数 | 其中**有源可核** | 本次**逐条回源核过** |
|---|---|---|---|
| **A9** 计数（`n/m 种`） | 12 | 6 | **6** |
| **A13** 比值 / 复合区间（`x 倍` / `数量级` / `a~b`） | 22 | 3 | **3** |
| **A1 / A7 / A10** 定性词（单调 / 全部 / 唯一 / 不变 / 系统性） | 28 | 2 | **2** |
| **A2** 用了已否决过的旧口径 | 4（`±3.1` / `±0.51` / 协议混用） | 4 | **4** |
| **A3** 同表内非同实例 / **A4** 叠加 vs 次可加 | — | — | **0**（未发现候选） |

**覆盖率如实声明**：A9/A13/A1-A7-A10 的**枚举**是系统性的（grep 全覆盖），
但**回源核验是定向的** —— 只核了**有产物文件可算**的那些。
**未逐条回源的那些记为"候选"，本报告不判。**

---

## L1：**CONFIRMED** ｜ 台账 §13.5 判据 1

```
源文件：outputs_cifar100/v5_full_recipe/recipe_simple_cnn_mp.csv  （3 head seed × 7 α）
算式  ：逐分布重加权 soft 与 oracle，再数 soft > oracle 的分布个数
        ① U[±0.3]       soft 55.08 vs oracle 54.96 → +0.122  ✓
        ② 仅正向         soft 56.07 vs oracle 55.87 → +0.196  ✓
        ③ 仅负向         soft 55.50 vs oracle 55.56 → −0.053  ✗
        ⑤ 尾部 δ(±0.3)   soft 49.11 vs oracle 48.57 → +0.542  ✓
        （④⑥ 需高斯权重约定，本次无法独立重算，取台账自报 +0.38 / +0.59）
我算出：超过 oracle 的是 ①②④⑤⑥ = 5/6
台账原文：「（且 4/6 种下超过"逐 α 硬选优"的 oracle）」（§13.5 判决 1）
处置  ：**已改** —— 4/6 → 5/6，并附【2026-09-24 计数勘误】注记（含回源算式与上述四个逐位复现值）
```

**注**：①②③⑤ 四个分布**逐位复现**台账表（55.08 / 56.07 / 55.50 / 49.11 与 54.96 / 55.87 / 55.56 / 48.57），
⇒ 方法本身可回源；**错的只是计数**。

**该错已扩散到两处本会话领地之外的文件**（**未改**，仅报告）：

- `paper/coverage-audit.md:52` —— 「6 种漂移分布下软门控全部最优（4/6 超过 oracle）」
- `第五阶段报告.md:34` —— 「软门控在 6 种漂移分布下**全部最优**（4/6 超 oracle）」

---

## L2：**CONFIRMED（写法问题）** ｜ 台账 §12.11

```
源文件：outputs_cifar100/task2_simplecnn/… 与 outputs_cifar100/v3_methods/…
        （s42 来自 task2_nat.py，s43/s44 来自 v3_methods_simplecnn.py —— 两个脚本）
算式  ：混协议集合的 std vs 纯协议集合的 std
        混协议（§12.11 表）：clean ±0.51 / 尾部 ±3.15
        纯协议（§13.10 重算）：clean ±0.419 / 尾部 ±0.392
我算出：§12.11 的 ±3.15 里绝大部分来自协议混用（把 4.62 的跨脚本差混进 3-seed 集合，
        方差从 ±0.39 抬到 ±2.56，约 6.5 倍）
台账原文（§12.11 判决 1）：「**clean 指标极稳定（±0.51）**，但**尾部指标噪声达 ±3.1 pp**（≈ 6 倍）」
问题  ：**§12.11 自身没有指向 §13.10 改判的指针**。§13.10 写了「历史混合值降为脚注保留」，
        但那是**写在 §13.10 里**；§12.11 的读者（以及所有引用 §12.11 的人）看到的是一个未标注的活数。
处置  ：**建议改**（加一个指向 §13.10 的【后续更新】注记）—— **本次未改**，因为这不是"写错了"，
        而是"当时是那样想的"，按红线 4 的边界应由你裁。
```

**下游仍在用这个已失效的数**（同样未改，只报告）：

| 位置 | 原文 | 问题 |
|---|---|---|
| §13.4 判据 4.d | 「本条四行对照**全部为单 seed**，而尾部 seed 噪声为 **±3.1 pp**」 | 用已被降级的值当**活门槛** |
| §13.4 判决 5 | 「鉴于尾部 seed 噪声 **±3.1 pp**，+10.20 与 +4.18 的分解应在第六阶段用 3 seeds 复核」 | 同上 |

⇒ **这正是 A2 形状的本体**：**用错口径把方差估高约 6 倍，然后拿这个虚高的方差去否决本可成立的效应。**
（§13.4 的 top-2 判定也受影响，见 §16.8 的第 5 条。）

**另一处相关的已处理项**：§12.11 的「clean 极稳定（±0.51）不可外推到深层」已在 §13.6b 正确记录 ✓。

---

## L3：**低置信（措辞宽松，非硬错）** ｜ 台账 §9.7

```
源文件：experiments_ledger.md §9.7 表（572–575 行）自身的四个数
算式  ：成本比 = 全网络训练 ÷ readout-NAT
        6.5 h = 390 min；readout-NAT 4~15 min
        ⇒ 390/15 = 26×  ～  390/4 = 97.5×   ⇒  1.4 ~ 2.0 个数量级
台账原文：「成本低**两个数量级**」（§9.7）
我算出：1.4 ~ 2.0 个数量级 —— 取上端才够"两个"
处置  ：**只记录**（同段 §9.7 另有一处写「成本 4~15 min vs 全网络训练 1~7 h」，
        按 7 h 算 105× = 2.0 个数量级，**可辩护**）。**建议**改「约 1.5~2 个数量级」更稳。
```

---

## L4：**已记录项（不需新处置）** ｜ 台账 §13.7 C4

`§13.7` 的 C4 写「**1/7**（CIFAR-100 为 3/7）」——该计数**已由 §16.7 独立复现并确认为设备脆弱**
（C10 的 α=−0.2 裕度仅 **0.0100 pp**，CPU 下变 **2/7**）。
**已在 §16.7 结果段记录，且已列为 §16.8 的第 1 条缺陷。** 本条不重复处置。

---

## 核过但**未发现错**的（回源逐位一致）

| 位置 | 断言 | 回源结果 |
|---|---|---|
| §14.2 | VGG-11「**4/7** 个点超过两头较优者」 | ✓ 从 `recipe_robust_vgg11_exp2vgg11.csv` 逐 α 重算 = **4/7**（−0.3/−0.2/−0.1/+0.1 四点） |
| §14.2 | ResNet-18 逐 α 表与 $m$ = −1.14 | ✓ 逐位一致 |
| §9.7 | 「读出端适配是训练端最优配方的 **1.6~2.6 倍**」 | ✓ 40.14/24.69 = **1.63**、16.27/6.22 = **2.62** |

---

## 关于 A3 / A4（未发现候选）

- **A3（同表内非同实例）**：台账的跨主干/跨协议并列**普遍带口径注**（如 §9.7「同为 200ep 比较」、
  §13.8「2×max 不重训、仅作锚点、不参与同协议对比」），**未发现无标注的混实例并列**。
- **A4（叠加 vs 次可加）**：§12.10 的"两个杠杆可叠加"表述与其表一致（+6.65 / +5.74 vs +11.07），
  **未发现措辞与表算不出的情形**。

---

## 四件套

- **里程碑**：台账错误形状扫描（`docs/verify/ledger_sweep.md`）
- **关键数字 + 出处**：
  - §13.5 计数 **5/6**（非 4/6）—— 源 `outputs_cifar100/v5_full_recipe/recipe_simple_cnn_mp.csv`
  - §12.11 的 ±3.15 为**混协议读数**，纯协议 ±0.392 —— 源 §13.10 的 3 同脚本种子
  - §9.7「两个数量级」实为 **1.4~2.0 个数量级** —— 源 §9.7 自身的成本列
- **判据命中哪个分支**：本任务未设分支判据（claude-0b 给的是形状清单）。
  按性质归类：**1 条 CONFIRMED 事实错（已改）、1 条 CONFIRMED 写法问题（建议改，未改）、
  1 条低置信措辞（只记录）、1 条已记录**
- **有没有触发红线**：**没有**。未改任何既有判据；只改了 1 处**计数事实错**（§13.5 的 4/6→5/6，附勘误注记）
  与 1 处**授权改动**（`docs/NEXT_STAGE_TASKS.md` 的 M4 半句）。
  **`paper/**`、`results_master.csv`、`docs/verify/` 下他人文件：一律未碰。**

---

## 给 claude-0b 的三条建议

1. **L1 的两处外溢要改**：`paper/coverage-audit.md:52`、`第五阶段报告.md:34` 的 4/6 → 5/6（**不在我领地**）。
2. **L2 是我认为比 L1 更严重的一条**：`±3.1 pp` 是一个**虚高约 6 倍**的噪声值，
   它在 §13.4 被当作**否决效应的门槛**使用。**建议给 §12.11 与 §13.4 各加一个指向 §13.10 的注记**——
   加注不改结论，但能止住继续外溢。
3. **本报告只扫了"可回源"的子集。** 台账里还有约 50 处定性/比值断言**未逐条回源**；
   若要全覆盖，建议**按形状分给多个会话并行**，每个形状配一个"必须回源"的硬要求。
