# `paper/journal/main.tex` A3–A14 修复报告

> **执行者**：claude-0b 派的零 GPU 工人（`paper/journal/main.tex` 的独占写者）。
> **任务**：修 `docs/INDEPENDENT_REVIEW.md` 的 A3–A14 共 12 条。
> **纪律**：不采信任何复核报告的原文数字，每一处**自己打开源文件重算**；
> 算不出来的不写进 `.tex`。
> **产物范围**：只改 `paper/journal/main.tex`；本文件为报告。未 commit。
> **版本**：起点 = 工作区 `HEAD=9e722f6` 的 1315 行版（A1/A2 已由 claude-0b 在 `781d8c2` 修过），
> 终点 = **1447 行**（编译 25 页）。
> ⚠ **A4 依据 claude-0b 转来的第二批独立重算（`docs/verify/A_cluster_independent.md`）作了口径升级**，
> 但升级所用的每个数我都自己复算过（见 A4 段，与它的 8.58 有 0.07 的分歧，已写清）。

---

## A3：CONFIRMED（并修正一处适用域）

**源文件**
- `outputs_cifar100/task1_vgg11/alpha_sensitivity.csv`（`0.3` 行）= **16.92**
- `outputs_cifar100/task1_resnet18/alpha_sensitivity.csv`（`0.3` 行）= **2.70**
- `outputs_cifar100/task1_simplecnn/alpha_sensitivity.csv`（`0.3` 行）= **28.09**
- `outputs_cifar100/v3_readout_repair/readout_repair_c100_clean_ep24_alpha+0.30_n20000.csv`
  与 `…_plain_s43_…_s43.csv`、`…_plain_s44_…_s44.csv` 的 `a_original_fc` 行
- `outputs_cifar100/v3_readout_repair/readout_repair_c100_exp2_vgg11_s42_ep24_….csv`（`a_original_fc`）
- `outputs_cifar100/v3_readout_repair/readout_repair_c100_exp3_resnet18_s42_ep24_….csv`（`a_original_fc`）
- `experiments_ledger.md` §9.7（Exp2/Exp3 表）
- `results_master.csv`（逐行 `pos_0.3`）

**算式**
```
第 1 行（表 2 原写"干净模型（不处理）"）= task1_*/alpha_sensitivity.csv 的 α=+0.3
第 2 行读出适配 = mean(e_readout_NAT @α=+0.3)，三个读出种子
    SimpleCNN  = (43.38+43.09+43.45)/3 = 43.3067 → 43.31
    VGG-11     = (46.98+44.36+52.02)/3 = 47.7867 → 47.79
    ResNet-18  = (22.26+27.81+30.40)/3 = 26.8233 → 26.82
第 2 行的"原始头" = 同一批 CSV 的 a_original_fc @α=+0.3
身份核验（runbook 的恒等式）= a_original_fc @α=0 是否等于该主干 metrics.json 的 best_test_acc
```

**我算出**

| 列 | 论文原写（第 1 行） | 读出所依附主干的原始头 | 该主干的 α=0 | 该主干 metrics.json |
|---|---|---|---|---|
| SimpleCNN | 28.09 | **28.09**（同一块） | 59.99 | `outputs_cifar100/simple_cnn/metrics.json` = 59.99 ✓ |
| VGG-11 | 16.92 | **15.98** | 68.05 | `outputs_cifar100/extension6_deep_robust/vgg11/metrics.json` = 68.05 ✓ |
| ResNet-18 | 2.70 | **6.22** | 72.69 | `…/extension6_deep_robust/resnet18_exp3/metrics.json` = 72.70（差 0.01） |

⇒ 三行**确实不是同一个模型**，且**读出适配依附的是 Exp2 / Exp3 配方主干**（runbook
`docs/V7_M4_RUNBOOK.md` §2 给出的 `--ckpt` 就是 `Exp2_Calib+Layerwise_vgg11_*` / `Exp3_FullRobust_resnet18_*`）。
复核者说"第三行恰是第二行的训练基底"——**这一点只在 ResNet-18 列成立**：
ResNet-18 的 `a_original_fc@+0.3 = 6.22` 与第三行同值；VGG-11 的读出基底是 Exp2（15.98，即第一行），
第三行是**另一块**主干 Exp3（24.69）。
（复核者说：三行三个模型、第三行是第二行基底。）

**改法**（已落盘）
1. 第 1 行标签与取值 → `\textbf{原始头}（读出适配所依附的主干）` = `28.09 / 15.98 / 6.22`；
2. 表下新增整段口径注：三块主干身份 + 核验恒等式 + "第二行与第三行不是两个独立选项" +
   另一批干净主干（task1 的 16.92 / 2.70）不列入本表；
3. 顺带把该行第三行的成本比写成逐主干配对（见 A13）。

**同表顺手修的（claude-0b 转来的"新 1"，我自己复算后确认）**
- 第三行 SimpleCNN 原写 `26.57` 并标"（最好）"，但 `results_master.csv:39`
  `task3_Exp3_FullRobust`（cifar100 / simple_cnn / 120ep / **pairing 0.0000 PASS**）在 `pos_0.3` = **29.89**；
  我用脚本对 `results_master.csv` 排除 mp 变体与读出臂后排序，**29.89 是全网络训练的最高值**
  （次高 28.09 即干净模型本身，v3 的鲁棒配方在 26~27）。
  26.57 是 `v3_methods/plain_uniform` 的 3 seed 均值，**不是最好**。
- ⇒ 第三行改为 `29.89 / 24.69 / 6.22`（VGG-11 24.69、ResNet-18 6.22 我逐列复算确认是各自主干的最优全网络值），
  并在表注写明三格来自两个 harness、且都是单次运行；读出领先随之由 +16.74 变为 **+13.42**（方向不变）。
- 附录"关键数值汇总"里同一行的 `26.57 / 24.69 / 6.22` 与出处一并同步。

---

## A4：CONFIRMED（"次可加"成立）／但复核者给的**理由**被否证，且**建议区间**被修正

**源文件**
- `论文v3.md:641–683`（含 661/662 行那张"次可加"表与 681–683 行的分数更正）
- `outputs_cifar100/v3_readout_repair/readout_repair_c100_clean_ep24_alpha+0.30_n20000.csv`
  → `e_readout_NAT`@0.3 = **43.38**，`a_original_fc`@0.3 = **28.09**
- `outputs_cifar100/v3_readout_repair/readout_repair_c100_mp_clean_alpha+0.30_n20000.csv`
  → `e_readout_NAT`@0.3 = **49.12**，`a_original_fc`@0.3 = **42.47**
- `…_mp_s43_ep24_…_s43.csv` = **48.82**；`…_mp_s44_ep24_…_s44.csv` = **49.59**
- `outputs_cifar100/v3_methods/simple_cnn_mp_clean_uniform{,_s43,_s44}/alpha_sensitivity.csv`
  → 42.47 / 38.77 / 39.64

**算式**
```
（我实际写进论文的 3 seed 口径）
读出单独 3-seed 均值   = (43.38+43.09+43.45)/3 = 43.3067 → 43.31
叠加配置 3-seed 均值   = (49.12+48.82+49.59)/3 = 49.1767 → 49.18
单独增益之和           = (42.47−28.09) + (43.31−28.09) = 14.38 + 15.22 = 29.60
加性预期               = 28.09 + 29.60 = 57.69
次可加缺口             = 57.69 − 49.18 = 8.51 ；  21.09/29.60 = 71.2%
（复核报告用的单 seed 口径：14.38+15.29 = 29.67，57.76 − 49.12 = 8.64）
```

**我算出**
- "近似正交可叠加" 与主稿的 "次可加" **互相排斥**，期刊版用的是被主稿改掉的那一种 ⇒ **断言成立**。
- 但复核者的**理由**「42.47 是 3-seed、49.12 是单 seed，违反种子纪律」**不成立**：
  `simple_cnn_mp_clean_uniform` 的三次运行是 **42.47 / 38.77 / 39.64，3 seed 均值 = 40.29**，
  而 42.47 是**单次数运行**；且它与 49.12 出自**同一块主干（seed 42）**，是本段最干净的一对。
  （复核者说：42.47 是 3-seed 均值。→ **REFUTED**）
- 真正并存的是：叠加 49.12（1 seed）对上文通行的读出 43.31（3 seed）。**我改用 3 seed 消掉这个混用。**
- 与 claude-38 的 0.07 分歧：它给的缺口 **8.58** 是用 43.38（单 seed）配 49.18（3 seed）算的；
  同口径应为 43.31 ⇒ **8.51**。我用 8.51。

**改法**（已落盘，五处）
- 摘要 `:68`：`两个杠杆在尾部可叠加` → `两个杠杆在尾部可以同时受益，但\textbf{不独立相加}（合起来拿到单独增益之和的六到七成）`
- 贡献 3 `:132`：→ `两个杠杆在尾部可以同时受益，但\textbf{不独立相加}。`
- §3.6 小节标题 + 正文 `:758–768`：
  `两个杠杆在尾部可以同时受益但不独立相加`；
  正文改为「达 **49.18**（三个读出种子：49.12 / 48.82 / 49.59），比架构单独（42.47）高 6.71、
  比读出单独（43.31）高 5.87。**但两个杠杆并不独立相加**：$+14.38$ 与 $+15.22$ 之和的预期是 $+29.60$，
  实际只拿到 $+21.09$——**次可加**，合起来拿到约七成、损失约三成。」并附口径括注。
- 结论 `:1341`：→ `两个杠杆在尾部可以同时受益，但不独立相加。`

---

## A5：CONFIRMED（代码级 + 数据级双重确认）

**源文件**
- `v7_probe_audit.py:293–308`（`run_onprotocol`）：`Xtr, ytr = f["Xtr"][:n_train], …`，
  其中 `f = feats[name]` 是该条件**自己的钩子**下抽的特征；对照臂写死用 `clean["Xtr"]`。
  ⇒ **探针逐 α 训练，是 oracle-α 量**。
- `outputs_cifar100/v7_probe_audit/ra_onprotocol_main.csv`（列名即 `probe_same_cond` / `probe_clean_train` / `headroom_same`）

**算式 / 我算出**（逐格读 CSV）

| 条件 | probe_same_cond | probe_clean_train | 自带头 | headroom_same |
|---|---|---|---|---|
| clean | 53.19 | 53.19 | 59.99 | −6.8 |
| +0.3 | **48.35** | **18.1** | 28.09 | **20.26** |
| −0.3 | 47.67 | 34.94 | 42.91 | 4.76 |
| σ=0.3 | 11.37 | 3.18 | 3.87 | **7.50** |

- `20.26 = 48.35 − 28.09` ✓；与 α 无关的那一列在 +0.3 只有 **18.10**，**低于自带头 28.09** ✓
- 开放问题原写的"约 4 个百分点"来源过期（4 出自 `论文v3.md:524–525` 的 47.89 − 44.09，两者期刊里都不出现）。
  期刊现值两两相减：`48.35 − 43.31 = 5.04`；`51.47 − 43.31 = 8.16`；`55.72 − 43.31 = 12.41`——**没有一对是 4** ✓
- `51.47` = `v7_readout_capacity/capacity_ladder.csv` 的 `torch_linear`@0.3；
  `55.72` = `v7_readout_samplesize/samplesize_ext.csv` 的 `0.3 / n=50000` 行 ✓
（复核者说：同上；它的对照臂表与我逐格一致。）

**改法**（已落盘，四处，措辞一致）
1. §2.4 定义处：加「**该探针逐 $\alpha$ 训练**……headroom 是一个 **oracle-$\alpha$ 诊断量**……
   **不是可部署读数**」，并给出 18.1% 的对照列；
2. 摘要：加同义口径句 + 「真正与 $\alpha$ 无关的部署路径见第二条——它在同一 $\alpha$ 点上拿到 43.31；
   而只把同一个头在干净分布上重训，**增益为零**」；
3. §3.2：把"在同一条件下比较"改为"在同一 $\alpha$ 条件下比较"，并加"三个对照夹住"段
   （① 干净探针 18.1% ② 可部署读出 43.31 ③ b 臂零增益）——**即 D1 建议的"把 b 臂提到中心结论旁边"**；
4. §3.3 的样本量段加"（同为 oracle-$\alpha$ 口径）"；
5. 开放问题第 3 条整条改写为「**$\alpha$-无关读出的可达上界在哪？**……只拿到 43.31（3 seed），
   而 oracle-$\alpha$ 探针为 48.35，两者相差 **5.04** 个百分点（$48.35-43.31$，同一 $\alpha=+0.3$ 点）」。

---

## A6：CONFIRMED（且命中的是 **8 个**数，不是 5 个）

**源文件**
- CIFAR-10 侧：`outputs/extension5_dithering_vgg11/{activation_stats,arch_dithering_summary,flip_statistics,group_b_noise_injection,group_c_pure_noise}.csv`
- CIFAR-100 侧：`outputs_cifar100/extension5_dithering_vgg11/` 同名 5 个文件

**我算出**（左 C10 / 右 C100，逐格读文件）

| 量 | CIFAR-10 | CIFAR-100 |
|---|---|---|
| 4 bit − 8 bit（VGG-11） | 43.27 − 36.74 = **+6.53** | 19.75 − 16.91 = **+2.84** |
| 峰度 clean / 8 / 4 / 3 / 2 bit | 1.486 / 0.1096 / 0.2144 / 0.3843 / 0.0686 | 2.5406 / 0.8473 / 0.9802 / 1.0227 / 0.0879 |
| 4 bit 救回:杀死（净） | 860/207 = **4.15:1**（+653） | 655/371 = **1.77:1**（+284） |
| 8 bit + 最优剂量噪声（净） | 高斯 **+7** / 均匀 **−19** | 高斯 −20 / 均匀 **−9**（最优 −0.07） |
| 剂量匹配纯噪声（高斯/均匀） | **−2.45** / **−1.87** | −3.12 / −2.48 |
| ResNet-18（0 MaxPool）增益 | **+0.39** | **−0.21** |
| SimpleCNN（2 MaxPool）增益 | **−13.68** | **−14.71** |

⇒ 正文 H1–H3 引用的 `−2.45/−1.87`、`−0.03`、`+7/−19`、`+653`、`4.15:1`、`1.486/0.110/0.214/0.384`、
`+0.39`、`−13.68` —— **全部落在 CIFAR-10 目录**（8 个数族）；CIFAR-100 侧的对应量**全部不同**。
定性结论（H1 否决 / H2 确认 / H3 支持）两数据集一致。
（复核者说：5 个；实际 8 个——`−2.45/−1.87`、`−0.03`、`+7/−19` 也是 C10 的。
我按 8 个标注。）

**改法**（已落盘）
1. 该节正文加"**先标注数据集**"整句：图 10 的扫描是 C100，H1–H3 的机理量取自 C10 侧；
2. H1/H2/H3 每一处数字**逐个标 CIFAR-10**，并把 C100 的对应值就地给出；
3. `3 bit 峰度更高但精度略降` 限定为 **CIFAR-10 口径**（43.27 → 42.97），
   并注明 C100 口径下是 19.75 → 10.87（不是"略降"）；
4. **附录新增一节 + 一张平行表**（`tab:dither-c100`，四块 (a)~(d)），
   把 C100 侧全套量列出，并给出出处文件清单——即复核者建议的"白送一条稳健性证据"。

---

## A7：CONFIRMED

**源文件**：`outputs_cifar100/v7_probe_audit/ra_onprotocol_main.csv`
第 7/9/10 行（`all|alpha=+0.3` / `+0.4` / `+0.5`）的 `headroom_same` 列 = 20.26 / **30.9** / **30.82**

**算式 / 我算出**：`30.82 − 30.90 = −0.08` ⇒ **不是单调放大**，是"放大后趋平"。
（复核者说：同上。）

**改法**（已落盘，三处）
- 贡献 1：`随失真增强单调放大（+0.4 时 30.90、+0.5 时 30.82）` →
  `随失真增强放大后\textbf{趋平}（+0.3 时 20.26、+0.4 时 30.90、+0.5 时 30.82——最后两个点几乎持平，"信息差被拉大"这一解读自 +0.4 起饱和）`；
- 图 2 图注：`且该差距随失真增强而放大` → `该差距随失真增强先放大、后趋平（+0.4 为 30.90、+0.5 为 30.82）`；
- 正文：补"放大只到 $\alpha=+0.4$ 为止……**信息差在 $+0.4$ 之后不再被拉大**"。

---

## A8：CONFIRMED

**源文件**：`outputs_cifar100/v7_readout_capacity/capacity_ladder.csv` 的 `0.3` 行
（`logreg_C0.01` 49.23 / `linear` 48.25 / `knn_k20` 38.12 / `torch_linear` **51.4733** /
`mlp_bneck_32` 43.38 / `mlp_2layer_256` 48.62）

**算式 / 我算出**
```
−13.35 = 38.12 − 51.4733
 −8.09 = 43.38 − 51.4733
 −2.85 = 48.62 − 51.4733  ← 2.85 < Δ_proc 3.45
```
（复核者说：同上。）⇒ "差距**远超**门槛"对第三行不成立。

**改法**（已落盘）：改为按门槛分开说——k-NN（−13.35）与瓶颈 MLP（−8.09）**远超门槛**；
两层 MLP 差 2.85、**落在门槛之内**，按自订规则这一臂**不可裁决**，只能说方向上仍劣于线性头。

---

## A9：CONFIRMED（实为 **5/6**）

**源文件**：`outputs_cifar100/v5_alpha_drift/drift_stress.csv` 的 `simple_cnn_mp` 六行
（`acc_softgate` 与 `oracle` 两列）

**算式 / 我算出**
```
① 55.08−54.96 = +0.12 ✓   ② 56.07−55.87 = +0.20 ✓   ③ 55.50−55.56 = −0.06 ✗
④ 54.40−54.02 = +0.38 ✓   ⑤ 49.11−48.57 = +0.54 ✓   ⑥ 49.83−49.24 = +0.59 ✓
⇒ 5/6
```
（复核者说：5/6。claude-38 从 `v5_full_recipe/recipe_simple_cnn_mp.csv` 另推得 ③ = **−0.05**、④ = +0.37，
与期刊表自身的两格（55.50 / 55.56 与 54.40 / 54.02）**差 0.01**——我以**期刊表自己的两个数**为准，
故正文写 `差 0.06`，未采用 −0.05。）

**改法**（已落盘）：`（其中 4 种超过 oracle）` →
`（其中 \textbf{5} 种还超过 oracle。唯一不超过的是③仅负向：55.50 对 55.56，差 0.06——这是全表最小的裕度，
其余五行的裕度分别是 +0.12 / +0.20 / +0.38 / +0.54 / +0.59）`。
⇒ 按复核者建议**把唯一不满足的那一行点出来**。

**⚠ 未修（写锁不在我）**：同一错数也写在 `experiments_ledger.md:1332`（"（且 4/6 种下超过……）"）。
台账写锁在 claude-65，我一个字没碰——**请持锁者代改**。

---

## A10：CONFIRMED

**源文件**：`outputs_cifar100/v3_methods/{plain_uniform,budget_front,budget_rear,budget_sens,adv_uniform,curriculum_uniform,range_0.50}/alpha_sensitivity.csv` 的 `0.3` 行

**算式 / 我算出**
```
基准（plain_uniform, seed42） = 26.03
预算分配 19.56 / 25.46 / 26.13 → 最好 +0.10
对抗训练 24.32 → −1.71   （|Δ| < 3 ⇒ 门槛内，不可分辨）
课程式   21.78 → −4.25   （显著）
外推±0.5 20.48 → −5.55   （显著）
```
（复核者说：同上。）⇒「四条**全部**失效」与自订的 3 pp 纪律冲突。

**改法**（已落盘，六处）
- 小节标题：`四条补救路径全部失效` → `四条补救路径没有一条改善尾部`（并加 `\label{sec:fourpaths}`，见 A11）
- 正文：改为按门槛分开说（后两条显著劣于基准 / 对抗训练落在门槛内记为不可分辨 / 预算分配与基准同级）
- 图 4 图注、摘要、贡献 2、结论、讨论节（`:1191`，claude-0b 补点的那一处）同步改为"没有一条改善尾部"。

---

## A11：CONFIRMED

**源文件**：`paper/journal/main.tex`（改前）§5「局限与未来工作」= `\label{sec:limit}`；
"四条路"所在小节只有 `\subsection{}`、**无 label**。

**改法**（已落盘）：给该小节加 `\label{sec:fourpaths}`；部署表末行的
`（第 \ref{sec:limit} 节）` → `（第 \ref{sec:fourpaths} 节）`。

---

## A12：CONFIRMED

**源文件**：`paper/journal/main.tex` §5 的六条局限（失真模型 / 深层主干单次训练 / 池化定量分解 /
秩-恢复三点 / 跨实现未归因 / 读出层可写）——**没有 CIFAR-10 单 seed**；
而 `:571` 承诺"这一点记在局限里"。

**改法**（已落盘）：§5 新增第 **(7)** 条——
「CIFAR-10 侧的读出只有单 seed，跨数据集的"恢复率标度律"因此是提示性的」，
并写明理由：恢复率差（85.4 / 88.7 / 48.2 对 72.2 / 70.2 / 36.9）与本文自测的
$\Delta_{\text{proc}}=3.45$ pp **同量级** ⇒ 落在自订的不可裁决区间；
同时给出"下一步：CIFAR-10 补到 3 seed"。承诺句也改为指向"局限 (7)"。
（放在末尾而非插在 (2) 之后，是为了**不动已有编号**——`Touch only what you must`；
正文那条指向局限的交叉引用见下方"未修第 1 条"。）

---

## A13：CONFIRMED（但复核者建议的替换值 **1/5~1/30 也被我否证**）

**源文件**：期刊表 2 自己的成本列（读出 `12~30 min`、全网络 `1~6.5 h`）；
逐主干时间转述在 `论文v3.md:518–520` 与 `第五阶段报告.md:274–295`（`≈12 / ≈25 / ≈30 min`）。
⚠ 训练时间**在仓库里没有产物文件**（复核者 §己 1 与我的复核一致），故**只改比值、不改时间**。

**算式 / 我算出**
```
逐主干配对：12/60 = 1/5 ；25/390 = 1/15.6 ；30/390 = 1/13   ⇒ 1/5 ~ 1/16
跨主干极值（都允许）：12/390 = 1/32.5 ；30/60 = 1/2
⇒ 原写"1/15~1/30"既不是逐主干配对区间，也不是整体区间 —— 无出处
⇒ 复核者建议的"1/5~1/30"同样不成立（30 min 对 1 h = 1/2，比值可以大过 1/5）
```
（复核者说：改为"1/5~1/30、低约一个数量级"。→ **区间部分 REFUTED**，我用 **1/5~1/16**。）

**改法**（已落盘，五处）
- 摘要与结论：`1/15$\sim$1/30` → **`1/5$\sim$1/16`**
- 贡献 2：`成本低 1$\sim$2 个数量级` → `成本低约一个数量级`
- §3.4 小节标题：`以两个数量级更低的成本` → `以一个数量级更低的成本`（原写"两个数量级"= 100×，与 1/32 上限也冲突）
- §3.4 正文：`低 1$\sim$2 个数量级` → `低约一个数量级`
- 表 2 表注新增算式：`读出 12 / 25 / 30 min 对全网络 1 / 6.5 / 6.5 h ⇒ 1/5、1/16、1/13`

---

## A14：CONFIRMED

**源文件**：改前参考文献 11 条（`cim_survey, puma, nat, quant, dither, resnet, vgg, cifar, lpft, mtjcal, gjepa`）
——**无 fragility 来源**；而 §3.3「对照二」把它当作四路加固之一并复制其"RMS 归一化"告警。
`docs/LITERATURE_NOTES.md:41–50` 登记该文献为"逐字核实"。

**我自己联网核实了这条文献**（没有只信转述——尤其本项目刚查出 `\bibitem{nat}` 在 Crossref 上结构性不存在）：
arXiv:2606.11375 真实存在，作者 Orion Reblitz-Richardson，2026-06-09，
**全名带副标题**："When Probing Accuracy Saturates, Fragility Resolves: *A Complementary Metric for LLM Pre-Training Analysis*"；
其 fragility 定义（探针精度崩溃处的激活噪声水平）与"原始 σ 受激活尺度混淆、须 RMS 归一化"的告警均与本文引述一致。
⇒ 我把副标题补进了 bibitem（复核者未给全名）。

**改法**（已落盘）
1. 新增 `\bibitem{fragility} Reblitz-Richardson O. When probing accuracy saturates, fragility resolves: a complementary metric for LLM pre-training analysis[R]. 2026. arXiv:2606.11375.`
2. 对照二首句加 `~\cite{fragility}` 并写清"本文借用它作第二条独立读数"；
3. 陷阱段末尾补半句「**这一陷阱并非本文首见**：`\cite{fragility}` 自己就把它列为该指标的适用条件……
   本文在本场景下**独立复现**了它」。

---

## 附一（第二轮：claude-0b 裁决的四处）

### ① `journal:1289`「第一条局限」→ 局限 **(6)**：CONFIRMED，已改
"读出层可写"是 §5 的第 (6) 条；第 (1) 条是失真模型假设。已改为 `第 \ref{sec:limit} 节的局限 (6) 里`。

### ② `journal:449` 的斜率 3.23 → 2.71：**CONFIRMED 为错，已换成正确的 α 值**
- **源**：`experiments_ledger.md` §16.5a 的更正表（第 3805–3812 行）与
  `outputs_cifar100/v7_readout_samplesize/samplesize_ext_slope_correction.json`。
- **我算出**：正文那句讲的是 **α=+0.3** 的 headroom 曲线，而 `3.23 / 2.71` 是 **α=0** 曲线的
  `s_early / s_late`（3.2268 / 2.7102，"两个都取重算值"变体，**且不是执行的 `mixed` 变体**）。
  α=+0.3 的三个变体是：预登记字面 **5.0202 / 3.0558（r=0.6087）**、重算 4.6964 / 3.3796、混用 5.0202 / 3.3796。
  ⇒ **没有一个等于 3.23/2.71**。按项目自己指定的"预登记字面为主报值"，正文改为
  「对数刻度上的增量斜率从 **5.02 降到 3.06**，比值 0.61，仍高于预登记的"已接近饱和"阈值 0.5」。
  （该判据的分支是 ②"未见饱和"，两个 α 都命中，判决不变。）

### ③ 架构杠杆 42.47：**两个独立缺陷都确认，已按"同协议 + 3 seed"重做整条链**
| 缺陷 | 我复算的结果 |
|---|---|
| **跨协议** | 28.09 来自 `outputs_cifar100/task1_simplecnn/`（第一阶段脚本、其权重 `checkpoints_cifar100/simple_cnn/best_model.pth`）；42.47 来自 `v3_methods/simple_cnn_mp_clean_uniform/`。台账 §13.8（行 1565、4395）自己就写着这一对是"**异协议，仅作锚点**" |
| **单种子** | mp 干净臂三个**同协议**运行：`…_s43`(mode=clean, profile=uniform, **120ep**, seed 43) = 38.77、`…_s44` = 39.64；与 42.47 一起 ⇒ **40.29 ± 1.58**（总体标准差 1.5796）。`metrics.json` 显示三者的 mode/profile/total_epochs 完全一致 ⇒ **是同协议三次**，不是跨协议 |

**同协议配对**改用 `v3_methods` 内部的两臂（`plain_uniform` vs `simple_cnn_mp_plain_uniform`，
都是 mode=plain / profile=uniform / 120ep / seed 42）：**26.03 → 41.55（+15.52）**——此即台账 §2110–2111
已经裁定过的口径。**但这一对各只有单 seed**（mp 鲁棒臂没有 s43/s44），故**逐处标注"各单 seed"**。
⇒ **同协议与 3-seed 二者不可兼得**（同协议的一对是单 seed；有 3 seed 的干净臂基线是异协议），
我按 claude-0b 的要求**在两个口径下都显式标注**，没有留算不出来的数。

**改动点**（六处，全部落盘）
- 摘要：`从 28.09 提升到 42.47` → `同协议同 seed 配对下从 26.03 提升到 41.55（+15.52；单 seed）`；"不动训练脚本"这个不成立的定语删掉
- 摘要/贡献 3/结论里的"与之**正交**"三处：改为"零成本的第二杠杆"（"正交"与紧邻的"不独立相加"字面冲突）
- §3.6 正文：改成"主口径 = 同协议同 seed 配对 26.03 → 41.55（+15.52，各单 seed）"，
  干净臂（28.09 → 42.47）标注为**异协议锚点**，并给出该臂的 **40.29 ± 1.58**（3 seed）与 +12.20，
  明说"**42.47 是三个种子里最高的一个，不宜单独引用**"
- F07（`fig:maxpool`）图注：整段重写（见下）
- 部署节第一步与结论：`+14.38` → `+15.52`
- 附录"关键数值汇总"：拆成两行（同协议配对行 + 干净臂 3 seed 行）

**A4 那条链的复算（claude-0b 特别要求）**：**结论不变，链不用改数**。
§3.7 的"架构单独"必须用 **42.47** 而不是 40.29——**叠加实验只在 seed 42 那一块主干上做过**，
"架构单独"与"叠加"只能在同一块主干内相减（同实例配对）。我在正文里把这个理由写明了，
并给出替代口径的复算：若换成 40.29，预期由 +29.60 变 +27.42、缺口由 8.51 变 **6.33**、
实得占预期的 77%（原 71%），**"次可加"在两个口径下都成立**。

**F07 图注最终文字**（`.tex` 已落；请转给修图会话对齐图例）：
```
把 SimpleCNN 的最大池化层从 2 个增加到 5 个——参数量不变、训练成本为零。
\textbf{同协议同 seed 配对（120\,epoch、逐层独立采样）的读数是 $\alpha=+0.3$ 从 26.03 提升到 41.55（$+15.52$，各单 seed）。}
图中灰色虚线为\textbf{第一阶段脚本}的产物，与实线的 \texttt{v3\_methods} 分属两个阶段、
\textbf{不同协议，仅作锚点、不参与任何同协议对比}
（正文若引用 $28.09\to42.47$，即横跨这两条曲线，须显式注明其跨协议性；
该臂自身三个同协议种子的均值为 $40.29\pm1.58$，见正文）。
```
⚠ **要转给修图会话的一处事实核对**：图例现在写"2×MaxPool（**第一阶段 100 ep**，仅作锚点）"，
但那条曲线的源模型 `checkpoints_cifar100/simple_cnn/best_model.pth` 的 `metrics.json` 是
`total_epochs: 120 / best_epoch: 114 / resumed: false`，第一阶段自己的
`outputs_cifar100/task1_simplecnn/task1_simplecnn_summary.md` 也写"epoch 114/120"。
**"100 ep"在产物里找不到依据**（只有台账 §13.8/§4395 与 `论文v3.md:359` 的转述这么写），
所以我的图注里**不写 epoch 数**、只写"第一阶段脚本"。**请让修图会话定夺图例要不要改。**

### ④ 表 2 两格同值（6.22）的解释：CONFIRMED，已加表注
我复核过 claude-0b 的三条事实：ResNet-18 读出依附的 Exp3（6.22）**确是**该主干上全网络最优那一格；
SimpleCNN 28.09 < 29.89、VGG-11 15.98 < 24.69 —— 另两列读出依附的**确是弱主干**。
已按此加注（含"那两列的对照对读出侧是保守的"这句）。

### 附图注（F03/F04/F08/F09/F11）：**逐条核过 `paper/figures/gen_figures.py` 的绘制逻辑后才落**
- F03：`ax.plot(a0, m0, "o--", color=c)` 确认原头虚线**与主干同色**；`fill_between` 确认 ±1 总体标准差阴影 ✓
- F04：`WONG[5] if d < 0 else WONG[3]`，而 `WONG[5]=#0072B2`（蓝）、`WONG[3]=#009E73`（绿）⇒ "绿=高于基准、蓝=低于基准" ✓
- F08：`mk = ["o","s","^"]` 且三个 C10/mp seed 共用标签 ✓
- F09：`marker="s"` + `facecolors="none"` 的空心环叠在实心方上 ⇒ "重合的两枚标记" ✓
- F11：我**自己重算了源 CSV 全表**（15 点、±0.6）：scratch 正侧 +0.4 = +0.77、+0.5 = −10.77、
  负侧 −0.4 = +6.44 → −0.6 = +8.22；finetune 正侧 |Δ| 最大 1.63、+0.6 = −1.58、
  负侧 +1.21 → +0.14 —— **与转来的文字逐位一致** ✓

---

## 附一之二、claude-0b 转来的三条"新问题"与一条残留

| # | 判定 | 我的复算 | 处置 |
|---|---|---|---|
| 新 1 | **CONFIRMED** | `results_master.csv:39` = 29.89（PASS）；排除 mp 变体与读出臂后它是 SimpleCNN 全网络训练的最高值 | 已改（见 A3 段末） |
| 新 2 | **CONFIRMED** | 读出 CSV 的 `a_original_fc`@0 = **72.69**，`resnet18_exp3/metrics.json` = **72.70** ⇒ "逐位一致"不成立 | 已改为"最大差 0.01，即 10 000 张测试样本中的 1 张" |
| 新 3 | **已覆盖** | 我标的是 **8 个数族**（含 `−2.45/−1.87`、`−0.03`、`+7/−19`），比复核者列的 5 个多 | 无需再改 |
| A2 残留 | **CONFIRMED** | `journal:1189`（HEAD:1120）仍写 `换读出即取到（尾部 $+13\sim23$）`；`781d8c2` 只改了 120/1259 两处 | **已改**为 `$+15.2\sim+31.8$`（与 A2 的口径一致） |

---

## 附二、发现但**未改**的四处（超出 A3–A14 授权，报给 claude-0b 裁决）

1. **`:1272`**：「……因此它出现在第 `\ref{sec:limit}` 节的**第一条**局限里」——
   但"读出层可写"是第 **(6)** 条局限（第 (1) 条是失真模型假设）。属 A11 同类交叉引用错，未授权，未动。
2. **`:449`**：「（对数刻度斜率只从 **3.23** 降到 **2.71**）」——
   `samplesize_ext_meta.json` 里 α=**+0.3** 的斜率是 **5.02 → 3.38**；3.23 / 2.71 是 α=**0** 的 `both_from_recompute` 变体
   （且不是执行的 `mixed` 变体 3.0304 / 2.7102）。这一句是 B8 邻域，属 B 组，未动。
3. **架构杠杆 42.47 是单次数运行**（3 主干种子均值 **40.29**），而 §3.6 与摘要把它当该杠杆的定值引用；
   §3.6 的鲁棒训练臂 41.55 同理。这是 A4 顺带暴露的口径问题，**不在 A3–A14 清单里**，未动。
4. **`technical-report/main.tex` 继承 A2/A3/A4**（复核者 §己 5）：该文件由另一个会话在改，我不碰。

---

## 附三、`tools/check_papers.py` 完整输出（终版）

```
================================================================
论文提交前检查
================================================================

[journal]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：25 页 / 0 错误 / 0 未定义引用

[technical-report]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：22 页 / 0 错误 / 0 未定义引用

[competition]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：13 页 / 0 错误 / 0 未定义引用

================================================================
结果：全部通过 ✓
```
退出码 **0**（`python tools/check_papers.py; echo $?` → `0`）。
> 备注：我第一次运行时曾是 `✗ competition … 20 错误`——那是**另一个会话正在编辑竞赛版**的瞬时态
> （`git status` 显示 `paper/competition/main.tex` 处于 `M`）；复跑即全绿。journal 侧两次都是 0 错误。

---

## 附四（第三轮：B 组 B1–B8 + E 组 E1–E11）

> 方法同上：**每条自己回源复算**（两条核验结论另由两个只读子代理独立跑了一遍，我做交叉比对；
> 凡与子代理有分歧的地方，以我自己的复算为准并写清）。

### B 组（§乙 结论强于证据）

| 条 | 判定 | 我算出的 / 落盘动作 |
|---|---|---|
| **B1** | **CONFIRMED** | `proc_sensitivity.csv`：α=0 的极差 3.46 两端都是 Adam（50 轮 56.31 / 400 轮 52.85）✓；α=+0.3 的 argmin 是 **sgd_mom_150（48.12）** ✓。贡献 5 的"**证明**"降为"**表明**"并加适用范围；正文补上 α=+0.3 的分解（轮数在 Adam 内跨 **3.02**、同轮数换优化器跨 **2.25**、全距 **3.45** 由跨优化器那一对跨出）。⚠ 复核者"3.02 小于换优化器跨度"这句**在 150 轮等轮数读法下不成立**（2.25 < 3.02）——我按"两个因子在 +0.3 上同量级"写。 |
| **B2** | **CONFIRMED** | 实证范围确只有 A/B 两脚本的 5.55 pp（A 簇 {21.41,22.02,19.67} 均值 21.0333、std 0.9957；B 簇 6 个 run 均值 26.5783、std 0.2948，差 5.545）。摘要/贡献/正文/结论**四处**从"不可移植"降为"本文观测到…在归因之前本文不跨实现引用"。 |
| **B3** | **CONFIRMED** | 四路里只有对照二是独立第二读数；对照四把幅度压掉 16.23/20.26 = **80.1%** ✓。小结改为"四路**都未能否证** headroom；其中**只有**对照二提供第二条独立读数"。⚠ **顺带抓到复核者没发现的一处口径错**：原写"划分间标准差 0.14~1.33"——源文件 `c3_decoder_split.csv` 的列是 split_std **0.17~0.47**、bootstrap_std ≤0.38、overall_std **0.56~1.33**，原文把两列混成一个区间；已改为按列分列。 |
| **B4** | **CONFIRMED** | `c2_fragility.csv`：clean **0.4188**、α=−0.1 **0.3852**（论文把 −0.1 当成了负侧基准）、α=+0.3 **0.3230**、α=−0.3 **0.3310**、α=+0.5 0.1221；**文件里没有 −0.4/−0.5 行**。同一 \|α\|=0.3：−22.9% 对 −21.0%（约 1.1:1），而 headroom 同点是 4.3:1。已整段重写：删掉"互相印证"，改为"第二条读数在最关心的一点上**没有复现** headroom 的不对称"。⚠ 复核者说"两条读数**不同向**"也不精确——**方向是同的**（两侧都更脆），垮掉的是**幅度比**；我按后者写。⚠ 另：原文"从 clean **单调**降到 +0.5"有反例（+0.1 = 0.4273 **高于** clean），已一并去掉"单调"。 |
| **B5** | **CONFIRMED** | `results_master.csv` 152 行：**58 PASS / 74 n/a(读出适配) / 20 n/a(扫描复用权重) / 0 FAIL** ⇒ 62% 的行没真正接受自检。已改为"58 行通过、无一行 FAIL；其余 94 行该自检不适用"，并把"本论文引用的 152 行"改成"台账 152 行"（论文并未引用全部）。 |
| **B6** | **CONFIRMED** | 贡献 2 "唯一可行的一类修复" → "**投片后**唯一可行的一类修复（架构端那个零成本杠杆只能在投片前决定）"，与部署表的"设计阶段零成本 / 部署后不可行"对齐。 |
| **B7** | **CONFIRMED** | 我独立复算三主干 \|α\|=0.3 的不对称倍率 = **1.87 / 1.18 / 1.01** ✓（(59.99−28.09)/(59.99−42.91) 等）。图 1 图注与正文都补上三个倍率，并写明"**方向性在浅层显著、在最深的主干上几乎消失**"。 |
| **B8** | 🔴 **REFUTED** | 复核者按 `20.26−16.23 = 4.03` 去套 −2.2，判定"无出处"。**我算出不一样**：`c5_private_decompose_agg.csv` 的 `d_disjoint` 列在 α=+0.3 = **−2.19**（= 16.23 − 18.43），`d_sample_size` = −1.98；**4.03 是两个因子之和**，而论文的"约 −2.2"指的只是"不相交"那一项 ⇒ **有出处**，`27.63 − 2.19 = 25.44` ✓。**没改数字**，改为在正文写出算式与列名。残留问题（部分成立）：`docs/DERIVED_VALUES.md` **未登记** −2.2/25.4，该文件不在我的领地，**请其所有者补登**。 |

### E 组（§戊 可读性 / 术语 / 未标注口径）

| 条 | 判定 | 动作 |
|---|---|---|
| **E1** | **CONFIRMED** | 漂移表既无 caption 也无出处。已在引句里写明"$5\times$MaxPool 主干、CIFAR-100、各 3 seed、指标为 \texttt{mean7}、出处 \texttt{v5\_alpha\_drift/drift\_stress.csv}"。 |
| **E2** | **CONFIRMED** | "用六种分布**重做**" → "按六种分布对**已有的逐 α 结果重新加权**（权重固定、$\tau$ 不重选、零 GPU）"，并就地加一句限定：⑥ 那种 $\sigma=0.05$ 的窄峰在 7 点网格上只有 3 个非零点承载质量，需逐样本重采样才能确认。 |
| **E3** | **CONFIRMED** | "在⑥以外的⑤（尾部集中）处" → "在⑤（尾部集中）处…（差 −0.47）；**⑥（单峰）处同样如此**（48.57 < 49.08，差 −0.51）"。 |
| **E4** | **CONFIRMED（并两处纠正）** | 四个值并存确成立。**纠正一**：复核者说"headroom 的探针 = 表中的 torch 公平锚"——**错**，`v7_probe_audit.py:171–176` 的探针是 **sklearn `LogisticRegression(max_iter=1000)`**，与表中"sklearn 默认"那一行同族。**纠正二（我自己写的也错了）**：48.35 与 48.25 的 0.10 差**不是**"两个脚本各自实现"，而是**设备口径**——`v7_probe_audit/audit_meta.json` 是 **cuda**、`v7_readout_capacity/capacity_meta.json` 是 **cpu**（正是红线 §3.4 登记过的那类差）。已按设备口径改写，并把"51.47−48.35=3.12"从"正是 Δ_proc"降为"**与 Δ_proc 同量级**（这一对里同时含拟合过程、设备、种子三重差）"。 |
| **E5** | **CONFIRMED（我第一版改反了，已纠正）** | 我起初把"干净头"判为"重训出来的读出"，**错**。回源后：`v5_full_recipe.py:156–159` 的 `head_cln` 直接 `copy_` 自 `get_readout_module(model)`（模型自带层），且 `recipe_simple_cnn{,_mp}.csv` 的 `acc_clean_head@α=0` = **59.99 / 60.99**，与两块主干自己的 `best_test_acc` 逐位相同 ⇒ **干净头 = 模型自带头**（与 claude-0b 给的术语表一致）。已按定稿表落：`原始头`（首次对照：自带头/\texttt{a\_original\_fc}；门控节注明 = \texttt{acc\_clean\_head}）；\texttt{tab:arch} 行名后加"（后文简称 **mp** 主干）"；门控节首次出现处注明"鲁棒头**即读出适配头**，此处沿用产物列名 \texttt{acc\_robust\_head}"；部署节孤立的"$\alpha$-混合鲁棒头"加"（即上文的读出适配）"。**`鲁棒头` 未并入 `读出适配头`**（两条产物线不同，硬并会掩盖口径差）。 |
| **E6** | **CONFIRMED** | 附录"关键数值汇总"补一行：`过程自由度 Δ_proc（α=0 / +0.3）& 3.46 / 3.45 & v7_etf_readout/proc_sensitivity.csv`。 |
| **E7** | 🔴 **REFUTED** | 复核者说"0.39062 应为 0.39061"。**我算出不一样**：`rank_stats.csv` 确是 **0.39061**，但 `v7_nc_geometry/nc_geometry.csv` 的同一对是 **0.45669 / 0.39062**——**与论文逐位相同**。⇒ 0.39062 **不是笔误**，是另一条产物（高精度重算）。**没改数字**，改为在句中补出处并说明两个产物差最后一位。 |
| **E8** | **CONFIRMED** | 三个值定义不同、各有出处：5.55 = 26.58−21.03（B 侧 6 个 run 合并）；+5.52 = 26.55−21.03（两臂**同为 shared 粒度**）；5.54 是**已退役**的旧粒度效应（台账自己标了被否证）。已给 +5.52 与 5.55 各补口径，**没有强行统一**。 |
| **E9** | **CONFIRMED** | 唯一的 `⚠` 在 `\paragraph` 标题里，已去掉符号。 |
| **E10** | 🔴 **REFUTED** | 复核者说 21.78−20.60 = 1.18 与文中的 +1.17 冲突。**我算出不一样**：逐 seed 算 drop 再取均值，5×max **20.6033±1.5413**、5×avg **21.7767±0.9110**，差 **1.1733 → +1.17**，SE **1.0337**、z **1.135**（论文的"SE 1.03 / z≈1.1"逐位对）。1.18 只是"**先把均值舍入再相减**"的假象。**数字不改**，只在附录补未舍入算式。 |
| **E11** | **CONFIRMED** | 给陷阱表补了出处注（\texttt{c2\_fragility.csv} 的 \texttt{critical\_noise\_abs} / \texttt{critical\_sigma\_rms\_norm}，并注明负侧基准是 clean）；"10 对中 9 对反向"那句补全口径（**正侧** 5 点两两成对、唯一同向的是 $+0.4\to+0.5$），并明说它**只**说明沿 α 轴的方向同构、**不**说明正负不对称一致（与 B4 的限一致）。 |

### 附四·补：本轮我自己抓到的第 12 处（复核者与两个子代理都没报）

| # | 事实 | 动作 |
|---|---|---|
| 新 4 | `main.tex` 原写"（$5\times$MaxPool 主干的 $R^2$ 为 0.8323，低于**无池化**的 **0.933**）"。**两处都错**：① "无池化（plain）"是误命名——plain 主干是**带 2 个 MaxPool** 的 SimpleCNN（见表 \ref{tab:arch}）；② 0.933 在 CIFAR-100 的源文件里**不存在**，`recipe_simple_cnn.csv` 的 `ridge_R2` 是 **0.935**（0.933 是 CIFAR-10 侧的转述值，属于跨数据集搬数）。已改为"2$\times$MaxPool（plain）主干的 **0.935**"。 |

---

## 附五（第四轮：C 组 C1–C6 + D 组）

> 材料：`docs/verify/C_group_limits.md`、`docs/verify/related_work_draft.md`。
> **C7 未做**（§16.7a 已进论文）；**C5 只写"缺哪一臂"，不写任何结果**。

### C 组：新增局限 (8)–(12)，并修正 (7)

| 条 | 我核过的事实（源） | 落盘 |
|---|---|---|
| **C1** | 抽 3 份读出产物表头（C100 的 exp3/mp、C10 的 vgg11）**全是 `readout,-0.3,…,0.3` 七个点**；探针 @+0.4/+0.5 = 43.22/36.91，自带头 12.32/6.09，headroom +30.90/+30.82（`ra_onprotocol_main.csv`） | 新增**局限 (8)**：读出杠杆只在 $\|α\|≤0.3$ 评估过；明写"**该问题不可由现有数据推断**"（不是"我们没测"），并给下一步（把评估扩到 ±0.5，成本同级） |
| **C2** | `models/resnet.py:8` 明写"**去掉初始 MaxPool2d 层**"；`arch_dithering_summary.csv` 的 `maxpool_count`：simple_cnn 2 / vgg11 5 / **resnet18 0** | 新增**局限 (9)**：无池化主干（含注意力）**没有这个旋钮**，而"架构端先上"是落地第一步；下一步给零成本替代（stride 下采样换池化 / avg 换 max） |
| **C3** | C10 侧 7 份读出产物**全无 seed 后缀** ⇒ 单次运行 | **已由第一轮的局限 (7) 覆盖**（正文承诺已兑现）。⚠ 但**我修正了 (7) 自己的措辞**：原文写"恢复率差与 Δ_proc **同量级**"，我复算是 11.3~18.5 pp 对 3.45 pp = **3~5 倍**——**主效应远大于过程自由度**；真正卡住的是"单次运行"。已改成这个更准、也对本文更有利的表述 |
| **C4** | 参数量 0.24M~11.2M，全部 ReLU + 卷积/池化堆叠 | 新增**局限 (10)**；并把 **§6.2 的贡献级措辞降级**（见下） |
| **C5** | 探针为 oracle-α；缺"同构但 α 未知"的臂 | 新增**局限 (11)**：只写"缺这一臂、因此 5.04 未能分解"，**不含任何结果**（该实验仍在跑） |
| **C6** | 全库唯一 dither 目录的 `alpha` 列只有 `{0.0, 0.3}`，且证据取自**未加读出适配**的原始头 | 新增**局限 (12)**：两条杠杆从未组合，"可加还是互相挤占同一机制"未判定；下一步给 $2\times2$ 设计 |

**C4 的贡献级改动（你点名要我说清改成了什么）**
- 小节标题：`一个可迁移的测量顺序` → **`一个我们建议的测量顺序（在一族卷积主干上验证过）`**
- 正文：`这条原理的可迁移形式是一个测量顺序` → **`这条原理可以落成一个测量顺序……本文只在一族卷积主干（0.24M~11.2M 参数，全部为 ReLU + 卷积/池化堆叠）上验证过它，适用范围见局限 (10)`**

### D 组

**（甲）相关工作段：已插入 §1.3，但⚠ 我改掉了草稿里对 SHOT 的描述——它写反了。**
- 草稿写「SHOT 冻结特征提取器、只学习分类头」。**我联网核了 SHOT（arXiv 2002.08546, ICML 2020, Liang/Hu/Feng）**：
  它的机制是**冻结源模型的分类器（hypothesis）、学习目标域的特征提取器**〔多个权威来源一致：ACM DL / NeurIPS 引文〕。
  **草稿的说法与它正好相反**——按草稿写会被这个方向的审稿人一眼看穿。
- 落盘版改成：Tent **只更新 BN 仿射参数**~\cite{tent}；SHOT **冻结源模型分类器、只学特征提取器**~\cite{shot}；
  并据此给出**更锋利**的差异化："本文与这一支共享'只动极少量参数、不需要源数据'的取向，
  **但动的对象正好相反**——本文冻结的是特征（主干物理不可重训），更新的是分类头。"
- 三条差异按复核者的框架写，但把草稿里"两个杠杆的**正交叠加**"改成 **"可同时受益但次可加"**（与 A4 对齐）。
- 引文只加 2 条（**Tent**、**SHOT**），第三条 **Kumar 复用已有的 `lpft`**（不重复登记）。
  **两条新引文我都自己联网核过**（标题/作者/年份/出处），没有只信草稿的"OpenAlex 逐字核"
  ——OpenAlex 只核标题作者，**核不到方法描述**，这次的问题正出在这里。

**（乙）§2.3 补句：已落盘，oracle-α 的标注没有漏。**
"冻结主干、只重训一个线性头，在 α=+0.3 上**实际达到 43.31**（SimpleCNN，3 seed，24ep；原始自带头 28.09）——
**这是一个真实可部署的分类器**……同口径的 **oracle-α** 探针读数为 48.35，即**部署物取到了该上界的 89.6%**（43.31/48.35）。
（口径提醒：探针是 oracle-α 诊断量……**不是部署时能达到的绝对水平**。）"
⇒ 我按你的要求**把"oracle-α"写进了同一句**，并另加一句括注堵住误读。
（43.31 用的是 **ep24 三份**的均值 43.3067；同目录非 ep24 的三份给出 40.54，**未混用**。）

**（丙）用词禁令：遵守。** 全文无 `last-layer recalibration` / `重校准`（grep 证实），
也没有任何地方把它当作方法名使用。

### 第三项（摘要叙事重心）：**未动 `.tex`，只把改法写在这里，等你与用户过完再定**

- **现文**（`journal:61`）：`\textbf{第二，因此最有效的修复动作在读出侧。}`
- **建议改法**（复核者 §丁 D2 的意见 + 你的判断）：
  `\textbf{第二，诊断的推论：既然瓶颈在解码，那么最省力的修复动作在读出侧。}`
- 影响面：只此一句（摘要第二条的引导句）；该条其余文字与正文 §1.2 贡献列表、§6 讨论**均无需改**
  （§1.2 已经写过"该做法本身不是本文的贡献"）。
- **我的意见**：这句改动的实质是**把"我们发明了一个方法"改成"我们量出了一个诊断"**，
  与 §1.2 的既有立场一致、也与本轮新增的相关工作段（把本文放进 TTA/DA 谱系而非方法竞赛）呼应。
  **但它确实会改变摘要给审稿人的第一印象**，属作者层修辞，**不宜由我或由协调会话单方面落**。

### ⚠ 我自己在这一轮里犯并抓到的一处

写局限 (9) 时我手滑写成 `**本身不含池化**`（markdown 加粗），**这正是本项目"静默失效"的那个坑**
（`*` 在文本模式是普通字符，**编译不报错、渲染成字面星号**）。
**是我自己的静态检查抓到的**（`grep -n '\*\*'` 命中 1 处），已改成 `\textbf{}`。
⇒ 再次印证：**"改完必须编译/必须扫"不是形式主义**——这一处若漏，就是要等审稿人看出来的那种错。

### 这一轮的红线状态

- **按指示未跑 `tools/check_papers.py`**（M4 刚崩两次、物理可用 0.59 GB）。
  替代验证（零内存开销）：**未定义 ref 无**（唯一命中仍是第 30 行注释里的示例 `sec:xxx`）、
  **14 个 cite 键全部有 bibitem、无孤儿条目**、**markdown 残留 0**、**begin/end 60/60**。
- 未碰 `figures/`、未碰另两份稿件与台账；**未 commit**。

---

## 四件套

**里程碑**：四轮，全部落在 `paper/journal/main.tex`——
① A3–A14（12 条）+ 转来的 3 条新问题 + A2 残留 1 处；
② 四条裁决（含架构杠杆整段重做 + A4 链复算）+ 六处图注；
③ B1–B8 + E1–E11（19 条）；
④ C1–C6（新增局限 (8)–(12)、修正 (7)、§6.2 措辞降级）+ D 组的（甲）（乙）（丙）。

**关键数字 + 出处**
| 数字 | 出处 |
|---|---|
| 43.31 / 47.79 / 26.82（读出适配 3 seed） | `outputs_cifar100/v3_readout_repair/*ep24*.csv` 的 `e_readout_NAT@0.3` |
| 28.09 / 15.98 / 6.22（读出所依附主干的原始头） | 同上文件的 `a_original_fc@0.3`；主干身份经 `metrics.json` 的 `best_test_acc` 核验 |
| 29.89（SimpleCNN 全网络最优） | `results_master.csv:39`；`outputs_cifar100/task3_simplecnn/Exp3_FullRobust/alpha_sensitivity.csv` |
| 49.18 = (49.12+48.82+49.59)/3；43.31；缺口 8.51 | `outputs_cifar100/v3_readout_repair/readout_repair_c100_mp_*` 与 `…_clean/plain_*` |
| 40.29 = (42.47+38.77+39.64)/3（架构杠杆 3 主干种子均值） | `outputs_cifar100/v3_methods/simple_cnn_mp_clean_uniform{,_s43,_s44}/alpha_sensitivity.csv` |
| 20.26 = 48.35−28.09；18.10（α 无关探针） | `outputs_cifar100/v7_probe_audit/ra_onprotocol_main.csv`；`v7_probe_audit.py:293–308` |
| 5.04 = 48.35−43.31（开放问题新口径） | 同上 + `v3_readout_repair` |
| C10 / C100 dithering 两套（8 个数族） | `outputs{,_cifar100}/extension5_dithering_vgg11/*.csv` |
| 20.26 / 30.90 / 30.82（非单调） | `ra_onprotocol_main.csv` 第 7/9/10 行 |
| −13.35 / −8.09 / −2.85 vs 门槛 3.45 | `outputs_cifar100/v7_readout_capacity/capacity_ladder.csv` |
| 5/6 超过 oracle | `outputs_cifar100/v5_alpha_drift/drift_stress.csv` |
| +0.10 / −1.71 / −4.25 / −5.55（基准 26.03） | `outputs_cifar100/v3_methods/*/alpha_sensitivity.csv` |
| 1/5、1/16、1/13（成本比） | 期刊表 2 成本列 + `论文v3.md:518–520`（时间本身无产物文件） |
| B 组：3.46 / 3.45（Δ_proc）、3.02 / 2.25 / 3.45（α=+0.3 分解） | `outputs_cifar100/v7_etf_readout/proc_sensitivity.csv` |
| B 组：21.03±1.00 / 26.58±0.29（A/B 簇）、5.545→5.55 | `outputs_cifar100/v6_p4e_crosspath/crosspath.csv`；台账 §14.5/§14.6 |
| B 组：0.4188 / 0.3852 / 0.3230 / 0.3310 / 0.1221（fragility） | `outputs_cifar100/v7_probe_audit/c2_fragility.csv` |
| B 组：58 PASS / 74 + 20 n/a（152 行） | `results_master.csv` 的 `pairing_check` 列 |
| B 组：1.87 / 1.18 / 1.01（不对称倍率） | `outputs_cifar100/task1_*/alpha_sensitivity.csv` |
| B 组：−2.19 / −1.98 / 25.44（不相交校正） | `outputs_cifar100/v7_probe_audit/c5_private_decompose_agg.csv` 的 `d_disjoint`/`d_sample_size` |
| E 组：0.17~0.47 / ≤0.38 / 0.56~1.33（划分稳健性） | `outputs_cifar100/v7_probe_audit/c3_decoder_split.csv` |
| E 组：0.45669 / 0.39062（fisher） | `outputs_cifar100/v7_nc_geometry/nc_geometry.csv`（`rank_stats.csv` 为 0.39061） |
| E 组：1.1733（avg−max 未舍入）；SE 1.0337 | `outputs_cifar100/v3_methods/simple_cnn_mp_*_uniform*/alpha_sensitivity.csv` |
| E 组（新 4）：0.935（plain 主干 R²，非 0.933） | `outputs_cifar100/v5_full_recipe/recipe_simple_cnn.csv` 的 `ridge_R2` |

**判据命中哪个分支（三轮合计）**：
- **A 组 A3–A14：CONFIRMED 12 / REFUTED 0 / UNCLEAR 0**
- **B 组 B1–B8：CONFIRMED 7 / REFUTED 1（B8） / UNCLEAR 0**
- **E 组 E1–E11：CONFIRMED 9 / REFUTED 2（E7、E10） / UNCLEAR 0**
- 另：复核者的**建议改法**另被否证 3 处（A4 的"违反种子纪律"理由、A13 的 1/5~1/30 区间、E4 的"探针 = torch 公平锚"映射）；
  我自己另抓到 **4 处**复核者未发现的问题（表 2 的 26.57 不是最好、"逐位一致"在 ResNet-18 上不成立、A2 残留一处、
  §3.6 的 R² 0.933 与"无池化"误命名）。

**旧口径的四件套（第一轮，A 组）**：**CONFIRMED 12 条 / REFUTED 0 条 / UNCLEAR 0 条**。
其中 **2 条的"建议改法"被否证并修正**：
① A4 的"违反种子纪律"理由（42.47 是单次运行，不是 3 seed 均值）；
② A13 建议的替换区间 `1/5~1/30`（30 min 对 1 h = 1/2，区间上界不成立）⇒ 改用 `1/5~1/16`。
另有 1 条（A3）的适用域被收窄：**"第三行是第二行的基底"只在 ResNet-18 列成立**。

**有没有触发红线**：**没有**。
（唯一一次"红"是虚警：我第一次跑 `check_papers.py` 时 `competition` 报 20 个错——那是**另一个会话正在编辑竞赛版**
的瞬时态，复跑即全绿；journal 侧从头到尾 0 错误。）

### 终版 `tools/check_papers.py` 输出（claude-0b 放行后补跑，覆盖全部改动）

```
================================================================
论文提交前检查
================================================================

[journal]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：26 页 / 0 错误 / 0 未定义引用

[technical-report]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：22 页 / 0 错误 / 0 未定义引用

[competition]
  ✓ 源码静态检查：无 markdown 残留
  ✓ 编译：13 页 / 0 错误 / 0 未定义引用

================================================================
结果：全部通过 ✓
```
**退出码 0**（`python tools/check_papers.py; echo $?` → `0`）。
跑之前我按本机的内存纪律先查了余量：**提交可用 1.28 GB**（物理 0.40），跑完 **1.00 GB**，
**训练进程仍在（3 个 python 存活）**——即这次编译没有触发 1455。
（检查器本身不支持只查一份稿件；它按 journal → technical-report → competition 顺序串行编译。）

---

⚠ **下面这段是"放行前"的状态记录，保留作为过程证据**：

**最后一次编译验证的状态（当时按 claude-0b 的指示暂缓）**：
`check_papers.py` 的最后一次完整运行是在 **B 组改完、E 组编辑进行到一半**时——
**EXIT=0，journal 26 页 / 0 错误 / 0 未定义引用**。
之后我又落了 **E5 术语对齐 + `tab:arch` 行名**（纯文本插入）与一处把 `\ref{sec:readoutnat}`
（**该 label 不存在**）就地改掉的自我修正。
**这之后没有再编译**——因为 claude-0b 通知"M4 训练 16:37 CUDA OOM 后刚重启、内存仍在红区，
检查器要编译三份稿各两遍，先攒着，等通知错峰跑"。**我照办，没有为了凑绿而抢跑。**
作为替代，我做了**零内存开销的静态检查**（结果贴在下面），并在 claude-0b 放行后补跑正式检查器。

```
=== 未定义的引用（refs 里有、labels 里没有）===
sec:xxx        ← 这是第 30 行 LaTeX 注释里的示例文字，不是真引用
=== markdown 残留 ===
（空）
=== begin/end 配平 ===  begin: 60 / end: 60
=== 单行 $ 数为奇数 ===（空）
```
- `.tex` 未混入 markdown（`check_papers.py` 静态检查通过；我全程只用编辑工具逐处改，未用字符串替换脚本）；
- `tools/check_papers.py` **退出码 0**，journal 0 错误 / 0 未定义引用；
- 未碰 GPU、未跑训练、未写任何 `outputs_*` / `checkpoints_*`；
- 未碰 `paper/technical-report/`、`paper/competition/`、`experiments_ledger.md`；
- 未 commit。
