# 论文外部体检报告

> **读者**：本项目作者与总管团队（熟悉项目，需要外部视角）。
> **体检人**：外部顾问会话——**未参与写作、且先独立读完三份稿、之后才读内部自查材料**。
> **时间**：2026-09-25 18:25–18:45。
> **方法**：三份稿全文精读 → 回查台账/产物/产物 CSV 逐位复算 → 独立编译（输出到临时目录）→
> 外部文献 L1/L2 核实 → **最后**才读 `paper/coverage-audit.md`、`docs/verify/*`、`docs/*`。
>
> ⚠ **引文只对下面这三个版本有效**（本项目行号会变，故一律附**逐字原文**，行号仅供参考）：

| 稿 | sha256（我读的版本） | mtime |
|---|---|---|
| `paper/journal/main.tex` | `fa0f7f9c7aed5ad4ed3e74ce640a1258bb116972541ede6c8119e5376251a74e` | 2026-09-25 18:24:22 |
| `paper/technical-report/main.tex` | `82ffc89fd2959944d986cc2f59a40c56494bac83fcd32b4b8363c2da478ede02` | 2026-09-25 18:23:15 |
| `paper/competition/main.tex` | `402e01e41fd53835c5ae3a560c338a65c27cd67a4019aebe6ccbb591566442bc` | 2026-09-24 19:23:43 |

---

## 用户裁决记录（2026-09-25 20:0x，本报告问题的处置）

> 用户对"剩下的 4 项"的裁决原文：**「按影响分流」**。据此：

| 项 | 裁决 | 内容 |
|---|---|---|
| **H-1 / H-3 / H-4 / M-1 / M-2 / M-3 / M-5 / M-6** | ✅ **已修**（总管执行，我已在成品里逐条复核） | 见 §5 R2-5 |
| **R2-1 / R2-2 / R2-3**（圈码与 `⇒` 缺字形、tech 两处被裁） | ✅ **已修**（总管执行，我用日志 + 提取 + 目视三路独立复核） | 见 §5「⑤·更正」 |
| **H-2**（「方差」应为「标准差」） | ✅ **改** —— **推翻项目内部"不改"的原裁决** | 数值陈述 **7 处**（journal 5 + tech 2）+ 名词性用法 5 处建议一并统一；清单已转总管 |
| **M-4**（竞赛版两处） | ✅ **改** | 补 VGG-11 的 5.26 限定；局限的"未执行"改为时点/范围声明 |
| **L-2 / L-4 / L-5 / L-6 / L-7** | ✅ **改** | 语汇与著录，清单已转总管 |
| **M-7**（13 处无编号表） | ⏸ **缓** | 等**目标期刊定了**按模板一次性重排（现在改很可能白改） |
| **L-1**（孤儿图表）、**L-9**（派生值重扫） | 📌 **留档不动** | 随时可做 |

> ⚠ **本章第一轮的 H-2 计数写错**（初报"两稿共 5 处"）——实际数值陈述里是 **7 处**。
> 已在 §2 H-2 处更正并注明"初报漏数"。**这条本身也是"计数类结论会过期"的又一例**：
> 我写那张清单时用的是当时的 grep，之后文件一直在被改。

---

## 0. 我读了什么、没读什么

### 读了的

| 对象 | 范围 |
|---|---|
| 协调规则 / 状态 | `docs/V7_COORDINATION.md` 全文、`docs/HANDOFF.md` 全文 |
| **三份稿** | `journal/main.tex`（1665 行）**全文逐行**、`technical-report/main.tex`（1296 行）**全文逐行**、`competition/main.tex`（717 行）**全文逐行** |
| 一手台账 | `experiments_ledger.md` 的 **§12.9–12.11、§13.6b、§16.1** 区域精读 + 关键词全库检索 |
| 产物 | `results_master.csv`（152 行全字段）、`outputs_cifar100/v3_readout_repair/*.csv`、`outputs/v3_readout_repair/*.csv`、`outputs_cifar100/v7_M4_audit/M4_EXECUTION_PLAN.md` |
| 内部自查（**读完论文之后**） | `paper/coverage-audit.md`、`paper/health-check-report.md` §0–1、`docs/verify/` 下 17 份中的 `project_health_check.md`/`cross_doc_consistency.md`/`A_cluster_independent.md`/`C_group_limits.md`/`C5_third_arm.md`/`techreport_fixes.md`/`refs_other_docs.md`、`docs/DERIVED_VALUES.md`、`docs/LITERATURE_NOTES.md` |

### 跑了的（全部只读或输出到临时目录）

1. `tools/static_tex_check.py`（**零写入**）——三份稿静态错误 **0**；提示 journal 2 条、tech 29 条、competition 6 条（内容见 §2）。
2. **独立编译**：`xelatex -output-directory=/tmp/vcheck/<稿>` **两遍 × 3 份**（**源目录零写入**，未改动任何被 git 跟踪的 `paper/**/main.pdf`）。
   结果：**journal 29 页 / technical-report 24 页 / competition 13 页，三份都是 0 错误、0 未定义引用、0 "LaTeX Warning: Reference|Citation"**。
   ⇒ **HANDOFF 的"29/24/13 页、EXIT=0"这句话，我用一条不同的路径独立复现了**（我用 `-output-directory`，没有覆盖你们的 `main.pdf`）。
3. 逐位复算：读出适配三 seed 尾部均值与标准差、CIFAR-10 读出表、`results_master.csv` 的 pairing 计数、叠加实验算术。
4. **渲染并目视核对 4 页 PDF**（`pdftoppm`，看的是我编译到临时目录的那一份）：
   journal p.27（附录 A 表——**就是在这里发现 H-4**）、journal p.29（参考文献）、journal p.10（`tab:nat` 所在页，正常）、
   technical-report p.17（交叉评估图 + 四判定表，正常）。**其余 60+ 页我没有目视。**
5. **外部文献核实**（curl → arXiv API / Crossref）：
   - `2606.11375`（fragility）L1 命中、作者/年份一致；**L2 摘要逐句比对**：「Rescaling per layer removes most of the gradient… We recommend **RMS-normalized critical noise for cross-layer claims and raw critical noise for within-layer comparisons**」——与你们引的「跨层比较须用 RMS 归一化的临界噪声，层内比较才用原始值」**逐字相符**；「Where probing accuracy returns a flat answer, fragility returns a structured one」也在原文。
   - `2608.20516`（Graph-JEPA）L1 命中；**L2**：原文「attaining linear-probe accuracy **0.871** and effective rank 18-47, yet retrieval recovers **0.00 of 14.4 bits**」「**Rank, probes, and metrics can all saturate on an unsupportive evaluation**」——与论文两句引用**逐字相符**。
   - `2202.10054`（lpft）、`2006.10726`（tent）、`2002.08546`（shot）L1 全部命中。
   - `mtjcal`：Crossref 命中 Science Advances **10(38)**、2024、DOI `10.1126/sciadv.adp3710`、作者 Xiao/Naik/Lim——与著录一致；题名「…accurate and **on-chip-training-free** in-memory computing」与论文那句"查找表 + 自适应量化 + 不做片上训练"的描述**相符**。
   ⇒ **抽查的 6 条文献 L1/L2 全部通过**（这是在 `nat` 事件之后最值得确认的一类，我把能外部核的都核了）。

### 没读 / 不能核的

1. **只目视了 4 页**（见 §0"跑了的"第 4 项）——**其余 60+ 页的版面、图里画得对不对、图例清不清楚，我都没看**。
   我只核了图注与正文的一致性、图文件是否存在、`\includegraphics` 与 `\ref` 的对应（见 §2 L-1）。
   ⚠ 反过来说：**H-4 是我随手渲染 4 页就撞上的**，这提示**"通读 PDF"这件事本身可能还有别的收获**——建议你们自己做一遍。
2. **没读 `ucas-cod-lab-report/`**（简报指定三份稿）。
3. **没读 `论文v3.md`、四份阶段报告**（只读了三份稿 + 台账 + 指定文档）。
4. **没跑 `tools/check_papers.py`**——我读了它的源码，用**等价的独立路径**（`static_tex_check` + `-output-directory` 编译两遍）复现了它的三项结论，但**为了不覆盖被 git 跟踪的 `main.pdf`**，我没有直接跑它。**你们要的"检查器 EXIT=0"我确认成立，但用的是我的路径，不是你们的脚本。**
5. **目标期刊未定** ⇒ 格式合规无从查起（按简报要求，本条不报）。
6. **没跑任何 GPU 活、没起训练、没重跑任何实验**。
7. 我在 §3 列出了**我判断不了、需要你们自己裁的**项。

---

## 1. 总体判断（结论先行）

**三份稿的内容是完整的、数字的可追溯性明显好于同类项目，我没有找到任何"凭空数字"。**
我抽查的 30+ 个派生量（均值/差值/比值）**算术全部自洽**，跨文档共享量的值也基本一致；
第七阶段的阴性结果（M8b/§16.2a/§16.7/§16.8）都已按内部裁决落地；
M4 的三个同步点（局限 (2)、`tab:nat` 口径 ③、三级可分辨性下界）**确实改到了**。

**问题集中在四个地方**，按我判断的优先级：

1. **有一处"口径注自相矛盾"**（§2 H-1）：三级可分辨性那条注把 **clean 的标准差（±1.85）写成了尾部下界**，而同一个小节自己的正文、以及 `tab:nat` 的 ±0.16 都否证它。这是**我这次新发现的、你们内部没报过**的一条。
2. **成品 PDF 里有一张表被裁掉了右半**（§2 H-4）：journal 附录 A"本文引用的主要数值与出处"表的**出处列在页边界被切掉**（overfull **390.8 pt**），而这列正是该表存在的理由。你们内部记的是"1 处轻微"——那是 15 页版本的读数。**这一类只有看 PDF 或数日志才能发现，`check_papers.py` 不会报。**
3. **两稿对同一件事给出相反的状态描述**（§2 H-3）：技术报告说那条"与部署同构的读出臂**正在测量中**"，journal 说"**本文缺这一臂**"。按你们自己的 `docs/verify/C5_third_arm.md`，该臂**不存在、且受内存红线阻塞未执行**。
4. **论文自己的门槛纪律在做结论时被放宽了一档**（§2 M-1、M-3）：journal 摘要把"不可裁决的一臂"说成"没有一个超过"，competition 的结论第 1 条把"3.4 倍"当普适常数（同一份稿的正文写着 1.40 / 0.94）。

**没有 CRITICAL。** 最严重的一条（H-1）是"支撑性方法论表里的一处标错"，不改变任何中心结论；
但它恰好出现在你们**要求读者拿去当分辨率尺子**的那张表里，所以我放在第一位。

另外，有一条**统计术语问题**（§2 H-2）：journal 与技术报告共 **5 处**把"标准差之比 6.5"写成"**方差**估高约 6 倍"。
你们内部 `techreport_fixes.md` R4 **已判"不改（术语问题，项目一贯表述）"**——我把它列出来是因为
**投稿读者不会读你们的内部裁决**，而同一段里你们又正确使用了"标准差"，并置会显得矛盾。**要不要改由你们定。**

---

## 2. 问题清单（按严重度）

> 每条给全四件：**① 文件 ② 逐字引文 ③ 为什么是问题 ④ 建议怎么改**。
> 把握度：**确定 / 较有把握 / 存疑**。

### HIGH

---

#### H-1 🔴 三级可分辨性的口径注把 **clean 的标准差**当成了**尾部的下界**（同小节自己否证它）

**① 文件**：`paper/journal/main.tex`（§3.13 三级可分辨性表下第一句）

**② 逐字引文**：

> `第 ① 级含两类种子：读出种子（固定主干，尾部 $\pm1.85\sim3.40$）与主干种子（固定读出，尾部 $\pm0.25\sim2.61$，见第 \ref{sec:limit} 节的局限 (2)）。`

**③ 为什么是问题**——**三条独立证据都指向 ±1.85 是 clean 的标准差，不是尾部的**：

| 证据 | 原文 / 复算 | 结论 |
|---|---|---|
| **同小节上文自己说的** | `同协议实测：浅层鲁棒训练尾部 $\pm0.39$、架构变体 $\pm0.95\sim1.58$、深层读出 $\pm3.18\sim3.40$；而同组的干净精度只有 $\pm0.07\sim1.85$。` | **1.85 被明确写成"干净精度"的上界**；尾部的深层读数是 3.18 与 3.40 |
| **论文自己的 `tab:nat`** | `本文读出适配（24ep，3 seed） … \textbf{43.31}$\pm$0.16` | **读出种子的尾部标准差实测有 0.16**，比注里说的下界 1.85 小一个数量级 |
| **台账 §16.1 的读出级表** | `\| ResNet-18 \| 1.8520 \| 3.3956 \| 3.4011 \|`（列为 clean / α=+0.3 / drop） | **1.85 落在 clean 那一列**，α=+0.3 那列是 3.40 |

我另外**逐位复算**了那条 ±0.16（三个文件 `outputs_cifar100/v3_readout_repair/` 的
`readout_repair_c100_clean_ep24_alpha+0.30_n20000.csv` / `…_plain_s43_ep24_…_s43.csv` / `…_plain_s44_ep24_…_s44.csv`
取 `e_readout_NAT` 行的 `0.3` 列）：**43.38 / 43.09 / 43.45 ⇒ 均值 43.3067、总体标准差 0.1559**，
与 `tab:nat` 印的 `43.31 ± 0.16` 逐位吻合。

**为什么这值得单列**：这张表是全文唯一一张教读者"多大的差值才算数"的尺子。
按现在的注，读者会认为**读出种子的尾部噪声下界是 1.85 pp**——那么论文里 `b` 臂的 ±0.26 / −0.84、
`tab:nat` 的 ±0.16、门控的 +0.68 等**全部变成不可裁决**，与正文的处理相反。
**同一张表的表行也受影响**：表行写 `尾部 $\pm0.25\sim3.40$`，而下界 0.25 是**主干种子**（VGG-11）的值，
实际的读出种子下界是 0.16。

**④ 建议怎么改**（三选一，我推荐第 1 个）：

1. 注改为：「第 ① 级含两类种子：读出种子（固定主干，尾部 **±0.16~3.40**——0.16 来自 SimpleCNN、3.40 来自 ResNet-18）
   与主干种子（固定读出，尾部 ±0.25~2.61，见局限 (2)）」，并把表行的下界由 0.25 改成 **0.16**。
2. 或**照正文已有的写法**，注里只写深层：`读出种子（固定主干，深层尾部 ±3.18~3.40）`，避免与 §3.13 上文重复打架。
3. 若坚持要给一个"层级下界"，请把口径写成**"本表覆盖的测量集合"**并列出成员，否则读者无法复原这个 1.85 是怎么来的。

**把握度：确定**（1.85 属 clean 列、尾部实测最小 0.16，两项都可逐位复算）。

---

#### H-2 🔴 「方差估高约 6 倍」——把**标准差之比**写成了**方差之比**
**（19:55 更正计数：数值陈述里的误用是 7 处 —— journal 5 + tech 2；另有 5 处名词性用法
（「方差标定」「方差对比」「重训方差」）建议一并统一措辞。我初报时写"5 处"，是漏了 tech 的制度补丁与
journal 的「26 倍」那句，见下方清单。）**

**① 文件**：`paper/journal/main.tex`、`paper/technical-report/main.tex`

**② 逐字引文**（5 处）：

journal：
> 小节标题 `\subsection{这些效应有多大：用错协议会把方差估高六倍}`
> `\textbf{第一，尾部指标的种子方差远大于干净精度。}`
> `比另两个高 2.6$\sim$4.1 个百分点）。三个容量的干净方差跨度达 26 倍。`
> `\textbf{第二，混用协议会把方差估高约 6 倍。}一组 3 seed 的鲁棒训练尾部读数，若三个种子来自两个脚本，标准差是 $\pm2.561$；补跑同脚本的第三个种子后，同协议标准差只有 $\pm0.392$。`
> `& \textbf{否证}。三个种子来自两个脚本，同协议下标准差只有 $\pm0.39$；混协议把方差估高约 6 倍 \\[2pt]`（`tab:mystery` 第 1 行）

technical-report：
> `而混协议组是 $\pm2.561$——\textbf{方差被估高约 6 倍}。`
> `& 否证：三种子来自两脚本；同协议标准差只有 $\pm0.39$，混协议把方差估高约 6 倍 \\[2pt]`

**③ 为什么是问题**：所给的比值是 **2.561 / 0.392 = 6.53 ⇒ 标准差之比**；
若按**方差**比，是 **6.53² ≈ 42.6 倍**。
"26 倍"同理：`±0.07~1.85` 的比值是 **26.4（标准差）**，方差比是 **≈699**。
两处措辞都与它**自己给的那两个数对不上**——而同一句话里你们又正确地写了"标准差是 ±2.561"。
对投稿读者（尤其是审稿人），这属于**可当场算出来的数量级错误**：他若照字面理解"方差高 6 倍"，
会推出标准差只高 2.45 倍，从而**低估混协议的破坏力**。

**④ 建议怎么改**：把所有"方差"改成"**标准差**"（最小改动，5 处），
小节标题改为「这些效应有多大：用错协议会把**标准差**估高六倍」。
若想保留"方差"的说法，就必须把数改成 42.6 倍与 ≈699 倍——**不建议**，因为那会让读者以为效应大到不真实。

**把握度：确定**（算术确定）。**⚠ 注意：这条你们内部已判过**——
`docs/verify/techreport_fixes.md` R4：「`「方差被估高约 6 倍」：**不改（术语问题，项目一贯表述）**`」。
我把它保留在 HIGH，理由是**投稿读者读不到那条内部裁决**；若你们维持不改，建议在`报告/论文`里至少**统一成一种说法**（现在两种并存）。

---

#### H-3 🔴 同一条读出臂，技术报告说"**正在测量中**"，journal 说"**本文缺这一臂**"

**① 文件**：`paper/technical-report/main.tex`（局限 (10)）↔ `paper/journal/main.tex`（局限 (11)）

**② 逐字引文**：

| 稿 | 原文 |
|---|---|
| technical-report | `\textbf{(10) 探针是 oracle-$\alpha$ 读数，缺一条与部署同构的读出臂。}` … `一条用 $\alpha$ 混合数据训练的（与部署同构的）读出臂\textbf{正在测量中}；在它落定之前，\textbf{headroom 与部署可达之间的缺口有多大，无法回答}。` |
| journal | `\textbf{(11) 缺一条与部署同构的读出臂："不知道 $\alpha$"到底值多少。}` … `需要一条\textbf{同样用 $\alpha$ 混合数据训练、但按"不知道 $\alpha$"评估}的对照臂才能分开；` **`本文缺这一臂`**`，因此开放问题第 3 条只能停在"缺口有多大"。\textbf{下一步}：补该臂（与已有的读出实验同构，只换训练分布）。` |

**③ 为什么是问题**：

1. **两稿对同一件事给出相反的状态**：一份说"在测"，一份说"缺 + 下一步补"。并读必产生疑问。
2. **按项目自己的一手记录，"正在测量中"与事实不符**：
   `docs/verify/C5_third_arm.md` 第 5 行原文——`**当前状态**：**第一步完成（判定：第三臂不存在）｜第二步判据与脚本已写死并通过静态审计，受内存红线阻塞未执行**`；
   `outputs_cifar100/v7_probe_audit/` 目录里也**没有**混合-α 臂的产物（只有 `ra_onprotocol_*` / `c1`–`c5` / `r0`）。
   ⇒ **没有任何东西正在被测量**。
3. 这条不是小疵：它出现在"局限"里，读者会据此**预期一篇后续结果**。

**④ 建议怎么改**：技术报告那半句改成与 journal 同口径的事实陈述，例如：
> 一条用 α 混合数据训练的（与部署同构的）读出臂**尚未产出**（脚本与预登记判据已就绪，受资源限制未执行）；
> 在它落定之前，headroom 与部署可达之间的缺口有多大无法回答。

**把握度：较有把握**（两稿原文与产物目录我都核了；
"较有把握"而非"确定"，是因为**我不知道你们是否刚决定要马上跑它**——若 10 分钟内要开跑，那这句话可以留）。

---

#### H-4 🔴 journal 附录 A 那张"关键数值汇总"表**在 PDF 里被裁掉右半**——"出处"列基本看不见

**① 文件**：`paper/journal/main.tex`（附录 A，`\begin{table}` at 第 1573 行区块）

**② 证据**（**这不是我推的，是编译日志 + 渲染页**）：

`main.log` 原文：
> `Overfull \hbox (390.8186pt too wide) in paragraph at lines 1573--1594`

390.8 pt ≈ **13.8 cm**——而 A4 正文宽度只有约 16.6 cm。我把编译产物渲染成图看过：
**该表第三列"出处"在页面右边界被整段裁掉**，可见的只剩 `v7_pro` / `v3_rea` / `task3` / `v3_met` /
`v5_ran` / `v7_etf_` / `recipe` / `extens` / `台账 §` 这样的前缀残片。

**③ 为什么是问题**：这张表的**唯一用途就是给出处**（表题即为"本文引用的主要数值与出处"），
而**出处列在成品 PDF 里读不到**——表等于失效。这不是"排版不好看"，是**信息在成品里丢失**。
它同时是"**你们自己没发现**"的一类：`project_health_check.md` §0 记的是
`journal | 15 | … | Overfull hbox | **1（轻微）**`——那是 15 页版本的读数；
**现行 29 页版本的 journal 有 8 处 overfull，其中这一处 390.8 pt 属"严重"，不是"轻微"**。

**④ 建议怎么改**（任选）：
1. 第三列改用 `\texttt` + 允许断行（`\seqsplit`，或把长路径放进 `\url{}`/`\path{}` 并允许 break）；
2. 或把该表改为**两行式**（数值/取值一行，出处单独一行）；
3. 或把该表移到**附录横向页**（`\begin{landscape}`，需 `lscape`/`pdflscape`）；
4. 最省事：**出处列只留目录名**（如 `v7_probe_audit/`），完整文件名放正文已有位置。

**把握度：确定**（日志数值 + 渲染图双重证据）。

**顺带（同一次编译的全体 overfull，供你们排优先级）**：

| 稿 | 处数 | 最大 | 位置 |
|---|---|---|---|
| journal | **8** | **390.8 pt**（附录 A 表） | 另有 97.4 pt（§3.4 `b` 臂表注的路径行）、82.4 pt（§3.3 陷阱表）、37.5/35.2/17.6/16.2/1.0 pt |
| technical-report | **6** | 99.1 pt（§7.3(b) 陷阱表）、90.2 pt（局限 (8) 的 `\texttt{}` 路径） | 其余 ≤26.8 pt |
| competition | **1** | 41.3 pt（§优化二 第 510--519 行） | — |

**这些都不报编译错误**，`check_papers.py` 也不会因此退出非 0 ⇒ **只能靠看 PDF 或数日志发现**。

---

### MEDIUM

---

#### M-1 journal 摘要把"**不可裁决的一臂**"写成"**没有一个能超过**"

**① 文件**：`paper/journal/main.tex`（摘要第 62 行；正文 §3.3 第 495 行）

**② 逐字引文**：

| 位置 | 原文 |
|---|---|
| 摘要 | `用三种非线性读出（k-NN 与两种 MLP）替换线性头，\textbf{没有一个能超过它}——容量不是这里的瓶颈。` |
| 正文 | `而两层 MLP 只差 $2.85$，\textbf{落在门槛之内}，按本文自订规则这一臂\textbf{不可裁决}——只能说方向上仍劣于线性头。` |

**③ 为什么是问题**：正文花了整段篇幅、按自订的 Δ_proc=3.45 pp 门槛把三种非线性读出**分成两档**：
k-NN 与瓶颈 MLP **远超门槛**（可裁决），两层 MLP **落在门槛内**（不可裁决）。
摘要把两档合并成一句断言，**恰好丢掉了你们最想强调的"可分辨性门槛"这一方法学产出**，
也让摘要与正文在"证据强度"上不一致（这正是本项目反复自查的那类缺陷）。
注：**数值上"没有一个超过"是成立的**（−2.85 < 0），问题在**措辞的强度**：它把一个"分辨不了"读成了"确实更差"。

**④ 建议怎么改**：摘要改为
> 用三种非线性读出（k-NN 与两种 MLP）替换线性头，**其中两种远超本文自订的可分辨门槛、另一种落在门槛之内**——**容量不是这里的瓶颈**。

**把握度：确定**（两处原文均在，语义差异可逐字比对）。

---

#### M-2 同一叠加实验，两稿的头条值不同（49.18 vs 49.12），且摘要的"六到七成"取自技术报告口径

**① 文件**：`paper/journal/main.tex`（§3.9）↔ `paper/technical-report/main.tex`（§"两个杠杆的叠加与在线估计"）；`paper/journal/main.tex` 摘要

**② 逐字引文**：

| 稿 | 原文 |
|---|---|
| journal | `把两者叠（5×MaxPool 主干 $+$ readout-NAT）后，$\alpha=+0.3$ 达 \textbf{49.18}（三个读出种子：49.12 / 48.82 / 49.59），比架构单独（42.47）高 6.71、比读出单独（43.31）高 5.87。` |
| journal 摘要 | `两个杠杆在尾部可以同时受益，但\textbf{不独立相加}（合起来拿到单独增益之和的六到七成）。` |
| journal 正文 | `单独之和的预期是 $+29.60$，实际只拿到 $+21.09$——\textbf{次可加}，合起来拿到约七成（$21.09/29.60$）、损失约三成。` |
| technical-report | `\texttt{mp 主干 $+$ 读出适配} 的 $\alpha=+0.3$ 达 \textbf{49.12}（相对基线 28.09 即 $+21.03$），而两个手段单独的增益之和是 $+29.67$（架构 $+14.38$ 与读出 $+15.29$ 分别取自同批\textbf{单 seed} 产物的 42.47 与 43.38）——\textbf{合起来只拿到六到七成，损失三到四成}。` |

**③ 为什么是问题**：

- 两稿**各自都声明了口径**（journal：3 seed 均值；tech：单 seed），所以**不是算错**——
  但**头条数字不同（49.18 / 49.12）+ 百分比不同（71.3% / 70.9%）**，并读会以为有一份错。
  你们内部已把这一类（`+15.29` vs `+15.22`）判为"不同口径的两个量"并豁免，我同意；
  但**摘要里那个"六到七成"是技术报告口径的措辞**，
  而 journal 正文自己给的两个口径是 **71.3%（主）与 77%（另一口径）**——**77% 落在"六到七成"之外**。
  挂一个不覆盖自己另一口径的区间，是摘要级的不严谨。
- 另：technical-report 写「损失**三到四成**」，而 70.9% 对应的损失是 **29.1%（≈三成）**，不到"四成"。

**④ 建议怎么改**：
① 摘要把区间改成**"约七成"**或**"七成上下（两个口径 71% / 77%）"**，不要用"六到七成"；
② 技术报告的"损失三到四成"改成"**约三成**"；
③ 两稿都在那句话里**显式写出各自口径**（journal 已有"读出侧取 3 seed 均值"，建议把"架构一侧 42.47 是同一块主干的单种子"也点明，tech 已有）。

**把握度：确定**（4 处原文与算术均可核）。

---

#### M-3 competition 结论第 1 条把"**3.4 倍**"当普适常数——**同一份稿的正文写着 1.40 / 0.94**

**① 文件**：`paper/competition/main.tex`（结论第 1 条，第 674 行；对照 §2.1 第 168 行）

**② 逐字引文**：

| 位置 | 原文 |
|---|---|
| 结论第 1 条 | `\item 正向失真的破坏力是负向的 3.4 倍，误差从浅层向深层超加性累积；残差连接是误差放大器，ResNet-18 在 $\alpha=+0.3$ 下损失 75 个百分点。` |
| §2.1 正文 | `SimpleCNN \textbf{3.4}、VGG-11 \textbf{1.40}、ResNet-18 \textbf{0.94}——\textbf{方向性随深度衰减，在最深的主干上完全消失}。` |
| 摘要 | `（SimpleCNN 上为 3.4 倍，以 $|\alpha|=0.3$ 的跌幅计）`（**摘要这条有定语 ✓**） |

**③ 为什么是问题**：摘要与正文都给了定语/全部三个比值，**只有结论列表这一条是裸的**。
而"结论"是读者（含评审）最后记住的部分——**同一份文件内即可被自己的数据否证**。
你们内部对**同类错误**（journal 的"方向性跨架构稳定"）的定级是 **🔴 高**，并全部改掉了；
这一处是**最后一处未带限定的同类表述**。
（我注意到内部 `techreport_fixes.md:238` 决定"不动 competition:643 的『破坏力不对称（任务1 的 3.4 倍）』——
它已标注出处"；**但结论列表那条（674）没有出处标注**，与 643 不是同一处。）

**④ 建议怎么改**：结论第 1 条改为
> 正向失真的破坏力在浅层主干上强于负向（SimpleCNN 上为 3.4 倍），**但方向性随深度衰减、在最深的主干上消失（ResNet-18 上为 0.94）**；误差从浅层向深层超加性累积……

**把握度：确定**（同一文件内三处原文可比对）。

---

#### M-4 competition 拓展 2 的结论未带"架构依赖"限定，而这条限定是项目后来自己加的

**① 文件**：`paper/competition/main.tex`（第 396 行、结论第 4 条第 680 行）

**② 逐字引文**：

| 位置 | 原文 |
|---|---|
| competition §拓展2 | `\textbf{结论是负面的但重要}：非线性失真与高斯噪声的鲁棒性\textbf{不可跨域迁移}。针对高斯噪声设计的方法不能期待在非线性失真下有效。` |
| competition 结论第 4 条 | `\item 非线性失真与高斯噪声的鲁棒性\textbf{不可跨域迁移}，两者在 ResNet-18 上的排序完全反转。` |
| journal §3.10 | `⇒ 「两通路正交、迁移为零」这条结论\textbf{是架构依赖的}：在 SimpleCNN 与 ResNet-18 上成立（$\le$1.41），但在 \textbf{MaxPool 密集的 VGG-11 上被打破}（5.26）。` |
| technical-report | `\textbf{【第三阶段的细化：L3 两通路机制】} 上段"不可跨域迁移"在第三阶段被两步修订，此处补入归档` |

**③ 为什么是问题**：项目**自己**后来把这条"移动"改写成"**近正交**、且**架构依赖**"，
并在技术报告里用一段显式的"细化"补丁处理了同一句话；**只有竞赛版没有这个补丁**。
这属于**跨文档对同一断言给出不同强度**——而技术报告的处置（加补丁）恰恰说明项目认为这句话在成稿里是需要限定的。

**④ 建议怎么改**：竞赛版在该句后加一句（或在结论里加半句）：
> （后续阶段补充：这条"不可迁移"**是架构依赖的**——在 MaxPool 密集的 VGG-11 上迁移非零，$\max|\Delta_\sigma|=5.26$。）

若竞赛版是**按赛题范围冻结的前两阶段快照**，那就在**文件头**加一句时点声明
（技术报告的 `\date{}` 与 journal 的做法可参照），而不是让读者以为这是项目当前的全部认知。

**把握度：较有把握**（原句与 journal/tech 的对应句都在；我把"是否属刻意冻结"列为需你们裁决）。

---

#### M-5 journal 局限 (11) 对"**缺的那一臂**"的描述与**已有的那一臂**字面重合

**① 文件**：`paper/journal/main.tex`（局限 (11)）

**② 逐字引文**：

> `而与部署同构的读出（在 $\alpha\sim U[\pm0.3]$ 上训练、$\alpha$ 未知）与之相差 5.04 个百分点（$48.35$ 对 $43.31$…）。⇒ \textbf{这 5.04 里有多少是"不知道 $\alpha$"的代价、多少是读出训练不足}，需要一条\textbf{同样用 $\alpha$ 混合数据训练、但按"不知道 $\alpha$"评估}的对照臂才能分开；本文缺这一臂`

**③ 为什么是问题**：这段话说"**与部署同构的读出 = 在 α~U 上训练、α 未知 = 43.31**"，
紧接着又说"**缺的那一臂 = 同样用 α 混合数据训练、按'不知道 α'评估**"——**两句描述的是同一个东西**。
读者无法判断"缺的那一臂"与"已经有的那一臂"差在哪里，也就无法判断这个 limitation 到底在说什么。
按 `docs/verify/C5_third_arm.md` §1.2，第三臂与 `e_readout_NAT` 的差别在**四处协议**（特征来源/划分/…），
**这些差异一个字都没进论文**。

**④ 建议怎么改**：把那句改成**按差异描述**，例如：
> 需要一条**与探针同协议**（同特征提取、同划分）但**用 α 混合数据训练**的第三臂——
> 现有读出适配臂虽同为 α 混合训练，但它与探针在特征/划分上不同构，两者之差混入了协议差，因此拆不开。

**把握度：较有把握**（我读到的是语义重合；若你们本意另有所指，说明这句话没能传达出去——这本身也是问题）。

---

#### M-6 technical-report 的"CIM 已有先例"断言**没有引文**（同句在 journal 里是有引文的）

**① 文件**：`paper/technical-report/main.tex`（第 113 行；§1 定位说明）

**② 逐字引文**：

| 稿 | 原文 |
|---|---|
| technical-report | `在 CIM 领域，「冻结阵列 + 数字域补偿」已有先例，但形态是\textbf{标量或查找表级}校正；` |
| journal §1.3 | `在存内计算领域，「冻结阵列、只在数字域补偿」已有先例，但形态是\textbf{标量或查找表级}的校正：例如用器件特定的电导漂移查找表配合自适应量化，在\textbf{不做片上训练}的前提下达到软件基线精度~\cite{mtjcal}。` |

**③ 为什么是问题**：技术报告报告**声称"已有先例"却不给引文**，读者无法核。
而同句在 journal 里带着 `\cite{mtjcal}`（我已用 Crossref 核过该文献存在、卷期页作者与描述均相符）。
更值得注意的是：技术报告**自己的参考文献表里有 7 条从未被引用**
（`cim_survey` / `puma` / `quant` / `resnet` / `vgg` / `neat` / `dither`——检查器自报，见 §2 L-1），
所以**不是"没有可引的"**，而是这句话漏了引。
另外技术报告开篇自称 `所有数字均可追溯到一个具体文件或命令输出；无法追溯的陈述一律不写入`——
一条关于**他人工作**的断言同样应当可追溯，否则这句话只在"数字"上成立。

**④ 建议怎么改**：把那半句与 journal 对齐（补 `\cite{mtjcal}` 与"查找表 + 自适应量化"的说明）；
顺手处理 7 条孤儿 bibitem（要么在正文里引上，要么删掉）。

**把握度：确定**（两句原文与检查器输出都在）。⚠ 孤儿 bibitem 本身**已被你们内部记录**（`docs/verify/refs_other_docs.md`：技术报告 7 条、竞赛版 3 条），我不重复报；**我报的是"这半句断言无引文"这一层**。

---

#### M-7 journal 有 **13 处无编号表格**（用 `\begin{center}\begin{tabular}` 而非 `table` 环境）

**① 文件**：`paper/journal/main.tex`（技术报告 10 处、competition 3 处）

**② 证据**：`\begin{center}` 计数 journal **13** / tech **10** / comp **3**；
而 `\begin{table}` 只有 journal 6 / tech 8 / comp 6。受影响的包括：

- §3.1 四个指标的定义表；
- §3.3 的**容量阶梯**（六种读出）与 **fragility 陷阱**表；
- §3.4 的 **b 臂自我校验**表与 **CIFAR-10 恢复率标度律**表；
- §3.8 的**岭回归 R²** 说明；
- §3.9 的 **φ 相关**表、**软门控三主干种子**表、**六种漂移分布**表、**部署三档判定**表。

**③ 为什么是问题**：

1. **没有表号 ⇒ 无法被正文交叉引用、无法在版面中浮动**（会就地断开正文）；
2. 正文却在用**"表里"、"下表"**这类指代去引它们，例如：
   `\textbf{表里有两个"线性读出"，与本文 headroom 用的探针不是同一个数，这里对齐一下}`——
   而全文有 6 张**编号表**，读者不知道"表里"指哪个；
3. 对期刊投稿，**表格一律编号**是通行做法（评审意见里"表 1/表 2 的 caption 请补齐"是常见的格式退修项）。
   你们已经有 `caption`+`label` 的成熟写法（`tab:arch` / `tab:nat` / `tab:pool` / `tab:mystery` / `tab:dither-c100`），
   这 13 处只是没走同一条路。

**④ 建议怎么改**：把承载**独立信息**的表（容量阶梯、b 臂对照、C10 标度律、漂移分布、φ 相关）改为
`table` 环境 + `\caption` + `\label`，并在正文用 `\cref{}` 引用；
纯"指标定义""算式说明"这类可以在行内保留。

**把握度：确定**（计数可核）。**⚠ 这是形式项，不涉及数字**——按简报"格式合规不报"的豁免，
我理解那指的是"目标期刊的尺寸/字号/引用格式"，**表号与交叉引用属全文一致性**，故仍列出。

---

### LOW

> 这一档多为**形式/语汇**问题，且多数**已在你们内部报告里**（我标明出处以免重复劳动）。
> 我给的是"外部读者视角"的确认或补充。

| # | 文件 | 逐字引文 | 为什么是问题 | 建议 | 把握度 / 内部是否已知 |
|---|---|---|---|---|---|
| **L-1** | journal；competition | 检查器自报：journal `label 定义了但没被引用: tab:pool`、`app:dither-c100`；competition `fig:c03`、`fig:c06`、`eq:distortion` | §3.7 那张"池化保护三候选解释"表**全文从未被 `\ref`**（正文只用冒号引出）；competition 的消融图 `fig:c03` 与 dithering 图 `fig:c06` 也从未被引用（正文用"`\cref{tab:ablation} 给出三项结论`"代替） | 各补一个 `\cref{}`；`app:dither-c100` 若要留就在附录开头引一次 | **确定**。内部报了 competition 的 bibitem 孤儿，**未见表/图孤儿这一层** |
| **L-2** | journal | `\textbf{前九节}讨论的都是同一类扰动` | 该小节是 Results 的第 **12** 个 subsection，其前 11 节里 **10** 节讲该类扰动、1 节（量化/dithering）讲另一类 ⇒ 应为"前十节" | 改成"前十节"或改写成"以上各节" | **确定**。**已知**（`project_health_check.md` §6.1 第 1 条，仍未改） |
| **L-3** | journal | `本文的方差标定给出\textbf{两个数值}，其中一个足以改变结论。` | 紧接着是**三**条（第一/第二/第三） | "两个"→"三个" | **确定**。**已知**（同上，第 2 条） |
| **L-4** | journal | `上文早期版本写作"正侧失真下 Fisher 反而升高"，丢了比较对象，是可被本文表格否证的错误表述，已更正。` | **修订痕迹进投稿正文**：读者不知道"上文早期版本"指什么（是投稿历史？内部日志？） | 删掉"上文早期版本"这半句，只留正确的表述与限定 | **确定**。**已知**（同上，第 3 条） |
| **L-5** | journal | `详见第 \ref{sec:probe} 节与\textbf{第七阶段}的审计记录。` | "第七阶段"是**项目内部阶段名**，journal 里没有定义 | 改成"与内部的探针审计记录"或直接删 | **确定**。**已知**（同上，第 4 条） |
| **L-6** | journal | 摘要：`$\alpha=+0.3$ 时可达 48.35\%，而模型自带分类头只有 28.09\%，headroom 为 20.26 个百分点` | 摘要首个关键读数**未标主干与数据集**（实为 CIFAR-100 / SimpleCNN）；同段 `43.31/47.79/26.82` 也没点名是哪三个主干。**这与本项目反复强调的"定语纪律"不一致** | 摘要里补"（CIFAR-100/SimpleCNN）" | **确定**。**已知**（同上 §6.2 第 2 条） |
| **L-7** | technical-report | bibitem：`\bibitem{tent} Wang D, … [R]. 2020. arXiv:2006.10726.`；`\bibitem{shot} … [R]. 2020.` | 与 journal 的同一文献著录**不同**（journal：`[C]//International Conference on Learning Representations. 2021.` / `[C]//International Conference on Machine Learning. 2020: 6028--6039.`）。两者**各自都不算错**（tech 引的是 arXiv 版），但**同一项目两份稿对同一文献给出不同著录**；我已核 arXiv 2006.10726 发布于 2020-06、ICLR 2021，两说都成立 | 若两份稿会同时被看到，统一为会议版 | **确定**。**未知**（我未见内部记录） |
| **L-8** | competition | `\textbf{局限}：三项拓展的数据集多样性（评审建议 R4）在时间约束下未执行；` 与 `② 把数据集多样性补上，检验上述规律在细粒度任务上是否保持` | 项目**其后已在 CIFAR-100 上做了大量工作**（journal/tech 两稿的主体就是 C100）。本稿未注明成稿时点，读者会以为"细粒度任务"仍是空缺 | 加一句时点/范围声明（"本稿覆盖第一、二阶段（CIFAR-10）；细粒度任务的检验见同期报告"） | **存疑**（可能是刻意的赛题范围冻结） |
| **L-9** | `docs/DERIVED_VALUES.md` | `本文件自 2026-09-24 的论文大修（A/B/C/D/E 五组共数十处改动）之后\textbf{尚未重跑全文数字溯源扫描}` | 该文件 §一 举的三个待重扫例子中，**`缺口 8.51` 已不在现行 journal 里**（我 grep 零命中），而 `49.18` / `40.29` 仍在 ⇒ **登记表滞后于正文**，按它自己的规则 3，这是未闭环项 | 重跑一次溯源扫描并更新"2 项/3 项"的计数 | **确定**。**已知**（该文件自己声明） |

---

### 论证层（**只列不答**，属 REWRITE 范围）

> 按 `paper-form-writing` 模式 6 的协议，这一层我只描述现象、不给论证建议。

1. **journal §3.3 借引 `fragility` 的"适用条件"，但其分类与该场景不完全对应。**
   原文：`\textbf{这一陷阱并非本文首见}：\cite{fragility} 自己就把它列为该指标的适用条件（跨层比较须用 RMS 归一化的临界噪声，层内比较才用原始值），本文在本场景下独立复现了它`。
   我核了原文：该文建议确实是「RMS-normalized critical noise for **cross-layer** claims and raw critical noise for **within-layer** comparisons」，**引文与转述逐字相符** ✓。
   但**该文说的轴是"层深"**，而本文的轴是"**同一个探针在不同 α 下**"——按该文自己的分类，后者属 **within-layer**（原始值应有效）。
   ⇒ 现象：本文的观测**其实是把该规则扩展到了新轴**，而表述读起来像"复现了它列出的条件"。
   （属 claim-证据的措辞匹配问题，建议转 REWRITE 处理。）
2. **journal §1.3/§3.10 与 competition 之间的关系**：journal 保留 `它是\textbf{投片后}唯一可行的一类修复` 的唯一性断言，
   技术报告完全不作该断言（内部 `cross_doc_consistency.md` 乙.2 已记为"强弱不同，非矛盾"）。现象已记录，**是否统一由你们裁**。
3. **journal 摘要的密度**：一段摘要里含**9 处括号口径说明**（内部 §6.2 已记）。现象：摘要承担了"全文口径表"的功能。
   **建议转 REWRITE**（摘要层级的取舍不属形式检查）。

---

## 3. 存疑 / 没能验证的

1. **`fig:c03` / `fig:c06` 是"漏引"还是"刻意不引"**——competition 的两张图从未被正文引用。我判为漏引（因为同稿其余 6 图都引了），但不能排除刻意。
2. **技术报告的 14 处未引用 label**（`fig:c01/c04/c06/c07`、`fig:crosspath`、`fig:direction`、`fig:dithering`、`fig:gating`、`fig:maxpool`、`fig:rank`、`fig:rankrec`、`fig:readoutnat`、`fig:training`、`tab:deploy/entry/mystery/nat/parking/phase12/pool`、`sec:setup`、`sec:v7`）——
   对**归档文档**而言可能是有意（读者顺序阅读），我**不判为问题**，只报告事实。若按期刊标准就要全改。
3. **"±0.16 属不属于第 ① 级"** 有解释空间：若你们把第 ① 级**限定为深层**，那么 H-1 的注应改成"深层读出 ±3.18~3.40"，
   **但"读出种子…±1.85~3.40"仍然不成立**（1.85 是 clean）。**无论哪种解释，现状都是错的**，只是改法不同。
4. **M-3 / M-4 是否受"竞赛版按赛题范围冻结"豁免**——我不知道竞赛版的提交时点与是否还会更新。
5. **图的图像内容未核**（只核了图注、引用、文件存在性）——图里数字对不对、图例清不清楚，我没有渲染核对。
6. **`ucas-cod-lab-report/` 未纳入**（按简报的三份范围）。
7. **`\cref`/`\ref` 的渲染结果未目视核对**——我只核了"0 undefined reference"与源码级对应关系，
   没有打开 PDF 看"图 3"是不是真的指对了那张图。**如果要做，这是我能想到的、本次唯一还没做的机械项。**
8. **M4 的 4 个新读出产物我没做配对复算**——台账 §16.1 的闸门核验表（4/4 通过、1 张样本的偏移）我**读了并对得上**，
   但**没有自己重新跑一遍配对检查**（需要重跑读出特征提取，属 GPU 活，红线禁止）。

---

## 4. 与项目内部自查报告的差异

> 这一节按简报要求，"读完论文之后再看那两份"。我读的是
> `paper/coverage-audit.md`、`paper/health-check-report.md`、`docs/verify/project_health_check.md`、
> `cross_doc_consistency.md`、`A_cluster_independent.md`、`C_group_limits.md`、`C5_third_arm.md`、
> `techreport_fixes.md`、`refs_other_docs.md`。
> **总体评价：你们的自查密度罕见地高**——40+ 共享量逐条取上下文比对、每条给原文字符串、
> 明确写"未能核查的项"，还把"提出来又被自己数据否掉"的记录保留下来。我下面只报**差异**。

### 4.1 我报了、你们**没有**的（我认为是新增的）

| 我的编号 | 内容 | 我的把握 |
|---|---|---|
| **H-1** | 三级可分辨性注把 clean 标准差 ±1.85 当成尾部下界；表行下界 0.25 与你们自己的 `tab:nat` ±0.16 不符 | 确定。你们内部**核过 1.85 是 clean**（`project_health_check.md` §4.4 原文：`ResNet-18 三个读出种子的 clean 均值 64.9967 ≈ 65.00、总体 sd = 1.852 ≈ ±1.85`），**但没有把它与 §3.13 那句注对撞** |
| **H-4** | journal 附录 A 表 390.8 pt 超宽，**成品 PDF 里"出处"列被裁掉** | 确定（日志 + 渲染图）。你们 `project_health_check.md` §0 记的 `journal … Overfull hbox 1（轻微）` 是 **15 页版本**的读数，**未随版本更新**；我渲染了现行 29 页版本的第 27 页确认 |
| **H-3** | 技术报告"正在测量中" vs journal"本文缺这一臂" | 较有把握。你们有 `C_group_limits.md` C5 的"建议只写正在测量"**以及** `C5_third_arm.md` 的"第三臂不存在、阻塞未执行"——**两份内部文件给出了相反的处置**，落到两稿上就成了矛盾 |
| **M-1** | 摘要"没有一个能超过它" vs 正文"不可裁决" | 确定 |
| **M-2** | 49.18 vs 49.12、摘要"六到七成"不覆盖正文的 77% 口径 | 确定。你们**豁免了** `+15.29/+15.22`（我同意），但摘要的区间问题在这一层没被提到 |
| **M-3** | competition **结论**第 1 条裸写 3.4 倍 | 确定。你们处理了 journal 的同类问题、也提到了 competition 的另一处（643），**漏了结论列表那条（674）** |
| **M-6** | 技术报告"CIM 已有先例"半句无引文 | 确定。孤儿 bibitem 你们记了，**"该句该有引文"这一层没记** |
| **M-7** | 13 处无编号表（journal） | 确定 |
| **L-1** | `tab:pool` 孤儿表、competition 的 `fig:c03/fig:c06` 孤儿图 | 确定 |
| **L-7** | tent/shot 两稿著录不一致 | 确定 |

### 4.2 我**复核后确认已修**的你们历史发现（供你们关闭这些条目，不必再查）

| 你们的历史发现 | 我 18:25 实测 |
|---|---|
| `tab:nat` 第三行 SimpleCNN 26.57 → 29.89（journal 已改、tech 未同步） | ✅ **两稿都已是 29.89**，且 tech 表注已同步（`同协议 3 seed 的 v3 路径逐层独立 NAT 均值是 26.57，低于 29.89，故本行取 29.89`） |
| journal"方向性跨架构稳定"被 0.94 否证 | ✅ **已改**：小节标题现为 `正向失真更致命，但方向随深度衰减——在最深的主干上反号`；正文补了 `连\textbf{方向}本身也不是跨架构常数——CIFAR-10 的三个主干依次为 $3.4$ / $1.40$ / \textbf{$0.94$}，在最深的主干上反号` |
| journal 三处干净代价 `3.19~6.67`（8ep 旧口径） | ✅ **已改为 3.08~7.69**（我 grep `3.19`/`6.67` 在 journal 零命中） |
| 技术报告 SHOT 描述与原文相反 | ✅ **已改**为 `\textbf{SHOT} 冻结源模型的分类器、只学目标域的特征提取器`（我另行用 arXiv 2002.08546 核过原文一致） |
| tech 摘要 `+14.38` vs journal `+15.52` | ✅ **tech 摘要现为 `（同协议同 seed 配对 $+15.52$ 个百分点）`** |
| tech 残留"没有一条超过基准" | ✅ **已删**（现为 `预算分配的 $+0.10$ 与基准同级。四条都没有改善尾部。`） |
| 成本比 1/16 vs 1/30 | ✅ **tech 现为 `约 1/5$\sim$1/16`** |
| 附录表缺 19.75/10.87 | ✅ **已在 `tab:dither-c100` 的 (a) 精度块**（`4 bit & --- & 43.27 & 19.75` / `3 bit & --- & 42.97 & 10.87`） |
| 技术报告 NC 那条"过强断言" | ✅ **已改**为 `神经坍缩（NC）几何 & \textbf{未能构成稳健排除}` 并带 centered/uncentered 两版说明 |
| M8b / §16.2a / §16.7 / §16.8 的覆盖缺口 | ✅ **M8b 在 journal §3.3**（以 `等角紧框架` / `最近类均值` 的写法，不是缩写 ETF——**grep `ETF` 会长 0 命中，别用这个词判覆盖**）；**§16.2a 在 journal §3.6**（`\Delta\log S_w` 那段）；**§16.7 在 journal §3.9 与技术报告**；**§16.8 在技术报告 §7.3(d)** |
| `results_master.csv` 的 152 行 / 58 PASS / 94 n/a | ✅ **逐字复现**：152 行、PASS 58、读出适配 n/a 74、复用权重扫描 n/a 20 —— journal §2.1 与 tech §2.2 的数字**都对** |
| 检查器 EXIT=0 / 29-24-13 页 | ✅ **独立复现**（我的路径，见 §0） |

### 4.3 我**没报**、因为你们已经报过或已裁决的

- **M9b（神经坍缩）不进 journal**——你们已在 `project_health_check.md` §B② 给出三个方案并倾向 (a) 不补。
  我**同意 (a)**：journal 本来就没有"我们排除了 NC"这个断言，补进去反而引入一条你们自己已降级的不稳测量。**我不重复报。**
- **`0.39062` vs `0.39061`**（journal §3.6 自带口径说明）——你们已判 REFUTED（是两个产物），我复核后同意。
- **`+15.29` vs `+15.22`**——不同口径的两个量，我复核 43.38−28.09=15.29 ✓、43.31−28.09=15.22 ✓，**同意豁免**。
- **`paper/health-check-report.md` §4 的"未引用 0"**——已知未落地（`refs_other_docs.md` 已建议改）。我确认该结论**对 journal 成立、对另两份不成立**。
- **术语同义词**（原始头/自带头/冻结头/干净头）——journal §2.4 有显式映射声明，我**同意豁免**。
- **`tools/check_papers.py` 本身的可靠性**——我读了源码：`strip_comment` 按字符对跳过 `\%`，**没有**你们担心的那个注释起点 bug；它比你们某次临时脚本更可靠。（这与你们 `V7_COORDINATION.md` 的结论一致。）

---

## 5. 第二轮：三份稿 **全部 66 页 PDF 目视检查**（2026-09-25 18:45–19:20，用户批准）

> 触发：总管指出第一轮 H-4 是"随手渲染 4 页撞上的"，建议全量通读。
>
> **方法与第一轮不同——这一轮不看 `.tex`，只看成品**：
> ① `pdftoppm -r 100` 渲染**全部 66 页**（journal 29 + tech 24 + comp 13）逐页目视；
> ② 可疑处 `-r 200` 局部放大复核；
> ③ **`pdftotext` 做端到端字符比对**（源里有几个 vs PDF 里剩几个）——
> **⚠ 这条当时没写清用的是哪个 `pdftotext`，见下方「⑤·更正」**；
> ④ 读 `main.log` 的 `Missing character`（编译日志里一直有，只是没人看）；
> ⑤ 在 `/tmp` 写**最小复现**验证修法（不碰仓库）。
>
> **⑤·更正（2026-09-25 19:40，总管质疑后自查，结论：原结论成立，但方法学表述有缺陷）**
>
> 总管报「`pdftotext` 提不出这三份 PDF 的中文（`grep -c "结论"` → 0）」，据此提醒我重新评估依赖它的结论。
> 我做了对照实验，**根因不是 PDF，是机器上有两个 `pdftotext`**：
>
> | 二进制 | 默认行为 | 对同一个 PDF 的提取 |
> |---|---|---|
> | **`/mingw64/bin/pdftotext`**（**默认 PATH 命中它**） | **默认按 Latin1 输出**，**静默丢掉全部非 ASCII** | `结论` 0、`α` 0、`⇒` 0、`①` 0 ——**看起来像"全是 0"** |
> | `/d/texlive/2025/bin/windows/pdftotext`（**我二轮实际用的**） | UTF-8 输出 | `结论` 37、`α` 202、`逐` 45、`⇒` 1、`①` 0 |
>
> - 我二轮用的是 **TeX Live 那个**（证据：修前 PDF 里 `结论`=37、`α`=202 均非零——mingw64 那个在同样输入下恒为 0，**不可能是它**）。
>   所以 **R2-1/R2-2 那两个 0 不是工具盲区**，它们与该 PDF 里 `α` 能提出 27 次**在同一个进程里**——同一个能提 Unicode 的工具，提得到 α，提不到 ①。
> - **修后复核**（19:46 那一版——总管 19:44 改完导言后重编译的；**不是我审的那一版**，我审的是 18:39 那版）：
>   用同一条提取路径再提一次，`⇒` **1 → 26**、`①`–`⑥` **0 → 7/8/7/1/3/2（合计 28，与源文件逐一对上）**。
>   ⇒ **两只手互相印证**：0 是真的没有、26/28 是真的有了。
> - **我对总管那三处修复的独立复核**（不看它的结论，只读日志与 PDF）：三份稿 `Missing character` **均为 0**、
>   `Overfull > 68.2pt` 的**均为 0 处**、导言区三份都出现了那两行 ⇒ **R2-1 / R2-2 / R2-3 三条闭环**。
> - **一个天然的对照**：修前那唯一被提出来的 `⇒`（1 个）出自源码里的 `$\Rightarrow$`（数学模式），
>   而 24 个字面 `⇒` 全部丢失。同一个符号、两种写法、两种结果——**这条比任何推断都硬**。
> - **给后来者的操作要点**：本机用默认 `pdftotext` 时必须加 **`-enc UTF-8`**（实测：`-enc UTF-8` 后 mingw64 版提到 `结论`=37、`①`=7，与 TeX Live 版一致）。
>   不加就是"只看得见 ASCII"，而它**不报错**——与本项目已有的「控制台编码」坑同源。
> - **我的责任**：原报告只写了"用 pdftotext"，**没写用的是哪一个**，导致别人复现会拿到不同的结果。这是我的表述缺陷，已按本节更正。
>   （顺带：这也正是本项目那条**「报工具抓到 X 之前先验工具本身」**——这次该被验的是我自己的工具调用。）
>
> **⑥ 再补一层：`grep -o … | wc -l` 在这种管道里会欠计数**（19:50 发现，**两次复现**）。
> 同一份 PDF、同一个模式（模式写进文件、绕开命令行传参），`/mingw64/bin/grep` 给出：
>
> | 计法 | `结论` | `⇒` |
> |---|---|---|
> | `grep -c -f pat` （**行**数） | **36** | **26** |
> | `grep -o -f pat \| wc -l` （出现**次数**） | **7** ❌ | **1** ❌ |
> | Python `str.count` / `re.findall`（码点，**次数**） | **37** | **26** |
>
> - `grep -c`（行）与 Python 的行级计数**完全一致**（36 / 26）；Python 的次数计数是 37 / 26（37 次落在 36 行里，合理）。
>   ⇒ **`grep -o` 的 7 与 1 是错的**，且不是命令行编码问题（模式从文件来，两次复现；同一模式下 `-c` 正常）。
> - **机制（19:55 追到底了，答案与双方各自的推测都不同）**：把两个二进制 `-enc UTF-8` 的输出**落盘做字节层比对**：
>
>   | | 字节数 | 行数（CRLF） | **NUL 字节** |
>   |---|---|---|---|
>   | `/mingw64` 版 | 97 906 | 1 292 | **0** |
>   | `/d/texlive` 版 | 100 228 | 2 372 | **1 019** |
>
>   ⇒ **两个输出并不"逐位一致"**：行数差近一倍（同样的文字，断行位置不同），
>   且 **TeX Live 那份含 1019 个 NUL**。**含 NUL 的输入会被 `grep` 当二进制处理**，
>   这是 `-c` 与 `-o` 行为分岔的合理机制。
>   （NUL 出现在**成串的 ASCII 残片**里，例如 `…\x0c结果\x006\x00L\x00P\x00S\x00O\x00H\x00&\x001\x001…`，
>   **不是**落在中文位置上——同一份输出里 `结论` 照样能提 37 次。**其来源我没查明**，
>   按本项目规矩记为已知未知；但可以确认：**它不是"PDF 的中文提不出来"**。）
> - ⚠ **另一个必须承认的事实**：同一条 `grep -o` 命令，我在 bash 里跑给 **7**、在 Python 里跑给 **0**。
>   ⇒ **它的行为不可稳定复现** —— 所以本条的结论不是"`grep -o` 少报多少"，
>   而是**"在这个环境里，不要用 shell 管道数 CJK 次数"**。
> - **对本报告的影响：无。** R2-1 / R2-2 的**次数**数字来自 Python（码点），与我后补的行级计数一致；
>   那两个 `0` 另有 `Missing character` 逐处点名与 200 dpi 目视两路佐证。
>   （我第一轮比对时 PDF 一侧确实用过 `grep -o`；查下来它给出的 `⇒ 1` / `① 0` 与 Python 相同，**没有改变结论**，
>   但当时如果它少报了，我会得到一个假阴性——**这条要记下来**。）
> - **操作要点（给后来者）**：在本机数中文/CJK 的**次数**，用 `grep -c`（行）或 Python `str.count`，
>   **不要用 `grep -o … | wc -l`**。
>
> **被检版本**（第一轮 10 处修复之后重编译的那一版）：

| 稿 | `.tex` sha256 | `.pdf` sha256 | pdf mtime |
|---|---|---|---|
| journal | `af94b00ba2e1f23f27253781ceb65acb0fb9fc982ca056001fa99a7f65bf72df` | `b238b12ca3e224129a49b59bd833ac6587a64c222cf1110eb4637ed887d185b0` | 18:39:46 |
| technical-report | `de458732f1db69dc0b060cfa7a476e7f55beeb949d110a34a411081662322a37` | `b1108d5e4cc54baf40174d59ba180ba87ce425a65c6034883908142e71659a87` | 18:39:52 |
| competition | `9898a0e6c04879d24229b854ac0cbcf4dbb3261f0389d727b6581cb33ac65d26` | `811d2156563685cefb5587d8f2e14df9fd4404dbccd10d320d0bb84b8cc30f9f` | 18:39:57 |

**这一轮只读**：渲染用 `pdftoppm`（读 PDF、写 `/tmp`），**没有重编译、没有改任何仓库文件**。

---

### R2-1 🔴 CRITICAL：**①②③④⑤⑥ 在成品里 100% 不渲染**——三份稿共 46 处，全部变成空白

**① 文件**：`paper/journal/main.tex`（**28 处**）、`paper/technical-report/main.tex`（**15 处**）、`paper/competition/main.tex`（**3 处**）

**② 证据（端到端字符比对，不是推测）**：

| 稿 | 源文件里有 | 成品 PDF 里剩 | `Missing character` 日志行 |
|---|---|---|---|
| journal | ①×7 ②×8 ③×7 ④×1 ⑤×3 ⑥×2 = **28** | **0** | `There is no ① (U+2460) in font [lmroman10-regular]` 等 |
| technical-report | ①×5 ②×7 ③×3 = **15** | **0** | 同上 |
| competition | ①×1 ②×1 ③×1 = **3** | **0** | 同上 |

**最小复现（我在 `/tmp` 里做的，与你们的类文件同构）**：一个 `ctexart` 里写 `原样：①②③④⑤⑥ ⇒ 结束`，
编译后 **`Missing character: 7`**，PDF 里只剩 `原样：������ � 结束`。**⇒ 一个字符都不剩，也没有吐司框**——
是"静默消失"，不是"显示成方块"。

**③ 为什么是问题**——它不是"少个装饰"，而是**把编号句和交叉引用打穿了**。逐处列（引文为你现在 `.tex` 里的原文，右侧是读者在 PDF 里看到的）：

| 源文件（逐字） | PDF 实际渲染 |
|---|---|
| journal §3.13：`\textbf{①ResNet-18 一列的第一行与第三行是同一块主干}` … `\textbf{②读出适配自身的增益只由第一行}` … `\textbf{③本表第二行的 $\pm$ 是"读出种子"口径}` | `由此有两点必须说明：ResNet-18 一列… 读出适配自身的增益… 本表第二行的 ± 是…`（**"三点"变成三个没有编号的句子，且"两点"与三条并存**） |
| journal §3.13 三级可分辨性表：`① 种子噪声` / `\textbf{② 过程自由度}` / `③ 跨实现` | 三行**没有编号** |
| journal §3.13 表下注：`第 ① 级含两类种子` / `第 ② 级是这样测的` | **`第  级含两类种子`** / **`第  级是这样测的`** ← **指向一个成品里不存在的标号** |
| journal §3.9 漂移表：`① $U[\pm0.3]$（训练分布，参照）`…`⑥ 单峰 $N(+0.3,0.05)$` | 六行**全部没有编号** |
| journal §3.9：`唯一不超过的是③仅负向`、`在⑤（尾部集中）处`、`⑥（单峰）处同样如此`、`⑤ 处软门控 47.70` | `唯一不超过的是 仅负向`、`在 （尾部集中）处`、`（单峰）处同样如此`、` 处软门控 47.70` ← **读者不知道在说哪一行** |
| journal §1.3：`三条差异：\textbf{①偏移的性质不同}…\textbf{②可动参数的性质不同}…\textbf{③给出的量不同}` | `三条差异：偏移的性质不同…可动参数…给出的量不同` |
| journal §1.3：`① 把它搬到一个新场景…② 在该场景下主干物理上不可重训…③ 给出该场景特有的上界…` | 三个分句没有编号 |
| journal §3.2：`这一点由三个对照夹住：\textbf{①}…\textbf{②}…\textbf{③}…` | 同上 |
| journal §4.3：`（第 \ref{sec:gating} 节的前提②）` | `（第 4.10 节的前提）` ← **"前提②"变成"前提"，指向两前提中的哪一个没了** |
| technical-report §1：`\textbf{本文与这三条的差异有三点}：① \textbf{偏移的性质不同}…② …③ …` | 同上，编号全丢 |
| technical-report §3：`\textbf{① 迁移层面——"零迁移"是架构依赖的。}` / `\textbf{② 训练层面——不是零迁移，而是单向强干扰。}` | 两个小标题丢了编号 |
| technical-report §3：`\textbf{门控有效需要两个前提同时成立}：① 鲁棒头干净代价足够大；② 两个头…不能崩。` | 编号全丢 |
| technical-report §7.3(b) 陷阱表：`判决从分支②变③` | **`判决从分支 变`** ← 半句话 |
| technical-report §5.3(a) 三级可分辨性表 + `第②级是这样测的` | 表行无编号 + `第 级是这样测的` |
| competition §9.3：`\textbf{后续方向}：① 层间相关或对抗式的 $\alpha$ 采样，…② 把数据集多样性补上，…③ 探索…` | `后续方向：层间相关或对抗式的 α 采样，…` |

**④ 怎么改（我已在 `/tmp` 里实测三种方案，**结论是方案 D**）**：

在**三份稿**的导言区加两处（`xeCJK` 已由 `ctexart` 载入，`\xeCJKDeclareCharClass` 直接可用）：

```latex
\usepackage{newunicodechar}
\xeCJKDeclareCharClass{CJK}{"2460 -> "24FF}   % ①②③… 交给中文字体（有这些字形）
\newunicodechar{⇒}{\ensuremath{\Rightarrow}}  % ⇒ 交给数学字体
```

实测结果（同一个最小文件的四种写法，看 `Missing character` 计数与 PDF 实际内容）：

| 方案 | 写法 | `Missing character` | PDF 里读到的 |
|---|---|---|---|
| A 原样 | 什么都不加 | **7** | `原样：������ � 结束` |
| B | `newunicodechar` + `\textcircled{1}`… | 0 | 能显示（但 `\textcircled` 是画圈，字重与中文字体不搭） |
| C | 只加 `\xeCJKDeclareCharClass{CJK}{"2460 -> "24FF, "21D2}` | **1** | 圈码 ✓、**⇒ 仍缺**（`⇒` 不在该中文字体的覆盖里） |
| **D** | **`\xeCJKDeclareCharClass{CJK}{"2460 -> "24FF}` + `\newunicodechar{⇒}{\ensuremath{\Rightarrow}}`** | **0** | **`方案 D：①②③④⑤⑥ ⇒ 结束`、加粗的 `②过程自由度` 也正常** |

**把握度：确定**（源/PDF 字符计数、日志、最小复现、修法实测，四重证据）。
**⚠ 补一条给你们的检查器**：`tools/check_papers.py` 现在完全不看 `Missing character`。
加一行 `log.count("Missing character")` 与编译错误数并列，就能把这一类**永久拦在闸门里**。

---

### R2-2 🔴 HIGH：**`⇒` 在成品里被丢掉**（journal 丢 24、tech 丢 8）

**① 文件**：`paper/journal/main.tex`（源 25 → PDF **1**）、`paper/technical-report/main.tex`（源 8 → PDF **0**）
（competition 源里没有 `⇒`，不受影响；`→`（U+2192）**不受影响**，我逐个查过，它渲染正常。）

**② 证据**：

| 稿 | 源文件 `⇒` 数 | 成品 PDF 里 | 日志 |
|---|---|---|---|
| journal | **25** | **1** | `There is no ⇒ (U+21D2) in font [lmroman10-regular]` ×24 + `[lmroman9-regular]` ×1 |
| technical-report | **8** | **0** | 同上 ×8 |

**③ 为什么是问题**：`⇒` 是这两份稿**最主要的逻辑连接词**——它标记"所以/于是"。
丢掉之后，读者看到的是断了半句的推导。渲染实例（PDF 里就是这样）：

- journal §3.3：`…而 CIFAR-100 的随机水平是 1.0%，最大偏离 0.15 个百分点。 探针管线没有泄漏。`
  （源文件是 `…最大偏离 0.15 个百分点。\textbf{⇒ 探针管线没有泄漏。}`——**"所以"没了**）
- journal §3.4：`b 在三个主干上全部零增益… 起作用的不是"换了一个头"，而是头是在哪种分布上训练的。`
- journal §5 局限 (8)：` 探针显示更远处缺口反而更大：…`
- 全文还有 20 余处同类位置。

**④ 怎么改**：与 R2-1 同一处导言两行（方案 D 的 `\newunicodechar{⇒}`）即可一并解决。
若不想加宏包，也可以把源码里的 `⇒` 全部换成 `$\Rightarrow$`（33 处，纯文本替换）——但**导言两行更稳**。

**把握度：确定**（字符计数 + 日志 + 渲染目视）。

---

### R2-3 🔴 HIGH：**技术报告有两处内容被裁到纸外**（journal 那处已修，tech 没修）

**① 文件**：`paper/technical-report/main.tex`（p18、p22）

**② 证据**：`main.log` 里 tech **仍有 2 处超过右边距 2.4 cm（≈68.2 pt）**：

```
Overfull \hbox (99.06235pt too wide) in paragraph at lines 946--956     ← p18
Overfull \hbox (90.20203pt too wide) in paragraph at lines 1172--1178   ← p22
```

我把两处都放大到 200 dpi 目视确认，**右侧确实被页边裁掉**：

| 页 | 位置 | 被裁的内容（PDF 里读到的就是这样） |
|---|---|---|
| **p18** | §5.3(b) 两条口径陷阱表第 3 列 | `α < 0 把深层激活抬到 1.87 倍，固定绝对噪声相对更"小"，失真组虚假显得更` ▍（**后面没了**）<br>`NC 的定义是"以全局均值为中心"的单纯形框架；不去均值则度量的是别的几` ▍ |
| **p22** | §8.1 局限 (8) 的长路径行 | `…（outputs_cifar100/extension5_dithering_vgg11/arch_dithering_summary.` ▍（**`.csv）` 在纸外**） |

**③ 为什么是问题**：与第一轮的 H-4 同类——**成品里信息丢失**，而且这次丢的是：
① 那两条口径陷阱的"为什么会"解释（正是你们最想让人记住的方法学内容）；
② 一条产物路径的尾部（读者按它去找文件会找不到）。
**你们已按 H-4 修了 journal（我复核：journal 现在 5 处 overfull，最大 37.5 pt，全部 < 68.2 ⇒ 不再裁）——
但同一轮里 tech 的这两处没跟着修。** 检查器不看这个，所以它不会提醒。

**④ 怎么改**：
- p18 表：与 journal 附录 A 同样的手法（第三列改 `p{}` 定宽 + 长路径改 `\path{}`／允许断行）；
  这张表只有两行，`\small` 之类**不要加**——总管在 journal 那边已经踩过一次"加 `\small` 反而把表撑高"。
- p22：那条 `\texttt{...}` 路径改用 `\path{}`（`url` 宏包，允许在 `/` 处断行），或把路径挪进脚注。

**把握度：确定**（日志数值 + 200 dpi 渲染目视，两处都看到了截断）。

---

### R2-4 🟢 LOW：几张图的**图内标题与图注重复**

- journal 图 7 图内写 `逐点 Pearson r = −0.960（三点，不足以定曲线形状）`，图注里同样有这句话；
  journal 图 9（`七个点，逐点 Pearson r = −0.533`）、tech 图 10/图 12 同型（同一套 `gen_figures.py`）。
- 这是**风格**不是错误（重复一次不算错），但如果要投稿，图内标题通常会去掉、只留图注。
- 反例（保留是对的）：comp 图 5 的图内标题 `Δ< −1 超加性（相互放大）；Δ> +1 次加性（相互抵消）` 是**图例说明**，不是重复。

---

### R2-5 ✅ 第一轮 10 处修复——我在**成品里**逐条复核，全部到位

| 项 | 在 PDF 里核实到的 |
|---|---|
| **H-4** | journal p27 附录 A 表 **出处列逐行完整可读**（`v7_probe_audit/ra_onprotocol_main.csv（全量 10k 口径；旧 8k 为 +20.12/+7.00）` 等全在），且已回到附录标题同一页 ✅ |
| **H-1** | journal p22 表行现为 `尾部 ±0.16 ~ 3.40`；注现为 `第 ① 级含两类种子：读出种子（固定主干，尾部 ±0.16 ~ 3.40——0.16 来自 SimpleCNN、3.40 来自 ResNet-18）与主干种子（固定读出，尾部 ±0.25 ~ 2.61…` ✅（**注：这里的 `第 ① 级` 在 PDF 里是 `第  级`——即 R2-1，两者是独立的两个问题**） |
| **H-3** | tech p22 局限 (10)：`一条用 α 混合数据训练的（与部署同构的）读出臂尚未产出（脚本与预登记判据已就绪，受资源限制未执行）` ✅ |
| **M-1** | journal 摘要：`其中两种远超本文自订的可分辨门槛、另一种落在门槛之内——容量不是这里的瓶颈` ✅ |
| **M-2** | journal 摘要现为 `合起来拿到单独增益之和的约七成`；正文 `损失约三成` ✅ |
| **M-3** | comp p12 结论 1：`正向失真的破坏力在浅层主干上强于负向（SimpleCNN 上为 3.4 倍），但方向性随深度衰减、在最深的主干上消失（ResNet-18 上为 0.94）` ✅ |
| **M-5** | journal p26 局限 (11)：`需要一条与探针同协议（同特征提取、同划分、同拟合过程）但用 α 混合数据训练的第三臂才能分开——现有的读出适配臂虽同为 α 混合数据训练，但它与探针在拟合过程（24ep Adam 对一次拟合）、训练样本量（20k 对 8k）与评估路径上都不同构，两者之差因此混入了协议差` ✅ |
| **M-6** | tech p3 已补 `（例如用器件特定的电导漂移查找表配合自适应量化…达到软件基线精度 [13]）`；p24 参考文献增到 **13 条**（含 Xiao 等）✅ |

### R2-6 ⏳ 仍在等用户裁决的（我在 PDF 里确认它们还在）

- **H-2**：journal p21 小节标题仍是 `这些效应有多大：用错协议会把方差估高六倍`，`tab:mystery` 第 1 行仍是 `混协议把方差估高约 6 倍`；tech p14/p15 同。
- **M-4**：comp p6 `结论是负面的但重要：非线性失真与高斯噪声的鲁棒性不可跨域迁移`、p12 结论 4 同；comp p12 局限 `三项拓展的数据集多样性（评审建议 R4）在时间约束下未执行`。
- **M-7**：journal 仍有多处居中表格（p4 指标定义表、p8 陷阱表、p11 `b` 臂表与 C10 表、p15 表 3、p18 φ 表与种子表、p19 漂移表、p24 部署表）。
- **L 档**：`前九节`（journal p20）、`两个数值`（p21）、`上文早期版本`（p13）、`第七阶段的审计记录`（p4）都还在原处。

---

### 第二轮小结：**值回票价，而且不在"数字"这一层**

第一轮我报的 12 条全部是**文本层**的（引文、数字、口径、跨文档）。
第二轮一页一页看图，撞出来的 4 条**全部是文本层看不见的**：

| | 第一轮（读源码） | 第二轮（看成品） |
|---|---|---|
| 能不能发现 | ❌ 看不见 | ✅ 一眼看见 |
| 典型 | H-1 注写错列、M-1 摘要过强 | **圈码全丢（46 处）**、**⇒ 全丢（32 处）**、**tech 两处被裁** |
| 自动闸门 | 检查器能抓一部分 | **检查器全绿**（`Missing character` 与 `Overfull` 都不在它的判据里） |

⇒ **建议把两条并入提交前闸门**（都是一行代码量）：
① `Missing character` 计数 == 0；
② 所有 `Overfull` 的 pt 值 **≤ 68.2**（= 2.4 cm 右边距；超过就会被裁）。
这两条一加，**本轮 3 条 HIGH/CRITICAL 里有 3 条会被自动拦住**。

---

## 附录 A：我建议的修复顺序（若要动手）

> **第一轮的 H-1 ~ H-4、M-1/2/3/5/6 已经在成品里核实修复完毕**（见 §5 R2-5）。
> 下面是**第二轮新发现 + 仍待裁决项**的合并顺序。

| 序 | 项 | 成本 | 理由 |
|---|---|---|---|
| 1 | **R2-1 + R2-2**（①②③…与 ⇒ 不渲染） | **5 分钟**（导言 2 行，三份稿各一处） | 三份稿共 **78 处**（46 个圈码 + 32 个 ⇒）。圈码那部分**打穿了编号句与交叉引用**（`第  级含两类种子`、`唯一不超过的是 仅负向`）。修法我已在 `/tmp` 实测（方案 D，`Missing character` 归 0） |
| 2 | **R2-3**（tech 两处被裁：p18 表、p22 路径） | 15 分钟 | 与已修的 H-4 **同类**，journal 修了、**tech 漏了**；p18 被裁的正是那两条口径陷阱的解释 |
| 3 | **H-2**（方差/标准差） | 5 分钟 | 你们已判"不改"；我列在这里是因为**它是唯一一条你们明确裁决过、而我建议重新考虑**的 |
| 4 | **M-4**（competition 的"不可跨域迁移"与"多样性未执行"） | 5 分钟 | 等你们裁决"竞赛版是否按赛题范围冻结" |
| 5 | **M-7**（无编号表）+ **L-1**（孤儿图表） | 30 分钟 | 形式项，评审容易挑 |
| 6 | **L-2 ~ L-9**（语汇与著录） | 20 分钟 | 逐条见 §2 LOW |

**建议在收尾清单里加两条闸门**（都是一行代码量，加进 `tools/check_papers.py`）：

```
① main.log 里 Missing character 计数必须 == 0     ← 拦住 R2-1/R2-2 那一类
② 所有 Overfull 的 pt 值必须 ≤ 68.2（= 2.4 cm 右边距）  ← 拦住 H-4 / R2-3 那一类
```

**理由**：本轮 3 条 HIGH 以上里，**3 条都逃过了现有的全部自动闸门**——
它们不是"没人检查"，是"检查器的判据里没有这一项"。这两行加上之后，这一类会被永久拦住。

**改完必跑 `tools/check_papers.py`**（我复现了它的三项结论，但你们提交前的闸门仍应以它为准）。

## 附录 B：我核过、确认无误的（省你们复算）

1. 摘要 ↔ 正文 ↔ 附录的**主要数值链**：`20.26/7.50`、`48.35`、`28.09`、`43.31/47.79/26.82`、`+15.2~+31.8`、
   `1/5~1/16`、`26.03→41.55 (+15.52)`、`40.29±1.58`、`42.47`、`20.6/33.3/58.7`、`2.0/17.9/67.3`、
   `r=−0.960`、`Δ_proc 3.45/3.46`、`5.55 (z=9.4)`、`±0.392/±2.561`、`+16.23`、`+27.63`、`25.44`、
   `6.08/12.82`、`+4.12/−6.74`、`R² 0.827~0.935 / 0.9516 / 0.9778`、`+2.84/+6.53`、`1.486/0.214/0.384`、
   `−13.68/−14.71/+0.39/−0.21`、`85.4/88.7/48.2% vs 72.2/70.2/36.9%`、`±0.25/±2.61`、`46.82/22.91`。
   **我按"能复算的复算、不能复算的查原文"逐条过了一遍，未发现不一致。**
2. `outputs/v3_readout_repair/*ep24*.csv` → journal §3.4 的 C10 表**逐位吻合**：`55.08→72.51 (85.4%)`、
   `36.71→79.64 (88.7%)`、`17.09→44.37 (48.2%)`。
3. `outputs_cifar100/…` → `tab:nat` 的两枝：`43.38/43.09/43.45 → 43.3067±0.1559`（= 43.31±0.16 ✓）；
   `mp` 读出枝 `49.12/48.82/49.59 → 49.1767±0.3169`（= 49.18 ✓）。
4. **文献**：`fragility` / `gjepa` / `lpft` / `tent` / `shot` / `mtjcal` 六条的 L1（存在性）**与
   我能取到摘要的五条的 L2（内容）全部通过**——这在 `nat` 之后是最重要的一类确认。
5. **编译**：三份稿 0 错误 / 0 未定义引用 / 0 未定义 citation，页数 29/24/13。
   ⚠ **overfull hbox：journal 8 处、tech 6 处、competition 1 处**——其中 journal 的 **390.8 pt 那处是真的把成品裁坏了**，
   已单列为 **H-4**（见 §2 与附录 A 第 1 项）。**你们内部报告记的是"1/1/2 处轻微"，那是 15 页版本的读数。**
