# 终读：摘要 + 贡献列表 + 结论（作为一个整体）

> ⏱ **快照声明**：本文件的**所有计数类结论**（§丙 那份"词 → 次数"表：`可迁移 0`、`普遍 2`、`唯一 6`…）
> **只在该时点成立**——最后写入 **2026-09-24 16:55**。
> **这份文件已经亲身经历过一次失效**：`普遍` 在同一次终读里先数到 0、几分钟后变成 2（详见文末"时效更正"）。
> ⇒ **复查时请重跑计数**；凡我判为"有问题"的两处都附了**原文字符串**（字符串在就还成立）。

> **派活**：claude-0b。**问题**：这三块被逐点改过十几次（A2/A4/A7/A10、第③项、B1、B2、E5、C4…），
> **但没人把它们连起来读过一遍**——现在这一版还自洽吗、还像一个作者写的吗？
> **纪律**：**纯只读**（一个字没改）；**未跑 `check_papers.py`**；未用 GPU。
> **位置引用**：一律附**原文字符串**（journal 正在被第三轮改，行号不可靠）。
> **⚠ 时效声明**：我读到的是**本次读取时刻**的版本（journal 正在被 claude-4c 改；
> 我核过的 `+15.52`/`次可加`/`表 2 口径说明` 等都已落盘，但**若你复查时发现某处不同，以你读到的为准**）。

---

## 结论先行

**这一版是自洽的，而且读起来像一个作者写的。** 四类检查里：

| 检查 | 结果 |
|---|---|
| （甲）内在连贯 | ✅ **未发现自相矛盾**（有一处"最有效"的最高级措辞，见丙.1，属措辞不属矛盾） |
| （乙）数字与正文一致 | ✅ **未发现不一致**（逐条核了 20+ 个跨块数字） |
| （丙）过强措辞 | ✅ **基本清干净**（`可迁移`/`普遍`/`一定`/`必然`/`总是` **全为 0**；`唯一` 6 处中 5 处是事实用法；**2 处低优先残留**） |
| （丁）一句话检查 | ✅ **说得出来**（做什么/贡献/边界都能一句话答）；**唯一缺口**：摘要的边界陈述里缺最基础的那条（见丁.2） |

⇒ **没有需要立刻动手的问题。** 下面三条是"要不要再顺手一次"的可选项。

---

## （甲）内在连贯

### 甲.1 ✅ 摘要内部：四条结论互不打脸

逐条对过，**没有互相拆台的**：

- 第一条（瓶颈在读出）与第二条（修复在读出）是因果关系，且第一条**自己带了限定**
  （`需先说明口径：\textbf{探针逐 $\alpha$ 训练，因此 headroom 是 oracle-$\alpha$ 诊断量}——
  它给出的是"若失真强度已知、且读出可自由重训"时的线性可解码性，**不是可部署读数**`），
  紧接着把可部署那个数（43.31）与"干净分布重训增益为零"一起给出 ⇒ **口径与读数绑定，不悬空**。
- 第一条的"它是**下界**"与"仍未饱和"一致（下界 + 未饱和 ⇒ 真实值更高）✓。
- 第二条"没有一条改善尾部（后两条显著劣于基准，另两条落在门槛内或与基准同级）"与
  第一条的"四路都**未能否证**它"**不是同一件事的两说**（一个是训练端路径、一个是探针加固），不冲突 ✓。
- 第三条"两个杠杆在尾部**可以同时受益，但不独立相加**（合起来拿到单独增益之和的六到七成）"
  ⇒ **一句话里"受益"与"不相加"同时出现，但不是矛盾而是限定**（受益=正增益，不相加=非可加），
  且贡献列表第 3 条用**同一措辞**（`两个杠杆在尾部可以同时受益，但\textbf{不独立相加}`）✓ 措辞同步。

### 甲.2 ✅ 三块之间：贡献列表 ↔ 摘要 ↔ 结论 三条一一对得上

| 摘要 | 贡献列表 | 结论 |
|---|---|---|
| 第一，瓶颈在读出（20.26 / 7.50 / 四路加固 / 下界） | 第 1 条 同 | "headroom 20.26…高斯只有 7.50" |
| 第二，修复在读出侧 | 第 2 条 同（+ 成本、四条路径分级） | 同 |
| 第三，零成本第二杠杆 | 第 3 条 同（+ 三步落地顺序） | 同（+15.52） |
| 第四，通路边界（6.08 / 12.82 / σ-only） | 第 4 条 同 | （结论节从简，未复述） |
| 方法学（5.55 / 三级可分辨性） | 第 5 条 同 | 同 |

⇒ **三块是同一套话的三种长度**，没有"摘要说了结论里没有、或结论反过来说"的情况。

### 甲.3 ✅ 一处曾经的矛盾已被显式收口（值得记一笔）

"读出适配是**唯一**可行的一类修复" vs "还有**第二个**杠杆"——贡献列表第 2 条现在自己收口了：
`它是\textbf{投片后}唯一可行的一类修复（架构端那个零成本杠杆只能在投片前决定，见第三条）。`
⇒ **限定 + 交叉引用同时给出**，读者不会再读出矛盾。

---

## （乙）数字与正文一致

**逐条核过，未发现不一致。** 核对清单（左列 = 摘要/贡献/结论里出现的数，右列 = 正文对应处）：

| 三块里的数 | 正文对应 | 判定 |
|---|---|---|
| headroom 20.26 / 高斯 7.50 | §3.2 与附录同值 | ✅ |
| 48.35 / 28.09 | §3.2 同值 | ✅ |
| 43.31（"真正与 α 无关的部署路径"） | §3.4 表 2 第二行（3 seed 均值 43.31±0.16） | ✅ |
| +16.23（最严口径、规范口径的八成） | §3.3 同 | ✅ |
| +27.63 / +25.4（下界） | §3.3 同，**并给了算式** `27.63-2.19=25.44` | ✅ |
| 提升 +15.2~+31.8（C100，24ep/20k，各 3 seed） | 表 2 的 +15.22/+31.81/+20.60 区间 | ✅ |
| 成本 12~30 分钟、**1/5~1/16** | 表 2 口径说明里有**逐主干配对**：`读出 12 / 25 / 30 min 对全网络 1 / 6.5 / 6.5 h $\Rightarrow$ $1/5$、$1/16$、$1/13$` | ✅**（配对方式已写明）** |
| 28.09/15.98/6.22 → 43.31/47.79/26.82 | 表 2 第一→第二行；口径说明确认第一行 = 读出所依附那三块主干的自带头 | ✅ |
| 2.71 / 31.19（+0.5 两侧） | §3.6 同 | ✅ |
| 26.03 → 41.55（+15.52，同协议同 seed，单 seed） | §3.7 同（并注明单 seed） | ✅ |
| R² 0.83~0.98 | §3.8 与附录（0.827~0.978） | ✅ |
| 6.08 / 12.82 | §3.10 同 | ✅ |
| 5.55（一致超过的对照） | §4.10（`26.58-21.03`、3 对 6 个 run） | ✅ |
| 四条路径分级（后两条显著、另两条门槛内/同级） | §3.6 同 | ✅ |

**另外两处我特意去核的"疑似不一致"，结论都是"已披露、不是问题"**：

1. **"全网络鲁棒训练（最好）"从 26.57 变成了 29.89**（表 2 与附录都改了）。
   一开始我判为可能与正文冲突——**核完发现是显式披露的选择**，原文：
   `第三行的三格是\textbf{各列上已知最优的全网络配方}，来自两个不同的 harness（SimpleCNN 为 \texttt{task3} 的 Exp3 配方，VGG-11 / ResNet-18 为 \texttt{ext6} 的 Exp3），且都是\textbf{单次运行}；SimpleCNN 上同协议的 3 seed 均值是 26.57，低于 29.89，故本行取 29.89。读出适配相对该行的领先为 $+13.42$ / $+23.10$ / $+20.60$。`
   ⇒ 换了**更强的基线**（对读出侧更不利的方向），并把**同协议替代值、跨 harness 事实、单次运行**三件事都写了。
   **领先量 +13.42 远超本文自订的跨实现效应 5.55 pp** ⇒ 结论稳。**不构成缺陷。**
2. **成本比 1/5~1/16**——上一条已说明，配对方式写在正文里 ✓（这与跨文档审计里 journal↔techrep 的 1/16 vs 1/30 是同一处的两面：**journal 这次写明了自己用的配对**）。

### （乙）小结论

**没有"摘要是另一个口径但没带定语"的情况。** 我特别找的是这一类（因为它是本项目的主导病因），
结果是：**凡是口径特殊的地方，摘要都自己带了定语**——
`oracle-$\alpha$ 诊断量`、`同协议同 seed 配对`、`单 seed`、`下界`、`最严口径`、`（CIFAR-100，24 ep/20k，各 3 seed）`。

---

## （丙）还有没有过强的措辞

**全篇扫描结果**（`grep -o` 计数）：

| 词 | 次数 | 判断 |
|---|---|---|
| 可迁移 | **0** | ✅ C4 的降级已执行——讨论节标题现在是 `\subsection{一个我们建议的测量顺序（在一族卷积主干上验证过）}`，正文写 `本文建议的\textbf{测量顺序}` |
| 普遍 | **2**（⚠ 见下方"时效更正"） | ✅ **都在同一句 hedge 里**：`（这一条是\textbf{本文的观测}，不是一条普遍定律：一个未归因的差异，既可能是实现层的普遍不可移植，也可能是其中一个脚本里还有没找到的具体差别——本文没有排除后者。）` ⇒ **不是过强措辞，是主动降级** |
| 一定 / 必然 / 总是 | **0 / 0 / 0** | ✅ |
| 证明 | **1** | ✅ 合法：`线性探针"在分布偏移下已被证明是有效范式"`——说的是**文献里的已知结论**，不是本文自己的主张 |
| 全部 | 25 | ✅ 逐条看过：全是 `冻结全部主干权重` / `全部冻结` / `有效秩指标全部健康`（引文献）这类**事实性**用法，**没有**"全部失效/全部证明"式的断言 |
| 唯一 | **6** | 5 条事实性（`唯一同向的是 $+0.4\to+0.5$ 这一对`、`唯一显著的差异是空间元素数…`、`唯一不超过的是③仅负向：55.50 对 55.56`、`唯一差别是数据集与主干权重`）+ **1 条见下** |

### 丙.1 ⚠ **两处低优先残留**（都不影响结论，属"要不要再顺手一次"）

1. **摘要第二句的最高级**：`\textbf{第二，因此最有效的修复动作在读出侧。}`
   —— **"最有效"没有给出比较基准**。而在**同协议配对下**，架构杠杆在 SimpleCNN 上是 **+15.52**、
   读出适配是 **+15.22**（两者基线不同：一个对鲁棒训练的 26.03，一个对干净模型的 28.09）。
   两个数几乎相等，所以"最有效"这个最高级**在当前证据下不容易站住**。
   → **建议**：改成 `因此最省力的修复动作在读出侧`（"省力"有成本支撑），或补一个比较基准
   （`在三主干上幅度最大且唯一对全部三个主干成立`——这个说法正文是支持的）。
2. **§1.3 的 `而是唯一选择`**：原文 `② 在该场景下主干\textbf{物理上不可重训}，故读出适配不是"更优选择"而是唯一选择；`
   —— **前提就写在紧邻处**，可接受；但同句没有 `权重层面` 这个限定，而贡献列表那条**已经加了**（`投片后唯一可行`）。
   → **建议（可选）**：加四个字：`而是在\textbf{权重层面}唯一的选择`，与贡献列表对齐。

---

## （丁）一句话检查

**问：读完摘要，能不能说出这篇文章做了什么、贡献是什么、边界在哪？**

**答：能，而且四句话都说得出来**（这是我按摘要原文归纳的，不是我替它补的）：

- **做了什么**：`在 CIFAR-10 与 CIFAR-100 上，用四个主干（参数量 0.24M$\sim$11.2M）系统考察了这种失真的破坏形态、可达边界与修复路径`。
- **贡献**：四条 + 一条方法学——① 定位（瓶颈在读出，20.26 / 7.50）；② 修复（读出适配，+15.2~+31.8，成本低一个数量级）；
  ③ 第二杠杆（池化密度，零成本）；④ 通路边界（两种扰动不互相保护，σ-only 最优）；⑤ 方法学（5.55 未归因 ⇒ 跨实现不引用）。
- **边界**：`headroom 是 oracle-α 诊断量，不是可部署读数`、`四路都未能否证它`、`它是下界`、
  `门控有效需要两个前提同时成立`、`全部结论只依赖一个结构前提——读出层位于数字域或可重写`、
  `在归因之前…本文不引用`。

### 丁.1 ✅ 一个正面观察：这一版摘要**主动写了对自己不利的信息**

`只把同一个头在干净分布上重训，\textbf{增益为零}`（这是**排除替代解释**的对照）、
`四路都未能否证`（而不是"四路印证"）、`它是下界`、`不独立相加（六到七成）`、
`本文观测到…按已知未知记录`。**这类"自曝"句在一个摘要里出现五次，是我今天读到的四份文档里最克制的一份。**

### 丁.2 ⚠ **唯一缺口**：摘要的边界陈述缺了最基础的那一条

摘要把**方法学的**边界写得很全（oracle-α / 未能否证 / 下界 / 门槛 / 结构前提），
但**没有提模型层面的那条**——而它正是局限节的**第一条**：
`\textbf{(1) 失真模型是参数化假设，且无硬件实测。}全部收益都测在 \cref{eq:distortion} 这一族上。`
对一个**以存算一体硬件为题**的论文，读者从摘要里看不出"这是仿真 + 参数化失真模型"。
→ **建议**：在摘要里加半句（放在"用四个主干系统考察"那句之后最自然），例如：
`失真模型为参数化假设（三次多项式、无硬件实测），这一限制贯穿全文（见局限）。`
**成本一行字，收益是读者不会误以为有硬件验证。**

---

## 汇总

```
（甲）内在连贯      ✅ 未发现自相矛盾（1 处最高级措辞，归入丙）
（乙）数字与正文一致 ✅ 未发现不一致（逐条核 20+ 个跨块数字；2 处"疑似"核完均为已披露）
（丙）过强措辞      ✅ 基本清干净（可迁移/普遍/一定/必然/总是 = 0；2 处低优先残留）
（丁）一句话检查    ✅ 说得出来；唯一缺口 = 摘要缺"参数化模型 + 无硬件实测"这半句
```

**要动手的：1 条（丁.2 半句）**；**可选的措辞：2 条（丙.1）**；**其余无需处理。**

### ⚠ 时效更正（我自己抓到的一处）

我在**同一次终读**里对 `普遍` 做了两次计数：**第一次 0、第二次 2**——中间几分钟 journal 被又改了一次
（新增的正是上面那句 hedge）。**这正好印证 claude-0b 的提醒：我读到的是中间态。**
处理方式：**以第二次为准**（并给出原文字符串），上表已更正。
**这个 0→2 的变化方向是好消息**（新增的是降级句，不是过强断言），但它说明：
**本文件里任何一个"计数"都可能是某一时刻的快照**——凡结论依赖计数的，请以复查时的实测为准；
凡我判断为"有问题"的（丙.1 两处），我都附了原文字符串，字符串在就还成立。

---

## 四件套

**里程碑**：① 把摘要/贡献列表/结论**当整体**逐条读了（不是逐点看改没改）；
② 核了 **20+ 个跨块数字**是否与正文一致（含"口径差异 vs 真不一致"的区分，未发现问题）；
③ 扫了 8 个过强词（`唯一/全部/证明/可迁移/普遍/一定/必然/总是`）并逐条定性；
④ 做了"一句话检查"，并指出摘要边界陈述的唯一缺口；
⑤ 纯只读，一个字未改。

**关键数字 + 出处**：
| 事实 | 出处（原文字符串可检索） |
|---|---|
| 摘要已带定语的口径句 | `需先说明口径：\textbf{探针逐 $\alpha$ 训练，因此 headroom 是 oracle-$\alpha$ 诊断量}` |
| 成本比配对方式已写明 | `读出 12 / 25 / 30 min 对全网络 1 / 6.5 / 6.5 h $\Rightarrow$ $1/5$、$1/16$、$1/13$` |
| 换基线的显式说明 | `SimpleCNN 上同协议的 3 seed 均值是 26.57，低于 29.89，故本行取 29.89。` |
| 表 2 三行身份的收口 | `③ResNet-18 一列的第一行与第三行是同一块主干`（同一段口径说明内） |
| C4 降级已执行 | `\subsection{一个我们建议的测量顺序（在一族卷积主干上验证过）}` |
| 摘要缺的那条边界（局限第一条） | `\textbf{(1) 失真模型是参数化假设，且无硬件实测。}` |
| 两处残留 | `\textbf{第二，因此最有效的修复动作在读出侧。}` / `而是唯一选择` |

**判据命中哪个分支**：
- 派活的判据是"三块连起来读一遍，还自洽吗" ⇒ **命中"自洽"**（不是靠降低标准判的：
  我给了逐条核对清单与逐词扫描计数，且**没有为了有产出而硬凑**——丙组的 6 个词里有 4 个是 0 命中，我照实写 0）。
- **唯一一条我认为值得动手的是丁.2**（摘要加半句"参数化模型、无硬件实测"）——
  它不是"不自洽"，是**边界陈述不完整**，而这篇论文的题目就是硬件场景。

**有没有触发红线**：
- **没有**。journal 与其余三份**全程只读**，一个字未改；**未跑 `check_papers.py`**（内存红线）；
  未用 GPU；未 commit；未碰别人的 `docs/verify/*.md`。产物只有本文件。
- 一处**时效风险已如实标注**：journal 正在被第三轮修改，我读到的可能是中间态；
  凡我引用处都附了原文字符串，若与复查时不符，**以复查时读到的为准**。
