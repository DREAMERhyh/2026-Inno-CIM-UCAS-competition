# 全面体检报告（独立会话，冷读）

> **执行者**：由总管 `claude-a3` 派单的独立体检会话。**未参与本项目任何写作或修复。**
> **实测时刻**：**2026-09-24 19:45–20:0x**（本文件所有"命中/未命中"计数与磁盘状态均**只在该时刻成立**）。
> **方法**：先**冷读三份论文全文**（不查任何外部文件）→ 再实证回查产物 → **最后**才读已有审计，标记"已知/未知"。
> **纪律**：纯只读（**一个字未改**，唯一产物是本文件）；**未跑 `tools/check_papers.py`**（内存红线）；**未用 GPU**；未 commit；未开新会话。
> **位置引用**：一律附**原文字符串**，行号只作辅助（journal 今天 1258 → 1442 → 1615 行，行号不可靠）。

---

## 〇、先说结论

**新发现 12 条**，其中 **3 条我认为必须在投稿前处理**（1 条内容错误 + 2 条"改了这份忘了那份"）。
**项目的自我审计密度远超一般投稿**——我抽查的 30+ 处派生量算术**全部自洽**，
**没有发现任何"凭空数字"**。下面所有问题都是**可追溯、可复现、零成本可修**的。

---

## 一、项目现状（与磁盘事实核对后的结论）

### 1.1 🔴 HANDOFF 的"三十秒状态"与磁盘事实**相反**

**实测（19:51:54）**：

| 项 | `docs/HANDOFF.md` 写的 | **磁盘/进程实测** | 一致？ |
|---|---|---|---|
| 在跑的 M4 run | **s44**（PID 36656，16:44:11 起） | **s43**（PID 3864，**19:34:06** 起） | ❌ **相反** |
| s43 | ❌ 缺 | `best_model.pth` mtime = **19:51**（正在写） | ❌ |
| s44 | ⏳ 在跑 | `best_model.pth` mtime = **18:32**（已停 1h20m） | ❌ |
| 守护进程 | pid 34972 | pid **37628**（19:20:27 起） | ❌ 已换实例 |
| 内存 | 物理 0.2~0.9 / 提交 7.7 GB | 物理 **1.83** / 提交 **5.49 GB** | 量级一致 |

**实测命令与原始输出**：
```
Get-CimInstance Win32_Process -Filter "name='python.exe'" | ? { $_.CommandLine -like '*deep_robust*' }
→ ProcessId 3864, CreationDate 2026/9/24 19:34:06
   args: ...python.exe" task_extension6_deep_robust_train.py --dataset cifar100 --model resnet18 --variant exp3 --epochs 200 --seed 43 --tag _s43
```

**时间线重建**：18:32 s44 最后写盘 → **18:35:03 机器重启**（HANDOFF §1·补 自己已记录）→
19:20:27 守护被重新拉起 → 19:34:06 训练进程启动（**它按顺序先补 s43**，符合守护 v2 的设计）→ 19:51 仍在写盘。

**判据符合性**：两个目录**都没有 `metrics.json`** ⇒ 按 HANDOFF §4 的前置闸门，
**s43 与 s44 都还未"真完成"，M4 收尾的读出流程仍不得启动**。这一条 HANDOFF 写对了。

**为什么这是问题**：HANDOFF 是给"不想读全文的人"看的第一屏。它现在同时**与磁盘事实相反**、
且**与它自己的 §1·补 内部不自洽**（§1·补 记录了 18:35 重启把训练带走，§0/§3 却还写 16:44 起的 s44 在跑）。
**缓解因素**：它是 16:55 的快照（文件头已声明），且 §1·补 给了自查命令。

> **⚠ 一条给后来者的操作提示**：查守护进程时，`? { $_.CommandLine -like '*v7_m4_watchdog*' }`
> **会匹配到你自己那条查询命令所在的 powershell 进程**（命令行里含该字符串）。
> 我这次实测就多出一个"19:52:04 创建的守护进程"——那是**观察者效应**，不是真有第二个守护。

### 1.2 其余状态核对（一致的部分）

- 台账 `experiments_ledger.md` 已写到 **§16.8**（`§16.0 / 16.0a / 16.2 / 16.2a / 16.3 / 16.4 / 16.5 / 16.5a / 16.6 / 16.7 / 16.7a / 16.8`，`§16.1` 预留给 M4）✓ 与 HANDOFF 一致。
- `nat` 的裁决已落盘（`4337fd9`），journal §1.3 已改写为「别人规避、本文测量」✓ 复核无误。
- 论文工作树干净（除一个未跟踪的 `logs_v7_m4_restart_s43.err`）✓。

---

## 二、成果完整性

### 2.1 第七阶段成果的覆盖矩阵

逐个用**特征数字/产物路径**（而非关键词）核对：

| 第七阶段成果 | 产物 | journal | techrep | 备注 |
|---|---|---|---|---|
| M8 容量阶梯（§16.6） | `v7_readout_capacity/capacity_ladder.csv` | ✅ §3.3 六行表 | ✅ | 两版一致 |
| M8d 样本量曲线（§16.5/16.5a） | `v7_readout_samplesize/samplesize_ext.csv` | ✅ §3.3 | ✅ | 两版一致 |
| §16.4 过程自由度 Δ_proc=3.45（§16.4） | `v7_etf_readout/proc_sensitivity.csv` | ✅ §3.12 | ✅ | 两版一致 |
| §16.7 门控机制（φ 强正） | `v7_gate_mechanism/gate_errors.csv` | ✅ §3.10 | ❌ **缺** | 见 2.2 |
| **M8b ETF/原型读出（阴性）** | `v7_etf_readout/` | ❌ **缺** | ✅（"四条排除型结论"首条） | 见 2.2 |
| **M9b 神经坍缩几何（阴性）** | `v7_nc_geometry/nc_geometry.csv` | ❌ **缺** | ✅ | 见 2.2 |
| **§16.2a Fisher 分解（类内散布主导）** | `v7_nc_geometry/fisher_decompose.csv` | ❌ **缺** | ❌ **缺** | 见 2.2 |
| **§16.8 判据前提自检审计（30 条/8 条/5 条）** | 台账 §16.8 | ❌ | ❌ | 见 2.2 |

**（对照说明）**：`paper/coverage-audit.md` 是 **M1 阶段的**审计（它自己在 §五·补 里标注了时点），
它列的 9 条缺口（8 + `b_clean_fit_linear` 孤儿臂）**已全部闭环**，我逐条复核无误。
**但它的覆盖范围止于第七阶段之前** —— 上表这四行全部落在它的盲区里。

### 2.2 做了但没进论文的（逐条，附产物路径与原文）

**① M8b：ETF / 原型读出（阴性）—— journal 缺**

- **journal 侧实测**（19:5x）：`grep -c "ETF" paper/journal/main.tex` = **0**；`"原型"` = **0**。
- **技术报告有**（`technical-report/main.tex`）：
  > `ETF / 原型读出 & \textbf{无增益}。三种原型或 ETF 正则读出均未超过线性头，且几何诊断（下一条）事先预测了这一点`
  > 并明写：`\textbf{排除型结论的价值在于：它们各自堵死一个替代解释。}尤其第一条——原来"读出瓶颈"可以被反驳为"你只测了线性头"，现在这个反驳不成立了。`
- **为什么该进 journal**：journal §3.3 用六种读出堵住了"只测了线性头"的**容量侧**，
  但**结构侧**（原型 / ETF 归纳偏置）未堵。更关键的是：M8b 与 M9b 是**事先写死的互证**
  （台账 §16.3 原文：`\textbf{与 §16.2 的对照（预登记时已写死）：两条互证。}`）——
  journal 缺这两条，等于放弃了"两个独立测量互证同一个排除"这个很强的论证结构。

**② M9b：神经坍缩（NC）几何—— journal 缺**

- **journal 侧实测**：`"神经坍缩"` = **0**，`"NC "` = **0**（对照：`"Fisher"` = 6）。
- journal 保留的是 M9b 里的**一个量**（Fisher 判别比），丢掉了**它的框架与阴性判决**。
- **为什么该进 journal**：journal 在 §3.6 处理了一个反直觉结果并专门加了"注意措辞"段：
  > `更关键的是，\textbf{同强度下正侧的 Fisher 判别比反而高于负侧}`
  而 M9b 正是**排除这个异常的一个候选解释**（"是不是表征坍缩到 ETF 类几何了？"）。
  读者读完那一段一定会问这个问题，而答案就在仓库里。

**③ §16.2a：Fisher 正负不对称的分解（类内散布主导）—— 两份都缺**

- 产物：`outputs_cifar100/v7_nc_geometry/fisher_decompose.csv`
- **这是我认为最该补的一条**：journal 花了 5 行讨论那个 Fisher 异常（含一段"注意措辞"），
  却**没有用它的定量来源**——即"正侧高"来自类内/类间散布的分解。
  journal 的"注意措辞"段只做了**防守**（不许说"正侧升高"），没有做**解释**。

**④ §16.7 门控机制（φ 强正 ⇒「插值即集成」永久关闭）—— 技术报告缺**

- journal 有（§3.10，`outputs_cifar100/v7_gate_mechanism/gate_errors.csv`）；技术报告 0 命中。
- 技术报告自称"完整归档"，其"第七阶段"一节列了 M8 / M8d / §16.4 / 门控**两前提**，**独缺 φ 机制**。

**⑤ §16.8 判据前提自检审计——三份全缺**

- 内容：扫 30 条预登记判据，8 条同类缺陷（27%），其中 5 条足以翻转（3 条已实测确认）；
  其"顺带发现"明写**8 条全是判据写法问题，不是数据或执行问题**。
- **judgement**：journal 可豁免（非论文素材）；**技术报告的定位里含"方法论制度"，
  这条恰是本项目最"元"的方法学产出**，缺它与自身定位不符。

### 2.3 论文里没产物支撑的断言（"写了但没做"）

**总体结论：我未发现任何"凭空数字"。** 逐条反查后只有 **1 处明确的"引用了不含该数据的表"**，
另有 2 处"出处对不上"（详见下表）。**抽查的 30+ 处派生量算术全部自洽**（清单见 §四·附）。

| # | 原文（可直接检索） | 位置 | 问题 |
|---|---|---|---|
| 1 | `（CIFAR-100 口径下 3 bit 不是"略降"而是大幅下降，19.75 $\to$ 10.87，见附录表 \ref{tab:dither-c100}。）` | journal 附录前正文 | **附录表 `tab:dither-c100` 不含 19.75 与 10.87**。该表只有 4bit−8bit 差值、峰度、翻转统计、架构对照四块。读者按指引查不到。 |
| 2 | `池化同协议 3 seed 差（avg$-$max） & $+1.17$（SE 1.03；未舍入 $21.7767-20.6033=1.1733$）` | journal 附录·数值汇总表 | 用**所给的两个值相减得 1.1734**（21.7767−20.6033 = 1.1734）。真值可能确为 1.1733（原始值位数更多，`journal_fixes.md` E10 已论证），**但读者按这张表给的算式复核会对不上**；且所声称的出处"台账 §13.8"里**没有这两个未舍入值**（台账原文写的是 `21.78` / `20.60` / `+1.17`，19:5x 实测零命中所给的两个 4 位小数）。 |
| 3 | `读出适配付出了 3.19~6.67 个百分点的干净精度代价。` | journal §3.10（另见 §3.4 与 §3.4 末的 C10 对比句） | **8ep 旧口径**，见 §五.1·丙。 |

**补充说明**：`paper/health-check-report.md` 自称"**221 个 token，221 个有出处，0 个无出处**"——
那是**该文件撰写时点**的口径（它自己写了快照声明），其 token 集合**不含第七阶段新增内容**。
我不重跑 221 个 token（成本/内存），但上表 3 处说明"当前版本仍有可查的出处缺口"。

---

## 三、脉络与叙事逻辑（按 `rewrite-scientific-writing` 框架评）

### 3.1 层级运动（Data → Finding → 1-hop → 2-hop → Principle → New Question）

| 层级 | journal 是否完成 | 证据 |
|---|---|---|
| Data → Finding | ✅ | 13 个 Results 子节的标题**全部是 Answer 式**，无一条写"我们做了什么"（逐条核过） |
| Finding → 1-hop | ✅ | 每个子节末尾都有一步不远离数据的推进（例：§3.3 末"容量不是这里的瓶颈"） |
| 1-hop → 2-hop | ✅ | §4.1 用 R2+R3+R4 三条并置推出"修复的对象不是参数而是解码" |
| 2-hop → General principle | ✅ **且带适用范围限定** | §4.2 原文：`本文只在一族卷积主干（0.25M$\sim$11.2M 参数，以 CIFAR-100 计；全部为 ReLU + 卷积/池化堆叠）上验证过它，适用范围见第 \ref{sec:limit} 节的局限 (10)` —— 这是很规范的做法 |
| → New question space | ✅ | §4.4 四条开放问题，与局限一一对应 |

**结论：骨架是完整的，而且是"科学问题驱动"的**（不是实验流水账）。
这一点我核得很实：**13 个子节里没有一条以技术动作命名**。

### 3.2 贡献列表 → 正文 → 结论的闭合性

**摘要 4 条 + 方法学 1 条 → §1.2 贡献列表 5 条 → §5 结论 3 段**，逐条能对上正文 ✓。

**唯一一处不闭合**（措辞强度，非结构问题）：

- **摘要**（可直接检索）：`用三种非线性读出（k-NN 与两种 MLP）替换线性头，\textbf{没有一个能超过它}——容量不是这里的瓶颈。`
- **正文 §3.3 自己的裁决**：`而两层 MLP 只差 $2.85$，\textbf{落在门槛之内}，按本文自订规则这一臂\textbf{不可裁决}——只能说方向上仍劣于线性头。`
- ⇒ "没有一个超过"是**事实陈述**（确实都没超过）；但"容量不是瓶颈"是**推论**，
  被那一个"不可裁决"的臂轻微削弱。正文自己划了门槛却让结论句越过了它。

### 3.3 摘要能不能独立读懂（"做了什么 / 贡献是什么 / 边界在哪"）

- **做了什么** ✅ 能说出。
- **贡献是什么** ✅ 能说出（四条 + 一条方法学）。
- **边界在哪** ⚠️ **读不出来**。摘要里边界信息是**碎片化**的：`oracle-$\alpha$ 诊断量`、
  `单 seed，见第 \ref{sec:results} 节的口径说明`、`四路都未能否证它，但其中只有对照二给出独立的第二条读数`……
  **分散在 6 个不同句子里各说一点**，没有一个"集中的边界陈述"。
- **代价**：journal 摘要 **47 行**、含 **9 处括号口径说明**，信息密度远超一般期刊摘要。
  它试图在摘要里同时完成"报告结果"和"汇报全部口径"两件事——**这是它读起来累的根因**。

### 3.4 Discussion 的 2-hop 与 general principle

- **2-hop 站得住** ✓：由 R2（信息在线性可达范围内）+ R3（换读出即取到）+ R4（参数空间四条路径全败）
  **三条并置**推出，且明确排除了"再分配"这个替代解释（用单点 vs 分布式两种读出训练方式的对比）。
- **general principle 站得住** ✓：原文带三重限定（"在一族卷积主干上验证过"、"只依赖一个结构前提"、局限 (10)）。
- **一处偏强**：§4.2 的三步测量顺序说"在一族卷积主干上验证过"，但**第三步**（"再看架构端有没有零成本的降维空间"）
  在三个主干里有 1 个（ResNet-18，MaxPool 数 = 0）**根本没有旋钮**——这是论文**局限 (9) 自己承认的**。
  ⇒ "验证过"这个词对第三步偏强。不影响原理，但一句话能修。

### 3.5 Limitation → Future Study

**journal 12 条局限，逐条核过门槛（"因为没做 X，由本文发现引出的问题 Y 仍无法回答"）：**

| 局限 | 是否达标 |
|---|---|
| (1) 失真模型是参数化假设、无硬件实测 | ✅ 明确（"headroom 还有多大、读出杠杆是否成立，本文无法回答"） |
| (2) 深层主干单次训练 | ✅ 明确（"是否在主干种子方差内无法裁决"）**并恰好指向正在跑的 M4** |
| (3) 池化分解未定 | ✅ 明确（"无法裁决'降维 : 非均值聚合'的比例"） |
| (4) 秩-恢复只有三点 | ⚠️ **只说"只有三个点"，未明写"因此问题 Y 无法回答"**（§4.4 开放问题里有，局限本身缺后半句） |
| (5) 跨实现未归因 | ⚠️ 同上，略简 |
| (6)–(12) | ✅ 全部明确，且多数带"下一步"与成本估计 |

**总评：这是全文质量最高的部分之一。** 局限 (2) 把"深层全部差值无法裁决"写死，
**恰好是正在跑的 M4 要回答的问题** —— 论文已经做了正确的自我保护，这一点值得记为正例。

---

## 四、论证强度与边界

### 4.1 🔴 **如果我是审稿人，第一条拒稿意见会是：**

> **"本文最核心的对照表（表 3）内部含有跨训练实现的相减，而这正是本文自己立下的禁令。"**

**具体指控链**（每条都附原文位置）：

1. 表 3（`journal:555-570`，`\label{tab:nat}`）第三行的三格来自**两个 harness**——
   **论文自己在表注里承认**：
   > `第三行的三格是\textbf{各列上已知最优的全网络配方}，来自两个不同的 harness（SimpleCNN 为 \texttt{task3} 的 Exp3 配方，VGG-11 / ResNet-18 为 \texttt{ext6} 的 Exp3），且都是\textbf{单次运行}`
2. 论文由第一行 → 第二行给出读出增益（**同一块主干内，合法**）✓ 这一步没问题。
3. **但紧接着给出**：
   > `读出适配相对该行的领先为 $+13.42$ / $+23.10$ / $+20.60$。`
   —— **这三个差值是跨 harness 相减**（读出侧来自 `v3_readout_repair`，第三行来自 `task3` / `ext6`）。
4. 而论文 §3.12 与结论都主张：
   > `\textbf{在归因之前，本文的做法是不跨实现（含与文献）引用精度数字。}`
   > `在归因之前，精度数字不应跨训练实现引用（本文不引用）`

**为什么这条有力**：它**不需要否定任何数据、不需要新实验**，只需要指出**同一套标准没有被一致地适用**。
审稿人的原话会是："你们用一条纪律取消了与文献对比的资格，却在同一篇里用这条纪律所禁止的运算，
支撑自己最强的主张（读出适配优于所有全网络配方）。"

**可选的两种回应**（都很便宜）：
- (a) **收紧表述**：只报第一行 → 第二行（同一主干内），把"领先 +13.42/…"改成
  "第三行仅作各列已知最优的**上界式**参照，不作差值"；
- (b) **给出理由**：明确写"跨 harness 相减在此处的风险**上界**由 5.55 pp 给出，
  而三格的领先（+13.42 起）**远大于**该上界"——即把 5.55 当噪声上界用，把差值降级为"远超噪声"的定性主张。

> **与外部复核的分工**：`INDEPENDENT_REVIEW.md` 的 **D1** 提的是"oracle-α 口径 + 上界被误用"（口径问题）；
> 它的 **A3** 提的是"表 2 三行不是同一个模型"（身份问题，journal 已用表注修）。
> **我这条是第三个角度：差值的合法性**（纪律适用的一致性）。三者不重复。

### 4.2 证据强度配不上措辞（今天两轮降级之后**仍未降到**的）

| # | 原文（可直接检索） | 位置 | 为什么强了 |
|---|---|---|---|
| **1** | §3.1 **子节标题**：`正向失真更致命，且这个方向性跨架构稳定——但倍数不是常数` | journal §3.1 | **C10/ResNet-18 的比值是 0.94（反号）**。这不是"倍数不是常数"能吸收的——**方向本身反了**。兄弟文档两处都已写明（见 §五.1·乙），journal 未提 |
| **2** | `跨阶段稳定成立的只有\textbf{方向}。` | journal §3.1 末 | 同上。journal **全文不含 `0.94` / `1.40`**（19:5x 实测零命中），即 C10 侧的前两个比值也没报。`techreport_fixes.md:224` 已判定此句**"被 C10/ResNet-18 的 0.94（反号）否证"** |
| **3** | `三种非线性读出…\textbf{没有一个能超过它}——容量不是这里的瓶颈。` | journal 摘要 | 正文自判其中一臂"不可裁决"（见 §三.2） |
| **4** | `三条独立的排除线（可交换性、不变区集中、max 的选择性）逐一否证了候选解释。` | journal 摘要 | 正文说的是 `在池化输入（post-ReLU，非负）上\textbf{精确可交换}`，且 `\textbf{但可交换却非成因}`——被否证的是"池化吸收失真"这个**解释**。摘要的三项并列读起来像"可交换性被否证了" |
| **5** | `读出适配付出了 3.19$\sim$6.67 个百分点的干净精度代价。` | journal §3.10 | 8ep 旧口径混进 24ep 叙述；且**同一节内**与 v5 口径的 `−5.89` / `−3.45` / `−4.36` 并置（见 §五.1·丙） |
| **6** | `两个杠杆并不独立相加：架构单独增益 $+14.38$、读出单独增益 $+15.22$` | journal §3.9 | **这条是"被处理过"的**（正文有 3 行的口径说明，并给了替代口径 77% 的复核），我**不作为问题报**，只提示：`+14.38` 是跨脚本差值，与它上一节刚强调的"同协议同 seed 配对 +15.52 才是主口径"并置时，读者需要读完整段才能不掉坑 |

### 4.3 做了实验但结论没写进去的

见 §二.2 的五条。其中**①②③（M8b / M9b / §16.2a）**属"排除型证据没用在该用的地方"。

### 4.4 附：我抽查并确认**自洽**的派生量（供后来者不必重算）

下列 34 处我逐一手算复核，**全部自洽**（这是我对本文档可追溯性的正面结论）：

- 方向性比值 1.87 / 1.18 / 1.01（C100）与 3.37 / 1.40 / 0.94（C10）
- 超加性 31.90 − 23.33 = 8.57
- headroom 48.35 − 28.09 = 20.26；+27.63 − 2.19 = 25.44
- 读出增益 43.31−28.09 = 15.22；47.79−15.98 = 31.81；26.82−6.22 = 20.60
- b 臂零增益 +0.26 / −0.17 / −0.84
- C10 恢复率 72.51/84.90 = 85.4%、79.64/89.80 = 88.7%、44.37/92.09 = 48.2%
- C100 恢复率 43.31/59.99 = 72.2%、47.79/68.05 = 70.2%、26.82/72.69 = 36.9%
- 架构 3-seed 均值 (42.47+38.77+39.64)/3 = 40.293、总体 sd = 1.580、增益 12.20
- 次可加 14.38+15.22 = 29.60；49.18−28.09 = 21.09；21.09/29.60 = 71%；替代口径 27.42 / 6.33 / 77%
- 成本比 12/60 = 1/5、25/390 ≈ 1/16、30/390 ≈ 1/13
- 四条路径 −1.71 / −4.25 / −5.55（基准 26.03）
- Δ_proc 极差 56.31−52.85 = 3.46（α=0）、51.57−48.12 = 3.45（+0.3）；轮数跨度 3.02、同轮数优化器跨度 2.25
- 混协议 z 值 (26.58−21.03)/√(1.00²/3+0.29²/6) = 9.42
- 门控六分布裕度 +0.12/+0.20/+0.38/+0.54/+0.59/−0.06
- 六读出梯子相对线性头 −2.24 / −3.22 / −13.35 / −8.09 / −2.85
- **ResNet-18 三个读出种子的 clean 均值 64.9967 ≈ 65.00**、总体 sd = **1.852 ≈ ±1.85**
  （原始三值 63.54 / 63.84 / 67.61，实测自 `readout_repair_c100_exp3_resnet18_s4*.csv`）
  ⇒ 论文局限 (2) 与 §3.12 的"63.54 摆到 67.61 / ±0.15 → ±1.85"**逐位可复现**，
  且"按 2 个种子报 ±0.15"用的正是 s42+s44 那一对 ⇒ **完全自洽**。

---

## 五、跨文档一致性

> **判断法**：问"这两个数能相减吗？"——能相减才是同一个量。
> **总管豁免、我不报的两条**：① 术语同义词（journal 有显式映射声明）；② `+15.29`（techrep 单 seed）vs `+15.22`（journal 3 seed）——**不同口径的两个量，各自正确**（我复核了：43.38−28.09 = 15.29 ✓、43.31−28.09 = 15.22 ✓，两臂产物不同）。

### 5.1 🔴 真不一致（3 条）

#### 甲 · 「全网络鲁棒训练（最好）」· SimpleCNN 一格：journal = **29.89** / techrep = **26.57**

| 稿 | 原文 |
|---|---|
| journal | `全网络鲁棒训练（最好） & 全部 & 1$\sim$6.5 h & 29.89 & 24.69 & 6.22 \\` |
| journal 附录 | `全网络鲁棒训练 @$+0.3$（最好） & 29.89 / 24.69 / 6.22 & ...` |
| technical-report | `全网络鲁棒训练（最好） & 1$\sim$6.5 h & 26.57 & 24.69 & 6.22 \\` |
| technical-report 表注 | `\textbf{第三行}为各架构上最好的全网络鲁棒训练配方（SimpleCNN 取 v3 路径逐层独立 NAT 的 3 seed 均值 26.57，...）` |

**证据链**：
- `docs/verify/journal_fixes.md`（"**同表顺手修的新 1**"）记录：`第三行 SimpleCNN 原写 26.57 并标"（最好）"，
  但 results_master.csv:39 的 task3_Exp3_FullRobust 在 pos_0.3 = 29.89；26.57 是 v3_methods/plain_uniform 的
  3 seed 均值，\textbf{不是最好}` ⇒ **journal 已改 29.89**。
- **该修未同步到技术报告**。技术报告的表注**仍自称"最好的"**，而它取的不是最高值 ⇒ **注释与取值自相矛盾**。
- **另一层**：`docs/verify/cross_doc_consistency.md`（甲.3）的核对表现写
  「全网络鲁棒训练最好 | 26.57 / 24.69 / 6.22 | **同**」——**该结论已过期**
  （撰写时两份都是 26.57，之后 journal 改了）。

**下游影响**：读出适配相对该行的领先 = **+13.42**（journal 口径）vs 隐含的 **+16.74**（techrep 若照算）。

#### 乙 · 方向性：journal 说"稳定"，兄弟两稿说"反号/消失"

| 稿 | 原文 |
|---|---|
| journal §3.1 | `跨阶段稳定成立的只有\textbf{方向}。` + 子节标题 `正向失真更致命，且这个方向性跨架构稳定` |
| technical-report | `CIFAR-10 的三个主干为 3.4 / 1.40 / \textbf{0.94}：\textbf{方向随深度衰减，并在最深的主干上（CIFAR-10/ResNet-18）反号}。` |
| competition | `SimpleCNN \textbf{3.4}、VGG-11 \textbf{1.40}、ResNet-18 \textbf{0.94}——\textbf{方向性随深度衰减，在最深的主干上完全消失}。` |

**证据链**：`docs/verify/techreport_fixes.md` 已算出六个比值并判定：
> `② 正文「跨条件稳定成立的只有\textbf{方向}」也被 C10/ResNet-18 的 \textbf{0.94}（反号）否证。`
> `\textbf{改法}：…正文补全六个比值并改为「方向随深度衰减，并在最深的主干上（C10/ResNet-18）反号」。`

**该修落在了技术报告与竞赛版，journal 未改。**
**并且 journal 内部三处互相矛盾**：R1 子节标题（"方向性跨架构稳定"）／ §3.1 正文（"只有方向稳定"）／
§3.6 正文（`\textbf{"正侧更致命"这条（第 \ref{sec:results} 节 R1）在 ResNet-18 上并不成立}`）。

> ⚠ **这是三条里我认为最该优先处理的一条** —— 它同时是①跨文档不一致、②journal 内部自相矛盾、
> ③**子节标题级**的过强断言。而 journal 是要投出去的那一份。

#### 丙 · 读出适配的干净精度代价：journal = **3.19~6.67**（8ep 旧口径）／ techrep = **3.08~7.69**（24ep）

**我独立复现（19:5x，直接读产物）**：

| 口径 | 产物 | 三主干 α=0 的读出适配均值 | 对自带头 | 代价 |
|---|---|---|---|---|
| **24ep（现行）** | `outputs_cifar100/v3_readout_repair/*ep24*.csv` 的 `e_readout_NAT` | 55.81 / 64.97 / 65.00 | 59.99 / 68.05 / 72.69 | **4.18 / 3.08 / 7.69** |
| （另一套配方） | `v5_full_recipe/recipe*.csv` 的 `acc_robust_head` | 55.85 / 55.10 / 64.60 / 68.33 | 59.99 / 60.99 / 68.05 / 72.69 | 4.14 / 5.89 / 3.45 / 4.36 |

- **技术报告的 `3.08~7.69` 与 24ep 产物逐位吻合** ✓（我复现的三个均值 55.81 / 64.97 / 65.00 与它写的一致）
- **journal 的 `3.19~6.67` 不匹配上述任何一套**；其来源是台账 §4.7b 时代的 **8ep 口径**
  （`docs/verify/techreport_fixes.md` T9：`干净代价 3.19~6.67 是 8ep 口径，本表是 24ep`）
- **`techreport_fixes.md` T9 修的是技术报告**；**journal 三处未改**，且 journal §3.10 同一节里
  `3.19~6.67`（8ep/v3）与 `−5.89` / `−3.45` / `−4.36`（v5）**两个口径并置**。

### 5.2 已修的两条（列出供确认，不重报）

| 项 | 现状（19:5x 实测） |
|---|---|
| `cross_doc_consistency.md` 甲.1（池化增益 15.52 vs 14.38） | technical-report 摘要现为 `（同协议同 seed 配对 $+15.52$ 个百分点）` ✅ **已同步** |
| 乙.1（techrep 残留"没有一条超过基准"） | 该半句**已删**，现为 `基准（均匀采样）为 26.03。按本文自订的单种子门槛…` ✅ **已修** |
| 甲.2（成本比 1/16 vs 1/30） | technical-report 现为 `约 1/5$\sim$1/16` ✅ **已统一** |

### 5.3 已发现但修复未落地

- `docs/verify/refs_other_docs.md` 指出 `paper/health-check-report.md` §4 的
  「参考文献键完整性 … 三份稿件：**缺失 0、未引用 0**」**对另两份不成立**，并"建议把这条结论改掉"。
  **实测（19:5x）：`paper/health-check-report.md:148` 仍写着"未引用 0"** ⇒ 建议未落地。
- 我另核（19:5x）：competition 有 **8 条 bibitem / 4 处 `\cite`**（`cim_survey,puma` / `cifar` / `vgg` / `resnet`）
  ⇒ 未被引用的 **3 条：`neat` / `quant` / `dither`**。
  （`nat` 被换成 `neat` 后，孤儿数量不变——**换了一条孤儿为另一条孤儿**。）
  **这是已覆盖项**（`refs_other_docs.md` 已完整记录，当时记的正是 `nat`/`quant`/`dither` 三条），我不重复报，
  只更新事实：**当前是 `neat`/`quant`/`dither`**。

---

## 六、形式与表达（按 `paper-form-writing` 模式 6 的清单）

### 6.1 第一层·事实性缺失
见 §二.3 与 §五，此处不重复。**本层新增 4 条：**

| # | 原文（可直接检索） | 位置 | 问题 |
|---|---|---|---|
| 1 | `\textbf{前九节}讨论的都是同一类扰动` | journal §3.12 开头 | 该小节是 Results 的**第 12 个** `\subsection`（其前有 11 个）。"前九节"与结构不符 |
| 2 | `本文的方差标定给出\textbf{两个数值}，其中一个足以改变结论。` | journal §3.13 开头 | 后文紧接着讲**三**件事（`\textbf{第一…}` / `\textbf{第二…}` / `\textbf{第三…}`） |
| 3 | `上文早期版本写作"正侧失真下 Fisher 反而升高"，丢了比较对象，是可被本文表格否证的错误表述，已更正。` | journal §3.6 | **修订痕迹进正文**。读者（审稿人）看到"早期版本"会困惑：这是投稿论文还是项目日志？ |
| 4 | `详见第 \ref{sec:probe} 节与\textbf{第七阶段}的审计记录` | journal §1.3 | **"第七阶段"是项目内部阶段名**，投稿稿里无定义（技术报告里它是章节名，journal 里不是） |

### 6.2 第二层·形式层

| # | 项 | 评价 |
|---|---|---|
| 1 | **摘要长度与密度** | journal 摘要 **47 行**、含 **9 处括号口径说明**。它试图在摘要里同时"报告结果"与"汇报全部口径"。**建议**：口径说明移入正文或脚注，摘要只留一句集中的边界陈述 |
| 2 | **摘要的数字定语** | 首处关键读数 `$\alpha=+0.3$ 时可达 48.35\%，而模型自带分类头只有 28.09\%，headroom 为 20.26 个百分点` **未标主干与数据集**（实为 CIFAR-100 / SimpleCNN）；同段 `43.31/47.79/26.82（三个主干）` 也未点名是哪三个。**与本项目反复强调的定语纪律不一致** |
| 3 | **题目** | `逐层非线性失真下神经网络精度退化的成因：读出瓶颈与双杠杆修复` —— "双杠杆"是黑话（正文首现于 §1.2 第三条）；"成因"+"修复"并列使题目偏长。可读，但不算最优 |
| 4 | **图表（正面）** | journal **11 张图 + 8 张表**：我逐条核了 `\includegraphics` 与 `\ref` —— **无孤儿、无悬空**；图注普遍标了数据集、协议、n/seed 与**产物路径**。**这是全文做得最好的形式项** |
| 5 | **内部语汇残留** | journal 正文仍见 `按\textbf{本项目的纪律}，预登记未达标的记录必须留下`（§3.10）、`就是\textbf{产物列名} \texttt{acc\_clean\_head} 的那一个`（§2.4）。前者是内部流程语言，后者是代码语汇。§2.4 的同义词映射声明本身是**刻意保留**（总管已说明，我不报），但"产物列名"这个措辞可以中性化 |

### 6.3 第三层·论证层
见 §三、§四。**只列不答**（属 REWRITE 范围）。

### 6.4 ⚠ 未能核查的项（"查不到" ≠ "不存在"）

1. **未编译**。红线禁跑 `tools/check_papers.py`（六次 xelatex / 16 GB 内存）。
   ⇒ **交叉引用是否真的解析成功、有无未定义引用，本次未验证**，我只做了源码级核对。
   同理 `markdown 残留`（反引号/`**`）**本次未扫描**——我只在"读到的原文"层面检查，未做全文机器扫描。
2. **目标期刊未定**。`paper/format-checklist.md` 自己声明"目标载体未定"，因此**任何格式合规结论都无从谈起**。
   本次只做与载体无关的项。
3. **M4 产物未核语义**（正在跑；我只看 mtime 与 `metrics.json` 的有无）。
4. **`ucas-cod-lab-report/` 未纳入**（总管指定三份）。
5. **台账 152 行的 pairing 自检未重跑**（需跑 `build_ledger.py`，内存红线内我未执行）。
6. **`paper/figures/*.pdf` 的图像内容未核**（只看图注与正文的一致性，未看图本身画得对不对）。

---

## 七、与已有审计的对照

### 7.1 新发现 12 条

| # | 发现 | 严重度 | 性质 |
|---|---|---|---|
| 1 | **技术报告 §1 的 SHOT 描述与原文相反**（`SHOT 冻结特征提取器、只学习分类头`） | 🔴 高 | **内容错误**，且削弱该节的差异化论证 |
| 2 | **「全网络鲁棒训练（最好）」SimpleCNN 一格 29.89 vs 26.57** | 🔴 高 | 跨文档不一致（修复未同步） |
| 3 | **journal 的"方向性跨架构稳定"被 0.94 否证，未修**（标题级 + 内部三处矛盾） | 🔴 高 | 断言强于证据 + 跨文档 |
| 4 | **journal 三处的干净代价 3.19~6.67 是 8ep 口径** | 🟡 中 | 口径混用（修复未同步） |
| 5 | **M8b（ETF 阴性）未进 journal** | 🟡 中 | 覆盖缺口（排除型证据） |
| 6 | **M9b（NC 几何阴性）未进 journal** | 🟡 中 | 覆盖缺口（排除型证据） |
| 7 | **§16.2a（Fisher 分解）两份都缺** | 🟡 中 | 覆盖缺口（异常的解释） |
| 8 | **§16.7（φ 机制）未进技术报告** | 🟢 低 | 覆盖差异 |
| 9 | **§16.8（判据前提自检审计）三份全缺** | 🟢 低 | 与技术报告"方法论制度"定位不符 |
| 10 | **HANDOFF 的"三十秒状态"与磁盘事实相反**（s43/s44 弄反） | 🟡 中 | 状态文档过期 |
| 11 | **journal:1113 "前九节" / :1185 "两个数值" / :759 修订痕迹 / :227 "第七阶段"** | 🟢 低 | 形式层（4 小条合并） |
| 12 | **附录表不含正文指引的 19.75/10.87；1.1733 算式复核得 1.1734** | 🟢 低 | 事实性缺失（2 小条合并） |

**另有 1 条"骨架工作文件滞后"**（单列，因为它不在论文里）：
`paper/storyline.md:11` `1/15 ~ 1/30`、`paper/storyline.md:64` 与 `paper/results-outline.md:32` 的 R3 标题
`两个数量级`、`paper/results-outline.md:45` `clean 代价 −3.19 ~ −6.67` —— **均与论文现行口径不符**，
而这两个文件**都没有口径注记**（`discussion-notes.md` 有，做法是对的）。
**风险**：`results-outline.md` 头部自称"数字与 `论文v3.md`、`experiments_ledger.md` 逐字一致"——
该声称已不成立；而这三个文件是 REWRITE skill 的**持久状态**，后续会话会读它。

### 7.2 已覆盖（我核对后确认已被既有审计记录，**不重报**）

| 我的候选发现 | 已被谁覆盖 | 备注 |
|---|---|---|
| competition 3 条 bibitem 未被引用 | `refs_other_docs.md`（完整记录） | 我只更新了事实：`nat` → `neat` |
| 1.1733 的算法 | `journal_fixes.md` E10 + `techreport_fixes.md` R6 | 已判定"原文是对的"；我的增量只是"附录算式用 4 位值相减得 1.1734" |
| `0.39062` vs `0.39061` | `journal_fixes.md` E7 | REFUTED，0.39062 是另一产物 |
| 池化 avg−max = 1.17 | 同上 | 已核 |
| `+15.29` vs `+15.22` | 总管已豁免 | 不同口径的两个量 ✓ 我复核确认 |
| 术语同义词 | `cross_doc_consistency.md`（丙） | 总管已豁免，且有显式映射声明 |
| 表 2 三行不同源（A3） | `INDEPENDENT_REVIEW.md` A3 → journal 已表注 | 我补的是"差值合法性"这一层（§4.1） |
| oracle-α 口径（D1/A5） | `INDEPENDENT_REVIEW.md` | 我未重提 |

### 7.3 已发现但**修复未落地**（3 条）

1. `docs/verify/refs_other_docs.md` 建议改 `paper/health-check-report.md` §4 的"未引用 0"——**未改**（19:5x 实测）。
2. `techreport_fixes.md:224` 判定 journal 的"只有方向"被否证——**journal 未改**（= 我的 #3）。
3. `techreport_fixes.md` T9 把技术报告改成 24ep——**journal 未改**（= 我的 #4）。

---

## 八、我的一句话结论

### 8.1 这个项目够不够格投出去？

**内容够格；形式上还差一次"以投稿读者视角"的收尾，而不是差实验。**

具体说：

- **它的科学内核是完整的**：一个诊断（headroom 20.26，带四路加固与 oracle-α 标注）、
  一个修复（读出适配 +15.2~+31.8，成本低一个数量级）、一个机制（秩塌陷，Fisher 不降）、
  一个架构杠杆（池化密度，三条排除线）、一条方法学（三级可分辨性）。
  **证据链、排除线、局限与边界都到位**，且局限 (2) 主动指向正在跑的 M4。
- **它的可追溯性是我见过的最好的之一**：抽查 30+ 处派生量算术**全部自洽**，
  没发现任何凭空数字，图注普遍给到产物路径。
- **它的自我审计密度远超一般投稿**：A/B/C/D/E 五组 + 16 份 verify 报告 + 覆盖审计 + 旧值地图。

**差的三件事（全部零成本，不需要新实验）**：

1. **修 SHOT**（技术报告 §1）—— 这是唯一一条**内容错误**，且它让该节失去差异化论证。
2. **对齐三条跨文档不一致**（29.89/26.57、方向性 0.94、干净代价口径）——
   共同形态都是"**某个会话改了、另一个没跟上**"（正是 `cross_doc_consistency.md` 自己命名的失效模式）。
3. **清掉 journal 里的内部语言**（"第七阶段"/"本项目的纪律"/"上文早期版本"/"前九节"/"两个数值"）——
   这些在技术报告里是**正确的**（它就是内部归档），但在投稿稿里会暴露"这是项目日志转过来的"。

**唯一的实质性风险**不在写作，在 **M4**：论文局限 (2) 已承认"深层全部差值是否在主干种子方差内无法裁决"。
**投稿前应等 M4 出数**（或维持现有局限表述——那样也站得住，只是会弱一档）。

### 8.2 我没有查出来的（"查不到"≠"不存在"）

- 三份稿的**编译产物**（未跑 check_papers.py）——交叉引用是否真的解析成功，我**不知道**，只知道源码层是齐的。
- **完整的目标期刊格式合规**——目标刊未定，无从查起。
- **图像内容**（PDF 里画得对不对）——我只核了图注与正文的一致。
- **台账 pairing 自检的当前状态**——未重跑 `build_ledger.py`。
- **`ucas-cod-lab-report/` 与根目录的历史文档**——不在本次范围。
- **M4 的最终数字**——正在跑。

---

> **本文件的"命中/未命中"计数与磁盘状态快照时刻**：**2026-09-24 19:45–20:0x**。
> 凡我判定为"有问题"的，都附了**原文字符串**——字符串在，结论就还成立；行号与计数请以复查时实测为准。

---

# 修复（2026-09-24 20:0x，按 claude-a3 派单）

> **授权范围**：本轮**只有 `paper/**`** 可改，其余一律只读。改动**逐处 Edit，无字符串替换脚本**；
> **未引入任何未登记数字**（每个新数都指回产物文件）。
> **⚠ 一处请总管确认的边界**：`docs/V7_COORDINATION.md` §二的领地表现写着
> `paper/journal/main.tex` 归 claude-4c、`paper/technical-report/**` 归 claude-89。
> 我按本次派单改动了这两份——若那两个会话仍在写同一文件，请先协调再提交。

---

## A. 三处出处缺口

### A1 —— 附录表 `tab:dither-c100` 补入绝对精度行

**判断依据**：19.75 与 10.87 **确实属于 C100 口径**，且**就是同一张表的 (a) 块在讲的那个实验**——
`outputs_cifar100/extension5_dithering_vgg11/group_a_bits_scan.csv` 的原文逐行：
```
num_bits,accuracy,loss,dose
3,10.87,6.427069,0.564195
4,19.75,4.171697,0.25763
8,16.91,4.598096,0.014263
```
⇒ 19.75−16.91 = **+2.84**（表内已有的 4bit−8bit 值 ✓ 自洽）、19.75−10.87 = **8.88**（正文"8.88 个点"✓）。
**故选择"补进表"而非"改指引"**——数本来就在，缺的是它们没被列出来。

**改后原文**（`paper/journal/main.tex`，附录表 `tab:dither-c100` 的 (a) 块）：
```latex
\multicolumn{4}{l}{\emph{(a) 精度（VGG-11，固定 $\alpha=+0.3$，\%）}} \\
8 bit（基线） & --- & 36.74 & 16.91 \\
4 bit & --- & 43.27 & 19.75 \\
3 bit & --- & 42.97 & 10.87 \\
\midrule
4 bit $-$ 8 bit & --- & $+6.53$ & $+2.84$ \\
```
（同时把块标题从"精度增益"改为"精度"——现在它既含差值也含绝对值。
C10 侧三行取自 `outputs/extension5_dithering_vgg11/group_a_bits_scan.csv`：8bit 36.74 / 4bit 43.27 / 3bit 42.97 ✓ 逐位核对。）

### A2 —— 附录数值汇总表：**移除算不对的算式**

**问题复述**：原格写 `未舍入 $21.7767-20.6033=1.1733$`，而 **21.7767−20.6033 = 1.1734**；
且所声称的出处"台账 §13.8"里**没有这两个 4 位小数**（台账原文是 `21.78` / `20.60` / `+1.17`）。

**真值与真算式**（我独立复算，逐 seed 值取自台账 §13.8）：
```
5×max 的逐 seed drop = 60.99−42.47=18.52、60.97−38.77=22.20、60.73−39.64=21.09 → 均值 61.81/3 = 20.60333…
5×avg 的逐 seed drop = 61.34−38.29=23.05、61.48−40.51=20.97、61.33−40.02=21.31 → 均值 65.33/3 = 21.77666…
差 = 1.17333… → 1.1733 ✓（**原值是对的，坏的是那个用 4 位小数相减的算式**）
```
**改法**：**删掉算式、保留真值、写明算法**（不改展示值 ⇒ 不动任何下游）：
```latex
池化同协议 3 seed 差（avg$-$max） & $+1.17$（SE 1.03；未舍入 $=1.1733$——三个逐 seed 的 \texttt{drop} 各取均值后再相减） & 台账 §13.8 \\
```
⇒ 读者按"逐 seed 取均值后相减"去台账 §13.8 能找到逐 seed 值、能复算出 1.1733；**表里不再留一个照它算就对不上的式子**。

### A3 —— 三处 8ep 旧口径 → 24ep（照技术报告对齐）

`paper/journal/main.tex` 三处**全部改毕**（改后 `grep -n "3\.19\|6\.67"` = **零命中**）：

| # | 位置 | 改后原文 |
|---|---|---|
| 1 | §3.4 末 | `代价是干净精度下降 \textbf{3.08$\sim$7.69} 个百分点（$\alpha=0$ 处读出适配的三 seed 均值为 55.81 / 64.97 / 65.00，对各自主干的原始头 59.99 / 68.05 / 72.69）——` |
| 2 | §3.4 的 C10/C100 对比 | `\textbf{干净代价也小得多}（$-0.58\sim-0.84$ 对 $-3.08\sim-7.69$）。` |
| 3 | §3.10 开场 | `读出适配付出了干净精度代价：上文读出适配一节的三个主干（24\,ep、各 3 seed）为 \textbf{3.08$\sim$7.69} 个百分点。（\textbf{口径提醒}：本节以下七个点取自\textbf{另一批产物}（\texttt{v5\_full\_recipe}）——主干配置与读出训练都与那一批不同，故其代价与上面这个区间\textbf{不逐位对应}。）` |

**⚠ 关于"改完同节内是否还有两套口径并置"——有，且我做了显式处置**：
§3.10 后面用的 `−5.89` / `−3.45` / `−4.36`（含图 F08 的七个点）取自 **`v5_full_recipe`**，
与 §3.4 的 `3.08~7.69`（`v3_readout_repair`）**是两批产物**（我实测：v5 四组代价为
4.14 / 5.89 / 3.45 / 4.36，v3 三主干为 4.18 / 3.08 / 7.69 —— **两组区间不重合**）。
§3.10 原本写 `\textbf{鲁棒头即读出适配头}`，读起来像同一批数。
⇒ 我在 §3.10 开场加了上面那条**口径提醒**，把"同概念、两批产物"显式说出来。
**这不消除两批产物，但消除了"并置而不说明"** —— 若你认为还要更硬的处理（例如把该节七点全部重算到 v3 口径），请示下。

---

## B. 五条"做了没进论文"

### ① M8b（原型/ETF 读出，阴性）→ journal §3.3 —— ✅ 已补

**为什么该进（一句）**：上面六种读出堵的是"头不够**大**"，而这条堵的是"头缺少**结构先验**"——
**两条平行的排除线**合起来才把"你只测了线性头"这一族反驳堵死。

**改后原文**（`paper/journal/main.tex`，紧接容量阶梯的出处之后）：
```latex
\textbf{换一种归纳偏置也试过了：带结构先验的读出同样没有增益。}
上面六种读出都是"自由"的头，一个自然的反驳是"也许换成带结构先验的读出会更好"。
在同一批冻结特征上再测三种：最近类均值、以及两种强度的原型正则化线性头
（把类中心拉向等角紧框架，$\lambda=0.1$ 与 $1.0$）。以同一实现、同一 seed 下的线性头
（$\alpha=+0.3$ 处 50.37）为基准：最近类均值为 $36.50$（$-13.87$）；
原型两臂为 $50.39$（$+0.02$）与 $50.67$（$+0.30$）——按预登记门槛
（$\ge1$ pp \textbf{且} 超过该臂优化器噪声的 2 倍、即 $\approx0.10$），\textbf{两臂都不达标}
（前者连噪声都没超过，后者超过了噪声但幅度远小于 1 pp），干净精度下降 $0.07$ / $0.09$。
⇒ 与容量阶梯合起来，「信息仍线性可达」这条有了\textbf{两条平行的排除线}：
既不是"头不够大"，也不是"头缺少结构先验"。
（出处：\texttt{outputs\_cifar100/v7\_etf\_readout/etf\_readout.csv}。）
```
**数字逐项核对**（`etf_readout.csv` 第 7 行 `0.3,8000,10000,28.11,48.25,36.5,1.19,50.37,50.39,50.67`）：
基准 `L_0`=50.37、`NCM`=36.5、`ETF_0.1`=50.39、`ETF_1.0`=50.67 ✓ 全部逐位；
增益 −13.87 / +0.02 / +0.30 = 与台账 §16.3 的判决表逐位一致 ✓；
σ 与门槛取自 `etf_seed_check.csv`（`L_0` 3 seed 50.4433±0.0525）✓。
**注**：技术报告那条写的是"几何诊断（下一条）**事先预测**了这一点"——我**没把这句话搬进 journal**（理由见下「我不该补的」）。

### ② M9b（神经坍缩几何）→ journal —— ⚠ **未补，理由如下（请裁决）**

**我停下来是因为派单里那条纪律**（"如果改的时候发现理由不成立……就停下来报我，不要硬塞"）：

- 我在体检报告里给的理由是"**答案就在仓库里**"。**实测下来，仓库里有的不是答案，是一个不稳健的测量**：
  `experiments_ledger.md` §16.2 自己写着——**centered 口径判 ②（远离 ETF）、uncentered 口径判 ③（不分离）**，
  原文：`本节同时落了 centered 与 uncentered 两版。\textbf{两版给出不同的判决分支}：| A/V uncentered | A 升 4/5、V 升 1/5 | ③ 不分离 |`
- 而 §16.8 的审计**已经把它列为缺陷 #2**（`S2：符号计数未检验对预处理选择的稳健性`，判定"**足以翻转**"），
  处置是 `降级为仅记录（centered / uncentered 并列呈现，不据此下单一判决）—— 已在 §16.2 执行`。
- ⇒ **若照技术报告的写法（"远离 ETF ⇒ NC 被排除"）搬进 journal，等于把一个已被自己审计降级的结果当铁结论用**。
- **同时**：journal 现在**并没有**"我们排除了 NC"这个断言（§3.6 只说"类间可分性没有变差"）——
  所以**不补它不产生缺口**，补它反而引入一条不稳的排除。
- **另一层**：技术报告那条（`神经坍缩（NC）几何 & \textbf{远离 ETF}。… ⇒ 「正侧 Fisher 反而高于负侧」这条异常\textbf{不能用 NC 解释}`）
  **用的是降级前的版本，没带 uncentered 限定** —— 这其实是**一条新问题**（见下）。

**可供你选的三个方案**（我不自行决定）：
- **(a) 不补**（我的推荐）：journal 本无此断言；技术报告那处**单独带限定修**。
- **(b) 以"限定"形式补**：在 §3.6 加一句"我们测过 NC 几何，但该几何量对中心化预处理敏感（5/5 ↔ 1/5），
  **未给出稳健排除**"——如实，但会削弱该节。
- **(c) 补跑 uncentered 的完整版**（含 §16.2a 同口径分解）后再定——有算力成本。

**顺带报一条新问题**（不在本轮授权范围内，我**没改**）：
`paper/technical-report/main.tex` 的"四条排除型结论"表里，
`神经坍缩（NC）几何 & \textbf{远离 ETF}。类均值夹角偏离与模长变异系数在正侧 5/5 上升 ⇒「正侧 Fisher 反而高于负侧」这条异常\textbf{不能用 NC 解释}`
——**该表述早于 §16.2 的 centered/uncentered 降级与 §16.8 的缺陷判定**，属"过强断言"。建议按 (a) 带限定修。

### ③ §16.2a（Fisher 不对称的分解）→ journal §3.6 —— ✅ 已补

**为什么该进（一句）**：journal 花了一段讨论"Fisher 正侧反而更高"这个异常、并专门加了"注意措辞"作**防守**，
却**没给它的定量来源**——而那段分解就在 `fisher_decompose.csv` 里。补的是**解释**，不是新主张。
**且**：§16.2a **不在** §16.8 的 8 条缺陷清单里（只有 §16.2 在内）⇒ 它是可用的。

**改后原文**（插在"注意措辞"段**之后**——那段一字未动）：
```latex
\textbf{那这个不对称是什么？——类内散布的相对收紧。}
把 Fisher 判别比写成 $S_b/S_w$（类间 / 类内散布），看失真沿哪个因子推动它。
在 5 个 $|\alpha|$ 上，两类散布\textbf{都}随失真收缩（$\Delta\log S_b$ 与 $\Delta\log S_w$ 全为负），
但\textbf{类内那一个收缩得更快}：5/5 上 $|\Delta\log S_w|>|\Delta\log S_b|$，
故 $\log(\text{Fisher 比})$ 为正（$|\alpha|=0.3$ 时三者依次为 $-2.2318$、$-2.3881$、$+0.1563$；
分解含一条恒等自检，残差在机器精度量级）。
⇒ \textbf{Fisher 的正负不对称，是约掉公共幅度之后剩下的残差}：
正侧的 $S_b$ 与 $S_w$ 都远小于负侧（比 $9.32$ 与 $10.89$），
而 $S_b$ 的比基本就是"$\alpha>0$ 压低幅度"这一个已知效应
（幅度平方之比 $9.07$，差 $3\%$），\textbf{类内散布比类间多缩了约 $17\%$}——
这一份才是 Fisher 在正侧反而更高的来源。
⇒ 与本节"类间可分性没有变差、丢失的是维度"一致：变的是\textbf{散布的尺度}，不是类中心的结构。
（出处：\texttt{outputs\_cifar100/v7\_nc\_geometry/fisher\_decompose.csv}。）
```
**数字逐项核对**（`fisher_decompose.csv` 与台账 §16.2a 结果表）：
−2.2318 / −2.3881 / +0.1563 ✓；比 9.32（e^2.2318）与 10.89（e^2.3881）✓；
幅度平方比 9.07 = (35.18/11.68)² ✓；"多缩约 17%" = (10.89−9.32)/9.32 = 16.8% ✓（与台账结论句的"约 17%"一致）。

### ④ §16.7 门控机制（φ 强正）→ 技术报告 —— ✅ 已补

**为什么该进（一句）**：技术报告自称"完整归档"、并已列了门控的**两前提**，
但**独缺**那个把"插值即集成"**永久关闭**的机制解释——而它是门控这一节的结论性内容。

**改后原文**（插在"门控的边界"一节的"两前提无法做成装前自检"之后）：
```latex
\textbf{为什么它只能"弱支配"：一个机制级的回答。}
… 两个头的错误是否互补可以直接测：取两个头在\textbf{同一批样本}上的 0/1 错误指示向量，
算它们的 $\phi$ 相关。\textbf{只有当两头的错误负相关时，插值才可能"集成"出比两头都好的结果}…
实测（7 个 $\alpha$ 点、每个数据集 21 格）：$\phi$ 的范围在 CIFAR-100 上是 $+0.594\sim+0.706$、
CIFAR-10 上是 $+0.519\sim+0.830$；"两头都错"的格数相对独立基线为 $1.50\sim1.98$ 倍与 $2.76\sim6.58$ 倍。
⇒ \textbf{$\phi$ 在两个数据集上都是强正}…\textbf{「插值即集成」所依赖的互补性在两个数据集上都不存在}。
…
（出处：\texttt{outputs\_cifar100/v7\_gate\_mechanism/gate\_errors.csv}。
该诊断含一条恒等自检：$\text{soft}-\text{oracle}=(\text{rescue}-\text{hurt})\times100$，通过。）
```
（完整段落见文件；数字与 journal §3.10 的同一段**逐位相同**——我是从 journal 侧搬的，不是重算的。）

### ⑤ §16.8 判据前提自检审计 → 技术报告 —— ✅ 已补（写短）

**为什么该进（一句）**：技术报告的定位含"**方法论制度**"，
而这条是本项目最"元"的方法学产出——**它是制度立起来之后第一次回头扫自己**。

**改后原文**（"方法学产出"一节新增 (d)，约 10 行）：
```latex
\textbf{(d) 一次对存量判据的「前提自检」回溯审计。}
本阶段先撞出两次同形缺陷（§16.5a、§16.7），据此立了一条要求：
\textbf{凡拿某个量当参照或判据依据的，都要补该量的前提自检}。
制度立起来之后的第一件事，是拿它\textbf{回头扫存量}：
扫 \textbf{30} 条显式预登记判据，得 \textbf{8 条同类缺陷（命中率 27\%）}，
其中 \textbf{5 条足以翻转判决}、\textbf{3 条已由实测确认翻转}。
\textbf{一条时间上的观察}：8 条缺陷\textbf{全部落在 §4$\sim$§13} 这一段（早期阶段），§14 及以后一条都没有——
不是"后来的判据写得好"，而是\textbf{制度立起来之后，同类缺陷在当场就被抓了}。
\textbf{顺带发现：无}——本次未发现任何"实验结论本身"的问题，
\textbf{全部 8 条都是判据写法问题}，不是执行或数据问题。
（出处：台账 \S16.8；本节无新产物文件。）
```
**数字核对**（台账 §16.8）：30 条 ✓ / 8 条（27%）✓ / 5 条足以翻转 ✓ / 3 条已实测确认 ✓ /
8 条全落在 §4–§13 ✓ / "顺带发现：无"原文 ✓。

---

## （另）我判断"不该补"或"未按原样补"的

1. **② M9b → journal**：**未补**，理由见上（理由不成立：仓库里的不是答案，是一个已被自己降级的不稳健测量）。
   **请你在 (a) 不补 / (b) 限定式补 / (c) 补跑后再定 之间裁决。**
2. **技术报告那条"NC 几何 ⇒ 排除"**：**未改**（超出本轮授权范围），但**报上来**（见 ② 末）。
3. **`cross_doc_consistency.md`、`health-check-report.md` 等审计文档里的过期结论**：
   **未改**（不在 `paper/**` 授权范围内，且它们是别人的产物）。
4. **§3.10 的两批产物**：我加了**口径提醒**而非合并——因为合并需要重算，超出"补"的范围。

---

## （末）检查器与内存

### 🔴 `tools/check_papers.py` **未跑** —— 原因：内存红线

**实测（20:0x–20:09，跨 12 分钟四次采样）**：
```
FreePhysicalGB : 0.56 → 0.76 → 0.42 → 0.50   （四次采样，红线是 1.5）
FreeCommitGB   : 1.11 → 2.19 → 0.65          （⚠ 比项目记录的"事故当时 4.9 GB"低一个数量级）
训练进程 PID 3864 仍在跑                       （M4 / s43）
```
**我先等了约 12 分钟**（两轮 sleep 后复查）——**无回升趋势**（提交余量反而从 2.19 掉到 0.65），
而 M4/s43 按实测还要跑到次日凌晨。⇒ **停止等待，如实交付"未跑"。**
`check_papers.py` 会跑 **六次 xelatex**（三份稿 ×2）。当前提交余量 1.11 GB 下运行**极可能打死训练**
（这正是本项目付过代价的那个失效模式：`WinError 1455` 打死的是训练，不是我的脚本）。
⇒ **按派单红线"低于 1.5 GB 就先等，别硬跑"，我停在这里，未跑。**
**若你要求必须跑，请在一台内存更宽的窗口/时刻重派**，或授权我在训练结束后跑。

### 我做了哪些**零编译**的替代检查（全部通过）

| 检查 | 方法 | 结果 |
|---|---|---|
| markdown 残留（`**` / 反引号 / 行首 `>` / 行首 `#`） | grep，两份稿 | journal **0 命中**；techrep 仅 lstlisting 代码块内的 `#`（合法） |
| 引号字符一致性 | python 逐行扫描 | 两份稿**均用 ASCII 直引号**（中文引号 U+201C/D = **0 处**），我插入的内容与全文一致 |
| A3 残留 | `grep "3\.19\|6\.67"` journal | **零命中** |
| 新增内容落盘 | grep + Read 逐段复核 | A1/A2/A3×3/B①/B③/B④/B⑤ **全部落盘**，无一处未写入 |
| 新引用的数字 | 逐个回查产物 CSV | 50.37 / 36.50 / 50.39 / 50.67 / 9.32 / 10.89 / 9.07 / −2.2318 / −2.3881 / +0.1563 **全部逐位命中** |

**未做的（因未编译）**：交叉引用解析、未定义引用计数、`.log` 错误数。
⇒ **这份稿目前的编译状态是"未验证"，不是"通过"。**

> **改动清单（供你 `git diff` 核对）**：
> `paper/journal/main.tex` —— 7 处（附录表 (a) 块、附录数值行、§3.4 末、§3.4 中、§3.10 开场、§3.3 末新增段、§3.6 新增段）
> `paper/technical-report/main.tex` —— 2 处（门控节新增段、方法学产出节新增 (d)）
