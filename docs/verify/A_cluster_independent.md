# A 簇独立复算（A3–A10）

**执行者**：claude 会话 `claude-e9`（独立复核会话，未参与 A 簇原始审计）
**日期**：2026-09-24
**方法纪律**：本文件的 8 条判定**只从断言出发、只读原始产物**（论文 / CSV / 脚本），
**在对 `docs/INDEPENDENT_REVIEW.md` 一无所知的状态下完成**。
对照节（§12）是在 §1–§11 全部写死之后才补的，补写内容不改动任何一条原判定。
**只读**：未写入 `paper/**`、`experiments_ledger.md` 或任何既有文件；本文件是唯一产物。
**环境**：`/d/anaconda3/envs/pytorch_env/python`，`PYTHONIOENCODING=utf-8`，全程无 GPU。
**复算脚本**：`%TEMP%/verify_A.py`（仓库外临时文件，不污染工作区）。

判定统计：**CONFIRMED 8 / REFUTED 0 / PARTIAL 0 / UNCLEAR 0**。

---

## 1. A3：表 2 三行不是同一个主干实例，且第三行恰是第二行的训练基底

**判定：CONFIRMED**（细节：那个"恰是基底"的等式**只在 ResNet-18 列成立**，见下）

**源文件**
- `paper/journal/main.tex:446-461`（表 2 = `tab:nat`，行在 455/456/458）
- `outputs_cifar100/v3_readout_repair/*ep24*.csv`（每份的 `a_original_fc` 行 = 该次读出适配的**冻结主干原头**）
- `outputs_cifar100/task1_*/alpha_sensitivity.csv`（表 2 第 1 行的来源）
- `results_master.csv`（第 13/39/53 行等；`pos_0.3` 列）
- `experiments_ledger.md:525-580`（§9.7）、`:2030-2110`（§13.12）
- `v3_readout_repair.py:242`（默认主干 = `<ckpt_root>/<model>/best_model.pth`，即 clean 权重）

**算式**
对每一列，把"表 2 第 2 行（读出适配）的主干原头"与"表 2 第 1 行（干净模型）"
逐点比对（7 个 α 点），并在 `results_master.csv` 里查第 3 行的出处记录：

```
第2行主干实例身份 = v3_readout_repair/*ep24*.csv 的 a_original_fc 行（7 点向量）
第1行身份         = task1_*/alpha_sensitivity.csv 的 7 点向量
第3行身份         = results_master.csv 里 pos_0.3 = 26.57 / 24.69 / 6.22 的三条记录
```

**我算出**（7 点向量，α=0 与 α=+0.3 两个关键点列出）

| 列 | 第 1 行（干净模型） | 第 2 行**主干**（a_original_fc） | 第 3 行（全网络鲁棒训练） |
|---|---|---|---|
| SimpleCNN | 59.99 / **28.09**（task1_simplecnn） | 59.99 / **28.09**（`readout_repair_c100_clean_ep24…`，默认 clean 权重） | **26.57**（v3_methods per-layer NAT 3-seed 均值） |
| VGG-11 | 67.09 / **16.92**（task1_vgg11） | 68.05 / **15.98**（`…exp2_vgg11…`，= Exp2 120ep） | **24.69**（Exp3 200ep） |
| ResNet-18 | 72.09 / **2.70**（task1_resnet18） | 72.69 / **6.22**（`…exp3_resnet18…`，= Exp3 200ep） | **6.22**（Exp3 200ep） |

逐列结论：
- **SimpleCNN 列**：第 1 行与第 2 行的主干**是同一实例**（7 点逐点相同，且默认权重路径就是 clean）；
  第 3 行是另一个实例（v3_methods 鲁棒训练）。
- **VGG-11 列**：**三个实例互不相同**（task1 clean / Exp2 / Exp3）——第 2 行不是建在第 1 行上。
- **ResNet-18 列**：第 2 行的冻结主干 = **Exp3 全网络鲁棒训练网络**，而**第 3 行那一格（6.22）
  正是这个网络自己的原头**（`results_master.csv` 第 53 行：`cifar100|vgg11|exp3_robust` 型记录，
  resnet18-exp3 行 `clean 72.7 / pos_0.3 6.22`；`experiments_ledger.md:545` 同值）。
  ⇒ **第三行恰是第二行的训练基底**，逐字成立。

配套旁证（不是我的推断，是项目内部已记录的事实）：`docs/V7_COORDINATION.md:187` 已实测发现
`readout_repair_c100_exp2_vgg11_s4{2,3,4}_ep24_*.csv` 三份的 `a_original_fc` **全是 68.05/15.98**
（同一个 s42 主干，变的只是读出种子）。我的复算与之逐位一致。

**（对完复核报告后）** 见 §10.1。

---

## 2. A4："近似正交可叠加" vs 主稿的"次可加"；49.12 是单种子

**判定：CONFIRMED**

**源文件**
- `paper/journal/main.tex:64`（摘要）、`:131`（贡献 3）、`:713`（小节标题）、`:717`（正文）、`:1274`（结论）
- 全文 grep `次可加` → **main.tex 零命中**（只有 `可叠加` / `近似正交`）
- `论文v3.md:642-680`（含 661/662 行的次可加表）
- `outputs_cifar100/v3_readout_repair/readout_repair_c100_mp_clean_alpha+0.30_n20000.csv`
  （`e_readout_NAT` 行 α=+0.3 列 = 49.12）

**算式**
```
v3 口径：单独增益之和 = Δ架构 + Δ读出 = (42.47−28.09) + (43.38−28.09) = 14.38 + 15.29 = 29.67
        加性预期     = 28.09 + 29.67 = 57.76
        次可加缺口   = 57.76 − 49.12 = 8.64
v5 口径：加性预期 = 28.19 + 15.02 + 18.31 = 61.52 ；缺口 = 61.52 − 48.58 = 12.94
```

**我算出**：缺口 **8.64 / 12.94**（与断言给的 8.6 / 12.9 一致）。
main.tex 仍在 5 处写"可叠加 / 近似正交可叠加"，而 `论文v3.md:655-679` 已改判为
"**两个口径给出的都是次可加，不是相加**"（并附"拿到的是六到七成"的分数更正）。
49.12 的确为**单种子**：`论文v3.md:646/650` 自标"单 seed / 单 seed 这个口径的数"，
产物文件名 `readout_repair_c100_mp_clean_alpha+0.30_n20000.csv` 无种子后缀；
**main.tex 未标单种子**（`:713-721` 全段无口径说明）。

**（对完复核报告后）** 见 §10.2。

---

## 3. A5：中心读数 headroom 是 oracle-α 探针，全文未标注，开放问题却称"可达上界"

**判定：CONFIRMED**

**源文件**
- `v7_probe_audit.py:288-307`（`run_onprotocol`）——决定性的几行：
  ```python
  f = feats[name]
  Xtr, ytr = f["Xtr"][:n_train], f["ytr"][:n_train]     # 探针用"同条件"训练特征
  pA = probe_accuracy(Xtr, ytr, Xte, yte)
  pB = probe_accuracy(clean["Xtr"]…, Xte, yte)          # 对照臂：用 clean 特征训练
  rows.append({… "probe_same_cond": pA, "probe_clean_train": pB, "headroom_same": pA - <自带头>})
  ```
  其中 `Xtr` 由 `run_condition(..., hooks)`（`v7_probe_audit.py:162-173`）在**该条件自己的钩子下**提取，
  即 α=+0.3 的探针是在 α=+0.3 的**训练集特征**上拟合的。
- `outputs_cifar100/v7_probe_audit/ra_onprotocol_main.csv`（列名自己就叫 `probe_same_cond` / `probe_clean_train`）
- `paper/journal/main.tex:256-263`（§2.4 探针定义）、`:301-323`（§3.2）、`:317-321`（脚注）、`:1211`（开放问题 3）

**算式**
```
headroom(α) = probe_same_cond(α) − head_acc(α)
对照臂       = probe_clean_train(α) − head_acc(α)
```

**我算出**（`ra_onprotocol_main.csv`）

| 条件 | probe_same_cond | probe_clean_train | 自带头 | headroom_same | headroom_clean_train |
|---|---|---|---|---|---|
| +0.3 | 48.35 | **18.10** | 28.09 | **+20.26** | **−9.99** |
| +0.4 | 43.22 | 6.55 | 12.32 | +30.90 | −5.77 |
| +0.5 | 36.91 | 3.24 | 6.09 | +30.82 | −2.85 |
| σ=0.3 | 11.37 | 3.18 | 3.87 | +7.50 | −0.69 |

三件事同时成立：
1. 论文引用的头号数字 20.26 来自 **same-cond 探针**（oracle-α）；
2. main.tex **未在任何地方说明探针是在测试同 α 的特征上训练的**——最接近的只有 §2.4 的
   "重训一个线性分类器"与 §3.2 的"在同一条件下比较"（说的是"比较"，不是"探针的训练分布"）；
3. 开放问题 3（`main.tex:1211`）把它当作**"线性可达上界"**用来度量读出适配的差距。

**（对完复核报告后）** 见 §10.3。

---

## 4. A6：Dithering 一节的机理数字全部是 CIFAR-10，却挂在 CIFAR-100 图注的节里

**判定：CONFIRMED**

**源文件**
- `paper/journal/main.tex:893-926`（图注 `:898-906` 写 CIFAR-100，机理段 `:908-922`）
- `outputs/extension5_dithering_vgg11/`（**CIFAR-10**）与 `outputs_cifar100/extension5_dithering_vgg11/`（**CIFAR-100**）
  的同名四件套：`activation_stats.csv` / `flip_statistics.csv` / `arch_dithering_summary.csv` /
  `group_b_noise_injection.csv` / `group_c_pure_noise.csv`

**算式**（每条机理数字都拿两个目录各算一遍，看论文抄的是哪一个）
```
峰度：            classifier_input_kurtosis(condition)
翻转比：          saved_vs_8bit / killed_vs_8bit ；净 = net_gain_vs_8bit
等剂量纯噪声：    group_c accuracy − group_a(num_bits=8) accuracy
最优剂量加性噪声：max_σ( group_b accuracy ) − group_a(8bit)
架构对照：        arch_dithering_summary.gain_4bit_vs_8bit
```

**我算出**（论文括号内数字 → 命中目录）

| main.tex 句 | 论文里的数 | CIFAR-10 目录 | CIFAR-100 目录 | 命中 |
|---|---|---|---|---|
| `:908` 4bit 增益 | +2.84（已标 C100）/ +6.53（已标 C10） | +6.53 | +2.84 | 两者都标了，此处**没问题** |
| `:916` 峰度 clean→8bit→4bit→3bit | 1.486 / 0.110 / 0.214 / 0.384 | **1.486 / 0.1096 / 0.2144 / 0.3843** | 2.5406 / 0.8473 / 0.9802 / 1.0227 | **C10** |
| `:914` 翻转比与净增益 | 4.15:1、净 +653 | **860/207=4.15、+653** | 655/371=1.77、+284 | **C10** |
| `:915` 噪声条件净翻转 | +7 / −19 | **+7 / −19** | −20 / −9 | **C10** |
| `:913` 等剂量纯噪声 | −2.45 / −1.87 | **−2.45 / −1.87** | −3.12 / −2.48 | **C10** |
| `:913` 8bit 叠加最优剂量噪声 | −0.03 | **36.71−36.74 = −0.03** | 16.69−16.91 = −0.22 | **C10** |
| `:920` ResNet-18 免疫 | +0.39 | **+0.39** | −0.21 | **C10** |
| `:921` SimpleCNN 负增益 | −13.68 | **−13.68** | −14.71 | **C10** |

⇒ 机理段 8 个数里 7 个逐位等于 CIFAR-10 目录的值，且与 CIFAR-100 目录的值**全部不同**；
而该节图注（`:901-904`）与数据出处（`group_a_bits_scan.csv`，C100 目录）都是 CIFAR-100 口径。

**（对完复核报告后）** 见 §10.4。

---

## 5. A7：贡献 1 说 headroom "单调放大"，括号里就是反例

**判定：CONFIRMED**

**源文件**
- `paper/journal/main.tex:113-115`（贡献 1）、`:310`（图注"随失真增强而放大"）、`:322-323`（正文"失真越强，headroom 越大"）
- `outputs_cifar100/v7_probe_audit/ra_onprotocol_main.csv` 第 7/9/10 行

**算式**：`headroom = probe_same_cond − head_acc`，取 α=+0.3/+0.4/+0.5 三点看单调性。

**我算出**：48.35−28.09 = **20.26**（+0.3）；43.22−12.32 = **30.90**（+0.4）；36.91−6.09 = **30.82**（+0.5）。
序列 20.26 → 30.90 → **30.82** 在 +0.4 → +0.5 **下降 0.08**。
论文括号里自己写的"（+0.4 时 30.90、+0.5 时 30.82）"就是这条"单调"断言的反例。

**（对完复核报告后）** 见 §10.5。

---

## 6. A8："三种非线性读出的差距远超门槛 3.45 pp"，但其中一种只差 2.85

**判定：CONFIRMED**

**源文件**
- `paper/journal/main.tex:387-409`（六读出表在 `:389-401`，断言在 `:403-404`）
- `outputs_cifar100/v7_readout_capacity/capacity_ladder.csv`（α=0.3 行）

**算式**：`gap = 该读出精度 − torch 线性锚(51.4733)`；与门槛 `Δ_proc = 3.45` 比。

**我算出**（α=+0.3，`capacity_ladder.csv` 第 3 行）

| 读出 | 精度 | 相对线性锚 |
|---|---|---|
| 逻辑回归强 L2 | 49.23 | −2.24 |
| 逻辑回归默认 | 48.25 | −3.22 |
| **k-NN (k=20)** | 38.12 | **−13.35** |
| 线性头（torch，锚） | 51.4733 | — |
| **瓶颈 MLP (128→32)** | 43.38 | **−8.09** |
| **两层 MLP (128→256)** | 48.62 | **−2.85** |

三种非线性读出的差距为 **13.35 / 8.09 / 2.85**；其中 **2.85 < 3.45**，
即它**低于**本文自己立的可分辨门槛，谈不上"**远超**门槛"。
（`main.tex:403-404` 的措辞覆盖面是三种读出全体，因此该断言在第三行上不成立。）

**（对完复核报告后）** 见 §10.6。

---

## 7. A9："6 种分布下…其中 4 种超过 oracle"，实为 5/6

**判定：CONFIRMED**（并且我**从原始 CSV 独立重算了整张表 24 格**，全部复现）

**源文件**
- `paper/journal/main.tex:862-882`（表在 `:866-874`，断言在 `:880`）
- `outputs_cifar100/v5_full_recipe/recipe_simple_cnn_mp.csv`（3 seed × 7 α 点的逐点原始读数；
  列 `acc_clean_head / acc_robust_head / acc_router / acc_softgate / oracle`）
- `experiments_ledger.md:1313-1331`（§13.5 表与判决）

**算式**（我自己反推出的口径，与台账一致后即成复算）
```
① U[±0.3]           : 7 个 α 点等权平均
② 仅正向 / ③ 仅负向 : {0,+0.1,+0.2,+0.3} / {−0.3,−0.2,−0.1,0} 等权平均
④ 双峰              : w(a) = 0.5·N(a;−0.2,0.05) + 0.5·N(a;+0.2,0.05)
⑤ 尾部集中          : w(±0.3) = 0.5，其余 0
⑥ 单峰              : w(a) = N(a;+0.3,0.05)
oracle(a) = max(acc_clean_head, acc_robust_head) 逐 seed 取后加权；每格再对 3 seed 平均
计数规则：acc_softgate(分布) > oracle(分布)
```

**我算出**（左=我的复算，右=论文表）

| 分布 | clean | robust | 硬路由 | 软门控 | oracle | 复现论文表？ | 软门控−oracle |
|---|---|---|---|---|---|---|---|
| ① U[±0.3] | 54.12 | 52.19 | 54.49 | 55.08 | 54.96 | ✅ | **+0.12** |
| ② 仅正向 | 54.53 | 52.85 | 55.49 | 56.07 | 55.87 | ✅ | **+0.20** |
| ③ 仅负向 | 55.43 | 52.25 | 55.10 | 55.50 | 55.56 | ✅ | −0.05 |
| ④ 双峰 | 53.71 | 52.03 | 53.25 | 54.40 | 54.02 | ✅ | **+0.37** |
| ⑤ 尾部集中 | 45.63 | 48.57 | 48.09 | 49.11 | 48.57 | ✅ | **+0.54** |
| ⑥ 单峰 | 44.51 | 49.08 | 48.57 | 49.83 | 49.24 | ✅ | **+0.59** |

**超过 oracle 的分布数 = 5/6**（唯一没超的是 ③：55.50 < 55.56）。
论文 `:880` 写"其中 **4 种**超过 oracle"，与它自己那张表不符。

**（对完复核报告后）** 见 §10.7。

---

## 8. A10："训练端四条路径全部失效"，但 −1.71 落在项目自设的 3 pp 门槛内

**判定：CONFIRMED**

**源文件**
- `paper/journal/main.tex:535`（小节标题）、`:543-547`（图注）、`:551-553`（正文）、`:60`（摘要）、`:1262`（结论）
- `paper/journal/main.tex:219-220`（项目自设门槛："单种子的尾部差值**小于 3 个百分点不作为结论**"）
- `outputs_cifar100/v3_methods/*/alpha_sensitivity.csv`（α=+0.3 行）
- `outputs_cifar100/v3_methods/_summary.csv`

**算式**：`Δ = acc@+0.3(路径) − acc@+0.3(均匀基准 26.03)`；`|Δ| < 3 → 落在门槛内，不构成结论`。

**我算出**（四路径 + 相关轮廓）

| 路径 | @+0.3 | Δ vs 基准 26.03 | 是否落在 3 pp 门槛内 |
|---|---|---|---|
| 采样预算分配（最好轮廓 `budget_sens`） | **26.13** | **+0.10** | **是**（且**高于**基准） |
| α 空间对抗训练（PGD，`adv_uniform`） | 24.32 | **−1.71** | **是** |
| 课程式采样 | 21.78 | −4.25 | 否 |
| 训练范围外推 ±0.5 | 20.48 | −5.55 | 否 |
| （附）预算轮廓 `budget_rear` | 25.46 | −0.57 | 是 |

四条路径**全部是单种子**（图注 `:546` 自标"单种子"），而项目自己的规则（`:219-220`）恰恰是
"单种子的尾部差值小于 3 个百分点不作为结论"。⇒ 四条里有**两条**（+0.10 与 −1.71）
按本文自己的纪律**不能判定为"失效"**；只有 −4.25 / −5.55 两条越过了门槛。
"全部失效"因此是**超出自身判据的强断言**。

**（对完复核报告后）** 见 §10.8。

---

## 9. 复算时点与"活稿"警告

- 我核实的是 `paper/journal/main.tex` 的 **1315 行 / 80232 字节**版（mtime 16:02）。
  本文件 §1–§8 的行号都属于那一版；**每条同时给了引文原文**，可据此在任何新版里重新定位。
- 写本报告期间该文件被另一会话持续修改到 **1442 行**（16:16）。行号会漂，不要复用行号。
- 复核报告 `docs/INDEPENDENT_REVIEW.md`（688 行，mtime 15:43）的 `journal:NNN` 锚点对应**更早**的一版
  （它把 A9 标在 `journal:824`，我核的那版在 `:880`，现版在 `:929` 附近；A6 差 56 行）。
  **引用该报告时不要复用它的行号**，它的内容我逐条核过（见 §10）。
- 我最后一次读取（16:16–16:17）时，A3–A10 的修改**已经或正在落地**：`近似正交可叠加` / `3-seed 的 42.47` 类表述、
  `其中 4 种超过 oracle`、`远超门槛`、`没有一条超过基准`、`全部不优于`、`全部失效` 都已不在文中；
  `次可加` 出现 1 次、`不可裁决` 3 次、`趋平` 2 次、`种还超过 oracle` 1 次。
  **唯一残留的强断言**是讨论节 `main.tex:1190`「而参数空间的四条路径**全部无效**」（A10 类）。

---

## 10. 对照节（先算后对）

> §1–§8 全部写死（16:1x）之后才读的 `docs/INDEPENDENT_REVIEW.md`，逐条对照如下。

### 10.1 A3 — 一致
复核报告（`:116-143`）说：表 2 三行不是同一个模型；第三行是第二行的训练基底（ResNet-18）；
b 臂表（它标 `:481-490`）与摘要用的是正确配对 15.98/6.22；表 2 隐含的 ResNet-18 增益 24.12，
比真实配对增益 20.60 高 3.5 pp。
我的复算：**逐条一致**——24.12 = 26.82 − 2.70、20.60 = 26.82 − 6.22、差 3.52，与我 §1 的实例表同源。
（现版状态：16:16 已改——表 2 第一行变成「**原始头**（读出适配所依附的主干）28.09/15.98/6.22」并加了口径表注。）

### 10.2 A4 — **有一处不一致（复核者算错）**
复核报告（`:147-178`）说：主稿已改判次可加（8.64/12.94）；49.12 是单 seed；
**且期刊把 49.12 与"3-seed 的 42.47"、"3-seed 的 43.31"相减，违反自订种子纪律**。
- 一致部分：次可加缺口 8.64 / 12.94 我独立算出同值；49.12 单 seed 我独立确认。
- **不一致**：`42.47` **不是 3-seed 的读数**。它是
  `outputs_cifar100/v3_methods/simple_cnn_mp_clean_uniform/alpha_sensitivity.csv` 的单次运行
  （默认种子）；同族 `..._s43` = 38.77、`..._s44` = 39.64，**3-seed 均值是 40.29（总体 std 1.51）**。
- 由此，"期刊违反种子纪律"这个推论也要降级：49.12 与 42.47 出自**同一块主干、同一实例**
  （两个文件的 `a_original_fc` 都是 60.99 / 42.47），这一对相减 +6.65 反而是该段里种子口径最干净的一对。
  真正**并存**的是 49.12（1 seed）与正文通行的 43.31（3 seed）——这一点报告自己也写了（5.74 vs 5.81），
  我只改它前半句。
- 报告另一句"43.38 也是单 seed"✓ 我确认。

### 10.3 A5 — 一致
报告（`:182-214`）说：headroom 由 `probe_same_cond`（oracle-α）算得；`probe_clean_train` 在 +0.3 只有 18.1，
比自带头 28.09 还低；"约 4 个百分点"出自过期口径（`论文v3.md:525` 的 47.89 / 44.09），
现值两两相减 5.04 / 8.16 / 12.41，"没有一对是 4"。
我的复算：**全部一致**（我算 48.35 − 43.31 = 5.04，同值）。无新增。

### 10.4 A6 — 一致，且我把命中集从 5 个数扩到 8 个
报告（`:218-254`）列了 5 个命中 C10 的数。我另外复核出**另外三个数也全是 C10**：
等剂量纯噪声 **−2.45 / −1.87**（C100 应为 −3.12 / −2.48）、
8 bit 叠加最优剂量噪声 **−0.03**（C100 为 −0.22）、
噪声条件的净翻转 **+7 / −19**（C100 为 −20 / −9）。
⇒ 机理段命中 C10 的数是 **8 个**，不是 5 个（论文该段一共就 8 个量化读数）。
（现版状态：16:16 已加"**先标注数据集**"与 CIFAR-10 口径限定。）

### 10.5 A7 — 一致
报告（`:258-274`）说 30.82 < 30.90，并引台账已记此细节。我的复算同值；
台账那句我核到了：`experiments_ledger.md:2977`「（唯一同向的是 (+0.4, +0.5) 对，因 $H$ 在该处微降 30.90 → 30.82。）」
——报告写 `:2970`，实际 `:2977`（差 7 行，不影响实质）。

### 10.6 A8 — 一致
报告（`:278-293`）的三个差 −13.35 / −8.09 / −2.85 与我的复算逐位一致；
对 $-2.85 < 3.45$ 的定性（"这一臂按本文自订规则不可裁决"）我也认同。
（现版状态：16:16 已按门槛分开表述。）

### 10.7 A9 — 一致，且我的复算更深
报告（`:297-310`）说 5/6，并指错误源自台账 §13.5 且被论文与 `coverage-audit.md` 继承。我：**一致**
（台账出处我也独立追到 `experiments_ledger.md:1332` 一带的「且 4/6 种下超过…oracle」）。
差异在**深度**：报告做的是"把论文表的六行两两比大小"；我是**从 `recipe_simple_cnn_mp.csv` 的逐 seed 原始行
反推六种分布的加权口径、把整张表 24 格重算**，全部复现（±0.01）。⇒ "5/6" 建立在一张被独立复现过的表上。

### 10.8 A10 — 一致
报告（`:314-334`）说四条为 +0.10 / −1.71 / −4.25 / −5.55，只有后两条能称"失效"。我：**一致**。
我另补：预算轮廓 `budget_rear` = 25.46（−0.57）也在门槛内——按"全部六次运行"算是 3 条带内。

---

## 11. 我算完之后，与复核报告不一致的地方

1. **A4 的"3-seed 的 42.47"是错的**（`INDEPENDENT_REVIEW.md:169`）。42.47 是单次运行；3-seed 均值是
   **40.29**（s43 = 38.77、s44 = 39.64）。据此推出的"期刊违反种子纪律"不成立（详见 §10.2）。
   这是本次对照里唯一一处**复核者算错**。
2. **A3 的"三行分别是三个不同的模型"**（`INDEPENDENT_REVIEW.md:128`）在**两列**上不成立：
   - SimpleCNN 列：第 1 行与第 2 行的主干**是同一块**（都是 clean；59.99/28.09，7 点逐点相同）；
   - ResNet-18 列：第 2、3 行是同一块；
   - 只有 VGG-11 列是三块互不相同。
   它的下一句（"第三行是第二行的训练基底"）承认了后者，所以是前后两句话打架，不是结论错。
   但这条影响**修表方式**：报告建议的"第一行统一改成 28.09 / 15.98 / 6.22"对 SimpleCNN 列是 no-op，
   而现版采纳后把 task1 那组数（16.92 / 2.70）整组移到了表注里——我核到 `main.tex:495-500` 的处理**与我的复算口径一致**，
   所以只是理由要写准，不是改法有问题。
3. **行号**：报告的全部 `journal:NNN` 锚点对应一版更早的稿子（见 §9）。引用时需重新定位。

---

## 12. 复核者没发现、但我发现的问题

1. **（最重要）SimpleCNN 的"全网络鲁棒训练（最好）"不是最好。**
   `results_master.csv:39`：`cifar100|simple_cnn|task3_Exp3_FullRobust|7pt`，clean 60.82、
   **`pos_0.3` = 29.89**，`pairing_check` PASS、120 ep、CIFAR-100 —— 比表 2 第 3 行 SimpleCNN 格的
   **26.57 高 3.32 pp**。而该行另外两格（VGG-11 24.69 / ResNet-18 6.22）**本来就取自 task3/ext6 harness**
   （`experiments_ledger.md:537/545`），SimpleCNN 格却取自 `v3_methods` 的 3-seed 均值
   （26.03 / 26.73 / 26.95 → 26.57）。⇒ "同一行跨列混 harness"这件事在**修表前**就存在。
   影响：读出适配相对"全网络最优"的领先从 **+16.74（43.31 − 26.57）缩到 +13.42（43.31 − 29.89）**；
   方向不变（仍 > 13 pp），但"**最好**"这个标签在那一格不成立。
2. **读出文件与主干 `metrics.json` 的恒等式在 ResNet-18 上差 0.01。**
   项目自订的核验手段是"`a_original_fc`@α=0 必须等于该主干 `metrics.json` 的 `best_test_acc`"
   （`docs/V7_COORDINATION.md:187`）。实测：
   SimpleCNN **59.99 = 59.99** ✓、Exp2-vgg11 **68.05 = 68.05** ✓、
   **Exp3-resnet18 读出文件 72.69 ≠ `outputs_cifar100/extension6_deep_robust/resnet18_exp3/metrics.json` 的 72.70** ✗。
   现版新加的表注写"实测 59.99 / 68.05 / **72.70**，逐位一致"——第三个数取自 `metrics.json` 而非读出文件，
   **"逐位"不成立**。建议写 72.69，或注明"两条评估路径在该主干上差 0.01"。
3. **A4 的叠加配置其实有 3 个种子可用，论文只引单 seed 且不标口径。**
   `v3_readout_repair/readout_repair_c100_mp_{clean,s43,s44}_ep24*.csv` 的 `e_readout_NAT`@+0.3 =
   **49.12 / 48.82 / 49.59 → 均值 49.18（总体 std 0.32）**。用 3-seed 均值算次可加缺口是
   `57.76 − 49.18 = 8.58`（vs 单 seed 的 8.64）——**结论不变**，但口径应披露（主稿 `论文v3.md` 披露了，期刊版没有）。
4. **A6 的命中集比报告大**：另有 3 个数也是 C10（§10.4）。
5. **（小）A10 的带内值不止 1 个**：`budget_rear` = 25.46（−0.57）也在 3 pp 门槛内。

---

## 13. 末尾四件套

**里程碑**：A3–A10 独立重算 —— 8 条全部**独立判真**（先算后对，未采信复核报告的任何推导），
并额外从原始逐 seed CSV 重算了 6 分布门控表**全部 24 格**。

**关键数字 + 出处文件**（下列每个数都是我自己算的，不是转引）

| 编号 | 判定 | 我算出的关键值 | 出处 |
|---|---|---|---|
| A3 | CONFIRMED | 读出所依附主干原头 = **28.09 / 15.98 / 6.22**；第 3 行 = 26.57 / 24.69 / **6.22** ⇒ ResNet-18 列第 2、3 行同块 | `v3_readout_repair/*ep24*.csv` 的 `a_original_fc` 行；`results_master.csv:39/53`；`experiments_ledger.md:537/545` |
| A4 | CONFIRMED | 加性预期 **57.76 / 61.52**；实得 49.12 / 48.58 ⇒ 缺口 **8.64 / 12.94**；49.12 单 seed | `论文v3.md:661-662`；`readout_repair_c100_mp_clean_alpha+0.30_n20000.csv` |
| A5 | CONFIRMED | **20.26 = 48.35 − 28.09**（same-cond 探针）；对照臂 18.10；σ=0.3 处 7.50 | `v7_probe_audit/ra_onprotocol_main.csv:7/18`；`v7_probe_audit.py:288-307` |
| A6 | CONFIRMED | **8 个**机理读数逐位命中 C10 目录；C100 对应值全不同（如 +0.39 vs −0.21） | `outputs{,_cifar100}/extension5_dithering_vgg11/*.csv` |
| A7 | CONFIRMED | 20.26 → 30.90 → **30.82**（−0.08） | `ra_onprotocol_main.csv:7/9/10` |
| A8 | CONFIRMED | 三个非线性读出的差 −13.35 / −8.09 / **−2.85 < 3.45** | `v7_readout_capacity/capacity_ladder.csv:3` |
| A9 | CONFIRMED | 软门控−oracle：+0.12 / +0.20 / **−0.05** / +0.37 / +0.54 / +0.59 ⇒ **5/6** | `v5_full_recipe/recipe_simple_cnn_mp.csv`（自推口径重算全表） |
| A10 | CONFIRMED | Δ = **+0.10** / **−1.71** / −4.25 / −5.55（基准 26.03） | `v3_methods/*/alpha_sensitivity.csv` |

**判据命中哪个分支**：**CONFIRMED 8 / REFUTED 0 / PARTIAL 0 / UNCLEAR 0**。
两处"核心成立但细节有出入"（A3 的"第三行=第二行基底"只在 ResNet-18 列成立；A4 的 8.6 / 12.9 我算出 8.64 / 12.94）
按判定规则仍归 CONFIRMED，出入已写在正文与 §11。

**有没有触发红线**：**无**。
- 未写 `paper/**`、`experiments_ledger.md` 或任何既有文件；唯一写入是本文件（新建）。
- 未碰 GPU；复算只在本地 Python 上读 CSV/脚本。
- 未在算完之前读 `docs/INDEPENDENT_REVIEW.md`（§1–§8 先写死，§10 对照节后补）。
