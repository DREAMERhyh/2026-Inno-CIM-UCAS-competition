# 第七阶段多会话协调规则

> 本阶段由**多个 Claude Code 会话并行**推进。这份文件是硬规则，任何会话动手前先读它。
> 立此文件的直接原因：三条会话在**同一个仓库**里干活，没有领地划分必然互相覆盖。

---

## 一、角色与在跑的工人

> ⚠ **本节于 2026-09-25 16:25 第四次重写**。总管会话换名史：
> `claude-e9`（上下文占满终止）→ `claude-08` → `claude-0b` → `claude-a3` → `claude-58 [e39964]` → **`claude-58 [436705]`（现任）**。
> **角色比名字重要**，且**会话名会变**——以 `ListAgents` 实时列表为准。
>
> ⚠ **两条会让人误判的史实**：
> ① **2026-09-24 18:35 与 2026-09-25 早上各重启过一次机器**，**重启前的会话全部不存在了**
>    （`claude-4c` / `89` / `38` / `6f` / `65` / `cf` / `cb` / `0b` / `a3` …）。
> ② **这些会话的历史产出没有丢**——A/B/C/D/E 五组论文修复、两次体检、M0c–M12 各里程碑
>    **都已在仓库里**（见 §六 状态板与 §一·补）。**消失的是会话，不是成果。**

| 会话 | 角色 | 当前任务 | 状态 |
|---|---|---|---|
| **`claude-58 [436705]`** | **总管**：派活、独立复算校验、协调台账、**维护 `paper/` 与 `docs/`** | 盯 M4 收尾（s44 训练 + 读出衔接脚本） | 在岗 |
| `claude-e2 [2abb68]` | 未分配 | 空闲 | idle |
| ~~`claude-4c` / `89` / `38` / `6f` / `65` / `cf` / `0b` / `a3`~~ | — | 已随重启消失；**成果在仓库** | 不存在 |

> **当前真正"在跑的东西"是三个，不是会话**（详细命令见 `docs/HANDOFF.md` §0 / §2·补）：
> ① M4 的 s44 训练（pid 10820）② 训练守护 `tools/v7_m4_watchdog.ps1`（pid 19356）
> ③ **读出衔接 `tools/v7_m4_readout.ps1`（pid 16608，2026-09-25 16:21 起）**。

### 一·补、本轮（2026-09-24 16:20–17:00）的产出与裁决

| 项 | 结果 |
|---|---|
| **A1/A2** | ✅ 总管已修（`781d8c2`）；A2 的**第三处漏网**由 claude-4c 补上（`main.tex:1191`） |
| **A3–A14** | ✅ claude-4c **12/12 CONFIRMED**，检查器退出码 0（journal 25 页）。**它否证了复核者两条建议值**（A13 的"1/5~1/30"、A9 的口径），理由都对 |
| **A3–A10 独立重算** | ✅ claude-38 **8/8 确认**，**并推翻复核者的 A4 论证**（42.47 不是 3-seed）——**总管的急件拦住了正按错误理由改论文的写者** |
| **A9 计数错** | ✅ 已改（台账 + 论文 + `coverage-audit.md` + `第五阶段报告.md` 四处） |
| **8 张图** | ✅ 已修并逐张复看（`f4e282d`）；取数逐位未变 |
| **三条新问题** | ✅ claude-38 发现、claude-4c 复算确认：表 2 第三行 SimpleCNN 那格 26.57 应为 **29.89**；"逐位一致"在 ResNet-18 上差 0.01（72.69 vs 72.70）；A6 命中 8 个 C10 数不是 5 个 |
| **🔴 `nat` 参考文献** | **结构上不存在**——见下 |

### 一·补二、🔴 待用户裁决：`nat` 参考文献

`paper/*/main.tex` 与 `ucas-cod-lab-report/main.tex` **四份文档**里的 `\bibitem{nat}`
（Zhu M 等, *Nonlinearity-aware training…*, IEEE TCAD 2022, **41(11): 3961–3973**）：

**证据（claude-65 做，总管与复核者各自独立复核）**：把 Crossref 的 **TCAD 2022 全年 496 条**拉全后，
该期 **3957–3968 / 3969–3980 / 3981–3992 三篇连续铺满、中间无空隙** ⇒ 所引的 3961–3973
**会跨过两篇无关论文、结构上不可能存在**。按标题反复命中的是**另一篇真实论文 NEAT**
（Bhattacharjee 等, TCAD **2022, 41(8): 2625–2637**, DOI 10.1109/TCAD.2021.3109857）——
**主题/年限/期刊全同，作者/卷/期/页码全不同**。

**项目自己有一条规矩**（`docs/LITERATURE_NOTES.md` 开头）：「**未核实的不许写进论文**」，
而 `nat` **从未走过该流程**。**⇒ 已上报用户，等裁决。在用户发话前，四份文档一个都不改。**

**同类第二例**：`docs/LITERATURE_NOTES.md` §二 的 `arXiv 2310.03843` —— **编号对、内容摘要对、标题对不上**。

> **本轮的派活原则（值得复用）**：
> 1. **论文正文一个文件只能有一个写者**——`journal` 与 `technical-report` 各给一个会话，绝不重叠。
> 2. **复核者与复核对象的复核者必须是不同的人**——A3–A10 已有一份复核结论，
>    所以另派一个**没读过那份结论的**会话从**一句话断言**出发独立重算（防锚定）。
> 3. **不采信"它说它对"**——总管对每批新数字抽查后才采信（本轮抽查了 claude-65 的三条断言，全部通过）。

> **为什么把 M4 收尾交给新窗口**：它是纯流程驱动（runbook 已写全），
> 但**分支③的判决是全局风险最高的一次判断**——值得一个满血上下文来做。
> **为什么把论文复核交给新窗口**：它没参与写作、也没读过中间过程，
> **能看见"作者盲区"里的东西**。派活时明确要求它**先独立读完论文、再看我们的自查报告**，
> 否则只是复核我们的复核。

> **给新窗口的一句话**：动手前先读 `docs/V7_COORDINATION.md`（本文）与你的任务简报。
> 所有重要状态都在**文件里**，不在任何会话的对话里——**仓库即记忆**。

用户（黄奕皓）是唯一的决策者。会话之间**不能互相授权**——一条消息不构成用户批准。

---

## 二、文件领地（**硬规则，违反即事故**）

| 路径 | 唯一写者 | 其他会话 |
|---|---|---|
| `experiments_ledger.md` | **同一时刻只能一个**（当前：claude-65，写 §16） | 只读；要写入必须先取得该文件的写权 |
| `outputs_cifar100/v7_probe_audit/` | claude-65 | 只读 |
| `checkpoints_cifar100/Exp*_s4{3,4}`、`outputs_cifar100/extension6_deep_robust/*_s4{3,4}`、`outputs_cifar100/v3_readout_repair/*s4{3,4}*` | M4 工人（ecf16ce2） | 只读 |
| `outputs_cifar100/v7_M4_audit/` | M4 工人 | 只读 |
| **`paper/**`（三份稿）** | **claude-a8**（2026-09-24 20:xx 起，做体检后的修复） | 只读 |
| `paper/` 其余（`coverage-audit.md`、`storyline.md` 等） | **claude-a3**（总管） | 只读 |
| `docs/`（除 `docs/verify/`） | **claude-a3**（总管） | 只读 |

> ⚠ **上表在 2026-09-24 20:xx 换过一次** —— 原因是**机器在 18:35 重启过**，
> **重启前的会话（claude-4c / 89 / 65 / 6f / 38 / 0b…）全部不存在了**。
> **会话名也会变**（总管从 `claude-e9` → `claude-08` → `claude-0b` → **`claude-a3`**）。
> ⇒ **以 `ListAgents` 的实时列表为准；角色比名字重要。**
| **`docs/verify/`** | **共享区**——每个会话**只写自己那一个文件**，文件名已在各自简报里写死 | 别人的文件只读 |
| `results_master.csv` | 由 `build_ledger.py` 重建——**重建前必须确认没有别的会话正在写产物** | — |

> ⚠ **2026-09-25 16:25 现状**：上表列出的写者会话（`claude-65` / `cf` / `a8` / `a3`）**都已随重启消失**。
> **当前唯一在岗会话是总管 `claude-58 [436705]`** ⇒ `paper/**`、`docs/**`、台账**目前都由总管临时接管**。
> 🔴 **派新工人之前，必须先把对应行的写者改成新会话名，再发简报** —— 否则简报里的"独占写者"是假的。

**为什么台账要单写者**：多个会话同时编辑同一个 markdown 会互相覆盖，而台账是本项目最权威的一手记录，
一旦损坏，前六个阶段的全部预登记判据都失去依托。

> ⚠ **"写入"包括改头部注记**（2026-09-24 自曝）：我在 claude-65 持锁期间改了台账的头部状态行，
> 理由是"只动头部、不碰 §16 正文"。**这不成立**——单一写者的对象是**文件**，不是文件里的某一段。
> 实测这次没出事（两处改动不重叠，`git diff` 确认：我的在 3–6 行、它的在 4025 行之后），
> 但那是运气，不是规则。
>
> **改正后的做法**：要改台账的**任何一行**（含头部、含错字），
> **要么先向持锁者要锁，要么把改动内容发给持锁者让它代改。**
> 本轮我采用了后者：头部改动留在工作区，由 claude-65 随它的 §16.7 一并提交，并已告知它。

**台账的汇总流程**：工人**不直接写台账**时，把结果写进自己的 `REPORT.md`，
由 claude-e9 在工人停机后**串行**并入 §16，并在 commit message 里注明来源会话。

---

## 三、并行度的真正上限（不是会话数）

1. **GPU 串行**——项目出过三次事故（一次 CUDA OOM、两次 Windows 提交内存耗尽 error 1455）。
   **同一时刻只能有一个训练在跑。** M4 在跑期间，其他会话不得碰 GPU。
2. **台账写入串行**——见上。
3. **产物零覆盖**——新产物一律带 `_v7` / `_s43` / `_s44` / `_smoke` 后缀；跑前核验目标目录不存在。

⇒ 实际可并行的形态是「**一个跑 GPU 的 + 若干做零 GPU 的**」，不是"同时开五个都去训练"。

### 3.4 ⚠ 新增红线：**设备口径也是口径**（2026-09-24 实测）

**本项目的规范数字全部是 CUDA 产物**：`v3_probe_info_death.py` 的原始表、
`task1_simplecnn` 的 59.99 / 28.09，**都是 CUDA**。

实测（同一权重、同一口径、CPU vs CUDA 双向对照）：

| 设备 | α=+0.3 的 head | 与 v3 的差 |
|---|---|---|
| **CUDA** | **27.76** | **0.00（逐位）** |
| CPU | 27.77 | 差 **1 张样本**（0.0125 pp = 1/8000） |

⇒ **`head` 是纯前向的确定性量，但跨设备不逐位相同**（特征张量实测 \|Δ\|max ≈ 4e-3）。

**规则**：**任何要与既有数字并列的结果，必须在同一设备上产出。**
把 CPU 读数混进一张 CUDA 表，与红线 7「跨协议数字不可移植」是**同一类错误**，
只不过混的维度是设备而不是脚本。

**操作细则**：
- 重跑/复算既有产物 → **跟着原产物的设备走**，不要图省事换设备
- 新表内部必须同设备；跨设备的数字只能定性对照，不能并列相减
- **判据写"必须逐位一致"时，先确认设备相同**——否则判据物理上不可满足

---

## 四、通信协议

### 派活简报必带六件套
1. 任务编号（对应 `docs/NEXT_STAGE_TASKS.md` 的里程碑）
2. 先读清单
3. **预登记判据 + 分支判决**（跑前写死）
4. 产物路径
5. 红线
6. 文件领地

**判据不写清的，不派。**

### 四·补、🔴 **派"审查/复核"类任务，先过人员独立性**（2026-09-26 新立）

**立这条的直接原因**：总管把一个"独立体检查论文"的任务派给了
**上一任总管 `claude-58 [e39964]`——也就是这份论文的作者**。
对方**拒绝了**，理由是：*"我的作者盲区是全场最大的那个。让我做这份体检，
等于让你最想看的那件事彻底失败。"* **它是对的。**

> **一份"看起来独立、实际是作者自我复核"的报告，会被当成独立证据使用。**
> **那是把作者偏见洗成独立结论——比"没人审"更糟。**

**判据（派活前逐条核）**：

| 要审的东西 | 出局条件 |
|---|---|
| 论文 / 文档 | **参与过 `paper/**`（或该文档）的任何改动** |
| 台账 | 写过该节 |
| 实验结论 | 跑过该实验、或做过它的判决 |

**核验手段（不能凭会话名猜）**：

```bash
# ① 该会话是谁？—— ListAgents 的名字会变，且不显示对话起点
cat ~/.claude/sessions/*.json     # 拿 sessionId + pid
# ② 它有多久历史？—— transcript 的首条时间戳 = 这条对话真正的起点
head -c 400 "~/.claude/projects/<proj>/<sessionId>.jsonl" | python -c "..."
#    · 首条是「项目交接指令（第 N 阶段…）」⇒ 它就是那一任总管，出局
#    · transcript 不存在 / 首条是今天的普通提问 ⇒ 干净
# ③ 谁动过 paper/？
git log --oneline -- paper/
```

⚠ **`ListAgents` 的 "started Xm ago" 是「进程启动时刻」，不是「对话起点」** ——
被测会话的进程重开过，界面上就显示成"刚启动"，**看起来像全新的，其实聊了两周**。
本次就是靠 transcript 首条时间戳才定下来的。

⇒ **新增第 7 件套：人员独立性判据 + 核验证据（首条时间戳 / `git log`）。**
**不写清的，同样不派。**

### 工人回报格式（固定四段）
```
里程碑：
关键数字 + 每个数字的出处文件：
判据命中哪个分支：
有没有触发红线：
```

### 顾问（claude-e9）的校验动作
不采信"它说它对"。每批新数字进来必做：
| 校验项 | 工具 |
|---|---|
| 台账重扫是否自洽 | `build_ledger.py` → 行数 + `pairing FAIL` |
| 派生量算得对不对 | `tools/recalc_mean7.py` |
| 数字有没有出处 | 反向检索（抽数值 token 去源材料找） |
| **有没有混协议** | `docs/IMPL_AUDIT.md` 的方法：产物路径 → 归属实现 → 查跨实现并列 |

---

## 四·补、自主工人的正确开法（2026-09-24 实测通过）

**用户已明确授权**：可以给工人预先授权，让它们不必逐条确认就能跑命令。

**可用命令**（两条实测都通过，全程零权限提示）：

```bash
# 最小（纯读 + 跑命令）
claude --bg --allowedTools="Bash" "<简报>"

# 完整（工人要写自己的报告）
claude --bg --allowedTools="Bash,Read,Write,Edit,Glob,Grep" "<简报>"
```

**两个必须知道的细节**：

1. **必须用 `=` 号连接**。`--allowedTools` 是可变参数（`<tools...>`），
   写成 `--allowedTools "Bash" "简报"` 会把**简报当成第二个工具名吞掉**，
   会话起来后是空的（`idle — send a prompt to start`）。**实测踩过。**
2. 实测返回：三条 Bash 命令（`pwd` / `ls` / `echo`）连跑 + `Write` 写文件，
   全部免确认，`Worked for 8s · done` / `Cogitated for 6s · done`。

**但授权不等于放开纪律**——领地划分（§二）与红线（§三）仍然逐条有效，
而且**恰恰因为工人现在能自主写文件，领地划分变得更重要**。
派活时必须在简报里写死：**独占写哪些路径、绝对不碰哪些路径**。

**仍然不要做的事**：不要用 `--dangerously-skip-permissions`。
`--allowedTools` 是**枚举授权**（列什么给什么），而后者是**全量绕过**（含权限判定本身）。
用户授权的是前者，不是后者。

---

## 五、已知的坑（实测所得，别再踩）

| 坑 | 事实 | 应对 |
|---|---|---|
| **`claude --resume` 用短 id 会卡死** | background 会话有两个 ID：`id`（给 `logs`/`stop`）与 `sessionId`（短 id + UUID） | `--resume` **必须用完整 sessionId**，且前提是会话已停止 |
| **`--bg` 会话是单向的** | 我开得出、读得到日志、停得掉，但 `SendMessage` 到不了它 | 简报必须**一次写死**；靠 `--bg --resume <完整sessionId>` 续接追问 |
| **两个版本不一致** | PATH 上的 `claude` 是 npm 版 v2.1.246；VS Code 扩展自带的是 v2.1.280 | 同一批任务固定用一份，不跨版本 |
| **跨会话来消息需要用户批准** | 当两个会话权限模式不同时，接收端会 hold 消息等用户点 | 这是设计如此，不是故障 |
| ⚠ **`--bg` 工人不预授权就会卡死** | 实测：我派出的 M4 工人在执行 `cd *` 时弹出"Do you want to proceed?"，而 headless 后台会话**没有人能点"是"**，于是永久卡住 | **见下方「自主工人的正确开法」——已解决** |
| ⚠ **`--allowedTools` 是可变参数，会吞掉提示词** | `claude --bg --allowedTools "Bash" "提示词"` → **提示词被当成第二个工具名吞掉**，会话起来后是空的（`idle — send a prompt to start`） | **必须用 `=` 号**：`--allowedTools="Bash,Read,..."` |
| 🔴 **`joblib` 的 `n_jobs=-1` 会复现红线 2 的历史事故** | M0c 首次冒烟炸出 `OSError: [WinError 1455] 页面文件太小` + `Error loading torch\lib\cufft64_11.dll`。根因：`LogisticRegression(n_jobs=-1)` 在 20 核机上让 joblib 开 **20 个子进程**，Windows 下每个子进程重新 import `__main__` → 每个加载一整套 torch DLL → 提交内存打爆 | **一律用 `n_jobs=None`（串行）**。这条已写进 M0c 的执行条件。**任何 sklearn/joblib 的多进程默认值在使用前都要检查** |
| **内存余量薄** | 实测：物理 15.7 GB / 可用约 3.7 GB；提交 48.15 GB / 可用约 9.32 GB；页面文件 32.45 GB（峰值 7.41 GB）。读数随负载波动很大（事故当时是 0.9 / 4.9 GB） | 开新任务前先看提交内存；**不要同时开多个吃内存的活** |
| 🔴 **不要在 `.tex` 里混写 markdown 语法** | **本阶段踩过三次**（均 claude-e9 犯）。两种残留的失效方式不同：<br>· **反引号**：LaTeX 里是开引号 → 首次踩时编译报 6 个错（`Missing $ inserted`、`Extra alignment tab`）<br>· **`**加粗**`**：`*` 在文本模式只是普通字符 → **完全不报错，静默渲染成字面的星号**（第三次踩，journal 6 处 + technical-report 1 处） | 编辑 `.tex` **一律只用 LaTeX 宏**（`\textbf{}` / `\texttt{}`）。<br>**但不要靠记性**——手动检查已验证会漏（第三次仍漏 1 处）。<br>⇒ **提交 `paper/` 之前必须跑 `tools/check_papers.py`**（退出码 0 才提交）。它做四件事：扫 markdown 残留 + 编译两遍 + 数错误 + 数未定义引用。 |
| ⚠ **"先编译后提交"必须检查编译结果** | 实测踩过：我把 `xelatex` 与 `git commit` 写在同一条命令里，`xelatex` 报了 6 个错、提交照样执行了 —— **把一个编译不过的版本提交了** | **提交前显式确认 `grep -c '^!' main.log` 为 0**，不要靠"跑过编译"就当作通过了 |
| 🔴 **`v3_readout_repair` 的 `--backbone_tag` 命名会骗人** | 实测（M4 准备工作时查出）：`readout_repair_c100_exp2_vgg11_s4{2,3,4}_ep24_*.csv` 三个文件的 `a_original_fc` **全是 68.05/15.98** —— **同一个 s42 主干**，它们变的只是**读出种子**。但 tag 里写着 `s43`/`s44`，**读起来像"主干种子 43/44"**。而 M4 新训的 s43 主干 best = **67.86**（不同） | **新主干一律用 `bs<seed>`（backbone seed）区分**，如 `exp2_vgg11_bs43_ep24`。**不要用 `s43`**（那个已被读出种子占用）。核验手段：跑完读出后，`a_original_fc` 在 α=0 的值必须等于该主干 `metrics.json` 的 `best_test_acc` |
| ⚠ **M4 的耗时被低估了两次，两次都是同一个错** | **第一次**：VGG-11/Exp2 120ep 实测 **110 分钟/次**（初估按冒烟 52s/epoch 外推到 ~60 分钟）。**第二次**：`docs/HANDOFF.md` 又写"resnet18 200ep ≈ 2.4 小时"，那是按 **5.45 it/s 的纯训练吞吐**（71.8 秒/epoch）外推的，**漏了每 epoch 的验证开销**。**2026-09-24 16:07 实测：419 秒走 3.98 个 epoch = 105 秒/epoch** ⇒ **单次 200ep ≈ 5.8 小时** | **排期一律按实测**。且**注意两条推论**：<br>① **吞吐数字（it/s）不是 epoch 时间**——后者含验证；<br>② **run4 会跨到次日凌晨**（见下），不要假设"白天会完" |
| **M4 收尾的三条操作要点**（claude-65 查出，已核实） | 收割器**不需要改**（M4 的数据源是 `v3_readout_repair/*.csv`，第 273–297 行已在 glob 它）。**但有三个坑**：<br>**(a)** `build_ledger.py` 第 291 行**硬编码 `"seed": 42`**——那是**读出种子**，**主干 seed 只出现在 `run_id`/`model` 字符串里**（`..._bs43_ep24`）⇒ **算主干方差必须从文件名解析，不能读 `seed` 列**<br>**(b)** 第 295 行把 `pairing_check` 直接填成 `"n/a(读出适配)"` ⇒ **runbook 要求的那条配对自检收割器不执行，必须手工做**<br>**(c)** `a_original_fc` 就是主干自己的头精度 ⇒ **它既是方差来源、又是"有没有接错主干"的校验，是同一个数**——这也是为什么 (b) 是不可省的闸门 | 见 `docs/V7_M4_RUNBOOK.md`；M4 收尾时逐条执行 |
| **新产物不会自动进台账** | `build_ledger.py` 的 ext6 收割读的是固定的 `alpha_wide_scan_deep_robust.csv` 与 `{model}/metrics.json`，**不 glob 带 `_s43` 后缀的目录** | 汇总台账时需扩展收割器或手工补行（记在 §六） |
| **`\` 在 Python heredoc 里会被吞** | 用 heredoc 批量改写 `.tex`/正则时，`\t` `\b` 会被解释成控制字符（本项目实际踩过一次，写坏了标题行） | **不要用字符串替换脚本批量改 LaTeX 源文件**；要改就用编辑工具逐处改 |
| 🔴 **`run_in_background` 的任务不跨会话存活** | **已出过一次事故**：上一个会话上下文占满被杀时，把当时跑到 2/4 的 M4 训练链一起带走了（见 `docs/HANDOFF.md` §2）。原因是后台 Bash 作业**是那个会话进程的子进程**，会话死它就死。<br>⚠ **重启后仍用的是同一机制**（`logs_v7_m4_train_part2.log`），所以**同一个事故还会再犯一次**。 | **见下方「怎么开一个真正脱离会话的长任务」——已实测通过。**<br>短期兜底：`docs/HANDOFF.md` §2 已写"开跑前先写清楚怎么接住"，但那是**止损**不是**解药**。 |

| 🔴 **内存余量已跌到历史事故水位以下** | **2026-09-24 16:20 实测**：提交限 57.6 GB / 已提交 56.14 GB ⇒ **可用仅 1.46 GB**。<br>而本文件同表上方记的**事故当时是 0.9 / 4.9 GB** —— **当前比事故水位还低 3.4 GB**。<br>大占用者：VS Code 7.86 GB、**M4 训练 python 6.2 GB**、另两个 python 各 2.28 GB。 | **开任何吃内存的 CPU 活之前先量提交余量**；**余量不足就停下来报告，不要硬跑**（claude-38 本日按此停手，正确）。<br>⚠ **注意赌注不对称**：Windows 的提交内存是**全局**的，你的进程吃掉的余量会让**训练**的下一次分配失败 —— **`WinError 1455` 打死的是训练，不是你的脚本**。 |
| 🔴 **CUDA OOM 会在显存空着的时候发生**（2026-09-24 实测事故） | run3 跑到 **16/200 epoch** 时崩掉，报 `torch.AcceleratorError: CUDA error: out of memory`。<br>**但当时显存只用了 3.8 / 8 GB，空着 7 GB。**<br>⇒ **不是显存不够，是系统内存 / WDDM 撑不住**（提交余量当时仅 1.46 GB、物理可用 0.6 GB）。<br>崩溃时刻**恰好是用户那批程序（WPS / SpaceSniffer / Wallpaper Engine / PowerToys）出现在 GPU 进程列表的时候**。 | **看到 CUDA OOM 先查系统内存与提交余量，不要只看 `nvidia-smi` 的显存。**<br>显存空着也能 OOM —— 这个反直觉的事实让"显存够就没事"的判断失效。 |
| ⚠ **GPU 利用率必须连采，单次会骗人** | 同一段训练里连采 8 次得到 `3 2 0 3 3 3 87 96` —— **前 6 次像卡死，后 2 次满负荷**。<br>⇒ 这个训练**卡在 dataloader 上**（GPU 等数据），不是 GPU 密集。 | 判断"训练在不在干活"**一律连采 ≥8 次取均值或看趋势**。<br>更可靠的判据：**产物文件的 mtime**（`best_model.pth` 在动 = 在跑）。 |
| ⚠ **守护进程首版漏了输出重定向** | 2026-09-24 16:37 守护进程第一次真实接手时，**重启的训练输出全部丢失**，`logs_v7_m4_train_part2.log` 停在崩溃前的内容。<br>训练本身没受影响（GPU 96%、产物在更新），但**监控者会误判"训练没在跑"**。<br>**已补 `-RedirectStandardOutput`（落 `logs_v7_m4_restart_s<seed>.log`）；但正在跑的实例加载的是旧代码、不会重载。** | **监控训练一律用**：① 进程在不在 ② **产物 mtime** ③ GPU 连采。<br>**别只盯日志。** |
| ~~⚠ **并行做论文维修会拖慢 GPU 训练**~~ —— **已撤回（2026-09-24，同日）** | 总管提出：M4 的 epoch 时间在漂（113 → 135 s/epoch，+19%），同期开了 6 个工人会话在做 CPU 活。<br>**但实测推翻了它**：claude-cf 给的数 —— **GPU 10 连采 `3 3 3 3 3 0 0 0 0 0`（均值 1.5%），而 `python.exe` 只有 2 个**——<br>**比假说"成立"时测到的 3 个还少，按假说应该更快，实测却更慢。** | **撤回理由**：变量方向与假说相反。<br>**现解释（证据更硬）**：瓶颈是**系统内存枯竭导致训练在 swap/卡死边缘** ——<br>**崩溃时显存只用了 3.8 / 8 GB、空着 7 GB 却报 CUDA OOM** ⇒ 那是 WDDM 在系统内存告急时拒绝分配，**不是 VRAM、也不是 CPU 争抢**。<br>⇒ **保留这条是因为它是一次"提出后被自己的数据否掉"的完整记录**：本项目要求假说必须能被数据推翻，**推翻也算留档**。 |

| 🔴 **本阶段的主导病因：跨数据集/跨口径"搬数"**（2026-09-24 一天内抓到 **三次**） | ① **A6**：dithering 一节的机理数（峰度/翻转/架构对照）**全是 CIFAR-10 的**，却挂在图注写 CIFAR-100 的那一节；<br>② **「100 ep」**：`results_master.csv` 显示 `cifar100\|simple_cnn\|clean` = **120** 而 `cifar10\|...` = **100** —— **C10 的 epoch 定语被搬进了 C100 的表**（图例、台账三处、两版论文全中）；<br>③ **`0.933`**：`recipe_simple_cnn.csv` 的 `ridge_R2` 是 **0.935**，`0.933` 是 **CIFAR-10 侧**的转述值。<br>另有 **A3**（同一张表三行来自不同主干实例）、**E7**（`0.39062` 被误判为笔误，实为另一条合法产物）、**E4**（48.35 与 48.25 的差是**设备口径** cuda vs cpu）同族。 | **改一个数之前，先问三句**：**这是哪个数据集？哪个主干实例？哪个协议/设备？**<br>⇒ 本项目已经证明：**数字的可追溯性很好，坏的是"数的定语没跟着搬"**。<br>**操作建议**：凡是在两处见到"同一个量有两个值"，**先查是不是两个产物**，别默认其中一个错。 |
| 🔴 **同族的新变体：内容「搬错了载体」**（2026-09-26 由上一任总管自曝、总管核出） | 该会话转述**某份文件的"内容"**时，把**另一个文件**里的东西记成了这一份的。**实测不成立**——它说的那些信息在另一个文件里，它那份脚本采集的项里根本没有。<br>⇒ 历史那三次是**跨数据集 / 跨主干实例 / 跨设备**，这次是**跨文件**：**同一个病的第四个载体。**<br>⚠ **代价**：这条若不被核，会作为**注释被写进 `.gitignore`** ⇒ 在仓库里**留下一条假的记载**。 | **判据（与"能相减吗"并列：那条管数值，这条管内容）**：<br>**「这条内容的出处，我能不能指着原件念出来？」**<br>**念不出来 = 搬错了。**<br>⇒ 适用面比"能相减吗"更宽：那个只判数值，这个判**任何内容**（一句话、一个结论、一条转述）。<br>⇒ **转述别人的话时尤其要用**——**转述是"内容搬家"的高发区**，因为转述者只经手"记忆里的样子"，不经手原件。<br>⇒ **核验成本极低**（回原件看一眼）；**不核的代价是"假记载入库"**。 |
| ⚠ **"只加注不改数"也要落在正确的层**（2026-09-24 新增边界） | 本文件 §二 给过一条边界：**改"写错了"直接改；改"当时是那样想的"加注、不改原文。** 实测中又分出一类：**"术语把两样东西合并了"** > 典型的：`鲁棒头`（v5 `recipe`）与 `读出适配头`（v3 `readout_repair`）**概念同、产物不同**，<br>**硬合并会掩盖口径差异**。 | **术语统一的目标是消歧，不是减少词数。**<br>两样东西若"看上去一样但产物不同"，**保留两个名字 + 首次注明对应关系**，不要合并。 |

| 🔴 **文献核实分两级：L1 著录 ≠ L2 内容 —— 且三种失败模式 L1 只抓第一种**（2026-09-24，三个会话各撞一次） | 项目的纪律是「引用前必须逐字核实」（红线 9，因 `nat` 而立）。<br>**但实际执行用的是 OpenAlex / Crossref —— 它们只核标题/作者/年份/卷期页码，核不到方法描述。**<br>**三种失败模式实测都出现过**：<br>**① 引用不存在** ⇒ `nat`（页码被两篇无关论文占着）。**L1 能抓。**<br>**② 引用存在、描述不符** ⇒ NEAT。论文 §1.3 说 NAT「把失真注入训练过程」，<br>而 NEAT 原文是「**regularize the weights … to exist within the linear operating range**」——**它约束权重让器件留在线性区，即"规避"非线性**。<br>**③ 引用存在、描述相反** ⇒ SHOT。草稿写「冻结特征提取器、只学分类头」，<br>原文是「**freezes the classifier module (hypothesis) … learns the target-specific feature extraction module**」。<br>**②③ 都是 L1 全过、L2 出问题。** | **两级必须分开记**：<br>**L1 著录核实**（这篇存不存在、卷期页码对不对）→ 元数据 API 可做，成本极低；<br>**L2 内容核实**（它到底做了什么）→ **只能读原文**（arXiv abs 页 / PDF 正文 / 官方页），**且不能用 L1 顶替**。<br>⇒ **引用一条文献去支撑某个具体断言时，至少要对那句断言所依赖的原句做 L2。**<br>⇒ **"我核对过这条文献" ≠ "我核对了它的机制描述"。**<br>🔴 **本条由一次总管自己的失误推出来**：总管曾推荐「把 `nat` 换成 NEAT」（理由：主题/年限/期刊全同）——<br>**那是纯 L1 的判断**。做 L2 后发现 **NEAT 的策略与本文相反**，换了反而变成"真引文配错描述"（比假引文更难被发现）。<br>**正收益**：改对之后差异化从含糊的"场景不同"变成**可验证的结构差异** ——<br>SHOT 那条：「共享'只动极少量参数'的取向，**但动的对象正好相反**」；<br>NEAT 那条：「**别人规避、本文测量**」（此处连"问题类"都同类，只有策略相反，对照更干净）。 |

| 🔴 **"被取代"有两种，处置相反**（2026-09-24 由 claude-65 在"旧值存留地图"里提出） | 本阶段改过一批数字，但**它们不是同一种东西**：<br>**① 同一个量换了口径**（旧口径作废）⇒ 例：`4/6`→`5/6`、`100ep`→`120ep`、`20.12`→`20.26`、`13~23`→`+15.2~+31.8`<br>**② 两个口径下的两个量**（名字像、其实不是同一个量）⇒ 例：`+14.38 = 42.47−28.09`（clean 训练、**跨脚本**）与 `+15.52 = 41.55−26.03`（NAT 训练、**同协议同 seed**）<br>**② 类如果把旧值直接换成新值，就错了** —— 它们**不是旧版和新版，是两个不同的量**。 | **判断方法只有一句**：**问"这两个数能相减吗？"——能相减才是同一个量。**<br>**② 类只能"补定语"，不能"换数"。**<br>⇒ 这与本阶段的主导病因是**同一枚硬币的两面**：<br>那个病是「**把不同的量当成同一个量**」；这个陷阱是「**把不同的量当成旧版和新版**」。<br>**同族的第三种形状**：**同名异物**（`20.12` 既是 headroom 旧值、也是 ResNet-18 的精度 20.12%）——<br>**剔除同名异物要先做，否则会造出虚假命中**（claude-6f 排掉过一起，claude-65 这次又排掉一批）。 |
| ✅ **正例：旧值不该被删干净，该"带着作废标记"留着**（claude-65 发现） | 全仓扫下来，**`paper/**` 是最干净的一侧**：探针旧值在论文里的三处命中，**全部是明确的"已作废"注记**：<br>「（早期版本的探针表曾用 8000 样本子集，给出 27.76 / 59.60 与 headroom +20.12；M0c 已重跑全量口径并替换）」<br>⇒ **旧值出现在"它已被作废"的句子里 —— 这是正确用法。** | **为什么正确**：读者可能从别处看到过旧值。**留着作废标记，才能防住"从旧文档引到旧值"**；<br>**删干净反而会让旧文档变成陷阱**（那里没有标记）。<br>**本项目的 `论文v3.md` 与各阶段报告正是缺这个** —— 所以才需要那份"旧值存留地图"，<br>并由它统一补上头部总注（**比逐处注更清楚**，适用于旧值密集的历史快照）。 |

| 🔴 **守护进程脱离得了"会话"，脱离不了"机器重启"**（2026-09-24 实测事故） | 16:47 起的守护进程（pid 34972）在 **18:35:03 的系统重启**中**一起消失**，训练进程也是。<br>后果：**M4 停在 18:32 无人接手**，直到 19:20 才被发现（空转了 48 分钟）。<br>**没有任何自动机制会告诉你它停了** —— 它是**静默失败**。 | **重启后必做**：<br>1. 查训练进程在不在（`Get-CimInstance Win32_Process -Filter "name='python.exe'"`）<br>2. 查**有没有 `metrics.json`**（真完成的唯一判据）<br>3. 不在 ⇒ **重新拉起守护进程**（一条 `Start-Process`，见下）<br>⇒ **并写进 `HANDOFF.md`**：接手者开机后的第一个动作就包含这一条。<br>⚠ **推论**：任何"靠后台进程保活"的方案，**在重启面前都失效**；<br>能跨重启的只有**开机自启动**（注册表 / 计划任务），**本项目没有做**。<br>⇒ **当前的兜底是"人发现"**，所以 **`HANDOFF.md` 的醒目位置必须写着这条**。 |
| ⚠ **并发会话数也是内存预算的一部分**（用户 2026-09-24 明确要求） | 本机**物理内存只有 16 GB**。本日一度同时开了 **6 个工人会话** + 训练 + VS Code。<br>实测：6 个 Claude 会话工作组合计约 **664 MB**（**不是最大头**，但也不是零）；<br>大头是 VS Code（~5 GB）+ 训练（~6 GB）+ 用户自己的程序。 | **用户已明确要求收紧并行度。**<br>⇒ **按需开会话，任务做完就让它空闲/关闭**，不要"先开一堆放着"。<br>⇒ 派活时**优先串行**：一个文件一个写者、同一时刻只跑一个吃内存的活。<br>⇒ 本机**唯一不可省的常驻**是：① 训练进程 ② 它的守护进程 ③ 总管会话。 |

| 🔴 **浮点边界会让"容差判据"误报 —— 同一现象两次判决相反**（2026-09-25 实测，本项目第 N 次"工具本身出错"） | 新写的读出衔接脚本用 `[math]::Abs($a-$b) -le 0.01` 做配对容差。实测：<br>· `73.09 − 73.08 = 0.010000000000005116` ⇒ **> 0.01 ⇒ 误判 FAIL**<br>· `72.80 − 72.79 = 0.0099999999999909` ⇒ **< 0.01 ⇒ 侥幸 PASS**<br>**两者是同一个物理量**（0.01 = 10 000 张里的 1 张，runbook 明写"记 PASS"）。<br>**决定性证据**：误报那次的浮点值与既有 s42 基线（`72.70 − 72.69`）**逐位相同**，而 s42 被明确判为 PASS。 | **判据要和它描述的东西同单位**：容差若以"张样本"定义，就比较**张数**——<br>`[int][math]::Round([math]::Abs($a-$b) * 100) -le 1` ⇒ PASS。<br>已做边界自测（1 张 PASS / 2 张 FAIL）。<br>⇒ **推论**：**"两次相同性质的比较给出不同判决"本身就是工具可疑的信号**，不用等外部证据。 |
| 🔴 **`\cref{sec:...}` 在 journal 里不能用**（2026-09-25 实测，2 个编译错误） | journal **第 30 行有明文约定**：`% 章节引用一律写成「第 \ref{sec:xxx} 节」。` 全文 35+ 处章节引用**全部用 `\ref`**；cleveref **没有为 section 类型配置名称**，用 `\cref{sec:limit}` 会报 **`! Extra \endcsname.`**（错在 `\cref` 处，但报的是 `\endcsname`，**字面信息会把人引向错误方向**）。<br>⚠ 注意：`\cref{fig:..}` / `\cref{tab:..}` **是正常的**（有配置），只有 **section 类型**不行。 | **改 `.tex` 之前先读该文件的头部约定注释**。<br>**这类错误检查器能抓到**（本次就是它抓的）⇒ **改完必跑 `tools/check_papers.py`**，`EXIT=0` 才算完。 |
| 🔴 **缺字形：xelatex 把字体里没有的字符「静默丢掉」**（2026-09-25 外部体检查出，三份稿共 46 处） | 圈码 `①`–`⑥` 与 `⇒` 在中文字体里**没有字形** ⇒ xelatex **静默丢弃**：不报编译错误、不影响退出码，但**成品 PDF 里那些字符一个都没有**（不是吐司框，是空白）。<br>**后果是编号句与交叉引用被整句打穿**：「第 `①` 级含两类种子」渲染成「第 ␣ 级含两类种子」；「判决从分支`②`变`③`」渲染成「判决从分支 ␣ 变 ␣」。<br>**当时所有闸门全绿**（`check_papers.py` 三项 + 静态检查）。证据只在 `main.log` 的 `Missing character` 行里。 | **导言区加三行**（三份稿都要，已加）：<br>`\usepackage{newunicodechar}`<br>`\xeCJKDeclareCharClass{CJK}{"2460 -> "24FF}`<br>`\newunicodechar{⇒}{\ensuremath{\Rightarrow}}`<br>⚠ **第一条单独不够**：中文字体**有** `①`-`⑥` 但**没有** `⇒`（实测仍缺 1 个）。<br>**并已把 `Missing character == 0` 写进 `check_papers.py` 的判据。** |
| ⚠ **全模型反传 + 每 batch 注册 hook ⇒ 慢 7 倍**（2026-09-26 M8c 实测，实验被迫中止） | M8c 的 FT 阶段实测 **>13 分钟/epoch**，而同主干**从头训练是 105 s/epoch** ⇒ 慢 7 倍。两条叠加：<br>① FT 直接复用 `build_loaders` 的 **batch 512**（那是为冻结主干的前向/浅层训练选的），做**全模型反传**时显存与时间都爆（实测 7.8 GB vs LP 阶段 1.5 GB）；<br>② 每 batch 调 `register_nonlinearity_hooks` **遍历 154 个模块**注册 + 再移除，开销随模块数线性增长。<br>后果：30 ep 需 **>6.5 h** ⇒ 拿到 2/3 结果后**中止**（台账 §16.9 已如实记录"第三个主干未测"）。 | **微调类实验要单独配 batch 与 hook 策略**，不要直接复用前向/浅层训练的配置。<br>⚠ **冒烟也不是万能的**：M5 的 wide_cnn 冒烟报 **37.1 s/epoch**，实际稳态 **34~60 s/epoch** —— 冒烟只跑 1 ep、**没进稳态**（含 dataloader 预热）。<br>⇒ **成本估计一律给区间，且在跑到第 3~5 个 epoch 时复核一次**（那时才脱离预热）。 |
| 🔴 **验证 PDF 成品：「两个 pdftotext」+「计数路径整体不可靠」—— 一次三层嵌套的工具事故**（2026-09-25，顾问四轮查出、总管逐条复现，**含总管两次犯错**） | ① **两个二进制**：`/mingw64/bin/pdftotext`（**默认命中**）不加 `-enc UTF-8` **静默丢掉全部非 ASCII**（`结论`/`α`/`①` 全 0）⇒ 看起来像"PDF 里没有"；`/d/texlive/2025/bin/windows/pdftotext` 默认 UTF-8。<br>② **计法**：`grep -c` 数的是**行**（31 vs 36，随换行位置变）；`grep -o \| wc -l` 对 TeX Live 版给 **1**（真值 37）。Python `str.count` 两个二进制都给 37/26/7。<br>③ **字节层（根因）**：两份输出落盘比对 —— **行数差近一倍**（1292 vs 2372），TeX Live 那份含 **1019 个 NUL** ⇒ **`grep` 按二进制处理**，这才是计数分岔的机制（NUL 来源未查明，记已知未知）。<br>🔴 **失误是三层、不是一层**：顾问**没写清用的是哪个二进制**；总管**拿到 0 没做阳性对照**就下结论；总管**还用"计数相同"推出过"内容逐位一致"**（超范围推断，被顾问纠正）。三层各自都"看起来没问题"。 | **报告"成品里没有 X"或"工具报 0"之前，先拿一个确定存在的东西做阳性对照**；**对照也是 0 ⇒ 是工具问题，不是结论**。<br>⇒ **一般化（顾问提出、总管采纳）**：**一个数如果换个跑法就变，那它不是"数"，是"这一次跑出来的东西"** —— 第一反应该问"**它能不能稳定复现**"，而不是"它为什么少报"。<br>⇒ **数 CJK 次数一律用 Python `str.count`**（本机口径）。<br>⇒ **可靠的两条路**：`Missing character`（编译器硬证据）与 `pdftoppm` 渲染目视。**细节与逐条复现见 `docs/EXTERNAL_REVIEW_DISPOSITION.md` §8。** |
| 🔴 **`check_papers.py` 全绿 ≠ 成品没问题 —— 闸门有盲区**（2026-09-25 外部体检实测） | 三份稿"0 错误 / 0 未定义引用 / 0 markdown 残留"，但 **journal 附录 A 的「关键数值」表有一整列在 PDF 里读不到**：`Overfull \hbox (390.8186pt too wide)`（≈13.8 cm，而右边距只有 2.4 cm）⇒ **出处列被裁到纸外**。这张表的唯一用途就是给出处。<br>根因：长 `\texttt{}` 路径**不能断行** ⇒ 整行溢出。<br>**检查器抓不到它，因为 `check_papers.py` 不查 `Overfull`。**<br>内部 `project_health_check.md` 记的 `Overfull 1 处（轻微）` 是 **15 页版本**的读数，未随版本更新（现行 29 页版本是 8 处）。 | **改完 `paper/` 之后，除了跑检查器，还要**：<br>① `grep -c Overfull paper/*/main.log`；<br>② **目视最后几页 PDF**（`pdftoppm -png -r 110 -f <起> -l <止>`）。<br>**判据**：overfull **> 右边距（2.4 cm ≈ 68.2 pt）才会被裁到纸外**；小于它的只是压边线。<br>**修法**：长路径用 `\path{...}`（url 宏包，可在 `/` 处断行）替 `\texttt{}`；或第三列改 `p{}` 定宽。<br>⇒ **一般化**：**每一个自动闸门都有盲区，检查越自动化越容易忘记闸门没覆盖的那部分。** |
| ⚠ **"工具抓到了 X"之前，先确认工具本身不错**（2026-09-24 一次差点误信） | 一个会话用它**自己的临时脚本**做逐行 `$` 奇偶检查，**误报了 9 行**。<br>根因：它的脚本把 `\%` 当成了注释起点（`（…差 $3\%$）` 从 `3\` 处被截断，丢掉后面的 `$`）。<br>它由此怀疑**项目的正式检查器**也可能有同样的 bug —— **总管去读了源码，确认正式工具没有**：<br>`strip_comment` 按**字符对**扫描（`if c == "\\"` 就 `i += 2` 跳过两个字符），**`\%` 永远不会被当成注释起点**。 | ⇒ **纪律**：**报"工具抓到了 X"之前，先确认工具本身不错**（读源码 / 造已知坏例自测）。<br>⇒ **推论的方向也要小心**：那次它写"这类 bug **只多报、不漏报**（截断只会让计数变小）"——<br>**前半句对、括号里的理由不对，结论也就错了**：<br>截断会丢掉后面的 `$`，**若丢的是奇数个，奇偶性会翻转** ⇒<br>　真行 3 个 `$`（有错）截成 2 个（判没错）= **漏报**；真行 2 个 `$`（没错）截成 1 个（判有错）= **误报**。<br>**⇒ 同一个 bug 既会误报也会漏报，所以"它报出来的都是真的"推不出"它没报的都没问题"。**<br>（好在 journal 那次真错是真的 —— 它同时被 `xelatex` 的 `! Missing $ inserted.` 独立确认。<br>**但那属于运气好，不属于推理对** —— 与当天那次 `torch.load` 是同一个模式。） |

| 🔴 **`check_papers.py` 的页数正则读不出「1 页」文档**（2026-09-26 加 advisor-summary 时踩到） | 正则写的是 `Output written on main\.pdf \((\d+) pages`。**xelatex 对单页文档写的是 "(1 page"（单数、无 s）** ⇒ 匹配失败 ⇒ `pages=None` ⇒ 检查器报 **"✗ 编译：None 页 / 0 错误"**，把一个**完全正常**的稿子判成失败。<br>⚠ **这个 bug 藏了很久没暴露，只是因为此前四份稿都多于 1 页。** | 改为 `\((\d+) pages?`。**已修并复验**（advisor-summary 现在正确报 "1 页"）。<br>⇒ **一般化**：**"一直没出错的代码"不等于"对的代码"，只等于"没走到那条分支"**——新增一类输入（这里是"1 页的文档"）才会把它照出来。 |

### 五·补一、怎么开一个真正脱离会话的长任务（**已实测通过**）

**问题**：`run_in_background` 的 Bash 作业挂在会话进程下，会话一死任务就死。
**我们要的是"会话死了训练还在跑"。**

**实测通过的配方**（2026-09-24 验证）：

1. 写一个 `.bat`（**用绝对路径**，内含 `cd /d` 到仓库）：

```bat
@echo off
cd /d d:\deeplearning_practice\存算一体
set PYTHONIOENCODING=utf-8
D:\anaconda3\envs\pytorch_env\python.exe <训练脚本> <参数...>
D:\anaconda3\envs\pytorch_env\python.exe <下一个训练脚本> <参数...>
```

2. 用 `Start-Process` 启动它：

```bash
powershell -NoProfile -Command "Start-Process -FilePath 'cmd.exe' \
  -ArgumentList '/c','d:\deeplearning_practice\存算一体\<脚本名>.bat' -WindowStyle Hidden"
```

**两个必须知道的细节**（都实测踩过）：

- ⚠ **`-ArgumentList` 里的 bat 路径必须写绝对路径**。写相对路径（即使给了 `-WorkingDirectory`）
  **实测不执行、也不报错**——文件不生成、命令静默失败。
- **日志要重定向到文件**（bat 里用 `>> 日志 2>&1`），否则输出无处可去。
  注意 CMD 重定向 + Python 块缓冲 ⇒ 日志**可能要等缓冲区满才刷出来**；
  想确认存活请看 **GPU 利用率**与**产物目录 mtime**，不要只盯日志。

**验证脱离成功的方法**：启动后另起一条命令查进程——
`Get-CimInstance Win32_Process -Filter "name='python.exe'"` 应能看到训练进程；
**且它与当前会话不在同一进程树里**（会话结束后它仍在）。

**别再做的事**：不要用 `run_in_background` 跑长训练。**它不是"后台"，是"本会话的子进程"。**

---

## 五·补、上下文管理协议（**总管会话必须自己执行**）

> 立这条的直接原因：**有一次会话上下文占满、压缩失败、整个进程死掉，
> 同时把正在跑的训练链一起带走了**（见 `docs/HANDOFF.md` §2）。
> 总管会话是所有会话的头，**不能因为上下文占满就消失**。

### 能做与不能做（分清）

| 动作 | 能不能自己做到 |
|---|---|
| **压缩自己的上下文** | ❌ **做不到**。`/compact` 是**用户命令**，没有对应的工具可调 |
| **把自己的状态写成交接文档** | ✅ 能。**这是"复活"的手段** |
| **看到自己还剩多少上下文** | ✅ 能。每轮都能读到剩余 token 数 |
| **主动在临界点之前停下并交接** | ✅ 能。**这是必须做的** |

### 协议（总管会话执行）

```
每一轮：
  读一下自己还剩多少 token
  剩余 ≥ 20%          → 正常干活
  剩余 20% ~ 10%      → 立刻更新 docs/HANDOFF.md（快照当前状态）
                        并告诉用户：「我快到线了，请开一个新会话，第一句读 docs/HANDOFF.md」
  剩余 < 10%          → 停止接受新任务，只做交接：写完 HANDOFF.md → 报告 → 结束
```

**交叉评估的会话（工人）也同样适用**，只是它们的产出通常已经落在文件里，
交接成本更低。

### 为什么"交接"比"压缩"稳

- **压缩**只在这一条会话里生效；**会话一旦被杀，压缩也救不回来**（实测发生过）
- **交接文档落在仓库里**——符合本项目一贯的"仓库即记忆"，
  而且**新会话读到的是确定的内容**，不依赖压缩摘要的质量
- ⇒ **默认走交接；要压缩由用户决定**（用户随时可以 `/compact`，那是他的选择）

### 交接文档要写什么（`docs/HANDOFF.md` 的格式）

1. **三十秒状态**：正在跑什么、后台 ID、日志路径
2. **接手第一个动作**：3~4 条可直接粘的命令
3. **刚出的事故**（if any）——不写的话接手者会重踩
4. **在途任务的完整状态**：做到哪、还差几步、预计何时完成
5. **完成清单 / 未开始清单**
6. **最容易踩的几条**（指向坑表，不重复）

**原则：交接文档是快照，长期规则指向 `V7_COORDINATION.md`，不复制。**

### 配套纪律：长任务开跑前先写"怎么接住"

M4 那次之所以有惊无险，是因为**恰好前两次已经跑完、第三次还没存盘**。
换个时间点（比如第三次跑到第 150 个 epoch）就是几小时的损失。

⇒ **开长任务（>1h）之前，先在 `HANDOFF.md` 里写下它的后台 ID、日志路径、产物路径**，
以及"若被杀，从哪一步接着跑"。**这条比任何别的都省事。**

---

## 六、当前阶段的状态板

| 里程碑 | 状态 | 执行者 |
|---|---|---|
| M0b 文献核实与定位 | ✅ 完成（`docs/POSITIONING.md`） | claude-e9 |
| M0c 探针可信度审计 | ✅ 完成（§16.0 / §16.0a） | claude-65 |
| M1 补论文缺口 | ✅ **9/9 完成**（`paper/coverage-audit.md` §五·补） | claude-e9 |
| M2 单一实现自洽化 | ✅ 完成（`docs/IMPL_AUDIT.md`） | claude-e9 |
| **M4 深层主干方差** | ✅ **完成（2026-09-25 18:19），判分支 ②（1.5~3 pp）**：四个 run 全部以 `total_epochs` 达标（67.86 / 67.64 / 72.79 / **73.09**）。主干级 std（尾部）VGG-11 **0.25** / ResNet-18 **2.61**，**均小于读出级**（3.18 / 3.40）⇒ 论文印的 ± 没有低估总不确定度。**未触发止损线 1**。详见台账 **§16.1** | 训练链由总管发起；读出由 `tools/v7_m4_readout.ps1` 自动衔接；判决与论文同步见 §16.1 |
| **M8c LP-FT**（2026-09-26 凌晨） | ✅ **完成 2/3，判分支 ②（阴性，且强）**：SimpleCNN **−11.85 pp**、VGG-11 **−33.83 pp**（LP-FT 相对纯读出）。机制：FT 训练精度涨到 88.9% / 96.9%，而**各 α 测试全线变差、尾部降幅远大于 clean** ⇒ **在失真分布上微调破坏了预训练特征**。**支持 L4「训练端做的事越少越好」⇒ 论文表述不需要改**。ResNet-18 因 FT 慢 7 倍（>13 min/ep）**中止**，已如实记录。详见台账 **§16.9** | 总管亲做 |
| **M5 秩-恢复第 4 点**（2026-09-26 凌晨） | 🔄 **训练中**（wide_cnn 120ep，02:27 起，实测 36 s/ep ⇒ ≈03:39 完成；其后秩探针 + 读出 ×3 seed）。模型 `SimpleCNNWideV2`：1.40M 参数 / 133.3M MACs（0.87× VGG-11）。⚠ **第 4 点与前三点的差异不纯是容量** —— 还含"第二次下采样位置"，是 MACs 约束下的必然取舍，已如实标注 | 模型由 `claude-27` 准备；训练与读出由总管排队 |
| **M14b ReLU 非负性**（2026-09-26 凌晨） | ✅ **完成（08:37），判分支 ③（不可分辨）**：补 seed 后 **P(relu) = +11.81 / P(gelu) = +14.71 ⇒ D = −2.91 pp**（\|D\| < 4）。🔴 **方向对 seed 敏感**（单 seed 时 +2.57，3-seed 时 −2.91）⇒ 明确的"不可裁决"，**不是"接近显著"**。🔴 **噪声来源已定位**：**2×max 的 std 3.0 vs 5×max 的 0.3~1.5（差一个量级）** ⇒ 要真裁决此假说，预算该花在 **2×max 的更多 seed** 上。台账 **§16.11** | 变体由 `claude-c9` 准备并自测（diff 由总管独立复验）；训练由总管排队 |
| M8 读出容量阶梯 | ✅ **正面结论**（线性头最优 ⇒ 容量不是瓶颈，§16.6） | claude-65 |
| M8b ETF/原型读出 | ✅ 阴性（§16.3） | claude-65 |
| M8d 读出样本量曲线 | ✅ 阴性 + 附带发现（§16.5 / §16.5a） | claude-65 |
| M9b NC 几何 | ✅ 阴性（§16.2） | claude-65 |
| §16.2a Fisher 分解 | ✅（类内散布主导） | claude-65 |
| §16.4 过程敏感度 | ✅ **Δ_proc = 3.45 pp** | claude-65 |
| §16.5a 样本量外推 | ✅ 未饱和；**headroom 升至 +27.63** | claude-65 |
| M12 门控机制 | ✅ **已收工**：判 ③（不分离）→ 经裁决**升级为 ②（永久关闭"插值即集成"的机制解释）**。§16.7a（φ 强正 ⇒ 收益上界 +1.07 pp）已成稿 | claude-65 |
| **§16.8 判据前提自检回溯审计** | ✅ **完成，判 ①**：扫 **30** 条预登记判据，**8 条同类缺陷（27%）**，其中 **5 条足以翻转**（3 条已实测确认）。**其"顺带发现"明写：8 条全是判据写法问题，不是数据或执行问题** | claude-65 |
| **论文复核修复（本轮）** | **A1/A2 ✅ 已修**（`781d8c2`）。**A3–A14 已派**给 claude-4c（journal）与 claude-89（technical-report + competition），另派 claude-38 **独立重算** A3–A10 | claude-4c / 89 / 38 |
| M5 / M6 / M7 / M13 / M14b | 未开始（多数需 GPU，排队等 M4） | — |

**M4 的执行记录（供后来者）**：
- 冒烟：`--epochs 1 --tag _smoke_s43`，52 秒/epoch，产物落全新目录，既有目录 mtime 未变（零覆盖确认）
- 训练链：4 次**串行**（`&&` 链式），vgg11/exp2 各 120ep（s43/s44）、resnet18/exp3 各 200ep（s43/s44）
- 日志：`logs_v7_m4_train.log`；后台作业 ID `bqrdsv1wd`
- **实测耗时**：VGG-11/120ep = **110 分钟/次**（初估 60 分钟偏乐观）⇒ 全程按 ~9 小时估

**收尾按 `docs/V7_M4_RUNBOOK.md` 执行**（含读出适配命令、命名陷阱 `bs<seed>`、预登记判据①②③）。

---

## 六·补、v7 的产出清单（截至 2026-09-24 15:00）

**正面结论**（加强中心结论）：

| 结果 | 内容 | 落在哪 |
|---|---|---|
| **M8 容量阶梯** | 测过 k-NN 与两种 MLP 后，**线性头仍最优** ⇒ 容量不是瓶颈 | 论文 §3.2 |
| **§16.5a headroom 上抬** | 探针样本 8000→50000：headroom **+20.26 → +27.63**（未饱和，校正后约 +25.4） | 论文 §3.2 |

**阴性结论**（排除型，同样有价值）：M8b（ETF 无增益）、M9b（几何远离 ETF）、M8d（冗余不成立）、M12（进行中）

**方法学产出**：

| 结果 | 内容 |
|---|---|
| **§16.4 过程自由度** | 同数据同特征同架构，只换拟合过程 → **差 3.45 pp**；**主要来源是"训多久"而非"用哪个"** |
| **三级可分辨性** | 种子噪声 / 过程自由度 / 跨实现（5.55，未归因）——论文 §3.11 |
| **口径陷阱两条** | RMS 归一化（不做则负侧结论反号）、中心化（不做则判决变支） |
| **`tools/check_papers.py`** | 提交前检查（markdown 残留 + 编译 + 未定义引用） |
| **`docs/DERIVED_VALUES.md`** | 派生值登记表（均值/差值不以字面形式存在，必须写清算式） |

**下一步（M4 与 M0c 收工后）**：claude-e9 串行汇总台账 §16 → 校验两份产出 → 按 `NEXT_STAGE_TASKS.md` 的优先级派下一批。
