# 派生值登记表

> **为什么要这个文件**：本项目的纪律是"每个数字都能指向一个产物文件"。
> 但论文里有一类数字是**算出来的**（均值、差值、比值），它们在源文件里**不以那个形式存在**——
> 复核的人必须知道"怎么算的"才复得出来。
>
> **用途**：任何写进论文而**不能字面命中源文件**的数字，都在这里登记它的算式与原始行。
>
> **怎么发现的**：用一次全文数字溯源扫描（抽出稿件里每一个数值 token，
> 反向在全部源材料里找，允许四舍五入匹配）。扫出的"无出处"项就是这里要登记的。
> 最近一次扫描：journal 240 个 token、technical-report 191 个，**四舍五入后仍无出处 2 个**（都在 journal）。

---

## 一、当前登记（3 项）

### 1. `28.35` —— SimpleCNN 的 `b_clean_fit_linear` 读出臂，3 seed 均值

**位置**：`paper/journal/main.tex`，§3.4 的"第二项自我校验"表，`SimpleCNN` 行。

**算式**：对 `outputs_cifar100/v3_readout_repair/` 下三个文件
（`readout_repair_c100_clean_ep24_alpha+0.30_n20000.csv`、
`readout_repair_c100_plain_s43_ep24_alpha+0.30_n20000_s43.csv`、
`readout_repair_c100_plain_s44_ep24_alpha+0.30_n20000_s44.csv`）
取 `readout == "b_clean_fit_linear"` 那一行的 `0.3` 列，**三个数取算术平均**。

**为什么没有字面出处**：CSV 存的是**逐 seed 的行**，均值是算出来的。

**同表的其他格**：`28.09`（`a_original_fc` 三行同值，故字面可命中）；
`43.31` / `15.22` 是 `e_readout_NAT` 的均值与差，恰好与别处一致，故未被扫描标出。

### 2. `47.40` —— ResNet-18 的 `c_dist_fit_linear` 读出臂，3 seed 均值

**位置**：`paper/journal/main.tex`，§3.4 末尾关于"再分配极端形态"的括注
（与 `52.16` / `58.98` 并列）。

**算式**：对 `outputs_cifar100/v3_readout_repair/readout_repair_c100_exp3_resnet18_s{42,43,44}_ep24_alpha+0.30_n20000{,_s43,_s44}.csv`
各取 `readout == "c_dist_fit_linear"` 那行的 `0.3` 列，三个数取算术平均。
（`52.16` 与 `58.98` 是同一算法在 SimpleCNN / VGG-11 上的结果。）

**同句的其余数字**：`15.53`（同臂在 ResNet-18 上的 `0.0` 列均值）、`72.69`（`a_original_fc` 的 `0.0` 列，三行同值）——都字面可命中。

---

### 3. `-2.2` 与 `25.4` —— §16.5a 的"不相交划分校正"及其结果

**位置**：`paper/journal/main.tex`，§3.2 关于 headroom 上抬的一句
（「按第 §2.1 节的『不相交划分』校正（约 $-2.2$）折合约 $+25.4$」）。

**算式**：

```
Δ不相交(α=+0.3) = d_disjoint
源：outputs_cifar100/v7_probe_audit/c5_private_decompose_agg.csv
    取 d_disjoint 这一列在 α=+0.3 处的值 = **−2.19**   （= 16.23 − 18.43）
25.44 = 27.63 − 2.19                                （27.63 是 §16.5a 的 headroom）
```

**⚠ 登记这条的直接原因**（复核者 §乙 B8 报"缺算式"，**但复核者自己算错了**）：
它按 `20.26 − 16.23 = 4.03` 去凑，**那是两个因子之和，不是"不相交"这一项**。
源文件里 `d_disjoint` 与 `d_sample_size`（= **−1.98**）是**两列**。
⇒ **论文原写"约 −2.2"是有出处的**，只是没把"取哪一列"写出来。

**同源的其他列**：`d_sample_size = −1.98`（另一因子，**不是**这一处用的）。

**⚠ 提示**：本文件自 2026-09-24 的论文大修（A/B/C/D/E 五组共数十处改动）之后**尚未重跑全文数字溯源扫描**。
**规则 3 要求"每次往论文里加派生值后重跑一次"** —— 这一轮改出了几个新的合成值（例如 §3.6 的
叠加三 seed 均值 **49.18**、干净臂三 seed 均值 **40.29**、缺口 **8.51**），**它们是否字面可命中需要重扫确认**。
**在重扫完成前，本表的"2 项/3 项"不是最终数字。**

---

## 二、登记规则（以后照此办）

1. **只登记"不能字面命中"的**。均值/差值如果恰好等于某个已存值（例如三行同值），不必登记。
2. **必须写清三件**：① 哪几个文件；② 取哪一行哪一列；③ 怎么合成（平均/相减/相除）。
3. **每次往论文里加派生值后，重跑一次全文扫描**，把新出现的无出处项补进来。
4. **不许只写"由产物算出"** —— 那等于没写，复核的人复不出来。

---

## 三、扫描方法（可复现）

抽出稿件里所有形如 `\d+\.\d+` 的 token，排除纯格式数字（`0.5cm`、`11pt` 之类）
与一组通用常数（0.5 / 1.5 / 0.95 等）；
然后在**全部源材料**（论文 + 台账 + 四份阶段报告 + `deploy_notes.md` + `docs/*.md` +
`results_master.csv` + `outputs*/**/*.csv`）里查：
① 字面命中；② **四舍五入到 1/2/3 位小数后命中**（论文写 3 位、源文件常存 6 位，这一条是必须的）。

只用①会误报（第一版扫描报了 4 个，其中 2 个是四舍五入造成的假阳性）。
