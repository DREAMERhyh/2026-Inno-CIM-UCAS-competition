# 二阶段优化：实施记录与结果分析

## 0. 背景与路线
评委会三条建议：①扩展α扫描范围 ②增强方案迁移至深层网络 ③数据集多样性。
基于5天时间约束：执行①②，放弃③（需长期数据工程）。
三项完成状态：①已完成（D1） ②进行中 ③待启动。

## 1. 优化项一：α扫描范围扩展至[-0.6, 0.6]（已完成）

### 1.1 实验设置
- 新建脚本 task_extension4_alpha_wide_scan.py（零改动现有代码）
- 矩阵：3架构 × 10组权重 × 15个α点 = 150次纯推理，种子42
- 输出：outputs/extension4_alpha_wide/

### 1.2 数据有效性验证
6个锚点（各模型α=0及α=+0.3）与一阶段报告数字完全一致（偏差0.00pp），
外推区数据可信。

### 1.3 核心发现
F1 训练区间内优势确认：α=+0.3处 nat_scratch +9.15pp、exp2_robust +13.68pp（vs clean）。
F2 交叉现象（NAT鲁棒性的分布边界性）：增益随外推距离单调衰减，α≈0.4处归零，
|α|≥0.5反转为劣势，nat_scratch崩至随机水平。结论：NAT鲁棒性相对于训练分布
有效，不可外推；若部署失真可能超出训练范围，需扩大训练采样区间（future work）。
F3 负向外推保持：α=-0.6处nat_scratch仍+8.22pp，与"正向破坏力3.4×"发现一致。
F4 finetune外推衰减最缓，干净预训练特征是外推稳定性基础。
F5 架构容差窗口：SimpleCNN > VGG-11 > ResNet-18（容差仅|α|≲0.15）——
深层网络容差急剧收窄，直接论证优化项三的必要性。

### 1.4 图表引用
（指向 outputs/extension4_alpha_wide/ 下4张图与CSV，正文引用时列出）

### 1.5 附注
exp2_robust在α=-0.6处loss异常偏高（6.56）但精度正常（58.99%），推测校准
模块在极端负向失真下产生过自信错误预测，仅作观察记录，不展开。

## 2. 优化项二：Dithering效应定量机理解析（已完成）

### 2.1 实验设置
- 新建脚本 task_extension5_dithering_mechanism.py（零改动现有代码）
- 主分析模型：VGG-11干净权重，纯推理，固定α=+0.3
- 六组实验：(a)复现校验 / (b)可控噪声注入 / (c)纯噪声替代 / 样本级翻转统计 /
  H2激活统计 / (d)三架构对照
- 输出目录：outputs/extension5_dithering_vgg11/

### 2.2 数据有效性验证
组(a)五个锚点与extension3 CSV逐点一致（9.98/42.97/43.27/37.13/36.74，
容差0.00%）。flip_statistics中4bit行net_gain_vs_8bit=653，与+6.53pp×10000
样本精确吻合，参照系验证通过。

### 2.3 三假设判决

**H1（随机去相关）→ 否决**
组(b)在8bit QDQ上叠加高斯/均匀噪声（σ∈0.005~0.5），最优增益分别为-0.03%
和-0.06%，无任何提升。组(c)用等效剂量的纯噪声替代4bit量化，精度骤降至
34.29%（低于8bit的36.74%）。翻转统计显示噪声条件净翻转≈0（+7/-19）。
结论：随机扰动不能复现Dithering增益，效应非随机性所致。

**H2（分布重塑）→ 确认（核心发现）**
activation_stats.csv揭示了完整机制链条：
① 非线性失真(α=+0.3)将分类器输入分布峰度从1.486(干净)压平至0.110(8bit)，
  类别可分性被破坏（精度89.8%→36.74%）；
② 4bit QDQ通过粗粒度离散化（16电平）对压平分布做非线性再分配，峰度恢复至
  0.214，部分重建可分性（+6.53pp）；
③ 3bit峰度更高(0.384)但精度略低(42.97%)，存在最优甜点带——峰度恢复到一定
  程度后量化粒度不足开始抵消增益；2bit信息量不足致峰度坍缩(0.069)、精度
  崩溃(9.98%)。
结论：4bit量化增益的本质是"离散化诱导的分布重塑"——量化充当了非线性再分配
算子，部分修复了非线性失真对决策边界分布的破坏。

**H3（MaxPool交互）→ 强力支持**
三架构增益排序：VGG-11(MaxPool=5): +6.53% > ResNet-18(MaxPool=0): +0.39%
≈ 无效应 > SimpleCNN(MaxPool=2): -13.68%（负增益）。
无MaxPool的ResNet-18完全免疫，证明MaxPool是触发效应的必要条件。但VGG-11
(密集MaxPool)正向救回而SimpleCNN(稀疏MaxPool)严重负增益，说明MaxPool的
密度和位置决定了量化误差是修正还是放大非线性失真。

### 2.4 样本级翻转证据
4bit QDQ相对8bit：救回860样本、杀死207样本（比率4.15:1），净增益+653。
噪声条件净翻转≈0（gaussian +7, uniform -19），与H1否决一致。
（margin分布图见outputs/extension5_dithering_vgg11/margin_distribution.png）

### 2.5 图表引用
- bits_accuracy_curve.png：bits-精度曲线（标注36.74/43.27锚点）
- dose_accuracy_curve.png：剂量-精度曲线（组a/b/c同轴对比）
- flip_statistics.csv：样本翻转统计
- margin_distribution.png：三组样本margin分布
- activation_stats.csv：激活统计量vs bits（H2核心证据）
- arch_comparison_bits.png：三架构bits-精度叠加图（H3核心证据）
- arch_dithering_summary.csv：三架构增益汇总表

## 3. 优化项三：深层网络迁移验证（待启动，待填充）
## 4. 论文修改清单（最后统一填写：逐条列出论文需新增/修改的小节）
